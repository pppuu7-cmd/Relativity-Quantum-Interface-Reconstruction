# Research log — RQIR Candidate Gravity Iteration 273

**Date:** 2026-09-02  
**MODEL_READINESS: 24%**

Authoritative input: Iteration 272 translation-closure gate, with the Iteration-270 K!=0 B3 result retained only as an off-conservation-surface parent-kernel nonidentity certificate.

## Scientific step

Expanded all 15 frozen null-soft `B3=[Q A Q]_3` partitions through the exact routed `Q1/Q2` inverse recursion and imposed `k_s+k_a+k_b=0` only after constructing each primitive Q0 momentum argument.

Exact result: 23 primitive branches remain, but translation closure collapses the distinct denominator count to

- 1 branch: 2 Q0 factors / 1 distinct denominator;
- 10 branches: 3 Q0 factors / 2 distinct denominators;
- 12 branches: 4 Q0 factors / 3 distinct denominators.

No closed branch has four distinct denominators.

Freeze:

`PASS_EXACT_TRANSLATION_CLOSED_B3_DENOMINATOR_TOPOLOGY_REDUCTION`.

Interpretation: the closed object is compatible with the frozen Iterations-245/250 raised bubble/triangle bound. The open four-distinct-resolvent appearance of Iteration 271 was a property of the unclosed K!=0 kernel and disappears exactly on the translation-conservation surface.

This is not a consistency FAIL, exact comparator identity, regime-specific non-identifiability, near-degeneracy, or novelty certificate. It is a positive exact topology certificate.

The translation-closed physical `B3` nonzero question is still open because the existing Iteration-273 numerical rerun code has not yet produced a committed result file. Master reduction remains blocked until that K=0 numerator is numerically certified and promoted to a p-dependent integrand.

No `ANSATZ-003`. Fisher/resources forbidden.

MODEL_READINESS: 24%

Change from previous assessment: 0 percentage points. Comparator foundation remains 24/25; robust unique residual remains 0/20. The topology sub-blocker closes, but no readiness rubric block closes yet.

## Exact next gate

Execute/refine `candidate_gravity/code/iteration273_closed_kinematics_physical_b3.py` on the K=0 family. If the translation-closed B3 is stably nonzero above its finite-difference/transpose envelope, reconstruct the full p-dependent numerator basis and proceed to scoped raised bubble/triangle reduction. If unresolved, improve derivative authority before any master reduction.
