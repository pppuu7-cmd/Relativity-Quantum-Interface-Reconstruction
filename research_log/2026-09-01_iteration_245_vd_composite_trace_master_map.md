# RQIR Research Log — Iteration 245

**Date:** 2026-09-01  
**MODEL_READINESS: 24%**

## Starting authority

Recent commits superseded the lagging `CURRENT_QG_FRONT`: Iteration 244 had already closed the exact cubic VD insertion-series coefficients,

`Gamma_conn^(3) = +(i/2) Tr(U1 U2) - (i/6) Tr(U1^3)`,

with no primitive reduced `U3` term required.

## Goal

Determine the actual operator topology of the required `U1,U2` traces and whether ordinary single-operator CPT3 can be used directly.

## Primary-definition audit

Cho–Kantowski define `U1` with two ghost Green operators `N` and no field Green operator, while `U2` contains two `N` plus one field/graviton Green operator `G`.

Primitive inverse counts before any Ward/numerator cancellation:

- `Tr U1`: `(n_N,n_G)=(2,0)`;
- `Tr U2`: `(2,1)`;
- `Tr U1^2`: `(4,0)`;
- `Tr U1^3`: `(6,0)`;
- `Tr(U1 U2)`: `(4,1)`.

Therefore EOM degree must not be conflated with propagator count.

## CPT3 audit

Published Barvinsky–Vilkovisky CPT3 gives finite third-order form factors for a generic single Laplace-type operator. It does not automatically evaluate the above composite inverse-operator traces, especially `U2`, which mixes ghost-vector and graviton-tensor bundles. No direct single-operator CPT3 identity is claimed.

## Executable reduction

Because the frozen target is a finite `O(R^3)` expansion about Minkowski and Iteration 243 proved `e+c<=3`, the needed composite sector can instead be generated by finite resolvent expansions about the already-frozen flat operators `N0` and `G0`:

`N^-1=N0^-1-N0^-1 dN N0^-1+...`,

`G=G0-G0 dH G0+...`.

Required dressing orders are finite: `Tr U1` through `c=2`; `Tr U2` and `Tr U1^2` through `c=1`; `Tr U1^3` and `Tr(U1 U2)` only at `c=0`.

This converts the remaining C5 problem to a finite library of one-loop flat-space tensor integrals and same-parent source projection rather than an unspecified generic mixed-CPT3 calculation.

## Classification

`NO_DIRECT_SINGLE_OPERATOR_CPT3_IDENTITY_FOR_COMPOSITE_U1_U2_TRACES`.

New blocker:

`BLOCKED_4D_EINSTEIN_VD_RESOLVENT_VERTEX_LIBRARY_AND_SOURCE_PROJECTION`.

`BLOCKED_NOT_ZERO`.

This is operational/derivational BLOCKED, not consistency FAIL, not exact comparator identity, and not near-degeneracy.

## Compute decision

Full heavy CPT3 remains forbidden. A bounded symbolic derivation of the frozen 4D flat kernels is now authorized as the next upstream calculation. Fisher/resources remain forbidden.

## Readiness

`MODEL_READINESS: 24%`.

Change from Iteration 244: **0 percentage points**. The C5 route is more executable, but no physical comparator coordinate or robust unique residual has closed a readiness-rubric block.

## Next gate

Iteration 246: derive `N0^-1`, `G0`, linearized Einstein `E[h]` and `Q_;` in the exact frozen convention; build momentum-space `U1^(1)` and `U2^(2)` and reproduce the known quadratic VD connection contribution/divergence before evaluating cubic trace integrands.
