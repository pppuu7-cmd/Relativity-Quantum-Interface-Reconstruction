# RQIR Research Log — Iteration 171

**Date:** 2026-08-31  
**MODEL_READINESS: 24%**

## Starting point

Iteration 170 proved that any positive linear conserved-TT Källén–Lehmann spectral response is exactly C4-reproducible by an ordinary positive-norm mediator direct integral. Linear spectral shape is therefore a calibrated/shared layer, not the Candidate Gravity novelty carrier.

## Protocol change

Move to a linked post-Gaussian comparison based on the full CTP three-point kernel conditioned on the same measured two-point matrix.

Use the exact Legendre-transform relation

`G3_abc = - G_aa' G_bb' G_cc' Gamma3_a'b'c'`

in the fixed source-completed physical metric convention of Iterations 148–149.

## External-leg dressing control

A six-row scalar-projected control was dressed by nontrivial positive momentum-dependent factors on all three legs.

- raw nonlinear response relative change: `0.255556366906597`;
- maximum reconstructed amputated-kernel error: `1.11e-16`.

Thus two-point spectral dressing is removable from the linked vertex comparison once the common propagator is conditioned on.

## Closed-unitary r/a identity

For `S3=B(h,h,h)/3!` and `h_±=r±a/2`,

`S3[h+] - S3[h-] = 1/2 B(a,r,r) + 1/24 B(a,a,a)`.

With standard factorial vertex normalisation:

- `Gamma_arr = B`;
- `Gamma_aar = 0`;
- `Gamma_aaa = B/4`.

This is generic to a closed unitary cubic action. It applies to C5 quantum gravity tree actions and to ordinary quantum C4 cubic mediators; therefore it is not gravity-specific.

A bare classical-statistical response theory can omit the quantum `aaa` vertex, but this is only a quantum-vs-classical control. Broad/non-Gaussian classical noise can generate additional response-field vertices, so fixed C3 completion is still required.

## Retained results

- `CTP-NG-001 — CLOSED_UNITARY_CUBIC_AAA_OVER_ARR_RATIO_IS_NOT_GRAVITY_SPECIFIC`;
- `CTP-NG-002 — TWO_POINT_EXTERNAL_LEG_DRESSING_DISAPPEARS_AFTER_FIXED_CONVENTION_THREE_POINT_AMPUTATION`;
- `NG-FUNNEL-031 — CANDIDATE_RESIDUAL_MUST_BE_A_LINKED_CTP_VERTEX_RELATION_AFTER_TWO_POINT_AMPUTATION`.

## Readiness

`MODEL_READINESS: 24%` — unchanged.

No concrete multi-point residual has yet survived C3/C4/C5/nonlocal/AS subtraction. ANSATZ, Fisher and resources remain closed.

## Next gate

Iteration 172 should instantiate the first finite **relation-level comparator matrix** in amputated CTP coordinates. Start with tree C4/C5 closed-unitary structure plus the fixed C3 supported/blocked split, then add a Ward/source-completed tensor-lock coordinate so generic quantum C4 self-interaction is not mistaken for gravity-specific coupling.
