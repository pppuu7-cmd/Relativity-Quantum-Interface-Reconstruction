# RQIR Candidate Gravity — Iteration 369

Date: 2026-09-04

MODEL_READINESS: 24%

## Starting authority

Iteration 368 froze all 42/42 physical ordered cubic `Tr U1^2` placements as NONZERO on the matched timelike fixture and proved only the routed cyclic quotient `42 -> 21` after explicit loop-momentum translation. No reversal quotient was authorized.

## Scientific question

Which of the 21 routed cyclic classes have denominator multisets related by a common loop-momentum translation, without assuming anything about the transported physical traced numerator?

## Validated result

Workflow `rqir-iteration369-tru1sq-denominator-topology-candidates`, run `33806834672`, completed successfully. Artifact `9913242988` has digest `sha256:ce46271eecc107f82e5e8985483c7914c78eed51ab4f3224aad49a1e6c42ed41`; raw scientific JSON SHA-256 is `e088f9f1c154eaf30540101482137577d8080180c4d311c763505c1d9d3e82a5`. Sentinel/schema authority passed.

The 21 physical cyclic classes form 15 raw denominator-translation candidate groups. Six groups contain two classes each: `[10,11]`, `[17,18]`, `[16,19]`, `[2,5]`, `[9,12]`, `[3,4]`; the other nine groups are singletons.

Every one of the 21 classes contains at least one repeated denominator shift in the raw propagator word; maximum shift multiplicity is 2. This is not yet a physical repeated-pole certificate because the complete traced numerator may cancel a denominator factor.

## Classification

`PASS_TRU1SQ_21_CYCLIC_CLASSES_RAW_DENOMINATOR_TRANSLATION_CANDIDATE_TOPOLOGY`

This is denominator-topology candidacy only. Denominator equivalence is not numerator equivalence, so no physical family merging occurs in Iteration 369. It is not a Candidate Gravity consistency FAIL, comparator identity, non-identifiability, near-degeneracy or novelty certificate.

## Exact next gate

For each of the six multi-member candidates, transport the complete physical traced numerator under the exact denominator translation and test equality on several held-out loop momenta with a frozen threshold. Separately test whether the full numerator cancels any apparent repeated factor in all 21 classes. Only then freeze numerator+denominator families and classify cut support/singularity type.

The 48 cut-through-double-pole `Tr U2` channels remain independently unresolved after Iteration 364's operational cancellation; no blind duplicate is authorized.

## Readiness

MODEL_READINESS: 24%

Change from Iteration 368: `0 pp`. The physical topology candidates are organized, but no complete readiness bucket, complete eom-degree2 connection sector, or robust comparator-subtracted residual has closed. `ANSATZ-003` remains uncreated and Fisher/resources remain forbidden.
