# RQIR Candidate Gravity — Iteration 328

Date: 2026-09-03

## Result

Validated Action run `33737560245`, job `100591621644`, artifact `9886351787`, digest `sha256:712f7bf5f10325480716f464e50eb584e97f99cbca0fb03cba9e38b029e302b7` passed scientific execution, sentinel/schema audit, artifact upload and final scientific enforcement.

Frozen certificate:

`PASS_TRIANGLE_TWO_ORIENTATIONS_ONE_SIGNED_AFFINE_INTEGRATION_FAMILY`

The six cubic triangle routes form two translation-only denominator classes, `3+3`. Under the legitimate loop-variable transformation `p -> -p + C` together with translations, the two orientations collapse to one denominator integration family. The loop reversal has unit absolute Jacobian and preserves quadratic propagators.

This closes only the denominator-family orientation question. It does **not** establish numerator equivalence under reversal, a nonzero discontinuity, an integrated determinant cut, comparator residual or novelty.

The contemporaneous Iteration-327 common-background audit remains binding: denominator equivalence cannot bypass the requirement that graviton and ghost numerators be reconstructed on one shared closed-triad background.

## Readiness

MODEL_READINESS: 24%

Change from Iteration 327: `0 pp`. A denominator-reduction subgate closed but no complete stable-rubric bucket or robust comparator-subtracted residual closed.

## Next dependent gate

Use a new gate version on the common Iteration-322 closed background to certify H/N arbitrary-incoming-momentum routing for all 19 cubic requests. Only after that may numerator transformations be combined with the `3 bubble + 1 signed-affine triangle` integration-family structure.
