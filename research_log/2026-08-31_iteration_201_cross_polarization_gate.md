# RQIR Candidate Gravity Research Log — Iteration 201

Date: 2026-08-31

## Goal

Convert the Iteration-200 polarization sensitivity into a frozen anti-overfitting gate before any candidate target is constructed.

## Frozen rule

A future candidate must use one parent dynamics and one parameter convention to derive both `b_A` and `b_B` on the independently frozen v3-A and v3-B protocols.

After the physically authorized comparator subtraction in each protocol separately, require nonzero supported residuals in both A and B above the relevant numerical/model error envelope.

A pass in only one protocol is insufficient for candidate promotion. The polarization protocol may not be selected or reseeded after candidate residuals are known.

A future 24-row dual-setting protocol may be an additional validation layer, but cannot hide a failure of the separately frozen A/B gate unless a replacement protocol version is preregistered before candidate construction.

## Status

✅ Cross-polarization anti-overfitting rule frozen before candidate construction.

✅ Same-parent / same-parameter requirement frozen.

🟡 Numerical residual envelope: to be instantiated only when a concrete candidate tangent exists; no Fisher/resource forecast yet.

🟡 AS/C3: BLOCKED, not zero.

❌ Candidate residual: not tested.

❌ `ANSATZ-003`: not created.

`MODEL_READINESS: 24%`

Readiness unchanged: the protocol is stronger, but no rubric component closes.

## Next gate

Return to the remaining comparator-foundation gap. Continue AS/C3 authority/derivation work independently while preserving the frozen v3-A/v3-B cross-polarization gate. If no new authority closes those rows, investigate whether a scientifically justified bounded comparator envelope can be derived without zero-filling or arbitrary class masks.
