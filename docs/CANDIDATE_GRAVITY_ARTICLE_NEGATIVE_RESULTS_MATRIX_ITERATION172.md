# Candidate Gravity Article Matrix — through Iteration 172

## Forward-facing provenance

Preserve all earlier provenance corrections, especially `PROVENANCE-CORR-001`, the Iteration-166/167 definition of `A_odd` as the frequency-odd imaginary part of linear `chi1R`, and the Iteration-171 normalization `h_±=r±a/2` with factorial-normalized `Gamma_arr/2!` and `Gamma_aaa/3!`.

## New Iteration-172 results

| Result | Classification | Article-safe statement | Nonclaim |
|---|---|---|---|
| generic closed-unitary C4/C5 cubic amplitudes on six rows | exact comparator relation subspace | allowing an independent quantum cubic amplitude at every frozen row, all amplitude freedom obeying `Gamma_aar=0`, `Gamma_aaa=Gamma_arr/4` is removed by the relation map | not evidence that C4 and C5 have identical nonlinear dynamics |
| fixed PQCG tree nonlinear response | supported C3 relation direction | the classical Einstein drift produces one EH-shaped direction in `Gamma_aaa-Gamma_arr/4`, because the supported tree response has `Gamma_arr=B_EH` and no quantum `aaa` term | not the full C3 ordered/MSR comparator |
| source-completed Ward-lock coordinate | hard consistency gate | a nonzero WardLock is a consistency failure of the declared source/dynamics convention and cannot count as novelty | not a candidate residual |
| current relation-space complement | no novelty certificate | the first relation-level matrix is finite and rank-certified, but C3 diffusion/MSR, C4/C5 loop/noise and nonlinear nonlocal/AS CTP pieces remain BLOCKED | not evidence for ANSATZ-003 |

## Numerical certificate

Raw coordinates across six rows: `(Gamma_arr,Gamma_aar,Gamma_aaa,WardLock)`, yielding a `24x7` supported comparator matrix with rank `7/7` and `s_min/s_max=0.0126780602`.

Relation coordinates per row:

`(Gamma_aar, Gamma_aaa-Gamma_arr/4, WardLock)`.

The generic closed-unitary C4/C5 columns vanish exactly under this map. The fixed C3 tree leaves one relation direction with norm `4.917063349196141`; supported relation rank is `1`.

## New retained rules

`CTP-NG-003 — GENERIC_CLOSED_UNITARY_C4_C5_REMOVES_ROW_LOCAL_CUBIC_AMPLITUDE_BUT_NOT_RELATION_VIOLATIONS`.

`CTP-NG-004 — FIXED_PQCG_TREE_ADDS_ONE_EH_SHAPED_CLASSICAL_RELATION_DIRECTION`.

`NG-FUNNEL-032 — WARD_LOCK_VIOLATION_IS_CONSISTENCY_FAIL_NOT_NOVELTY`.

Retain `NG-FUNNEL-022` through `NG-FUNNEL-031` from earlier matrices.

## Article implication

The comparator funnel can now be stated at relation level: raw cubic amplitude is not the novelty carrier. Generic closed quantum C4/C5 amplitude freedom is quotientable while the fixed classical C3 tree occupies a distinct, known relation direction. A publishable Candidate Gravity claim must therefore be a source-completed relation that survives the missing ordered C3 and nonlinear nonlocal/AS comparator completions; Ward failure itself is disqualifying rather than novel.

## Current model-paper state

`ANSATZ-003` remains uncreated. Fisher/resources remain forbidden. `MODEL_READINESS: 24%` because comparator foundation is strong but no robust unique residual or parent Candidate Gravity dynamics has been established.
