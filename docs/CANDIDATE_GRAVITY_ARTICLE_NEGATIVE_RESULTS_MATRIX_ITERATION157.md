# Candidate Gravity Article Scaffold — Funnel / Negative-Result Matrix

**Iteration:** 157  
**Status:** working article table; scoped claims only.

| Construction | Furthest established point | Limiting / retained result | Article-safe interpretation |
|---|---|---|---|
| C3 PQCG linear stochastic | fixed `(D2,D0)`, scalar `N2` | rank `1/2`, only `5D2+D0` | regime-specific non-identifiability |
| C3 PQCG nonlinear symmetric | same covariant OM action | `(N2,C3sym_TT)` rank `2/2` | classical gravitational bispectrum can lift diffusion degeneracy |
| C3 PQCG tree causal response | same nonlinear Einstein drift | nonzero response but zero `(D2,D0)` tree tangent after GR calibration | common GR-boundary response; nonzero `chi2R` not a quantum witness |
| C3 diffusion-dependent ordered response | MSR/stochastic loop not derived | BLOCKED | unknown, not zero |
| C4 dRGT cubic TT | fixed ghost-free action, `(log m^2,alpha3)` | rank `2/2`; `alpha4` cubic-TT blind | positive nonlinear massive-spin-2 comparator |
| dRGT vs current C5 R^3 only | combined rank 4 | `C4-NG-002` | dRGT expands the implemented two-column C5 span |
| dRGT after common EH + gain quotient | combined rank 6, but strongly unequal residual conditioning | `C4-NG-003`; mass residual only `~0.13–0.32%`, alpha3 `~4.7–6.9%` | mass direction near-degenerate; alpha3 survives only scoped |
| C4 alpha4 | `L4` begins at quartic order | `C4-NG-001` | cubic TT protocol cannot identify alpha4 |
| C5 local nonlinear response | EH + `Ricci^3/Riemann^3` | `6x2`, rank `2/2`, Ward validated | scoped standard-QG EFT comparator |
| C5 higher local / loops | not implemented | BLOCKED | no full C5 closure |
| nonlocal / asymptotic-safety | classes known | no fixed finite tangent yet | next comparator target; labels are not matrices |

## New Iteration-156/157 article result

A fixed nonlinear dRGT comparator was mapped to the same six-probe TT response protocol. At `(m^2,alpha3,alpha4)=(0.04,0,0)`, the `(log m^2,alpha3)` tangent is rank `2/2`.

However, a stricter quotient including the common EH nonlinear response and a common response-gain nuisance changes the interpretation substantially.

Across raw coordinates and three invertible row normalizations:

- algebraic combined rank remains `6`;
- `log m^2` retains only `0.00126–0.00320` of its tangent norm outside the base span;
- `alpha3` retains `0.0472–0.0694`.

Thus an algebraically independent direction can still be nearly non-identifying after physically required shared-boundary/nuisance subtraction.

## Retained additions

- `C4-NG-001 — ALPHA4_CUBIC_TT_BLIND`.
- `C4-NG-002 — DRGT_EXPANDS_SCOPED_NONLINEAR_COMPARATOR_SPAN`.
- `C4-NG-003 — COMMON_BOUNDARY_GAIN_NEARLY_ABSORBS_MASS_DIRECTION`.
- `NG-FUNNEL-014 — ALGEBRAIC_RANK_REQUIRES_CONDITIONING_AUDIT`.

## Current model-paper implication

The funnel has now produced concrete examples of four distinct failure/limitation types:

1. genuine consistency failure (`CTP-001` Lorentzian pole);
2. exact comparator identity (`KL-002` Gaussian mediator identity);
3. regime/order-specific blindness (`alpha4` at cubic TT, `D2/D0` with one `N2` coordinate);
4. algebraically independent but near-degenerate directions after common-boundary/nuisance subtraction (dRGT mass direction).

This strengthens the eventual Candidate Gravity article: a future candidate must not merely add rank; it must leave a quantitatively robust residual after fixed strong comparators and nuisance directions are removed.

Full C3/C4/C5 closure is still incomplete, so Fisher/resources and `ANSATZ-003` remain inadmissible.
