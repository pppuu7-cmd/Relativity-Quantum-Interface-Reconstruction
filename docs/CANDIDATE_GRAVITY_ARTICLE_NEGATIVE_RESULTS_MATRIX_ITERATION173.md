# Candidate Gravity Article Matrix — through Iteration 173

## Forward-facing provenance

Preserve all earlier provenance corrections, especially `PROVENANCE-CORR-001`, the Iteration-166/167 definition of `A_odd` as the frequency-odd imaginary part of linear `chi1R`, and the Iteration-171 normalization `h_±=r±a/2` with factorial-normalized metric CTP vertices.

## New Iteration-173 results

| Result | Classification | Article-safe statement | Nonclaim |
|---|---|---|---|
| naive generalized-DeWitt JD/MSR completion of PQCG OM dynamics | literature-established mismatch | the published stochastic-mode analysis shows that the naive JD/MSR functional does not reproduce the OM two-point function; a conserved diffusion matrix is required | not a consistency failure of the full PQCG program |
| fixed linear covariance + nonlinear Einstein drift | underdetermined cubic response-field completion | lower-order authority fixes the linear Hessian and one-response-field drift vertex but not the two-response-field cubic vertex if the nonlinear conserved diffusion kernel is unspecified | not evidence that the missing vertex is zero |
| classical MSR response field vs RQIR metric CTP `a` leg | protocol mapping blocker | an explicit same-convention map is required before response-field vertices can be entered as metric CTP relation columns | not license to identify the fields by notation |
| Iteration-172 relation complement | no novelty certificate | the supported C3 tree direction remains valid, but the diffusion-dependent C3 ordered relation remains BLOCKED_NOT_ZERO | not evidence for `ANSATZ-003` |

## Structural certificate

For the minimal response functional

`S=t*(L*h+g*h^2/2-J)-1/2*t*(D0+lambda*h)*t`,

the background Hessian is

`[[0,L],[L,-D0]]`,

independent of `lambda`, while cubic vertices satisfy

`Gamma_t_h_h=g`, `Gamma_t_t_h=-lambda`, `Gamma_t_t_t=0`.

Therefore identical linear noise/response data and identical nonlinear drift do not fix the two-response-field cubic vertex.

## New retained rules

`C3-NG-005 — LINEAR_NOISE_PLUS_NONLINEAR_DRIFT_DO_NOT_FIX_ORDERED_MSR_CUBIC_VERTEX`.

`NG-FUNNEL-033 — OM_TO_MSR_CUBIC_COMPLETION_REQUIRES_NONLINEAR_CONSERVED_DIFFUSION_AND_EXPLICIT_CTP_MAP`.

Retain `CTP-NG-001` through `CTP-NG-004` and all earlier funnel rules.

## Article implication

The C3 comparator cannot be strengthened by zero-filling the missing ordered relation. The scientifically correct statement is that the fixed published PQCG authority has reached an operational boundary at nonlinear conserved diffusion / CTP mapping. This is useful negative information because it prevents a false Candidate Gravity residual from being manufactured by an incomplete classical comparator.

## Current model-paper state

`ANSATZ-003` remains uncreated. Fisher/resources remain forbidden. `MODEL_READINESS: 24%`: comparator foundation is strong and increasingly well-audited, but no robust unique residual or parent Candidate Gravity dynamics exists.
