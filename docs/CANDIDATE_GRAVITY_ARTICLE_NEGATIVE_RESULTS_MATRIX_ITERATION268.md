# Candidate Gravity article / negative-results matrix — Iteration 268

**Date:** 2026-09-02  
**MODEL_READINESS: 24%**

## New scoped result

Iteration 268 converts the Iteration-267 momentum-routing rule into an explicit physical orbit-metric inverse kernel layer. Physical `N1/N2` are extracted from the same finite-amplitude curved minimal ghost operator and exact `N_orb=W^-1 Nhat` factorization; `Q1/Q2` follow only from routed inverse recursion.

Freeze:

`PASS_SCOPED_PHYSICAL_ROUTED_N1_N2_Q1_Q2_KERNEL_LAYER`.

All frozen physical `Q1` and mixed `Q2` kernels are nonzero. First- and second-order coefficient identities from `NQ=I` close at machine precision (`<=8.89e-16`). A same-loop-momentum shortcut fails with O(0.2-0.9) residuals, so endpoint/intermediate momentum dependence is mandatory rather than a bookkeeping preference.

Guardrail:

`Q0 MUST BE EVALUATED AT EACH ROUTED ENDPOINT/INTERMEDIATE MOMENTUM; SAME-p RESOLVENT INSERTION IS FALSE`.

## Article-use classification

This strengthens a publishable methodological result for the C5 comparator construction: condensed-index Fourier support changes the inverse-resolvent algebra itself. A calculation that uses one fixed loop-momentum `Q0` through a background-shifting composite operator can satisfy local matrix manipulations while violating the actual convolution inverse relation.

This remains a scoped comparator-building result, not a nonzero final C5 coordinate, not a new-gravity residual, not a consistency FAIL, not an exact comparator identity and not a novelty certificate.

## Funnel status

- C3: retained formal nonlinear conserved-completion underdetermination blocker.
- C4: retained mediator-degenerate positive two-point information.
- C5: operationally BLOCKED, NOT ZERO; routed `N/Q` is now closed, routed physical `K/A`, assembled `B3`, tensor reduction and source projection remain open.
- nonlocal / asymptotic-safety comparator routes: unchanged frozen state.
- Candidate residual: absent.
- `ANSATZ-003`: not created.
- Fisher/resources: forbidden.

MODEL_READINESS: 24%

Change from Iteration 267: 0 percentage points. The physical routed inverse-resolvent subblock is closed and a false implementation class is excluded, but no readiness-rubric category closes.
