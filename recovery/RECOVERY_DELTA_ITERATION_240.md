# Recovery Delta — RQIR Iteration 240

Date: 2026-09-01

MODEL_READINESS: 24%

## New authority

Iteration 240 starts from authoritative Iteration 239 and audits only relational/asymptotic pure-gravity gauge-safe observables for an exact identity-preserving realization of frozen

`T_cut = D_s Gamma3_ret,soft - W[D_s K2]`.

Current 2026 relational authority shows that gauge-invariant effective actions can be defined by coupling sources to frame-dressed relational observables. Current 2026 null-infinity gravity formulations use Bondi/radiative data and avoid bulk/off-shell propagators.

## New result

Neither route is identical to frozen `T_cut` under current published authority:
- relational gauge safety changes the nonlinear source variable; functional derivatives acquire frame/dressing Jacobian and contact contributions, and no same-parent cubic source-reduction identity to bare-metric `h^3/h^2` was found;
- asymptotic/null-infinity gauge safety changes to boundary/on-shell radiative variables and therefore changes functional and analytic structure relative to the frozen bulk/source-completed hard-channel 1PI coordinate.

Freeze:

`OPERATIONALLY_NONEXECUTABLE_WITH_CURRENT_PUBLISHED_AUTHORITY`

and retain

`BLOCKED_NOT_ZERO`.

Also retain the more specific upstream status

`BLOCKED_T_CUT_NATIVE_H3_EXECUTABILITY_AT_GAUGE_SAFE_CUBIC_EFFECTIVE_ACTION_BOUNDARY`.

## New labels

- `REL-NG-020` — relational gauge safety changes nonlinear source variables and requires frame-dependent Jacobian completion;
- `REL-CUT-020` — null-infinity gravity is physical but has boundary/on-shell analytic structure rather than frozen bulk `T_cut`;
- `REL-BLOCK-005` — operationally non-executable with current published authority;
- `NG-FUNNEL-096` — no audited gauge-safe route gives an identity-preserving `h^3/h^2` linked coordinate without a new model/convention choice.

## Classification guardrails

This is operational BLOCKED/current-authority. It is not consistency FAIL, exact comparator identity, regime-specific non-identifiability, near-degeneracy, zero comparator column, or Candidate Gravity novelty.

## Candidate state

- robust Candidate Gravity residual: `NONE`;
- `ANSATZ-003`: `NOT_CREATED`;
- Fisher/resources: `FORBIDDEN`;
- heavy computation: not launched because comparator executability is upstream.

## Readiness

MODEL_READINESS: 24%

Change from Iteration 239: 0 percentage points. Comparator foundation remains `24/25`; robust unique residual remains `0/20`. The frozen linked target's operational status is now fully explicit under current authority, but no rubric block closes.

## Exact restart instruction

Iteration 241: audit the retained Candidate Gravity funnel itself. Determine whether any previously retained residual target is simultaneously (i) algebraically nonzero before profiling and (ii) executable under frozen C3/C4/C5/nonlocal/AS authority. Do not search for another proxy observable and do not redefine `T_cut`. If none survives, freeze `NO_EXECUTABLE_RESIDUAL_TARGET_UNDER_CURRENT_COMPARATOR_AUTHORITY` and separate future work into an authority-improvement program. Do not create `ANSATZ-003`; do not run Fisher/resources.