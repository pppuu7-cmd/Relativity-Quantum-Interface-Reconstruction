# Recovery Delta — RQIR Iteration 251

**Date:** 2026-09-01  
**MODEL_READINESS: 24%**

## Starting authority

Actual completed repository front was Iteration 250. It proved that surviving `e=1/e=2` nonlinear-EOM sectors require no scalar loop polygon beyond the already frozen raised bubble/triangle families and authorized a small explicit numerator-library calculation.

## New closed unit

In frozen `D=4`, `Lambda=0`, DeWitt `a=-1/2`, signature `(-,+,+,+)` convention, with

`N^alpha_beta = delta^alpha_beta Box + R^alpha_beta`,

the first TT-background variation of the ghost operator is now explicit:

`delta N^alpha_beta = (epsilon^{mu nu} p_mu p_nu) delta^alpha_beta - (p.q) epsilon^alpha_beta - q_beta p_mu epsilon^{alpha mu} + q^alpha p_mu epsilon^mu_beta`.

An independent finite-difference evaluation of the full covariant vector Laplacian plus Ricci term on the frozen hard TT mode agrees with this analytic vertex to `8.01e-11` maximum component error at step `1e-6`.

Freeze classification:

`PASS_SCOPED_E1_GHOST_RESOLVENT_VERTEX_FREEZE_AND_TT_VALIDATION`.

The resolvent identity

`delta(N^-1) = -N0^-1 (delta N) N0^-1`

must be inserted on **both** ghost inverse factors of `U1` in the cubic `E^(2) K^(1)` sector. The insertion raises/repeats an existing ghost segment and creates no fourth independent loop-momentum corner.

## Guardrail

This is only the ghost-resolvent component of the `E^(2) K^(1)` `Tr U1` numerator. It is not the complete `U1`, not a C5 comparator closure, not an exact comparator identity, not a consistency FAIL, and not Candidate Gravity novelty.

Retain umbrella blocker:

`BLOCKED_4D_EINSTEIN_VD_RESOLVENT_VERTEX_LIBRARY_TENSOR_REDUCTION_AND_SOURCE_PROJECTION`,

`BLOCKED_NOT_ZERO`.

## Remaining mandatory C5 work

1. derive `delta[R^i_gamma (D_i R^j_delta)] E^(2)_j`;
2. derive `delta Y * E^(2)`;
3. combine all `E^(2) K^(1)` placements into one complete `Tr U1` numerator;
4. retain flat-kernel `E^(3)` and surviving `E^(1)K^(2)` terms;
5. complete `e=2` numerator blocks;
6. perform tensor reduction only after the numerator/Ward library is closed;
7. source/Ward/contact completion and Lorentzian hard-channel discontinuity remain downstream.

## Readiness

`MODEL_READINESS: 24%` — unchanged from Iteration 250. A real numerator subblock closed, but no rubric block closed: comparator foundation remains `24/25`, robust residual `0/20`.

## Exact restart instruction

Resume at **Iteration 252**. Stay in the same convention and finish the other first-order kernel/vertex variations of the `E^(2) K^(1)` `Tr U1` block, especially `delta[R(DR)]` and `delta Y`. Combine them with both frozen `delta N^{-1}` placements and perform a condensed-index/Ward consistency test. Do not integrate heavily, do not launch Fisher/resources, and do not create `ANSATZ-003`.
