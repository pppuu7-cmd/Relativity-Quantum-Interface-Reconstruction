# Recovery Delta — RQIR Iteration 170

**Date:** 2026-08-31  
**MODEL_READINESS: 24%**

## Previous front

Iteration 169 profiled the leading and next-order massless C5 absorptive envelopes in the eight-row conserved-TT timelike protocol, leaving five open frequency-shape dimensions.

## New theorem-level result

The remaining linear spectral shape space cannot by itself support gravity-specific novelty against C4.

For any physical conserved-traceless TT two-point response admitting a positive Källén–Lehmann measure,

`chi_R^TT = Z0 D_R(0) + int dmu2 rho_TT(mu2) D_R(mu2)`, `rho_TT>=0`,

construct a direct integral of independent positive-norm massive spin-2 mediator fields with coupling density `sqrt(rho_TT)`.

The mediator retarded response is exactly the same two-point kernel. With matching Gaussian covariance/state, its Hadamard kernel and full Gaussian CTP influence functional are also identical.

This generalizes the Iteration-141 exact `KL-002` C4/mediator identity from one frozen spectral density to arbitrary positive TT spectral measures.

## Consequences

Within the current linear TT sector, all of the following remain C4-reproducible:

- positive narrow poles;
- positive continua;
- thresholds;
- branch cuts;
- arbitrary positive finite-frequency spectral shape;
- any vector surviving the Iteration-169 C5 shape profile, if it is only a linear positive spectral response.

A non-positive spectral density is not a novelty certificate. It triggers separate physical-observability/gauge/positivity/ghost/unitarity gates.

## Retained results

- `C4-NG-008 — POSITIVE_LINEAR_TT_SPECTRAL_RESPONSE_IS_EXACTLY_REPRESENTABLE_BY_ORDINARY_MEDIATOR_CONTINUUM`;
- `ABS-SHAPE-005 — FINITE_FREQUENCY_LINEAR_SPECTRAL_SHAPE_CANNOT_CERTIFY_GRAVITY_SPECIFIC_NOVELTY_AGAINST_C4`;
- `NG-FUNNEL-030 — LINEAR_SPECTRAL_RESIDUAL_REQUIRES_A_LINKED_NONLINEAR_OR_POST_GAUSSIAN_GRAVITY_RELATION_FOR_PROMOTION`.

## Strategic change

Do not continue Candidate Gravity design by searching for a special linear spectral shape.

Finite-frequency Lorentzian-AS reproduction is no longer a prerequisite for **candidate promotion** in the linear sector, although it remains useful comparator/article work.

Return to linked multi-point RQIR observables generated by one parent dynamics:

`(chi1R, C3sym, chi2R, Ward/soft/constraint relation, ...)`.

The two-point spectrum is now a calibrated/shared input, not the novelty carrier.

## Readiness

`MODEL_READINESS: 24%` — unchanged.

Comparator foundation remains `24/25`; robust unique residual remains `0/20`; ANSATZ, candidate consistency, Fisher and resources remain zero.

## Exact restart instruction

Resume at **Iteration 171 — linked multi-point residual protocol**.

Required order:

1. inherit the positive/calibrated TT two-point spectral kernel as non-novel shared data;
2. freeze a finite source-completed symmetric `C3sym` coordinate and ordered `chi2R` coordinate derived from the same dynamics;
3. define relation-level rather than independent-coordinate comparator tangents;
4. instantiate the strongest available C3/C4/C5 comparator relation blocks;
5. preserve unsupported C3/C4/nonlocal/AS higher-point entries as BLOCKED;
6. only a residual in the linked multi-point relation quotient may earn robust-residual readiness points or motivate `ANSATZ-003`.
