# Recovery Delta — RQIR Iteration 250

**Date:** 2026-09-01  
**MODEL_READINESS: 24%**

## Starting authority

Iteration 245 reduced the flat cubic Vilkovisky composite-trace sector to raised-power bubbles/triangles (`I22`, `I222`, `I212`) with no scalar loop polygon beyond triangle at total cubic background order. Iterations 246–247 showed that the three-EOM sector is null-soft killed but `e=1/e=2` are not guaranteed to vanish. Iterations 248–249 supplied an explicit nonlinear Einstein certificate: `G^(2)[h_s,h_h]` is nonzero for a null-soft TT mode plus hard TT mode and survives both frozen TT contractions.

## New conclusion

The surviving `e=1/e=2` nonlinear-EOM insertions modify local tensor numerators/background vertices and connection/kernel coefficients but do not introduce a fourth independent loop-momentum corner. Therefore no new scalar master polygon beyond the already authorized bubble/triangle families is required at the frozen cubic order.

Classification:

`PASS_SCOPED_E1_E2_NO_NEW_LOOP_POLYGON_BEYOND_TRIANGLE`.

This is a computational reduction, not a comparator closure and not Candidate Gravity novelty.

## Remaining C5 work

1. build the 4D pure-Einstein Vilkovisky tensor numerator library in the frozen `a=-1/2`, `Lambda=0` convention;
2. instantiate `e=1/e=2` nonlinear EOM and connection/kernel coefficients;
3. reduce the resulting tensor integrals onto the known raised-power bubble/triangle basis;
4. apply source/Ward/contact completion;
5. perform the Lorentzian retarded hard-channel discontinuity projection in the frozen RQIR observable.

## Readiness

`MODEL_READINESS: 24%` — unchanged. Comparator foundation remains `24/25`; no robust residual exists.

## Exact restart instruction

Resume at **Iteration 251**: construct the first minimal pure-gravity Vilkovisky numerator block for an `e=1` mixed soft-hard insertion, with all index/pole/source conventions frozen before integration. Prefer a small symbolic certificate over a full heavy run. Do not introduce new master topology unless the explicit algebra proves it unavoidable.
