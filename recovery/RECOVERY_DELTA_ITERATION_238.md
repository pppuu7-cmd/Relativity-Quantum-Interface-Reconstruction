# Recovery Delta — RQIR Iteration 238

Date: 2026-09-01

MODEL_READINESS: 24%

## New authority

Iteration 238 starts from authoritative Iteration 237 and completes the generalized-LSZ causal-response identity audit for the minimally coupled massive-scalar GR branch.

Published causal-response authority confirms:
- asymptotic gravitational radiation is an in-in observable obtained from generalized LSZ or amplitudes plus products/cuts;
- the directly causal formulation uses a soft limit of five-point amputated response functions;
- the causal completion restores retarded boundary conditions but does not change external field species or valence.

## New result

The completed massive-scalar radiation observable remains schematically a mixed `phi^4 h` causal response. Frozen RQIR requires a pure-gravity `h^3` retarded response linked to the same-parent `h^2` inverse kernel:

`T_cut = D_s Gamma3_ret,soft - W[D_s K2]`.

Soft factorization and generalized LSZ do not imply either

`phi^4 h -> h^3`

or

`phi^4 -> h^2`.

No unique same-parent source-reduction identity producing those maps was found in the audited authority. Adding one would be a new comparator/source model choice.

Freeze:

`CAUSAL_RESPONSE_BRANCH_PHYSICALLY_VALID_BUT_T_CUT_VALENCE_INCOMPATIBLE`

and

`BLOCKED_COMPARATOR_INCOMPATIBLE_FUNCTIONAL_VALENCE_MIXED_PHI4H_VS_H3_LINKED_H2`.

## New labels

- `REL-NG-018` — causal completion does not change functional valence;
- `REL-CUT-018` — massive-scalar causal radiation is executable but not frozen linked gravity cut coordinate;
- `REL-BLOCK-003` — frozen `T_cut` requires native `h^3` linked to `h^2`;
- `NG-FUNNEL-094` — physical calculability is insufficient when observable identity fails.

## Classification guardrails

This is comparator incompatibility / observable-identity failure for the proposed branch. It is not consistency FAIL of GR, exact comparator identity, regime-specific non-identifiability, near-degeneracy, zero comparator column, or Candidate Gravity novelty.

## Candidate state

- robust Candidate Gravity residual: `NONE`;
- `ANSATZ-003`: `NOT_CREATED`;
- Fisher/resources: `FORBIDDEN`;
- heavy computation: not launched because observable identity fails upstream.

## Readiness

MODEL_READINESS: 24%

Change from Iteration 237: 0 percentage points. Comparator foundation remains `24/25`; robust unique residual remains `0/20`. The branch has been removed from the promotable `T_cut` path, but no rubric block closes.

## Exact restart instruction

Iteration 239: search only for a **native causal pure-gravity `h^3` response linked to the same-parent `h^2` kernel** in one fixed convention. Prefer Schwinger-Keldysh/causal perturbative Einstein gravity or gravity EFT. Require explicit retarded leg assignment, hard-channel discontinuity, source/Ward completion and common normalization. Do not reopen the massive-scalar `2 -> 3` proxy, do not redefine `T_cut`, do not create `ANSATZ-003`, and do not run Fisher/resources.