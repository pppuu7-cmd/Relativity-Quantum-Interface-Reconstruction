# Recovery Delta — RQIR Iteration 174

**Date:** 2026-08-31  
**MODEL_READINESS: 24%**

## Previous front

Iteration 173 established a genuine comparator blocker, not a zero column: the fixed PQCG linear conserved-diffusion authority does not uniquely determine its nonlinear ordered metric-CTP cubic completion.

## New nonlocal result

Use the fixed covariant comparator `QG-NL-EXP-001`:

`S = Mpl^2/2 int sqrt(-g) [R + G_mn F(Box) R^mn] + S_m`,

`F(Box)=(exp(-lambda Box)-1)/Box`.

The cubic nonlocal action contains

`G2 F0 R1 + G1 F0 R2 + sqrtg1 G1 F0 R1 + G1 (delta F)_1 R1`.

The operator variation is exactly

`delta F(A)=int_0^lambda dalpha int_0^alpha du exp(-(alpha-u)A)(delta A)exp(-uA)`.

Its eigenmode coefficient is the divided difference `[F(a)-F(b)]/(a-b)`; the six-point validator, including the diagonal limit, agrees at floating-point zero error.

Thus propagator-only information misses a genuine cubic operator-insertion structure. A full covariant parent action is required.

For this exact frozen comparator the tree cubic is fixed in principle because no independent curvature-cubic potential is included. For the broad weakly-nonlocal class, the two-point form factor does not uniquely fix nonlinear response because independent higher-curvature potentials/form factors can enter without changing the quadratic TT kernel.

## Current relation quotient result

Iteration 172 relation coordinates are

`R_aar=Gamma_aar`,

`R_unit=Gamma_aaa-Gamma_arr/4`,

`R_W=WardLock`.

A closed-unitary cubic action gives `Gamma_aar=0`, `Gamma_aaa=Gamma_arr/4`. A source-completed diffeomorphism-invariant action gives `WardLock=0`.

Therefore the fixed nonlocal tree comparator is annihilated exactly by the current relation map independently of raw cubic amplitude.

Certificate:

- raw six-row amplitude rank `6`;
- relation rank `0`;
- maximum relation entry `0.0`.

This does NOT mean the nonlocal cubic amplitude is zero.

## Retained results

- `NL-NG-003 — COVARIANT_NONLOCAL_CUBIC_VERTEX_CONTAINS_OPERATOR_FRECHET_VARIATION_NOT_VISIBLE_IN_PROPAGATOR_ONLY_REASONING`;
- `CTP-NG-005 — CLOSED_UNITARY_DIFFEO_INVARIANT_NONLOCAL_TREE_ACTION_IS_ANNIHILATED_BY_CURRENT_COARSE_CTP_RELATION_MAP`;
- `NG-FUNNEL-034 — ZERO_WARD_LOCK_PLUS_GENERIC_UNITARY_RA_RELATION_CANNOT_DISTINGUISH_QUANTUM_GRAVITY_FAMILIES`.

## Strategic consequence

The current coarse relation map is now itself a limiting factor. Generic closed-unitary `r/a` structure plus a scalar zero Ward violation is shared by C4, C5 and covariant nonlocal quantum gravity.

The next novelty search must decompose the **tensor cubic vertex** into:

1. Ward-determined longitudinal part fixed by the same inverse two-point kernel;
2. independent transverse/off-shell cubic form factors.

Only the second part can carry a new relation after exact consistency projection.

## Readiness

`MODEL_READINESS: 24%` — unchanged.

No robust residual or parent dynamics exists.

## Exact restart instruction

Resume at **Iteration 175 — tensor/soft Ward decomposition**.

Required order:

1. freeze the 1PI diffeomorphism Ward identity in the existing source-completed metric convention;
2. construct a finite map from the two-point inverse kernel to the longitudinal soft cubic vertex;
3. project this consistency-fixed component out before comparator ranking;
4. define transverse cubic relation coordinates on the same frozen kinematics;
5. audit fixed C4/C5/nonlocal support in that transverse sector;
6. keep C3 ordered completion and AS real-time three-point components BLOCKED unless explicitly derived;
7. no `ANSATZ-003`, Fisher or resources before a nonzero full comparator-subtracted residual.
