# Recovery Delta — RQIR Iteration 234

**Date:** 2026-09-01  
**MODEL_READINESS: 24%**

## New authority

Iteration 234 starts from authoritative Iteration 233 and audits whether the 2026 physical AS scalar-scattering/timelike scalar–graviton-vertex result can directly populate the frozen linked RQIR discontinuity comparator.

Primary authority: Chiesa, Pawlowski, Reichert, arXiv:2603.10168.

The paper now supplies a same-construction RG-invariant Lorentzian graviton-mediated scalar amplitude. This is stronger AS physical authority than Euclidean-only multigraviton data or a standalone propagator.

## Decisive scope test

Frozen RQIR target:

`T_cut = D Gamma3_ret,soft - W[D K2]`.

The AS scalar amplitude instead uses `phi-phi-h` vertices plus a graviton propagator. It is not the same nonlinear observable and must not replace the frozen linked relation.

Its timelike vertex is reconstructed from Euclidean data using a procedure that the paper explicitly states is non-unique and may introduce artefacts. The paper does not provide an explicit retarded/in-in or `Disc_s` prescription for the frozen linked object. Tested spectral-function reconstructions are reported as unstable or highly oscillatory. Therefore a discontinuity cannot be promoted by choosing one continuation ansatz.

The full scalar amplitude would also contain `A4`; the reported work neglects that contact term and notes that it is required to address the forward divergence.

## Sharpened blocker

Retain umbrella:

`BLOCKED_AS_REALTIME_RELATION_COMPLETION`

Retain:

`BLOCKED_NOT_ZERO`

Sharpen substatus to:

`BLOCKED_AS_LINKED_RETARDED_DISCONTINUITY_MAP_DESPITE_PHYSICAL_SCALAR_AMPLITUDE`

## New labels

- `AS-NG-006` — same-construction physical Lorentzian scalar amplitude exists;
- `AS-NG-007` — timelike scalar-vertex reconstruction does not certify the RQIR retarded discontinuity;
- `REL-NG-014` — physical scalar scattering is not observable-identical to the frozen linked nonlinear graviton relation;
- `AS-BLOCK-002` — remaining AS gap is linked retarded/discontinuity/source-Ward completion;
- `NG-FUNNEL-090` — stronger AS physical authority narrows a blocker but does not create Candidate Gravity novelty.

## Classification guardrails

This is operational/scientific `BLOCKED`, not a consistency FAIL of AS, not exact comparator identity, not regime-specific non-identifiability, not near-degeneracy, and not a zero AS column.

## Candidate state

- robust Candidate Gravity residual: `NONE`;
- `ANSATZ-003`: `NOT_CREATED`;
- Fisher/resources: `FORBIDDEN`.

## Readiness

`MODEL_READINESS: 24%` — unchanged from Iteration 233. Comparator foundation remains `24/25`; robust unique residual remains `0/20`. The AS authority boundary is more precise, but the frozen comparator coordinate did not close.

## Exact restart instruction

Iteration 235: audit whether any same-parent AS calculation supplies a Lorentzian nonlinear **three-graviton** quantity with explicit retarded/spectral discontinuity and a normalization link to the two-point sector. Do not synthesize separate Euclidean three-/four-graviton vertices with an independently reconstructed Lorentzian propagator. If the object is absent, freeze AS at `BLOCKED_AS_LINKED_RETARDED_DISCONTINUITY_MAP_DESPITE_PHYSICAL_SCALAR_AMPLITUDE` and redirect the next primary effort to an executable linked nonanalytic comparator branch. Do not create `ANSATZ-003`; do not run Fisher/resources.
