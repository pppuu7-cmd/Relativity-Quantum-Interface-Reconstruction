# Recovery Delta — RQIR Iteration 164

**Date:** 2026-08-31  
**MODEL_READINESS: 23%**  
**Authoritative change:** the six-row TT saturation from Iteration 163 is not stable under a target-independent enrichment to 12 frozen TT rows; the corrected dRGT tangent columns become algebraically independent of the implemented local-C5+shared-boundary span, but the residual is not promotable until the full enriched comparator quotient is closed.

## New authorities

- `analysis/enriched_tt_protocol_iteration164.py`;
- `results/enriched_tt_protocol_iteration164.json`;
- `candidate_gravity/ENRICHED_TT_PROTOCOL_ITERATION164.md`;
- `research_log/2026-08-31_iteration_164_enriched_tt_protocol.md`;
- `recovery/RECOVERY_DELTA_ITERATION_164.md`.

## Mandatory provenance correction

Iteration 163 used the correct numerical arrays but mislabeled the two dRGT target columns.

Correct mapping from Iteration 156:

- first target = `d/d log(m^2)`;
- second target = `d/d alpha3`;
- `alpha4` is **cubic-TT blind** because `L4[K]` starts quartic.

Do not rewrite the historical Iteration-163 result; preserve it and cite `PROVENANCE-CORR-001` in all forward-facing summaries.  Its six-row numerical saturation remains unchanged.

## Frozen enriched rows

Keep the original six rows and add exactly six deterministic rows generated by seed `164031`.

Proposal:

- time components uniform in `[0.08,0.24]`;
- spatial components iid uniform in `[-0.68,0.68]`.

Acceptance, evaluated before any target residual:

- every `p^2,q^2,r^2` in `[0.18,1.05]`;
- spatial `|cos(q,r)| <= 0.82`;
- invariant ratio `max/min <= 4`.

Take the first six accepted proposals; this occurs after 16 proposals.  New polarization seeds are `2000+3*i+leg`.

## Core certificate

Enriched matrix:

`M12=[EH,Ricci^3,Riemann^3,Ricci^2_full,Ricci Box Ricci_full,dRGT_shared_reference]`, shape `12x6`.

Correct targets:

`T=[dRGT_dlogm2,dRGT_dalpha3]`.

All four fixed conditioning audits give `rank(M12)=6`, while `rank([M12,T])=8`.

Residual fractions across audits:

- `dlogm2`: `7.456744575e-4` to `4.741204454e-3` — nonzero but near-degenerate;
- `dalpha3`: `6.742776307e-3` to `9.142722672e-2` — scoped residual survives the implemented enriched local quotient.

Raw values:

- `dlogm2`: `1.478538450e-3`;
- `dalpha3`: `3.903479968e-2`.

Progressively adding the pre-frozen rows gives combined rank 7 with one new row and rank 8 with two through six new rows.

## Retained results

- `C4-NG-005 — SIX_ROW_SATURATION_DOES_NOT_PERSIST_UNDER_TARGET_INDEPENDENT_TT_ROW_ENRICHMENT`;
- `C4-NG-006 — DRGT_DLOGM2_DIRECTION_IS_NEAR_DEGENERATE_AFTER_ENRICHED_LOCAL_C5_QUOTIENT`;
- `NG-FUNNEL-021 — PROTOCOL_SATURATION_MUST_BE_TESTED_FOR_STABILITY_UNDER_PRE_FROZEN_ROW_ENRICHMENT`;
- `PROVENANCE-CORR-001 — ITERATION163 TARGET LABELS ARE DLOGM2 AND DALPHA3; ALPHA4 REMAINS CUBIC_TT BLIND`.

## Scientific scope

The Iteration-163 saturation remains a correct **six-row finite-protocol** result.

The Iteration-164 `dalpha3` residual is **not** a gravity-novelty certificate because the 12-row C3 diffusion-dependent, nonlocal causal, AS retarded, full C5 loop/non-TT, and dRGT helicity sectors are not all instantiated.  Missing entries are BLOCKED, not zero.

No `ANSATZ-003`.  No Fisher.  No resources.

## Readiness

`MODEL_READINESS: 23%`, up from 22%.

- comparator foundation `23/25`;
- robust unique residual `0/20`;
- frozen parent dynamics `0/20`;
- candidate consistency `0/15`;
- Fisher `0/10`;
- resources `0/10`.

The +1 is comparator-foundation progress only.  Do not award residual points until the complete enriched comparator quotient exists.

## Exact restart instruction

Resume at **Iteration 165** on the same 12 frozen rows.

1. Treat the C3 tree ordered response as the already-present common EH boundary after hard Newton calibration; do not add a duplicate nuisance column.
2. Attempt the strongest physically declared weakly-nonlocal and/or asymptotic-safety causal ordered-response completion on these rows.
3. Do **not** invent a retarded Green-function prescription merely to fill a matrix column; an underdetermined causal prescription remains a blocker.
4. Recompute the enriched fixed comparator rank only after a comparator column is physically supported.
5. If the `dalpha3` residual survives complete C3/C4/C5/nonlocal/AS subtraction, then and only then may residual-readiness points and parent-ansatz design resume.
