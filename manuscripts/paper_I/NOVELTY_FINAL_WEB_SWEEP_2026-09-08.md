# RQIR Paper I — final web-index novelty sweep

Date: 2026-09-08
Status: completed targeted web-index sweep; non-exhaustive priority audit

## Target conjunction

The search target was not any individual ingredient, but the conjunction used by Paper I:

1. finite source calibration map on physically admissible source-state perturbations;
2. a physical calibration null direction;
3. a positivity-preserving pair of calibration-equivalent states;
4. a separate ordered/retarded-response discriminator evaluated on that null direction;
5. a detector-facing constructive witness; and
6. calibration steering that rotates the unresolved direction while the source is held fixed.

## Search families

Targeted searches were run across general web/scholarly indexing, arXiv-focused results and INSPIRE-focused results using combinations of:

- `calibration nullspace` + quantum states / density matrix / response / positivity;
- `calibration-equivalent` + quantum response;
- `finite calibration map` + quantum nullspace / response;
- `ordered response` + tomography / nullspace;
- `retarded response` + informationally incomplete tomography;
- `physical null direction` + quantum state calibration;
- `quotient` + calibration uncertainty + quantum response / nullspace.

## Closest classes recovered

The final sweep recovered the same neighboring classes already documented in `NOVELTY_AUDIT_2026-09-08.md`:

- informationally incomplete quantum tomography;
- positivity-constrained quantum-state reconstruction;
- kernels/invertibility of retarded response functions (Giesbertz 2015/2016);
- quotient geometry under calibration/nuisance uncertainty (Wani & Al-Kuwari 2026).

No indexed result located in this sweep contained the full Paper-I conjunction above.

## Interpretation

This supports the manuscript's current **conjunctive** novelty wording. It does not justify an absolute priority statement such as “first ever”. The defensible claim remains that Paper I combines established ingredients into a calibration-aware gravity–quantum source reconstruction construction with a physical positive-state nullspace witness, separate ordered-response discriminant and calibration steering.

## Decision

- Keep C10 at `AMBER-GREEN` rather than over-promoting it to unconditional priority.
- Keep all existing non-claims concerning tomography, positivity, response kernels and quotient geometry.
- Do not use “first”, “unprecedented”, or equivalent absolute priority language unless a final journal-specific database/expert check supports it.
- The current novelty wording is submission-usable even if the priority audit remains conservatively non-exhaustive.
