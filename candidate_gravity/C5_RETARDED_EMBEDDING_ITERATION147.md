# Candidate Gravity — Iteration 147: retarded C5 embedding audit

**Date:** 2026-08-31  
**Status:** partial scientific closure with an exact operational blocker; no Candidate-Gravity promotion claim.

## Objective

Iteration 146 produced a full-rank finite **on-shell four-graviton** local-EFT tangent, but the post-Gaussian RQIR quotient requires a causal ordered second-order response `chi^(2)R`. This iteration freezes one real-time prescription and derives the exact tree-level retarded response factorization from the same EH + local-EFT dynamics, then audits whether the Iteration-146 frozen kinematics are sufficient to evaluate the finite RQIR `chi2R_even/odd` coordinates.

## Frozen CTP / response convention

For this comparator step use:

- background: D=4 Minkowski;
- state: interacting in-vacuum, adiabatically connected to the free graviton vacuum;
- perturbative evaluation gauge: de Donder;
- physical external sources/projectors: conserved;
- source convention: `S_J = integral d4x J_A h^A`;
- retarded response: generated by the Schwinger–Keldysh/in-in prescription, with retarded rather than Feynman propagators on response legs;
- dynamics: the Einstein–Hilbert action plus the same parity-even local gravitational EFT basis/order frozen in Iteration 146.

This convention is compatible with the standard use of Schwinger–Keldysh methods for initial-value gravitational observables, where retarded propagators arise directly in the in-in formulation.

## Tree-level nonlinear retarded kernel

Use condensed DeWitt indices `A=(mu nu,x)`. Expand the gauge-fixed equation of motion in the convention

`K h + (1/2) V[h,h] + J = 0`.

At first order,

`h^(1) = - G_R J`.

At second order,

`h^(2) = -(1/2) G_R V[h^(1),h^(1)]`.

Therefore the second functional response is

`chi2R_A;BC(x;y,z) = delta^2 h_A(x) / (delta J_B(y) delta J_C(z))`

and, for a linear source coupling,

`chi2R_A;BC = - G_R,AA' V^A'_{B'C'} G_R,B'B G_R,C'C`.

In momentum space,

`chi2R_A;BC(p;q,r) = -(2pi)^4 delta4(p-q-r) G_R,AA'(p) Gamma3^A'_{B'C'}(p,-q,-r) G_R^B'_B(q) G_R^C'_C(r)`.

This is the correct tree-level object to evaluate before any `chi2R` comparator rank claim. It is not the Iteration-146 four-point S-matrix derivative.

## Scoped causal / symmetry certificate

The formula has the required causal architecture:

- the output depends on two source insertions through retarded propagators;
- for a Bose-symmetric cubic vertex it is symmetric under `(B,q) <-> (C,r)`;
- physical source projections must be conserved, and the diffeomorphism Ward identity must remove gauge-artifact directions after the explicit projector contraction;
- loop/nonanalytic contributions must be derived with the same real-time prescription when they enter the frozen EFT order.

This is a structural CTP/response result. It is not yet a numerical Ward certificate because the finite projectors have not previously been frozen.

## Exact obstruction discovered

Iteration 146 froze twelve **on-shell 2-to-2** four-graviton samples described by

`(s,t,u,phi)`, with `s+t+u=0`.

Those data do **not** uniquely specify a retarded 1-output / 2-input off-shell three-point response. A finite evaluation of `chi2R_A;BC(p;q,r)` additionally requires at least:

1. off-shell momentum routing `p=q+r` and the virtualities `p^2,q^2,r^2`;
2. energy signs / retarded frequency prescription at each external response leg;
3. one output and two input tensor/source projectors;
4. time-window and spatial smearing functions;
5. normalization of the finite observable;
6. the explicit map from those contractions to the Iteration-145 scalar coordinates `chi2R_even` and `chi2R_odd`;
7. a numerical conserved-source Ward/gauge-artifact null test.

None of these objects is determined uniquely by the four-point Mandelstam fingerprint alone.

## NG-FUNNEL-007 — ON_SHELL_4PT_KINEMATICS_DO_NOT_FIX_OFF_SHELL_RETARDED_3PT

A finite on-shell four-graviton kinematic protocol does not uniquely determine the finite off-shell retarded three-point protocol required for `chi^(2)R`.

Consequences:

- the Iteration-146 `rank(V_amp)=10` remains valid in its on-shell amplitude space;
- it must **not** be reinterpreted as the rank of the C5 `chi2R` block;
- `chi2R_even/odd` remain `BLOCKED_PROTOCOL_UNDERSPECIFIED`, not zero;
- the local-EFT `chi2R` rank is `NOT_COMPUTABLE` until the finite off-shell projector/smearing protocol and cubic vertex contractions are supplied;
- no candidate residual, Fisher information or resource calculation is allowed yet.

This is a comparator-instantiation blocker, not a consistency failure of perturbative GR EFT.

## Remaining C5 rows

- `N2`: **BLOCKED** pending the declared CTP loop/influence-functional calculation;
- `C3sym`: **BLOCKED** pending the corresponding connected symmetric three-point state calculation;
- loop/nonanalytic C5 columns: **BLOCKED** until derived in the same CTP/renormalization convention;
- numerical Ward/soft relation in the finite `chi2R` coordinates: **BLOCKED** pending the explicit projectors.

## Reproducibility

Authorities:

- `analysis/c5_retarded_embedding_iteration147.py`;
- `results/c5_retarded_embedding_iteration147.json`.

The script is intentionally a protocol/derivation audit and refuses to manufacture a numerical `chi2R` rank from the on-shell four-point data.

## Literature anchors

- standard perturbative-gravity Feynman rules: de Donder graviton propagator and nonlinear graviton vertices;
- Schwinger–Keldysh/in-in gravitational calculations establish retarded initial-value propagation for observables;
- nonlinear Keldysh response functions are fully retarded ordered response objects;
- low-energy gravitational EFT still requires separate nonanalytic massless-loop contributions at the orders where they appear.

## Next scientific gate — Iteration 148

Freeze the first **finite off-shell C5 response protocol**:

1. choose a small sub-cutoff set of off-shell `(p,q,r)` with `p=q+r` and no propagator singularities;
2. choose explicit conserved tensor/source projectors and finite smearing/window normalization;
3. define `chi2R_even/odd` as concrete contractions;
4. evaluate the de-Donder Einstein–Hilbert cubic vertex and every local-EFT cubic vertex that contributes at the frozen order;
5. perform the conserved-source Ward/gauge-artifact null test;
6. compute the first actual `V_C5^(chi2R)` rank/SVD certificate;
7. leave loop/nonanalytic rows BLOCKED unless derived in the same CTP convention.

Only after that certificate should fixed C3 and nonlinear C4 tangents be compared in the same finite coordinates.
