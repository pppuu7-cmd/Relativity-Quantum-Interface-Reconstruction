# Iteration 596 — Prospective nonlinear Ward contract

Date: 2026-09-08
Status: PRE-RESULT FROZEN CONTRACT
MODEL_READINESS: 24%

This file prospectively fixes the nonlinear diffeomorphism/Ward convention required by Iteration 595 before any numerical contraction of the Iter594 cubic source object. It is not a Ward PASS, not a comparator residual, and does not authorize Source/Born subtraction.

## Parent authorities
- Iter587 exact one-graviton MSSC-001 identity: `q_mu V^{mu nu}=(p'^2-m^2)p^nu-(p^2-m^2)p'^nu`, with the symmetric route `p=-q/2`, `p'=+q/2` and hence `q_mu V^{mu nu}=(m^2-s/4)q^nu`.
- Iter588 exact three-mode fixture inherited from Iter368, with legs `(s,a,b)`, `s+a+b=0`, and for each gauge/singleton leg the complementary two legs retained as ordered spectator/source inputs.
- Iter589 single inverse-kernel normalization; no independent K1/K2/K3 rescaling.
- Iter590/594 complete cubic inverse-response family: K3 + six K1/K2 placements + six ordered K1^3 chains.

## Fourier and stripped-generator convention
Use `f(x)=∫_k exp(-i k·x) f(k)`. To keep the real numerical vertex convention used by Iter587–594, define the stripped diffeomorphism generator `Delta_xi := i delta_xi`; all Ward equations below are expressed with `Delta_xi`.

For one gauge mode `xi(l)` and one metric spectator mode `h(r)`, `k=l+r`:

`Delta^(0)_xi h_{mu nu}(l) = l_mu xi_nu(l) + l_nu xi_mu(l)`.

`Delta^(1)_xi h_{mu nu}(k) = xi^rho(l) r_rho h_{mu nu}(r) + l_mu xi^rho(l) h_{rho nu}(r) + l_nu xi^rho(l) h_{mu rho}(r)`.

The same rule is applied separately to each spectator graviton; no spectator is held inert in the full cubic Ward gate.

For the scalar/source endpoint,

`Delta_xi phi(k) = ∫_l xi^rho(l) (k-l)_rho phi(k-l)`.

Thus the endpoint transformation is retained on both ends of the scalar two-point response. No on-shell or transversality condition is imposed because Iter587 freezes the source route off shell.

## Exact fixture rule
For each cyclic choice `g in (s,a,b)`:
- gauge momentum is the exact Iter588 singleton momentum `q_g=M[g][0]` inherited from Iter368;
- the other two exact fixture momenta are spectators, never retyped or refitted;
- momentum conservation is the inherited `q_s+q_a+q_b=0`;
- the scalar route attached to the gauge singleton is exactly the Iter587 route `p=-q_g/2`, `p'=+q_g/2`.

No new momentum split, fitting parameter, gauge-dependent normalization, or result-dependent polarization may be introduced.

## Cubic recursive Ward target
Let `G=K^-1` and let `D_a,D_b,D_c` denote metric derivatives in the exact Iter588 directions. The numerical object under test remains exactly the Iter594

`D_a D_b D_c G = -G K_abc G + sum_6(G K_i G K_jk G) - sum_6(G K_i G K_j G K_k G)`.

For a chosen gauge leg `g` and spectators `(u,v)`, the **full** stripped cubic Ward residual is defined prospectively as

`W[g|u,v] := GaugeLeg(D_g D_u D_v G) + SpectatorLie_u(D_* D_v G) + SpectatorLie_v(D_u D_* G) + EndpointLeft(D_u D_v G) + EndpointRight(D_u D_v G)`.

Here every term is generated from the four explicit stripped transformation rules above using the same parent inverse kernel and the same Iter588 routing. `GaugeLeg` means replacement only of the chosen gauge-leg polarization by `q_g⊗xi+xi⊗q_g`; the two `SpectatorLie` terms are mandatory and are generated by `Delta^(1)_xi h`; the two endpoint terms are mandatory and are generated by `Delta_xi phi` on the incoming/outgoing scalar endpoint. No K3, K1/K2 placement, or K1^3 chain may be removed family-by-family.

The numerical PASS criterion for the later Ward-evaluation iteration must be specified before that evaluation from inherited numerical precision/envelopes; Iter596 itself does not choose a post-result tolerance.

## Mandatory reduction anchor
When both spectator metric perturbations are removed, the contract must reduce exactly to Iter587, with no spectator-Lie terms and with the endpoint terms producing

`q_mu V^{mu nu}=(p'^2-m^2)p^nu-(p^2-m^2)p'^nu`,

and on the frozen symmetric route

`q_mu V^{mu nu}=(m^2-s/4)q^nu`.

Failure of this reduction invalidates the contract and blocks numerical nonlinear Ward evaluation.

## Guardrails
- unsupported = BLOCKED, never zero-filled;
- no Source/Born subtraction in Iter596;
- no source-to-Iter582 map before full source Ward closure;
- no ANSATZ-003;
- no Fisher/resources;
- negative Ward results, once the full target is evaluated, are retained as scientific results;
- frozen Iter424/exact15/C5 closed sectors are not reopened.

## Next gate if contract audit passes
Evaluate `W[g|u,v]` for all three cyclic gauge-leg choices on the exact Iter588 fixture, using the full Iter594 13-family object and the frozen transformation recursion above. Only a raw-valid full-source Ward classification may unlock source-to-Iter582/native-linked mapping and then the fixed comparator quotient.
