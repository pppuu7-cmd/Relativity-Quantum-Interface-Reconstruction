# Candidate Gravity — Iteration 181: nonlocal soft-transverse resolution audit

**Date:** 2026-08-31  
**MODEL_READINESS: 24%**  
**Comparator:** `QG-NL-EXP-001`

## Result

The full action-level `B_T` projection of `QG-NL-EXP-001` is still not numerically instantiated because the rank-2 tensor Frechet insertion `G^(1) delta F(Box) R^(1)` has not yet been implemented in the exact six-row source-completed tensor pipeline. This is an operational implementation blocker, not a zero column.

Before promoting a blind fifth singular direction, the frozen six-row protocol was audited for its ability to resolve the characteristic analytic exponential form-factor shapes against the already-authorized local-C5 basis

`Riemann3_B_T * {1,-q2,q2^2,-q2^3}`.

For `F(Box)=(exp(-Box)-1)/Box` and the six frozen spacelike hard invariants `q2 in [0.2882,0.5076]`, representative soft-limit scalar structures generated by `F`, its derivatives, and the exponential itself are all formally rank-5 in exact arithmetic, but their fifth singular values are

- `F(-q2)`: `4.19e-8`;
- `q2 dF(-q2)/dq2`: `2.33e-7`;
- `q2^2 d2F(-q2)/dq2^2`: `8.33e-7`;
- `exp(q2)`: `1.86e-7`.

The largest is only `0.158` of the frozen Iteration-178 `B_T` extrapolation/error envelope `5.2626e-6`.

Therefore the current six-row momentum lever arm nearly polynomializes the tested analytic nonlocal shapes within the existing numerical resolution. A blind rank-5 SVD on such a column would not be a physical certificate.

## Classification

`NL-NG-004 — CURRENT_SIX_ROW_Q2_LEVER_ARM_NEARLY_POLYNOMIALIZES_TESTED_EXPONENTIAL_FORMFACTOR_SHAPES_BELOW_B_T_ERROR_ENVELOPE`.

`NUM-NG-002 — NONLOCAL_FIFTH_SINGULAR_VALUE_BELOW_FROZEN_EXTRAPOLATION_ENVELOPE_IS_NOT_A_PHYSICAL_RANK_CERTIFICATE`.

`NG-FUNNEL-039 — FULL_TENSOR_FRECHET_PROJECTION_AND_RESOLUTION_MARGIN_ARE_BOTH_REQUIRED_BEFORE_NONLOCAL_B_T_RANK_PROMOTION`.

This is **near-degeneracy / resolution insufficiency for the tested scalar form-factor structures**, not exact comparator identity. It is also not a consistency FAIL of nonlocal gravity.

## Literature consistency

The known field-redefinition results for Ricci/scalar weakly nonlocal gravity show equality with Einstein gravity for broad on-shell tree amplitudes, but those theorems do not erase off-shell/source-completed response differences. The present RQIR hard legs are off shell, so the full tensor action-level projection remains necessary rather than being replaced by an on-shell equivalence claim.

## Consequence

The exact nonlocal column remains `BLOCKED_NONLOCAL_B_T_TENSOR_FRECHET_IMPLEMENTATION_NOT_ZERO`. The next implementation must evaluate all four cubic action structures from Iteration 174, including the Frechet insertion, and must compare any new singular direction to the frozen numerical envelope. If the exact column also lands below that envelope, the protocol itself must be widened in hard `q^2` before novelty testing.

No `ANSATZ-003`. No Fisher/resources.
