# Candidate Gravity — Iteration 166

## Timelike absorptive / nonanalytic pre-gate

**Date:** 2026-08-31  
**MODEL_READINESS:** **24%** (unchanged)

## Question

Iteration 165 showed that a finite spacelike ordered-TT shape protocol can be saturated by an already-authorized local C5 derivative tower. Is there an observable sector in which adding more local Hermitian tree operators cannot reproduce the signal by finite interpolation?

## Answer

Yes, at the level of a scoped causal pre-protocol: use the **odd absorptive part of a timelike retarded source-response away from isolated poles**.

For positive-frequency timelike invariant `s>0`, freeze the retarded logarithmic branch

`L_R(+omega)=log(-s-i0)=log(s)-i*pi`,

while the negative-frequency branch is its conjugate,

`L_R(-omega)=log(-s+i0)=log(s)+i*pi`.

Define

`A_odd(s) = [Im chi_R(+omega)-Im chi_R(-omega)]/(2*pi)`.

This is a causal odd projection; the even leakage must vanish for the ideal retarded branch.

## Frozen benchmark rows

Use eight target-independent timelike rows

`s = 0.004, 0.008, ..., 0.032`,

with zero spatial momentum and `omega=sqrt(s)`.

All rows are below the already frozen dRGT TT pole `m^2=0.04`, so the tree massive pole is not sampled. This is a comparator-clean benchmark, not optimization around any Candidate Gravity ansatz.

## Theorem-level local-tree result

For a real Hermitian local tree EFT, the retarded transfer is analytic/meromorphic with real coefficients. Away from isolated poles its value on the real timelike axis is real. Therefore its branch discontinuity and odd absorptive projection vanish:

`A_odd^local-tree(s)=0`.

This statement is stronger than Iterations 163–165 finite-polynomial rank arguments. It does **not** depend on how many local curvature/derivative Wilson coefficients are allowed. A larger local tree derivative tower cannot produce a branch cut.

Numerical audit with an eight-column polynomial ladder gives absorptive rank `0` exactly.

Retain:

**C5-NG-004 — `LOCAL_HERMITIAN_TREE_EFT_CANNOT_SATURATE_OFF_POLE_ABSORPTIVE_BLOCK`.**

## Standard C5 massless-loop positive control

Low-energy quantum gravity contains nonanalytic contributions generated by massless propagation. A schematic curvature-squared logarithmic inverse-kernel contribution can be written

`K_R(s)=s[1+ell*s*L_R(s)]`.

At first order about GR,

`d chi_R/d ell = -L_R(s)`.

Hence

`A_odd(s)=1`

for the normalized leading logarithmic tangent.

On the eight frozen rows its rank is `1` and the causal-even leakage is zero at numerical precision.

A generic stress-test family `x^n A_odd`, `n=0..3`, with `x=s/0.032`, has rank `4/4` and singular values

`[3.6738287735, 1.2195863578, 0.2218816938, 0.02221900663]`,

with condition number about `165.35`.

This family is only an observable-capacity stress test. It is **not** a claim that all four directions are independent fixed C5 loop coefficients.

The important scientific point is that nonanalytic absorptive information cannot be reproduced by the local-tree interpolation mechanism that saturated the previous spacelike protocol.

## Lorentzian asymptotic-safety cross-check

A stronger contemporary comparator is available from

- J. M. Pawlowski, M. Reichert, J. Wessely, *Self-consistent graviton spectral function in Lorentzian quantum gravity*, arXiv:2507.22169 (2025).

That work uses a Källén–Lehmann representation for the TT graviton propagator and extracts the spectral function directly from the imaginary part of the retarded propagator. In the physical on-shell renormalisation scheme it finds a positive massless graviton peak plus a multi-graviton continuum.

Its IR propagator is

`G_hh^ph = z_spec^-1 [1/p^2 - A_h log(p^2) + sub-leading]`,

with

`A_h = 61/(60*pi) = 0.32361505095352056`,

and reported `z_spec ~= 1.486`.

The corresponding unnormalised IR scattering-tail onset is

`61/30 = 2.0333333333`.

Therefore the leading AS IR absorptive shape is the **same constant direction** as the leading perturbative C5 massless logarithm. On the frozen normalized shape quotient,

`rank([C5_log, AS_IR]) = 1`,

and the AS residual after C5-log shape projection is

`1.72e-16`.

Retain:

**AS-NG-004 — `LORENTZIAN_AS_LEADING_IR_LOG_IS_COLLINEAR_WITH_C5_MASSLESS_LOOP_SHAPE`.**

This is a leading-IR shape statement, not a finite-frequency theory identity.

## Important physical guardrail

The published AS coefficient belongs to a particular TT fluctuation field, gauge and on-shell field normalisation. RQIR ultimately needs a source-completed, operationally defined detector/source transfer. Therefore the numerical coefficient above is **not** substituted directly as the final RQIR observable coefficient.

Retain:

**NG-FUNNEL-025 — `BARE_TT_SPECTRAL_COEFFICIENT_IS_NOT_YET_A_SOURCE_COMPLETED_RQIR_OBSERVABLE`.**

## Comparator status in the new block

### C5

- local Hermitian tree: absorptive zero away from poles;
- massless-loop logarithm: supported nonzero leading shape;
- exact source-completed finite-frequency coefficient in the RQIR convention: BLOCKED.

### C4 dRGT

On the chosen rows the frozen tree TT pole at `m^2=0.04` is not sampled, so the supported tree contribution has no absorptive continuum there. This does **not** zero-fill dRGT loops, matter loops, helicity-0/1 or other thresholds; those remain BLOCKED.

### C3 postquantum-classical

The supported tree ordered response is the common EH boundary and is absorptively zero away from its isolated massless pole. Diffusion/MSR-loop ordered corrections remain BLOCKED and are not set to zero.

### Entire-form-factor nonlocal comparators

The frozen entire tree form factors do not themselves generate a branch cut away from the GR pole. Their full Lorentzian CTP/loop completion remains BLOCKED.

### Asymptotic safety

The Lorentzian spectral calculation supplies a genuine continuum and a supported leading IR logarithmic shape. Finite-frequency source-completed RQIR mapping remains BLOCKED.

## Funnel consequence

Retain:

**NG-FUNNEL-024 — `ABSORPTIVE_NONANALYTICITY_ESCAPES_LOCAL_TREE_INTERPOLATION_BUT_NOT_QUANTUM_COMPARATOR_SUBTRACTION`.**

A nonzero branch cut / absorptive response is a much better observable direction than another finite spacelike shape sample because local tree Wilson coefficients cannot fake it. But it is still **not** a Candidate Gravity signature: standard perturbative quantum gravity and Lorentzian asymptotic-safety gravity already populate the same leading IR direction.

## Candidate state

No Candidate Gravity residual is declared.

`ANSATZ-003`: **NOT CREATED**.  
Fisher/resources: **FORBIDDEN**.

## Readiness

`MODEL_READINESS = 24%`, unchanged.

The comparator foundation is materially narrowed but the final point is not awarded because the source-completed absorptive quotient is still incomplete for C3/C4/nonlocal/AS and has not yet been tied to the post-Gaussian `chi2R/N2/C3sym` block through one common dynamics.

## Exact next gate — Iteration 167

1. Turn the new absorptive pre-protocol into a source-completed conserved-stress response, so gauge/field normalisation is removed or explicitly profiled.
2. Derive the leading C5 massless-loop column in that same source convention, including the source/vertex pieces required for gauge invariance.
3. Map the Lorentzian AS spectral result into the same normalized source-response language, at least in the controlled IR limit.
4. Determine whether the first **shape beyond the universal constant log onset** survives C5+AS subtraction; do not optimize a candidate against that shape before the comparator map is frozen.
5. Preserve C3/C4/nonlocal unsupported loop sectors as BLOCKED.
