# Candidate Gravity — Iteration 204: low-energy EFT control versus analytic distinguishability

**Date:** 2026-09-01  
**MODEL_READINESS: 23%**

## Question

Iteration 202 showed why the present `x≈1` v3 range cannot justify a model-independent dimension-12 local-EFT truncation. A natural alternative is to move the protocol into a genuine low-energy region.

But does deep IR preserve enough shape information to distinguish an analytic nonlocal comparator from local EFT?

## Target-independent low-energy design

This iteration is a **K2/design diagnostic**, not a Candidate Gravity test and not yet a full cubic protocol.

Take the six historical base hard vectors and choose the high scale solely from

`x_max=0.1`.

This gives

`s_hi=sqrt(0.1/max(x_base))=0.4438530344`.

Freeze a second scale

`s_lo=0.65 s_hi=0.2885044724`.

No candidate or comparator residual is used to choose the scales.

The twelve resulting hard nodes span

`x in [0.0239883,0.1]`.

## Conditional EFT remainder

A local derivative truncation is not controlled merely because x is small: a Wilson-coefficient/remainder convention must still be stated.

For illustration only, declare the explicit envelope

`|c_n| <= C`

in the monomial normalization used by the current derivative tower.

For the cubic Riemann-chain dimension-12 truncation, retained `n<=3`, the omitted geometric tail obeys

`|R_omitted|/|r| <= (2/3) C x_max^4/(1-x_max)`

which at `x_max=0.1` is

`7.4074e-5 C`.

For a hard local K2 basis retained through `x^6`, the analogous omitted monomial tail beginning at `x^7` is

`1.1111e-7 C`.

These are **conditional** bounds. RQIR does not silently assume `C=1`, naturalness, or a UV matching prior.

## Analytic nonlocal degeneracy in the same IR regime

Compare the fixed analytic nonlocal hard tangent

`v_NL=x^2 exp(x)`

with local polynomial directions

`[x,x^2,...,x^6]`.

The augmented matrix remains algebraically rank 7, but becomes extraordinarily ill-conditioned:

- raw condition number: `5.5455e12`;
- column-normalized condition number: `8.9451e11`.

The best local polynomial reconstruction of the nonlocal tangent leaves

- relative L2 residual: `3.90e-12`;
- max absolute residual: `4.05e-14`.

In the illustrative coefficient normalization, the omitted K2 monomial bound is already larger than that residual unless

`C < 3.65e-7`.

Therefore deep IR makes the analytic nonlocal/local separation operationally meaningless unless one imposes an extremely strong high-order Wilson bound.

## Scientific result

There is a genuine tradeoff:

### High-energy analytic protocol

Pros:
- analytic form factors differ strongly from low-order local polynomials.

Cons:
- local EFT derivative truncation is uncontrolled near the cutoff without a remainder model.

### Deep-IR analytic protocol

Pros:
- higher powers can be parametrically suppressed once a coefficient envelope is declared.

Cons:
- analytic nonlocal form factors admit extremely accurate local Taylor interpolation and become near-degenerate with omitted local EFT terms.

This is not a numerical accident; it is the expected EFT distinction between local analytic short-distance contributions and nonanalytic low-energy information.

## Literature anchors

- J. F. Donoghue, Phys. Rev. D 50, 3874 (1994), arXiv:gr-qc/9405057: gravity EFT separates unknown local short-distance contributions from universal low-energy nonanalytic effects generated by massless propagation.
- M. Ruhdorfer, J. Serra, A. Weiler, JHEP 05 (2020) 083, arXiv:1908.08050: gravitational EFT admits systematic nonredundant operators of arbitrary dimension and extension to all orders.

## Retained results

### `EFT-NG-001 — DEEP_IR_IMPROVES_DERIVATIVE_REMAINDER_CONTROL_ONLY_AFTER_AN_EXPLICIT_WILSON_ENVELOPE_IS_DECLARED`

No hidden naturalness assumption is permitted.

### `REL-NG-017 — DEEP_IR_ANALYTIC_NONLOCAL_TANGENT_BECOMES_EXTREMELY_NEAR_DEGENERATE_WITH_LOCAL_C5_POLYNOMIALS`

An analytic form-factor difference is not a robust low-energy novelty carrier against unrestricted local Wilson freedom.

### `NG-FUNNEL-059 — HIGH_ENERGY_ANALYTIC_DISTINGUISHABILITY_AND_LOW_ENERGY_EFT_TRUNCATION_CONTROL_FORM_A_TRADEOFF`

Increasing energy to reveal analytic form-factor shape simultaneously weakens the EFT truncation argument.

### `NG-FUNNEL-060 — PRIORITIZE_LINKED_NONANALYTIC_MULTIPOINT_RELATIONS_OVER_FINITE_ANALYTIC_SHAPE_SEARCH`

The next serious Candidate Gravity witness should use nonanalytic/causal information **linked across multiple RQIR observables by the same parent dynamics**, not another finite analytic shape.

## Why standalone nonanalyticity is still insufficient

Iteration 170 already proved that an arbitrary positive linear TT spectral density is C4-mediator reproducible. Therefore the next witness cannot simply be a two-point branch cut.

The target must link nonanalytic real-time information across, for example,

`(K2 or chi1R, Gamma3/chi2R, noise/C3, Ward/soft/constraint data)`

with the same parent dynamics and shared parameters.

## Readiness

`MODEL_READINESS: 23%`, unchanged.

Iteration 204 identifies the correct direction but does not close the reopened C5 truncation gap or AS/C3 blockers.

## Next gate

Iteration 205 should freeze a **linked nonanalytic multi-point protocol** at the level of functional singularity/discontinuity rather than finite analytic interpolation. First task: define a comparator-safe relation involving a three-point retarded discontinuity or branch structure conditioned on the same two-point spectral kernel, while preserving the Iteration-170 C4 mediator no-go and the Iteration-171 amputation discipline.
