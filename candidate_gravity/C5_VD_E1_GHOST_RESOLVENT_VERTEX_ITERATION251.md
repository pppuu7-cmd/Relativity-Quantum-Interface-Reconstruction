# RQIR Candidate Gravity — Iteration 251

## First explicit `e=1` Vilkovisky ghost-resolvent numerator block

**Date:** 2026-09-01  
**MODEL_READINESS: 24%**

## Starting authority

The actual repository front is Iteration 250, not the lagging Iteration-245 `CURRENT_QG_FRONT`. Iteration 250 freezes `PASS_SCOPED_E1_E2_NO_NEW_LOOP_POLYGON_BEYOND_TRIANGLE` and instructs Iteration 251 to construct the first minimal pure-gravity Vilkovisky `e=1` mixed soft-hard numerator block before any heavy integration.

The primary same-parent authority remains Giacchini–de Paula Netto–Shapiro, *Vilkovisky unique effective action in quantum gravity*, Phys. Rev. D 102, 106006 (2020), arXiv:2006.04217. In its notation

`N^alpha_beta = delta^alpha_beta Box + R^alpha_beta`

for the frozen Vilkovisky value `a=-1/2`, while

`U1 = N^{-1} R^i (D_i R^j) E_j N^{-1} Y`.

Thus the cubic `E^(2) K^(1)` part of `Tr U1` necessarily contains a first background variation of at least one of the two ghost resolvents, together with the separate first variations of `R(DR)` and `Y`. This iteration isolates and computes the ghost-resolvent component only.

## Frozen conventions

- spacetime dimension `D=4`;
- `Lambda=0`;
- signature `(-,+,+,+)`;
- plane waves `exp(i k.x)`;
- DeWitt configuration-space parameter `a=-1/2`;
- ghost operator `N^alpha_beta = delta^alpha_beta Box + R^alpha_beta`;
- loop denominator convention `D_F(p)=1/(p^2+i0)`; the algebraic sign of `N0^{-1}` follows from `Box -> -p^2`;
- the hard TT polarization and momentum are exactly those used by Iterations 248–249, so no post-failure kinematic retuning is introduced.

## Analytic first variation

Let the background perturbation be a TT plane wave

`h_mu_nu = epsilon_mu_nu exp(i q.x)`

with

`q_mu epsilon^mu_nu = 0`, `epsilon^mu_mu = 0`.

Acting on a ghost plane wave `v^beta exp(i p.x)`, direct expansion of the vector covariant Laplacian plus Ricci term gives

`delta N^alpha_beta(p+q,p) =`

`(epsilon^{mu nu} p_mu p_nu) delta^alpha_beta`

`- (p.q) epsilon^alpha_beta`

`- q_beta p_mu epsilon^{alpha mu}`

`+ q^alpha p_mu epsilon^mu_beta`.

A useful internal cancellation occurs: in TT gauge the derivative-free `1/2 Box h^alpha_beta` contribution generated by the variation of the vector Laplacian cancels the linearized Ricci contribution `R^(1)alpha_beta = -1/2 Box h^alpha_beta`. The surviving vertex is therefore a local polynomial of degree at most two in loop/external momenta.

## Independent numerical validation

`candidate_gravity/code/iteration251_vd_e1_ghost_resolvent_vertex.py` evaluates the full covariant operator `delta*Box + Ricci` on the same finite-amplitude plane-wave metric and differentiates it symmetrically with respect to the hard TT amplitude. This does not reuse the analytic first-variation formula.

For the frozen test loop momentum the analytic vertex has Frobenius norm

`1.0048840729158761`.

At finite-difference step `1e-6`, the maximum componentwise mismatch is

`8.007333685000617e-11`.

The hard background remains TT to machine precision (`max |q.epsilon| = 8.16e-18`, trace exactly zero in the frozen representation).

Classification:

`PASS_SCOPED_E1_GHOST_RESOLVENT_VERTEX_FREEZE_AND_TT_VALIDATION`.

## Resolvent insertion identity

For either of the two ghost inverses in `U1`,

`delta(N^{-1}) = -N0^{-1} (delta N) N0^{-1}`.

Therefore the `E^(2) K^(1)` ghost-resolvent contribution consists of the two mandatory placements

`Tr[delta(N^{-1}) A0[E^(2)] N0^{-1} Y0]`

and

`Tr[N0^{-1} A0[E^(2)] delta(N^{-1}) Y0]`,

where `A0[E] = R0^i (D_i R0^j) E_j` in condensed notation.

The extra inverse repeats/raises a ghost propagator segment. It does **not** create a fourth independent external-momentum corner. Thus this explicit numerator block is compatible with the Iteration-250 bubble/triangle master-topology bound.

## What is not yet closed

This is not the complete `E^(2) K^(1)` coefficient and not the complete `e=1` sector. The following pieces are still mandatory in the same convention:

1. `delta[R^i_gamma (D_i R^j_delta)] E^(2)_j`;
2. `delta Y * E^(2)`;
3. flat-kernel `E^(3)`;
4. the allowed `E^(1) K^(2)` placements where the null-soft leg lies in the kernel rather than the isolated linear EOM;
5. the complete `e=2` sector;
6. tensor reduction, source/Ward/contact completion and Lorentzian hard-channel discontinuity projection.

The present calculation therefore cannot be promoted to a C5 comparator coordinate or Candidate Gravity residual.

## Scientific classification

This is a **scoped algebraic PASS** for one required C5 numerator subblock. It is not an exact comparator identity, not a consistency FAIL, not near-degeneracy, not regime-specific non-identifiability, and not a novelty certificate.

Retain umbrella blocker:

`BLOCKED_4D_EINSTEIN_VD_RESOLVENT_VERTEX_LIBRARY_TENSOR_REDUCTION_AND_SOURCE_PROJECTION`,

with

`BLOCKED_NOT_ZERO`.

The blocker is narrower because the first ghost-resolvent vertex is now explicit and independently checked.

## Candidate state

Robust Candidate Gravity residual: none.  
`ANSATZ-003`: not created.  
Fisher/resources: forbidden.  
Heavy full C5 run: not authorized.

## Readiness

`MODEL_READINESS: 24%`

Change from Iteration 250: **0 percentage points**. A genuine piece of the 4D VD numerator library is now closed, but the C5 comparator coordinate remains incomplete; comparator foundation therefore remains `24/25` and unique residual remains `0/20`.

## Exact next gate — Iteration 252

Freeze the remaining first-order `E^(2) K^(1)` vertex pieces in the same convention, beginning with

`delta[R^i_gamma (D_i R^j_delta)] E^(2)_j`

and `delta Y * E^(2)`. Combine them with both ghost-resolvent placements from this iteration into one complete `E^(2) K^(1)` `Tr U1` numerator before integration. Require a Ward/condensed-index consistency check before tensor reduction. Do not launch a heavy integral run and do not create `ANSATZ-003`.
