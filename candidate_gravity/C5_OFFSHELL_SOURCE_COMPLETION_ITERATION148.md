# Candidate Gravity — Iteration 148: off-shell C5 source/observable completion

**Date:** 2026-08-31  
**Status:** comparator-protocol progress; no Candidate Gravity promotion.

## Objective

Iteration 147 derived the correct tree-level retarded structure

`chi2R ~ G_R Gamma3 G_R G_R`

but left the numerical C5 `chi2R` tangent blocked because the old on-shell protocol did not fix off-shell virtualities, conserved probes, smearing, or scalar contractions.

Before freezing a numerical rank, Iteration 148 audits a more basic issue: whether an off-shell Wilson tangent inherited from the on-shell/EOM-reduced EFT basis is basis-independent.

## Result

It is not, unless the operational observable/source map is frozen and transformed together with the fields.

A local field redefinition changes off-shell Green/response functions even when on-shell scattering is unchanged. In an EFT this matters because equations-of-motion-redundant operators may be removed from an on-shell amplitude basis, while their effect reappears in source/contact terms for off-shell observables.

### Reproducible algebraic regression

Use the minimal nonlinear response model

`K phi + (g/2) phi^2 + J = 0`,

with retarded/invertible `G=K^-1` away from poles. The tree second response of the physical observable `phi` is

`chi_phi(p;q,r) = -g Gp Gq Gr`.

Under the local redefinition

`phi = psi + a psi^2 + O(a^2)`,

the coordinate response of `psi` is

`chi_psi = chi_phi - 2 a Gq Gr`.

The physical observable is still `phi`, so reconstruction adds the induced observable/source-map contact term

`+ 2 a Gq Gr`,

which restores `chi_phi` exactly.

The deterministic 12-point regression gives:

- maximum reconstruction error: `1.1102230246251565e-16`;
- minimum nonzero coordinate-response shift: `0.11688546786387487`.

Authorities:

- `analysis/c5_offshell_field_redefinition_iteration148.py`;
- `results/c5_offshell_field_redefinition_iteration148.json`.

## NG-FUNNEL-008 — ONSHELL_REDUCED_BASIS_NOT_OFFSHELL_RESPONSE_BASIS

An on-shell/EOM-reduced EFT operator basis does not by itself define a basis-independent off-shell `chi^(2)R` tangent.

For a valid C5 retarded comparator, one must freeze an **operational completion** containing all of:

1. the metric/field variable that the detector/source operationally couples to;
2. the complete source or matter coupling in that variable;
3. the field-redefinition convention used to reduce the EFT action;
4. induced source/contact operators generated by that reduction;
5. the finite conserved projector and smearing protocol;
6. the same CTP state, retarded prescription, EFT order and renormalization convention.

Otherwise the numerical rank of `V_C5^(chi2R)` can change under an allowed field-coordinate transformation and is not a physical comparator certificate.

## Consequence for Iteration 146 basis

The rank-10/10 on-shell amplitude certificate remains valid. It is not revoked.

However, its ten Wilson directions cannot simply be inserted into an off-shell `Gamma3` calculation after EOM reduction while ignoring the corresponding source/contact completion. Therefore the requested numerical C5 retarded SVD remains **BLOCKED_SOURCE_COMPLETION**, not zero and not a C5 consistency FAIL.

## Literature check

This distinction is consistent with modern field-redefinition analyses: off-shell correlators transform nontrivially under field redefinitions, whereas physical equivalence requires comparing appropriately transformed observables/probes. It is especially relevant in gravity, where the choice of metric variable and admissible probe is part of the operational observable definition.

## Frozen rule going forward

The physical comparator object is not merely

`(gravity action, Wilson basis)`

but

`(gravity action, field convention, matter/source map, CTP state, renormalization, finite probe/smearing protocol)`.

Only this completed object may generate `J/N/chiR/C3/chi2R` columns.

## Status of other C5 rows

- local on-shell amplitude tangent: PASS_SCOPED, rank 10/10;
- tree retarded factorization: PASS_SCOPED;
- off-shell local `chi2R` tangent: BLOCKED_SOURCE_COMPLETION;
- `N2`, `C3sym`: BLOCKED pending same-completion CTP derivation;
- loop/nonanalytic columns: BLOCKED pending same convention and order.

No Fisher/resource work is admissible and `ANSATZ-003` remains withheld.

## Next gate — Iteration 149

Freeze one **source-completed C5 operational metric convention**. The preferred path is to retain the physical metric minimally coupled to a declared conserved matter/source sector, undo or explicitly compensate every EOM/field-redefinition reduction relevant to the cubic off-shell response, then freeze finite off-shell momenta/projectors/smearing and compute the first basis-stable `V_C5^(chi2R)` Ward/rank certificate.
