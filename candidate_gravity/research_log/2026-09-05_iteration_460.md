# 2026-09-05 — Iteration 460

## Objective
Continue from authoritative Iteration 459 without duplicating active run `33951807833`. Audit whether the Iteration-458 assembled MP80/120 gate is sufficient to expose cancellation/conditioning provenance once all 28 distinct F(u,v) coordinates are locally certified.

## Source-of-truth audit
Read `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_459.md`, `research_log/2026-09-05_iteration_459.md`, recent commits, Iteration-455 support manifest, and live metadata for run `33951807833`. The run remains in progress and is not duplicated.

## Exact stencil result
Frozen central4 first-derivative coefficients are `[1/12,-2/3,+2/3,-1/12]/h`. Their one-dimensional L1 sum is exactly `3/2`, hence the tensor-product mixed-derivative coefficient L1 sum is exactly `9/4`. Therefore the Iteration-458 operator L1 norms remain BASE `9.0e10` and HALF `3.6e11`.

## New cancellation/provenance contract
After all 28 distinct F(u,v) coordinates have local MP certificates, the assembled BASE and HALF MP80/MP120 gate must report, independently for each level and precision:

- `D = sum_i w_i F_i` using all 16 source occurrences with level-specific central4 weights;
- `S_abs = sum_i |w_i F_i|`;
- `kappa_cancel = S_abs / max(|D|, tiny)` as a conditioning diagnostic only;
- `B_80_120 = sum_i |w_i| |F_i^80-F_i^120|`;
- direct `|D80-D120|` and the already frozen scaled discrepancy `|D80-D120|/max(1,|D80|,|D120|) <= 2e-6`.

The triangle inequality requires `|D80-D120| <= B_80_120` apart from explicitly bounded assembly-roundoff. Violation is an implementation/provenance BLOCKED condition, not a physics FAIL. A large `kappa_cancel` is near-conditioning/cancellation evidence only; it must not be relabelled as consistency FAIL, comparator identity, non-identifiability, or novelty, and it is not grounds to weaken thresholds.

This strengthens provenance without changing any frozen numerical or physical threshold and without promoting index 2.

## Classification
`PASS_ASSEMBLY_CANCELLATION_DIAGNOSTIC_CONTRACT_FROZEN__NON_PROMOTING`

## Readiness
MODEL_READINESS: 24%

Readiness change: **0 percentage points**. A numerical provenance contract is strengthened, but no stable model-readiness rubric component is completed.

## Exact next gate
Raw-consume run `33951807833` fail-closed at Iteration-455 distinct rank 3, `u=-1e-5, v=+1e-5`. If PASS, advance only to distinct rank 4 `u=-5e-6, v=-1e-5` under unchanged five-z/NPHI16/radial/direct-MP80/120 conventions. After all 28 coordinates are locally certified, execute the Iteration-458 assembly MP gate augmented by the cancellation diagnostics frozen here before any Iteration-424 physical reevaluation.
