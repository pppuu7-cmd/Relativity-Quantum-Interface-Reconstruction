# RQIR Candidate Gravity — Iteration 609

Date: 2026-09-08

## Scope
Derive, without inventing a new estimator, the concrete internal MSSC scalar-pole kinematic pullback required after Iter608. Preserve all 13 source families and the frozen Iter606 native-linked mapping contract. No Source/Born subtraction, comparator quotient, ANSATZ-003, Fisher or resources.

## Derivation
For the full routed Iter594/605 source object, every internal scalar propagator has momentum `r=p0+Q_A`, hence in MSSC `(+---)` convention

`r^2 = p0^2 + Q_A^2 + 2 p0.Q_A`.

For every K1/K2 placement, closure gives `Q_A=+/-q_i`, so the internal invariant is `p0^2+q_i^2+/-2 p0.q_i`. For each K1^3 permutation `(i,j,k)`, the two nontrivial internal lines are `r1=p0+q_i` and `r2=p0+q_i+q_j=p0-q_k`, hence their pole equations depend on `p0^2` and `p0.q_i`/`p0.q_k`. The local K3 family has no internal scalar propagator.

Iter588 separately fixes the isolated K1/K2 symmetric source-routing endpoints to `p=-q_i/2`, `p'=+q_i/2`, giving endpoint invariants `q_i^2/4`; however replacing the p0-dependent internal propagators of the already frozen full Iter594/605 object by this isolated-block value would change the observable and is not authorized.

The native authority through Iter608 fixes the external q2 buckets but not `p0^2`, `p0.q_i`, or an equivalent native cut variable/root map. Therefore no unique family-by-family `p_j^2(q^2)` exists from current authority alone.

## Classification
`BLOCKED_ITER609_INTERNAL_SCALAR_POLE_PULLBACK_NOT_DETERMINED_BY_Q2_ALONE__NON_RESIDUAL`

This is operational BLOCKED, not Candidate-Gravity consistency FAIL, not exact comparator identity, not regime-specific non-identifiability, not near-degeneracy, not a novelty certificate, and not a zero residual.

Iter608 distribution authority remains valid: `Disc[1/(m^2-r^2+i0)] = -2*pi*i*delta(m^2-r^2)`. What remains missing is the concrete root coordinate and Jacobian in the frozen native linked observable.

MODEL_READINESS: 24%

Readiness change: 0 percentage points. A genuine mapping obstruction has been localized exactly, but no complete readiness sector closes and no comparator-subtracted residual exists.

Exact next gate: locate or derive from the already frozen native `T_cut` observable the explicit cut/integration variable `z` and its relation to the MSSC routed scalar momentum `p0`, sufficient to express `m^2-r_j^2` as a function of `z` and fixed external invariants. Freeze that bridge prospectively before any projection. If no same-parent/native authority fixes it, preserve BLOCKED; never choose a post-hoc estimator or zero-fill.
