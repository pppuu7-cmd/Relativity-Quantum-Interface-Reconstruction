# Candidate Gravity Research Log — Iteration 537

Date: 2026-09-07

## Repository/source-of-truth check
At the start of this iteration, `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_536.md`, the Iteration-536 research log, recent commits, and live GitHub Actions were checked. Canonical QUARTER rank2 run `34102627559` remained `in_progress`; no duplicate heavy Action was launched.

## Exact conditioning of the h4/h6 component inversion
Iteration 536 froze the diagnostic inversion from the two assembled differences

- `d1 = BASE - HALF`,
- `d2 = HALF - QUARTER`,

under `X_h = D + A + B + O(h^8)`, with `A=a h^4`, `B=b h^6`.

The exact forward map is

`[d1,d2]^T = M [A,B]^T`,

with

`M = [[15/16,63/64],[15/256,63/4096]]`.

Its determinant is

`det(M) = -2835/65536 != 0`,

so the h4/h6 diagnostic components are algebraically identifiable from the two differences at this truncation order. The inverse is exactly

`M^{-1} = [[-16/45,1024/45],[256/189,-4096/189]]`,

reproducing the Iteration-536 formulas for `Ahat` and `Bhat`.

For the ordinary unscaled Euclidean norm in `[A,B]` coordinates,

`M^T M = [[57825/65536,968625/1048576],[968625/1048576,16260993/16777216]]`.

Exact invariants are

- `tr(M^T M)=31064193/16777216`,
- `det(M^T M)=8037225/4294967296`,
- discriminant `=962877176430849/281474976710656`.

Therefore

`kappa_2(M) = sqrt((31064193+sqrt(962877176430849))/(31064193-sqrt(962877176430849)))`

`≈ 42.7789181466055467416`.

This is a distinct conditioning question from Iteration 531. Iteration 531 showed that the normalized BASE/HALF/QUARTER stencil rows themselves are very well conditioned (`kappa_2 ≈ 1.04378`). The present result shows that separating the subleading h4 and h6 truncation components from only the two inter-level differences is substantially more sensitive in the unscaled `[A,B]` coordinate convention. Thus later post-assembly use of `Ahat`/`Bhat` must not inherit the stencil-conditioning certificate.

The value `kappa_2≈42.78` is not promoted to a physical or model-level near-degeneracy claim: it is coordinate/convention dependent and belongs only to this diagnostic inversion. It is a warning against over-interpreting small changes in inferred `Ahat`/`Bhat`, not a failure of the frozen physical gate.

Classification:
`PASS_ITER424_H4_H6_INVERSION_CONDITIONING_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`.

Reproducible audit: `candidate_gravity/code/iteration537_iter424_h4_h6_inversion_conditioning_exact_audit.py`.
Machine-readable result: `candidate_gravity/results/iteration537_iter424_h4_h6_inversion_conditioning_exact_audit.json`.

## Guardrails
No threshold, mass node, parent dynamics, parameter convention, precision setting, successor order, or Iteration-424 physical acceptance clause was changed. This conditioning audit cannot rescue a failure, promote physical index 2, or authorize ANSATZ-003, Fisher, or resources.

The fixed C3/C4/C5/nonlocal/asymptotic-safety comparator quotient remains operationally BLOCKED because the concrete upstream `Source/Ward/contact+K2` target and robust comparator-subtracted residual remain absent. No Candidate-Gravity consistency FAIL, exact comparator identity, regime-specific non-identifiability, model-level near-degeneracy, or novelty certificate is claimed.

## Model readiness
Comparator foundation `24/25`; robust unique residual `0/20`; frozen parent dynamics/ANSATZ `0/20`; consistency/positivity/Ward/causality `0/15`; identifiability/Fisher `0/10`; resource/experiment closure `0/10`.

MODEL_READINESS: 24%
Readiness change: 0 percentage points. The diagnostic conditioning ledger is stricter, but no additional stable model-readiness rubric sector is complete.

## Exact next gate
Do not duplicate rank2. After canonical run `34102627559` reaches terminal state, raw-consume its artifact fail-closed. Raw-valid PASS authorizes only frozen rank3 `(-1.25e-6,-2.5e-6)`; scientific FAIL/BLOCKED stops advancement; operational failure permits only minimal technical repair of rank2.
