# Candidate Gravity Research Log — Iteration 617

Date: 2026-09-08

## Classification

`PASS_ITER617_HISTORICAL_NORMALIZATION_AUTHORITY_AUDIT__N_NATIVE_GENUINELY_UNFIXED`

This is a PASS of the historical-authority audit with a negative scientific answer. It is not a Candidate-Gravity FAIL and not a residual.

## Question

After Iter616 reduced the source-to-native ambiguity to one common scalar `N_native`, does older repository authority already determine that scalar without using the Iter615/Iter582 numerical Candidate values?

Answer: **no**.

## Exact factors that are already frozen

- Iter338 fixes the connection-sector one-loop outer effective-action factor `+i`, including `D_s Gamma_det = +i D_s C_det`.
- Iter147/149 fix the retarded gravitational response convention `chi2R = -G_R Gamma3 G_R G_R`.
- Iter149/218 use `g=eta+kappa h` as the physical metric/source convention.
- Iter218 explicitly states that the displayed MSSC one-graviton scalar vertex strips the common gravitational coupling.
- Iter589 fixes all relative K1/K2 source factors from one scalar inverse kernel; later Iter594/605 retain the complete 13-family same-action object.
- Iter588/616 close mode/q2 identity and scalar endpoint amputation.

## Why these facts do not fix N_native

The repository never freezes an equation equating the **scalar-endpoint-amputated MSSC probe response** to the **gravitational retarded/1PI Gamma3 convention** with one absolute coupling/phase normalization. The retarded graviton response amputation in Iter147/149 and the scalar endpoint amputation in Iter616 are different operations and may not be combined by inspection.

The strongest historical negative evidence is Iter151: it explicitly preferred an action-level source-completed Ward identity because that follows from the same declared dynamics/field convention **without importing an incompatible amputated-vertex normalization**.

Repository commit-history searches also found no existing authority under `Legendre`, `generating functional`, or `1PI` that supplies the missing bridge.

Therefore setting `N_native=+i`, `-i`, `1`, or any fitted value would be an unsupported post-hoc convention change. Separate root/q2 normalizations remain forbidden.

## Exact next admissible work

Preserve `N_native` as the minimal native-binding blocker. Do not modify the Iter606/616 normalization contract after seeing Candidate values. Continue only normalization-invariant source diagnostics or other independent theory/comparator work that cannot tune `N_native`, while keeping Source/Born subtraction, comparator quotient, ANSATZ-003 and Fisher/resources locked.

MODEL_READINESS: 24%

Readiness change: `0 pp`.
