# Candidate Gravity Research Log — Iteration 554

Date: 2026-09-07

## Exact routed-invariant consequence of u↔v swap
The current frozen parent kinematics are
`p=-a+alpha*q+rho*n`, `alpha=-(s+u-v)/(2s)`, with `rho` symmetric under `u↔v` because it depends on the symmetric Kallen combination.

Define
`delta = alpha(u,v)-alpha(v,u) = (v-u)/s`.
Then, without approximation,

`p(u,v) = p(v,u) + delta*q`.

This gives the exact routed-invariant shifts

- `Delta(p.q) = delta*q^2`,
- `Delta(p^2) = 2 delta [p(v,u).q] + delta^2 q^2`,
- `Delta((p+q)^2) = 2 delta [p(v,u).q + q^2] + delta^2 q^2`.

Therefore

`Delta((p+q)^2)-Delta(p^2) = 2 delta q^2`

exactly. In the retained unresolved physical class `q^2=-1`, this specializes to

`Delta((p+q)^2)-Delta(p^2) = -2 delta`.

## Scientific meaning
This strengthens Iterations 552–553: the u↔v non-equivalence is not merely an empirical raw-output asymmetry. It follows exactly from the declared parent routing and produces a deterministic relative shift between the two quadratic routed invariants. The result is diagnostic/provenance authority only; it is not a Candidate-Gravity consistency FAIL, not an exact comparator identity, not regime-specific non-identifiability, not model-level near-degeneracy, and not a novelty certificate.

Classification:
`PASS_ITER424_UV_SWAP_EXACT_INVARIANT_SHIFT_CONTRACT__DIAGNOSTIC_ONLY_NON_PROMOTING`.

Reproducible code: `candidate_gravity/code/iteration554_uv_swap_invariant_shift_audit.py`.
Machine-readable result: `candidate_gravity/results/iteration554_iter424_uv_swap_exact_invariant_shift_contract.json`.

## Heavy route
No heavy computation was duplicated or reordered. Canonical QUARTER rank8 run `34153849866`, coordinate `(+1.25e-6,-1.25e-6)`, remains the only authorized active successor pending terminal completion and fail-closed raw consumption.

## Model readiness
Comparator foundation `24/25`; robust unique residual `0/20`; frozen parent dynamics/ANSATZ `0/20`; consistency/positivity/Ward/causality `0/15`; identifiability/Fisher `0/10`; resource/experiment closure `0/10`.

MODEL_READINESS: 24%
Readiness change: 0 percentage points. The iteration closes an exact routing/provenance subgate but does not complete any additional stable model-level rubric sector.
ITERATION_TASK_COMPLETION: 100%

## Exact next gate
Raw-consume canonical rank8 run `34153849866` only after terminal completion. Only raw-valid PASS authorizes frozen rank9 `(+1.25e-6,+1.25e-6)`.
