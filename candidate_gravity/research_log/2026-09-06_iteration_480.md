# RQIR Candidate Gravity — Iteration 480

Date: 2026-09-06

## Scope
Repository-first continuation from authoritative Iteration 479. No ANSATZ-003, no Fisher/resources, no threshold or dynamics change.

## Rank-11 raw authority
Canonical frozen Iteration-455 manifest rank 11 `(u,v)=(+5e-6,+1e-5)`, multiplicity 1, run `33989317870`, job `101368577097`, artifact `9977375478`, artifact digest `sha256:603e51865eb13242556c41700a6e9ee54b916fe18a2c2fdd9281976aadd3e71c`, scientific JSON SHA-256 `7cc7c36a6dd5fd628280f370ad68a3b93b018fc919a0948471420a6f945b8b9f`.

Observed: `80/80` finite; max scaled MP80↔MP120 `2.94779472003420316940302965078e-80 <= 1e-30`; max radial Richardson scaled error `2.56155487488387492604714966234e-15 <= 5e-4`.

Classification: `PASS_RAW_CONSUMED_MANIFEST_RANK11_FULL_Z_MP80_MP120__NON_PROMOTING`.

Certified occurrence-weighted precision support advances to `16/32 = 50.000%`, i.e. `1280/2560` frozen row occurrences. This is numerical support coverage only; physical double-double index 2 remains unresolved and unpromoted.

## Independent provenance failure localization
Post-479 digest-diagnostic raw-consume run `33994982284`, job `101383835758`, failed in step `Raw-consume digest semantics diagnostic`. Job logs prove the failure occurred before any digest assertion: GitHub CLI returned exit status 4 because `GH_TOKEN` was not set. Artifact download itself succeeded and verified digest `sha256:f022684784fcce7c27a2208baad4a9410261dddc75ab959f037fbef911dacab3`.

Therefore classification is strictly `OPERATIONAL_BLOCKED__GH_CLI_AUTH_MISSING`, not Candidate-Gravity consistency FAIL, not comparator identity, not non-identifiability, and not a novelty result.

The workflow was repaired minimally by adding `GH_TOKEN: ${{ github.token }}` at job scope. No scientific fixture, digest target, threshold, source run, ansatz, dynamics or support ordering changed. Commit for the repair: `b5a1f9f54ebf04eccb95d9d718d789932747f523`.

## Scientific interpretation
Rank-11 PASS remains authoritative independently of the separate digest-provenance workflow failure. The failed provenance job cannot invalidate or promote any physics because it did not reach its scientific assertions. The corrected digest gate must be allowed to rerun fail-closed before using its conclusion as provenance authority.

## Readiness
Stable rubric: comparator foundation `24/25`; unique residual `0/20`; frozen parent dynamics/ANSATZ `0/20`; consistency/positivity/Ward/causality `0/15`; identifiability/Fisher `0/10`; resource/experiment closure `0/10`.

MODEL_READINESS: 24%

Readiness change: 0 percentage points from Iteration 479. Rank-11 local support and the CI-auth repair close real numerical/provenance subgates, but no additional stable readiness-rubric component is complete.

## Exact next gate
1. Raw-consume the corrected post-479 digest diagnostic fail-closed; if it still fails, classify/localize operationally without altering science.
2. Separately, before any new heavy mass-support run, re-read the frozen Iteration-455 manifest and select only the next exact `UNTESTED` coordinate after rank 11; never infer by `u↔v` symmetry and never rerun already-certified rank 10.

Guardrails retained: no physical `D_s` promotion, no threshold relaxation, no support reordering/deduplication, no ANSATZ-003, no Fisher/resources.