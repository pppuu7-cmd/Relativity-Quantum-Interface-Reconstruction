# RQIR Candidate Gravity — Iteration 589

Date: 2026-09-08

## Repository authority / prerequisite consumption
Repository authority had advanced to Iter588. Its canonical fixture-binding workflow run `34204633941` completed successfully and produced artifact `10047224530`; Iter588 freezes the actual Iter368/Iter582 three-mode source geometry: for each singleton `s/a/b`, the complementary two modes form the mixed K2 pair, with both block orientations retained. No duplicate run was launched.

## Scientific question
Before assembling a full `K1-G-K2 + K2-G-K1` off-shell Ward block, fix the relative sign/factor between Iter587 K1 and Iter584 mixed K2 from one and the same MSSC-001 scalar dynamics. Otherwise a later apparent cancellation could be manufactured by a normalization error.

Parent action and convention:

`S_phi=-1/2 int sqrt(-g)[g^{mu nu} partial_mu phi partial_nu phi + m^2 phi^2]`,

`g=eta+kappa h`, `eta=(+---)`, `m=0.7`.

Use the common scalar inverse kernel

`K[g]=-p'_cov (sqrt(-g) g^-1) p_cov + m^2 sqrt(-g)`.

Its first variation is

`K1(h;p',p)=-p'_cov a1(h) p_cov + m^2 b1(h)=(1/2) h_{mu nu} V^{mu nu}(p',p)`.

Its mixed second variation is

`K2(h1,h2;p',p)=-p'_cov bt(h1,h2) p_cov + m^2 bs(h1,h2)`,

with exactly the raw-valid Iter584 coefficients

`bs=trH1 trH2/4 - Tr(H1 H2)/2`,

`bt=E h1 E h2 E + E h2 E h1 E -(trH1/2)E h2 E -(trH2/2)E h1 E + bs E`.

Therefore K1 and K2 are first and mixed-second derivatives of one `K[g]`; no independent factor or sign is allowed.

## Raw-valid audit
Prospective script commit: `c9f4b337a298b656bf0c3181a55b80b2dc52c6c5`.
Workflow commit: `50146e43c520a29812eec430cd1143f150b3c652`.
Canonical run: `34209731847`, artifact `10049270625`.
Raw result SHA256: `90cd8591f34deb9a7c0d49a21d554bc8f776c5c2fdbef30cbcd88455b2a0dfad`.
Audit classification: `PASS_RAW_AUTHORITY_AUDIT_ITER589_K1_K2_NORMALIZATION`; failures `[]`.

Numerical verification over 24 deterministic generic off-shell probes:
- max `|K1-(1/2)h.V| = 1.1102230246251565e-16`;
- max pure-gauge Ward mismatch `=3.552713678800501e-15`;
- prospective tolerance `2e-12`.

On the exact Iter368/Iter588 fixture the same-action mixed K2 values are:
- singleton `s`, pair `[a,b]`, source `q^2=1`: `0.017288072919771053`;
- singleton `a`, pair `[s,b]`, source `q^2=0.14`: `0.0053626356708712805`;
- singleton `b`, pair `[s,a]`, source `q^2=0.34`: `0.014363470813301032`.

Classification: `PASS_MSSC001_K1_K2_SAME_ACTION_NORMALIZATION_CONTRACT__NON_RESIDUAL`.

This is a same-dynamics normalization PASS only. It is not a full K1+K2 Ward cancellation test, not a source tree value, not a comparator residual, not Candidate-Gravity consistency PASS/FAIL, not exact comparator identity, not near-degeneracy, not model-level non-identifiability, and not a novelty certificate. Source/Born subtraction remains NOT PERFORMED. ANSATZ-003 and Fisher/resources remain forbidden.

## Readiness
Stable rubric remains:
- comparator foundation 24/25
- unique residual discovery 0/20
- frozen parent dynamics/ANSATZ 0/20
- consistency/positivity/Ward/causality 0/15
- identifiability/Fisher 0/10
- resource/experiment closure 0/10

MODEL_READINESS: 24%

Change from previous assessment: 0 percentage points. A real hard normalization subgate is closed, but the complete same-fixture Ward object and comparator-subtracted residual are still not closed, so no rubric sector gains a point.

## Exact next gate
Using exactly this normalization, Iter588 singleton/pair routing, and both frozen block orientations, assemble the complete same-fixture `K1-G-K2 + K2-G-K1` source response and test the correct off-shell Ward identity including the Iter587 inverse-propagator RHS. Only after that may it be mapped to Iter582 and passed into the unchanged fixed C3/C4/C5/nonlocal/asymptotic-safety comparator quotient.
