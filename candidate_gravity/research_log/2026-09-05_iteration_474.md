# Candidate Gravity Research Log — Iteration 474

Date: 2026-09-05

## Starting audit
Repository source of truth was read from `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, Iteration 473 recovery delta/research log, recent commits, and the canonical rank-9 Actions run. The active numerical gate remains Iteration-455 distinct rank 9, `u=+5e-6`, `v=-5e-6`, multiplicity 2, run `33983416847`. At the start and during this iteration the run remained `in_progress`, so no duplicate heavy calculation was launched.

Physical/operator authority remains Iteration 411. Physical blocker authority remains Iteration 421. Exact unresolved physical set remains `[2]`.

## Exact precision-nullspace result
For the frozen central4 coefficients

`c=(1/12,-2/3,+2/3,-1/12)`

we have exactly `sum(c)=0` and `||c||_2^2=65/72`.

Let the 4x4 MP precision-discrepancy matrix be `DeltaF = F80-F120`, with dimensionless assembled mixed-derivative discrepancy

`DeltaD = c^T DeltaF c`.

Define the exact double-centering projector

`P = I - (1/4) 11^T`.

Because `c^T 1 = 0 = 1^T c`,

`DeltaD = c^T P DeltaF P c` exactly.

Therefore the mixed derivative is exactly invariant under every row/column-separable contamination

`DeltaF -> DeltaF + a 1^T + 1 b^T + gamma 11^T`.

Only the interaction component `DeltaF_int=P DeltaF P` can affect the assembled mixed derivative. A correlation-aware exact bound follows from Cauchy-Schwarz/Frobenius:

`|DeltaD| <= ||c||_2^2 ||DeltaF_int||_F = (65/72)||P DeltaF P||_F`.

An exact counterexample uses `DeltaF=a 1^T`, `a=(1,2,3,4)`: the largest local absolute discrepancy is 4, while `P DeltaF P=0` and hence assembled `DeltaD=0` exactly. Thus Iteration 472 and Iteration 474 together establish that local MP precision status alone is neither sufficient nor necessary for the assembled mixed-derivative MP status.

Classification: `PASS_MIXED_DERIVATIVE_PRECISION_NULLSPACE__DIAGNOSTIC_ONLY_NON_PROMOTING`.

## Future assembled audit requirement
After all 28 distinct support coordinates are locally certified, the already-frozen assembled MP80↔MP120 audit must additionally:
- construct `DeltaF=F80-F120` for each BASE/HALF stencil;
- compute `DeltaF_int=P DeltaF P`;
- verify `c^T DeltaF c = c^T DeltaF_int c` up to arithmetic roundoff;
- report `||DeltaF_int||_F` and the bound `(65/72)||DeltaF_int||_F` alongside Iterations 460/471/472 diagnostics.

Violation of the exact double-centering identity is implementation/provenance `BLOCKED`, never Candidate-Gravity consistency FAIL. This diagnostic does not replace the frozen assembled scaled MP80↔MP120 threshold `2e-6` and changes no estimator, threshold, dynamics, support ordering, or promotion rule.

No literature update was required because this iteration establishes an exact internal algebra/provenance identity and makes no comparator, novelty, or external consistency claim.

MODEL_READINESS: 24%

Readiness change: 0 percentage points; the precision-attribution/provenance layer improved, but no additional component of the stable readiness rubric closed.

## Exact next gate
The numerical next gate remains raw-consumption of canonical run `33983416847` fail-closed. Only if rank 9 passes may Iteration-455 distinct rank 10 launch under unchanged frozen conventions. `ANSATZ-003` remains uncreated; Fisher/resources remain forbidden until a nonzero robust algebraic residual exists.
