# 2026-09-06 — Iteration 497

## Objective
Resume from repository authority while canonical rank18 remains live, avoid duplicate heavy Actions, and close one independent exact assembly/provenance gate without weakening frozen physics or numerical thresholds.

## Source-of-truth audit
Read first: `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_496.md`, `research_log/2026-09-06_iteration_496.md`, recent commits, and live canonical rank18 run `34029482604`. The run remains `in_progress`; no duplicate heavy run was launched.

## Exact parity/projection authority
Frozen mixed central4 uses nodes `[-2,-1,+1,+2]` and coefficients `c=[1/12,-2/3,+2/3,-1/12]`. Exact antisymmetry gives `c(-x)=-c(x)`. Therefore for tensor weights `w(x,y)=c(x)c(y)`:

- `w(-x,y)=-w(x,y)`;
- `w(x,-y)=-w(x,y)`;
- `w(-x,-y)=w(x,y)`.

For arbitrary sampled data define the odd-odd projector

`F_oo(x,y)=[F(x,y)-F(-x,y)-F(x,-y)+F(-x,-y)]/4`.

Direct exact coefficient comparison proves

`D_h[F] = D_h[F_oo]`.

Thus every sector even in `x` or even in `y` is annihilated exactly by the mixed estimator. Equivalently, the full 16-sample signed sum may be assembled as four positive-quadrant orbit differences

`sum_{a,b>0} c_a c_b [F(a,b)-F(-a,b)-F(a,-b)+F(-a,-b)]`.

Classification: `PASS_CENTRAL4_ODD_ODD_PARITY_PROJECTION_EXACT__NON_PROMOTING`.

This is estimator/assembly provenance authority only. It is not Candidate-Gravity consistency PASS/FAIL, not comparator identity, not regime-specific non-identifiability, not near-degeneracy, and not novelty authority. A future violation of the parity identity is implementation/provenance `BLOCKED`. Frozen thresholds, dynamics, source order, BASE/HALF convention and `ds=-d_base` are unchanged.

Reproducible audit: `candidate_gravity/code/iteration497_central4_odd_odd_parity_projection_audit.py`.
Machine-readable result: `candidate_gravity/results/iteration497_central4_odd_odd_parity_projection_audit.json`.

## Active numerical gate
Canonical frozen-manifest rank18 `(u,v)=(-2.5e-6,-5e-6)`, multiplicity 1, run `34029482604`, job `101476261793`, remains `in_progress`. Certified occurrence coverage therefore remains `22/32 = 68.75%`, i.e. `1760/2560` rows.

## Retained blockers
Physical/operator authority remains Iteration 411. Iteration 421 remains raw-valid `BLOCKED_CONVERGENCE` for unresolved set `[2]`. Robust comparator-subtracted residual is absent. `ANSATZ-003` remains uncreated. Fisher/resources remain forbidden.

## Readiness
MODEL_READINESS: 24%
Readiness change: 0 percentage points. Iteration 497 closes an exact assembly/provenance subgate but no additional stable model-readiness rubric component.

## Exact next gate
Fail-closed raw-consume rank18 run `34029482604` after completion. Only raw scientific PASS permits frozen manifest rank19 `(-2.5e-6,-2.5e-6)`; BLOCKED requires first-failure localization without threshold weakening, zero-fill, or dynamics changes.
