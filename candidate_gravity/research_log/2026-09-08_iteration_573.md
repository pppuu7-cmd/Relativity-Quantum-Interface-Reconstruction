# RQIR Candidate Gravity — Iteration 573

**Date:** 2026-09-08  
**Classification:** `PASS_ITER424_AUTHORITY_MAPPING_AUDIT__DIRECT_INHERITED_PASS__TENSOR11_OPERATIONAL_BLOCKED__NON_PROMOTING`

Repository source of truth advanced during this run: Iteration 571 raw-consumed QUARTER rank12 PASS (`12/12` new support, `16/16` grid), and Iteration 572 assembled the complete QUARTER **pre-spectral sample layer**. The corrected Iteration572 scope is retained: raw `(z,phi)` sample-layer assembly is not yet the spectrum-integrated physical `F(u,v)` or scalar `D_s`.

## Authority mapping audit

The frozen Iteration421 addendum defines the independent full tensor-degree-(1,1) clause as `fit_residuals_scaled.tensor11 <= 2e-5` in the symmetric-cross representation. The underlying Iteration420/421 code computes that statistic from cross-ratio values

`C(r,s)=[F(r,s)-F(r,-s)-F(-r,s)+F(-r,-s)]/(4rs)`

on the prospectively frozen squared-radius design with multipliers `[1, 0.75, 0.5, 0.25]^2`.

Iteration572 instead reports a diagnostic bilinear fit directly on the raw pre-spectral QUARTER `F(u,v)` 4x4 node grid. These are different observables and different design matrices. There is no prospectively frozen algebraic identity mapping one statistic to the other. Therefore the very small raw-grid residual (`4.588236089297399e-15`) may not be substituted for the frozen tensor11 physical criterion.

This is classified strictly as **OPERATIONAL_BLOCKED_NO_FROZEN_MAPPING**, not a Candidate-Gravity consistency FAIL, not exact comparator identity, not model-level non-identifiability, not near-degeneracy, and not a novelty certificate.

## Direct-original clause

Iteration421 raw authority reports maximum direct-original-integrand crosscheck scaled error `2.0658997659274425e-9`, safely below the frozen `2e-6` threshold. Iteration424 prospectively freezes the *same parent dynamics, routing, numerator, sign and normalization*. The direct-original check tests the identity of that unchanged parent representation rather than the later BASE/HALF/QUARTER derivative-fit geometry. Therefore this clause is inherited exactly as `PASS_INHERITED_FROM_RAW_VALID_ITERATION421`; no Iteration421 mass-step or fit-residual conclusion is inherited with it.

## Current frozen five-clause status

- direct original-integrand crosscheck: **PASS_INHERITED**;
- physical mass-step discrepancy: **BLOCKED** pending full spectrum-integrated BASE/HALF/QUARTER physical `D_s` reconstruction;
- fixed-node MP80/MP120 agreement: **BLOCKED** pending the same reconstruction;
- finiteness: **BLOCKED** at physical spectrum-integrated level;
- tensor-degree-(1,1) fit residual: **OPERATIONAL BLOCKED** because no prospectively frozen map from the Iteration572 raw-grid diagnostic to the Iteration421/424 tensor11 statistic exists.

No physical index2 promotion is authorized. `ANSATZ-003` remains uncreated; Fisher/resources remain forbidden.

## Readiness

MODEL_READINESS: 24%

Change: `0 pp`. A real authority ambiguity is resolved and one frozen clause is inherited with exact provenance, but no additional model-level rubric sector closes.

## Exact next gate

Reconstruct full spectrum-integrated `F(u,v)` at MP80/MP120 using only already raw-valid support and the frozen Iteration407 pipeline (phi-average at TRAIN_Z, degree-4 interpolation, frozen affine coefficients, analytic affine-log recurrence), then assemble BASE/HALF/QUARTER physical `D_s` and evaluate mass-step/cross-precision/finiteness. Separately search existing pre-result contracts for an already frozen tensor11 high-precision definition; absent such authority, the tensor11 clause must remain operationally BLOCKED rather than being redefined post hoc.
