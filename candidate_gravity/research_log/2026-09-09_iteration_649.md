# RQIR Candidate Gravity — Iteration 649

## Scope and authority

This iteration starts from authoritative Iter648 and treats the repository as source of truth. The gate is deliberately algebraic/provenance-only: no Iter645 numerical values, no matched residual, no comparator values, no ANSATZ-003, no Fisher/resource calculation.

Frozen authority used:

- Iter621 normalization-authority graph: source normalization and native normalization are two internally linked sectors with **zero cross-sector normalization equations**; after quotienting a common convention, exactly one relative nonzero complex scalar remains.
- Iter647: the pre-result absolute closed-CTP -> native Gamma3 normalization bridge is permanently provenance-BLOCKED for the already computed absolute branch.
- Iter648: the separately frozen projective observable removes exactly one common nonzero complex factor from the closed-Gamma3 amplitude ray.
- Iter589/594/616 remain the source-sector normalization lineage; they are not retrospectively redefined by the closed-CTP projective branch.

## Question

Can the Iter648 projective quotient make Source/Born subtraction legal without an additional normalization bridge?

Write the two frozen normalization classes as

\[
A_{\rm cand}=\lambda_{\rm native} a,
\qquad
A_{\rm Born}=\lambda_{\rm source} b,
\qquad
\lambda_{\rm native},\lambda_{\rm source}\in\mathbb C^*.
\]

Then

\[
A_{\rm matched}
=\lambda_{\rm native}a-\lambda_{\rm source}b
=\lambda_{\rm native}(a-\rho b),
\qquad
\rho\equiv\lambda_{\rm source}/\lambda_{\rm native}.
\]

The Iter648 projector

\[
P[x]=\frac{x x^\dagger}{x^\dagger x}
\]

removes the overall \(\lambda_{\rm native}\), but it does **not** remove the relative complex scalar \(\rho\):

\[
P[A_{\rm matched}]=P[a-\rho b].
\]

Changing the independent source/native normalization changes \(\rho\), and therefore generically changes the projective direction. Thus projectivization commutes with subtraction only if a stronger condition is already established before looking at results: (i) \(b\equiv0\), (ii) exact full-domain collinearity with prospectively fixed proportionality, or (iii) a same-parent authority equation fixes \(\rho\). None of these conditions is frozen authority at Iter648, and this iteration does not inspect Candidate values to manufacture one.

## Scientific result

\[
\boxed{\texttt{BLOCKED\_ITER649\_SOURCE\_BORN\_RELATIVE\_NORMALIZATION\_NOT\_BOUND}}
\]

\[
\boxed{\texttt{\_\_PROJECTIVIZATION\_CANCELS\_ONLY\_COMMON\_GLOBAL\_FACTOR\_\_NON\_RESIDUAL}}
\]

This is an **operational/provenance BLOCKED** result. It is not a Candidate-Gravity consistency FAIL/PASS, not exact comparator identity, not regime-specific non-identifiability, not near-degeneracy, and not a novelty certificate.

Negative result retained for the article funnel: a normalization-free projective closed-CTP observable does **not** by itself license subtraction of a source/Born object belonging to an independently normalized authority sector.

No Source/Born subtraction was performed. No fixed C3/C4/C5/nonlocal/asymptotic-safety quotient was evaluated. ANSATZ-003 and Fisher/resources remain forbidden.

## Readiness

MODEL_READINESS: 24%

Change from Iter648: **0 percentage points**. The subtraction obstruction is now algebraically isolated, but robust unique residual discovery remains 0/20 and no additional complete readiness-rubric sector has closed.

## Exact next gate

**Iter650:** prospectively determine whether a physically legitimate two-ray diagnostic/comparator can be defined on \(([a],[b])\) and be invariant under independent \(\mathbb C^*\times\mathbb C^*\) rescalings, without claiming that such a diagnostic equals Source/Born subtraction. A natural algebraic candidate to audit is the projector overlap \(\mathrm{Tr}(P_aP_b)\), but its physical/comparator semantics must be established from frozen authority before any values are read. If no such semantics exists, the Source/Born subtraction branch remains BLOCKED until an independently derived same-parent equation fixes \(\rho\).
