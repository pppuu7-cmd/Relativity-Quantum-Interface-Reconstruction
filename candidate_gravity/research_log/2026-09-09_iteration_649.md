# RQIR Candidate Gravity — Iteration 649

## Scope and authority

This iteration starts from authoritative Iter648 and treats the repository as source of truth. The gate is algebraic/provenance-only: no Iter645 numerical values, no matched residual, no comparator values, no ANSATZ-003, no Fisher/resource calculation.

Frozen authority used:

- Iter621 normalization-authority graph: source and native normalization are two internally linked sectors with **zero cross-sector normalization equations**; after quotienting a common convention, exactly one relative nonzero complex scalar remains.
- Iter629 open-vs-closed topology audit: the historical open scalar response and closed gravitational 1PI object have family-total vectors `[-1,6,-6]` and `[1,-3,2]`, hence ratios `[-1,-2,-3]`; therefore one common scalar `N_native` cannot identify them.
- Iter647: the pre-result absolute closed-CTP -> native Gamma3 normalization bridge is permanently provenance-BLOCKED for the already computed absolute branch.
- Iter648: the separately frozen projective observable removes exactly one common nonzero complex factor from the closed-Gamma3 amplitude ray.
- Iter589/594/616 remain the source-sector normalization lineage; they are not retrospectively redefined by the closed-CTP projective branch.

## Algebraic gate

Write

\[
A_{\rm cand}=\lambda_{\rm native} a,
\qquad
A_{\rm Born}=\lambda_{\rm source} b,
\qquad
\rho=\lambda_{\rm source}/\lambda_{\rm native}.
\]

Then

\[
A_{\rm matched}=\lambda_{\rm native}(a-\rho b).
\]

The Iter648 projector

\[
P[x]=\frac{x x^\dagger}{x^\dagger x}
\]

removes the overall \(\lambda_{\rm native}\), but not \(\rho\):

\[
P[A_{\rm matched}]=P[a-\rho b].
\]

Therefore projectivization alone cannot legalize subtraction across independently normalized sectors. The sufficient one-common-factor case requires a same-parent matched Source/Born contribution whose CTP/Legendre normalization is frozen before results. Iter629 additionally forbids using the historical open scalar response as a closed gravitational Born proxy: its topology/relative family combinatorics are not proportional to the closed 1PI object. Unsupported `b=0` is also forbidden.

## Scientific result

\[
\boxed{\texttt{BLOCKED\_ITER649\_MATCHED\_SOURCE\_BORN\_ONE\_COMMON\_FACTOR\_CLASS\_NOT\_ESTABLISHED}}
\]

\[
\boxed{\texttt{\_\_OPEN\_RESPONSE\_PROXY\_REJECTED\_BY\_ITER629\_\_NON\_RESIDUAL}}
\]

This is an **operational/provenance BLOCKED** result. It is not Candidate-Gravity consistency FAIL/PASS, exact comparator identity, regime-specific non-identifiability, near-degeneracy, or a novelty certificate.

Negative result retained for the article funnel: normalization-free projectivization does not commute with Source/Born subtraction when relative normalization is unbound, and the historical open response cannot supply the missing closed Born object by one scalar rescaling.

No Source/Born subtraction was performed. No native Y/T_cut projection or fixed C3/C4/C5/nonlocal/asymptotic-safety quotient was evaluated. ANSATZ-003 and Fisher/resources remain forbidden.

## Readiness

MODEL_READINESS: 24%

Change from Iter648: **0 percentage points**. The subtraction obstruction is now algebraically and topology-authority certified, but robust unique residual discovery remains 0/20 and no additional complete readiness-rubric sector has closed.

## Exact next gate

**Iter650:** derive or locate an explicit **same-parent Source/Born contribution inside the closed retarded gravitational \(\Gamma_3\) matched observable**, with CTP/Legendre normalization and pole/cut origin fixed before subtraction. If no committed definition exists, record that prerequisite as the terminal operational blocker. Do not use the Iter629 open response as a proxy, do not zero-fill the missing term, and do not replace Source/Born subtraction by a newly convenient projective diagnostic unless an independent derivation proves equivalence.
