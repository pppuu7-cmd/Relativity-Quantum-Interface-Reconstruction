# Candidate Gravity Research Log — Iteration 627

Date: 2026-09-09

Iteration 627 prospectively freezes the combined matter+gravity Schwinger-Keldysh measurement contract required by Iter626, before any native Candidate/comparator projection is inspected.

Classification:

`PASS_ITER627_PROSPECTIVE_COMBINED_MATTER_GRAVITY_SK_CONTRACT__RETARDED_SCALAR_PROBE__PHYSICAL_HR_INSERTIONS__INTERNAL_SCALAR_LINES_INDUCED_RETARDED__RAW_VALID_NON_RESIDUAL`.

The frozen contract is `MSSC001-GRAVITY-SK-MEAS-V1` with doubled action/source structure

`Z[J_phi+,J_phi-,J_g+,J_g-] = Integral exp{i S[g+,phi+] - i S[g-,phi-] + i(source+ - source-)}`,

and the same r/a rotation used by the historical gravitational CTP authority,

`X_+ = X_r + X_a/2`, `X_- = X_r - X_a/2`, for `X in {h,phi}`.

The physical metric background is `h_a=0` and the three metric insertions are derivatives with respect to `h_r`. The measured scalar-probe observable is frozen as the retarded component `G_phi^{ra}=G_R`.

For the same-branch unitary action `S_+-S_-`, the prospective validator gives the exact rotation coefficients `rrr=0`, `arr=rar=rra=1`, `aaa=1/4`. Using the causal scalar propagator matrix `G_rr=G_K`, `G_ra=G_R`, `G_ar=G_A`, `G_aa=0`, and the physical-`h_r` inverse-kernel insertion with off-diagonal entries, direct noncommutative matrix multiplication yields

`(G dK G)_{ra} = G_R dK_R G_R`,

`(G dK G dK G)_{ra} = G_R dK_R G_R dK_R G_R`,

and analogously at cubic order. Therefore every surviving internal scalar line in the measured `ra` response is induced as `G_R`; it is not assigned root-by-root.

Endpoint amputation remains identity-preserving: after the third `h_r` derivative, remove only the two common external scalar `G_R` factors. Retain local K3, all six ordered K1/K2 placements, all six ordered K1^3 chains, and every internal scalar propagator: total family census remains 13.

The contract reduces to the Iter171 gravitational r/a normalization in the overlap and leaves the Iter149 native gravitational observable in its retarded/in-in sector. No Iter582 connection values or Iter615 projected coefficients were used to select branch, phase, or normalization.

GitHub Actions run `34284449139` completed successfully on head `e701b1d0aa8a0222e7ec6cfbc6b5baf8263378c0`. Its fail-closed exact comparison passed and artifact `10078890134` was uploaded with digest `sha256:03a8167e23d946006024971ad02824070b700c94124465bb11982fed87dbe67e`.

Scientific consequence: the historical Iter608/615 `K+i0` coefficients remain valid only in their declared source-side convention. They are not hand-flipped. For the native bridge, the scalar-pole coefficients must now be recomputed from the same Iter594 13-family source dynamics using the prospectively frozen retarded internal-line prescription. This is a prescription change demanded by the newly frozen observable, not a post-hoc root repair.

Source/Born subtraction remains `NOT_PERFORMED`; comparator quotient, `ANSATZ-003`, Fisher and resources remain forbidden.

MODEL_READINESS: 24%.

Readiness change: 0 percentage points. The CTP measurement-map blocker is closed prospectively, but no robust comparator-subtracted residual exists and no additional full readiness-rubric sector is closed.

Exact next gate: Iteration 628 must derive and recompute the six supported scalar-pole discontinuity coefficients under the frozen retarded prescription, preserving Iter613/614 kinematics and all 13 source families, and must derive the common source-to-native phase/normalization from this same SK convention before any native projection.