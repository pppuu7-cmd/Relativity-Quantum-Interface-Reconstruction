# Candidate Gravity Research Log — Iteration 618

Date: 2026-09-08

## Classification

`PASS_ITER618_SIX_ROOT_PROJECTIVE_SOURCE_SHAPE_CERTIFICATE__N_NATIVE_INVARIANT_NON_RESIDUAL`

Iteration 618 uses only the six Iter615 source-side coefficients and the pre-existing Iter614 root coordinates. It does not determine or tune `N_native` and does not use Iter582 numerical values.

## Kinematic ordering / anti-bias anchor

The six roots are ordered by strict ascending `s`, an authority that existed before the source coefficients were evaluated. The projective anchor is the smallest-s root `D_b^-(s=0.013028588574858)`, not the coefficient with largest/smallest magnitude.

With that anchor, the five independent projective ratios are

`[-2.966563737084728, -0.7800526753639322, -5.158207913624242, +2.997420912767025, +8.57973421373656]`.

The raw source sign pattern in ascending-s order is

`[-,+,+,+,-,-]`.

Relative to the negative anchor the projective sign pattern is

`[+,-,-,-,+,+]`.

These ratios are invariant under any one common nonzero real or complex multiplication `c_r -> N_native c_r`. Deterministic regression under common scales `2.5`, `-3`, `+i`, and `2-1.5i` gives maximum ratio drift `8.881784197001252e-16`.

## Future fail-closed use

Any future source-to-native bridge that claims one common `N_native` must preserve all five independent ratios root-by-root. A disagreement is an implementation/projection corruption and blocks promotion before Source/Born subtraction or comparator quotient.

No roots are summed merely because they share an external q2 bucket. No native Candidate value is inferred. No residual is created.

MODEL_READINESS: 24%

Readiness change: `0 pp`.
