# Candidate Gravity Research Log — Iteration 526

Date: 2026-09-07

## Source-of-truth audit
Read `CURRENT_QG_FRONT.md`, Iteration-524 recovery, fresh commits, queued/in-progress Actions and recently completed relevant runs before changing the front. At the initial audit there were `0 queued / 0 in_progress`; prior assembly attempts had stopped at provenance resolution and had not evaluated the frozen assembly math.

## Historical provenance closure
Iteration-525 recursive exact-JSON preflight resolved 23/28 coordinates and left ranks `{1,2,8,9,10}`. Historical committed authority was then read directly. Ranks 1,2,8,9 each have exact single raw artifact plus scientific JSON SHA authority. Rank 10 `(+5e-6,+5e-6)` is different: its full five-z certificate is explicitly composite, using Iteration-449 artifact `9958661360` / scientific SHA `d7a148b1f55364145612e3c032aaa13a24634b87bad965a9f40a4d1db2b478bb` for z `{-0.86,0,+0.86}` and Iteration-450 artifact `9959560285` / scientific SHA `2467e807b8b5f1c8a93a83a1e5be2107d2c5ae3d8747bb2f5f586b16501d1c03` for z `{-0.43,+0.43}`.

Committed `results/post525_historical_support_provenance_bridge.json` records the five historical authorities without inventing a single-artifact rank-10 provenance. The assembly implementation now accepts 28 coordinates backed by 29 exact artifact parts, verifies every scientific payload SHA, verifies each historical z partition, rejects duplicate `(z,phi)` samples and requires exactly 80 merged samples at every coordinate.

Frozen science is unchanged: independent BASE/HALF central4 assembly, `ds=-d_base`, assembled BASE and HALF MP80/MP120 scaled discrepancy `<=2e-6`, BASE↔HALF mass-step scaled discrepancy `<=2e-5`, all finite, no Richardson promotion.

## Anti-idle continuation
With no other queued/in-progress RQIR gate, the exact multipart assembly was triggered once from commit `8ff6ef9d3b1a946ca3215c5905e1432adbb18fe8`. Canonical run `34089957999`, job `101641252363`; at logging time it is queued. No duplicate heavy run was launched.

Physical/operator authority remains Iteration 411; blocker authority remains Iteration 421 `BLOCKED_CONVERGENCE`, unresolved physical set `[2]`. Robust comparator-subtracted residual is absent; ANSATZ-003 and Fisher/resources remain BLOCKED.

MODEL_READINESS: 24%

Readiness change: 0 percentage points.
