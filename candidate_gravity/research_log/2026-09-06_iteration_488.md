# RQIR Candidate Gravity — Iteration 488

Date: 2026-09-06  
MODEL_READINESS: 24%

Canonical rank14 run `34008118612` remained `in_progress` at the start of this iteration; no duplicate heavy run was launched.

This iteration closes an exact spectral/provenance subgate for the frozen one-dimensional central4 operator with coefficients `c=[1/12,-2/3,2/3,-1/12]` on nodes `[-2,-1,+1,+2]`. Acting on a Fourier mode `exp(i k x)` with `theta=k h`, its exact dimensionless symbol is

`A(theta)=i sin(theta) (4-cos(theta))/3`,

so the derivative eigenvalue is `lambda_h=A(theta)/h` and the normalized transfer function is

`R(theta)=sin(theta)(4-cos(theta))/(3 theta)`.

Because `4-cos(theta)>0` for every real `theta`, the real zero structure is inherited entirely from `sin(theta)`. Therefore there are no interior zeros of the frozen odd derivative symbol for `0<|theta|<pi`; the only Nyquist-cell zeros are the continuum zero at `theta=0` and the Nyquist endpoint `|theta|=pi`. In particular, the central4 stencil introduces no additional interior Fourier blind spot in the resolved open Nyquist interval.

For the tensor mixed estimator, the exact symbol is `D_h=A(theta_x)A(theta_y)/h^2`; at HALF step `h/2` for the same physical wave numbers it is `D_half=4 A(theta_x/2)A(theta_y/2)/h^2`. This gives a future assembly sanity contract independent of the Iteration487 Taylor expansion: any implementation of the frozen BASE/HALF operator must reproduce these exact symbols mode-by-mode.

Classification: `PASS_CENTRAL4_FOURIER_SYMBOL_EXACT__NON_PROMOTING`.

Scientific scope is deliberately narrow. This is an exact estimator/provenance result, not Candidate-Gravity consistency PASS, not comparator identity, not regime-specific non-identifiability, not near-degeneracy, and not a novelty certificate. It does not authorize early assembly, Richardson promotion, support reordering, or physical promotion of index 2. A violation by implementation is provenance/implementation `BLOCKED`, not physics FAIL.

No comparator/novelty claim changed, so no literature update was materially required. `ANSATZ-003` remains absent; Fisher/resources remain forbidden.

Readiness change: 0 percentage points. The exact spectral sanity contract closes a numerical-estimator subgate only and no additional stable readiness-rubric component.

MODEL_READINESS: 24%
