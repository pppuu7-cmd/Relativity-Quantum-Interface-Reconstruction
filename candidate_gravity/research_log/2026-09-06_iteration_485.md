# RQIR Candidate Gravity — Iteration 485

**Date:** 2026-09-06  
**Authority type:** exact frozen-stencil provenance/assembly audit; non-promoting  
**Classification:** `PASS_SHARED_BASE_HALF_CERTIFICATE_WEIGHT_RATIO_EXACT__NON_PROMOTING`

Repository source of truth at launch: Iteration 484. `CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_484.md`, recent research authority, recent commits, frozen Iteration-455 manifest, and active Actions state were re-read before this step. Canonical rank13 run `34003038811` remained `in_progress`; no duplicate heavy run was launched.

## Exact result
Frozen central4 first-derivative coefficients on nodes `[-2,-1,+1,+2]` are

`c = [1/12, -2/3, +2/3, -1/12]`.

For the mixed derivative the level-specific stencil weight is `w_ij = c_i c_j / h^2`. The four exact BASE/HALF shared physical coordinates are `(±5e-6,±5e-6)`. On BASE (`h_B=5e-6`) they are the inner `±h_B` nodes, so each one-dimensional coefficient has magnitude `2/3`; on HALF (`h_H=2.5e-6=h_B/2`) the same physical coordinates are the outer `±2h_H` nodes, so each coefficient has magnitude `1/12`.

Therefore the coefficient-product ratio is `(1/12)^2/(2/3)^2 = 1/64`, while the inverse-step-squared ratio is `h_B^2/h_H^2 = 4`. Hence for every shared coordinate

`w_HALF / w_BASE = (1/64)*4 = 1/16`.

Signs are preserved coordinate-by-coordinate: the scaled BASE weights are `(+4/9,-4/9,-4/9,+4/9)` and the corresponding HALF weights are `(+1/36,-1/36,-1/36,+1/36)` before the common `1/h_B^2` normalization, giving exactly `w_HALF = w_BASE/16`.

## Scientific meaning
Iteration 457 remains valid: a local MP precision certificate may be reused at an exact BASE/HALF coordinate overlap because the sampled `F(u,v)` is identical. But the derivative contribution must never be reused with the same weight. BASE and HALF assemblies must apply their own frozen level-specific weights; treating a shared certificate as a shared contribution would introduce an exact factor-16 assembly error at all four overlaps.

This is an implementation/provenance identity only. Violation is `OPERATIONAL/IMPLEMENTATION BLOCKED`, not Candidate-Gravity consistency FAIL. It is not comparator identity, regime-specific non-identifiability, near-degeneracy, novelty evidence, or physical promotion.

Reproducible audit: `candidate_gravity/code/iteration485_shared_overlap_weight_audit.py`. Machine-readable result: `candidate_gravity/results/iteration485_shared_overlap_weight_audit.json`.

Frozen dynamics, parameter convention, source order, thresholds, `ds=-d_base`, and all physics gates are unchanged. `ANSATZ-003` remains uncreated. Fisher/resources remain forbidden.

## Readiness
Comparator foundation `24/25`; unique residual discovery `0/20`; frozen parent dynamics/ANSATZ `0/20`; consistency/positivity/Ward/causality `0/15`; identifiability/Fisher `0/10`; resource/experiment closure `0/10`.

MODEL_READINESS: 24%

Readiness change: **0 percentage points**. A provenance/assembly identity was closed, but no additional stable readiness-rubric component was completed.

## Next gate
Fail-closed raw-consume canonical rank13 run `34003038811`. Only on raw scientific PASS may coverage advance from `17/32=53.125%` to `18/32=56.25%` and frozen rank14 `(u,v)=(+1e-5,+5e-6)` be launched. If rank13 is BLOCKED, localize the first failing `z/phi/radial` sample without changing frozen thresholds or dynamics.
