# 2026-09-05 — Iteration 452

## Objective
Perform an independent source-order/multiplicity audit while the authorized Iteration-451 mass-node run remains in progress, without duplicating that run and without changing any physical or numerical gate.

## Repository-source-of-truth state
Physical/operator authority remains Iteration 411. Raw-valid physical blocker remains Iteration 421 for double-double index 2 / class 3 / q^2=-1; exact unresolved physical set remains `[2]`. Iteration 450 remains the latest completed full-training-z precision authority at one mass coordinate. Run `33935454815` at `u=v=-1e-5` remains the active numerical gate and is not duplicated.

## Frozen stencil multiplicity result
Iteration 407 evaluates `derivative_from_analytic(BASE_H)` and `derivative_from_analytic(HALF_H)` with nodes `[-2h,-h,+h,+2h]` in nested u-major/v-major order. The inherited constants are `BASE_H=5e-6`, `HALF_H=2.5e-6`.

Thus there are 32 source occurrences (16 BASE + 16 HALF), but only 28 distinct mass coordinates. The two grids overlap at exactly four coordinates:

- `(-5e-6,-5e-6)`
- `(-5e-6,+5e-6)`
- `(+5e-6,-5e-6)`
- `(+5e-6,+5e-6)`

Each overlap coordinate has multiplicity two; every other coordinate has multiplicity one.

This does **not** alter the frozen 32-source-occurrence denominator and does not weaken the gate. It adds a second, distinct-coordinate view used only to avoid redundant exact same-`F(u,v)` recomputation and to state coverage correctly.

## Consequence for Iteration 450 bookkeeping
The already raw-valid Iteration-450 full-training-z certificate at `u=v=+5e-6` applies to a mass-only `F(u,v)` sample path with the same z/phi/radial/precision conventions and therefore covers both the BASE and HALF source occurrences at that exact coordinate. Under the retained occurrence-based denominator this corresponds to 2/32 stencil occurrences, i.e. 160/2560 output-row occurrences = 6.25%. Under the auxiliary distinct-coordinate view it is 1/28 coordinates = 3.571428...%.

The previously recorded 80/2560 = 3.125% figure is therefore a conservative bookkeeping undercount of source-occurrence coverage, not a physics error. No prior scientific PASS or FAIL is changed.

## Active-gate source order
The active coordinate `u=v=-1e-5` occurs only once in the 32-source-occurrence manifest. If run `33935454815` raw-consumes as PASS, the next untested BASE source-order coordinate is prospectively fixed as

`u=-1e-5, v=-5e-6`.

No next-node run is launched before raw consumption of the active gate.

## Classification
`PASS_MASS_SUPPORT_SOURCE_OCCURRENCE_MULTIPLICITY_AUDIT__NON_PROMOTING`

This is not a Candidate-Gravity consistency FAIL, not comparator identity, not regime-specific non-identifiability, not near-degeneracy and not a novelty certificate. `ANSATZ-003` remains uncreated. Fisher/resources remain forbidden.

MODEL_READINESS: 24%

Readiness change: **0 percentage points**. This iteration corrects numerical-provenance bookkeeping and removes redundant-computation ambiguity but closes no stable model-readiness rubric component.

## Exact next gate
Raw-consume run `33935454815` fail-closed. PASS closes `u=v=-1e-5` and permits only the next source-order coordinate `(-1e-5,-5e-6)` with unchanged z/phi/radial/precision conventions. BLOCKED requires first-failure localization at the active coordinate. The 32-occurrence frozen denominator remains authoritative for gate accounting; the 28-distinct-coordinate manifest is auxiliary deduplication provenance only.
