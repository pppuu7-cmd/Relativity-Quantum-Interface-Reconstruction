# Candidate Gravity Research Log — Iteration 529

Date: 2026-09-07

## Source-of-truth audit
Read `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_527.md`, research log Iteration 527, recent commits, the frozen Iteration-424 fallback contract, the Iteration-527 assembled authority, the quarter-rank1 stage, and live Actions.

Race-created repository state already contained Iteration-528 staging. Its first run `34094343153`, job `101654453787`, failed before sampling. The failure was operational: the stage expected obsolete top-level keys `scientific_gate_pass` and `authoritative_iteration`, whereas the Iteration-527 raw authority stores `observed.scientific_authority_pass` and `iteration`. No quarter-step physical values were computed. The repository then repaired this authority binding and launched one repaired run `34094463024`; at the time of this audit it was `in_progress`. No duplicate heavy run was launched.

## Exact quarter-support manifest audit
The frozen Iteration-424 mass steps are `5e-6`, `2.5e-6`, and `1.25e-6`. For central4 support at `h=1.25e-6`, each axis uses `{-2h,-h,+h,+2h} = {-2.5e-6,-1.25e-6,+1.25e-6,+2.5e-6}`. Therefore the full quarter Cartesian support contains exactly 16 coordinates.

The already-certified HALF support at `h=2.5e-6` contains axis nodes `{-5e-6,-2.5e-6,+2.5e-6,+5e-6}`. Exact coordinate intersection with the quarter grid is therefore only `{(-2.5e-6,-2.5e-6),(-2.5e-6,+2.5e-6),(+2.5e-6,-2.5e-6),(+2.5e-6,+2.5e-6)}`: exactly 4 coordinates. Consequently the quarter central4 reevaluation requires exactly `16-4 = 12` new coordinates. No other HALF coordinate may be reused without violating exact coordinate identity.

Under the frozen deterministic u-major/v-major order after removing those four exact overlaps, the first new coordinate is `(-2.5e-6,-1.25e-6)`, exactly the target of the staged quarter-rank1 computation.

Classification:
`PASS_ITER424_QUARTER_SUPPORT_MANIFEST_EXACT__NON_PROMOTING`.

This is an exact support/provenance PASS. It does not satisfy the frozen Iteration-424 physical mass-step, direct-integrand, tensor-fit, assembled 80/120 `D_s`, or finite-output clauses. A local quarter-support precision PASS remains only a prerequisite for those physical clauses.

The failed first Iteration-528 run is strictly `operational BLOCKED`, not a Candidate-Gravity consistency FAIL, comparator identity, regime-specific non-identifiability, near-degeneracy, or novelty result.

## Model readiness
Comparator foundation `24/25`; robust unique residual `0/20`; frozen parent dynamics/ANSATZ `0/20`; consistency/positivity/Ward/causality `0/15`; identifiability/Fisher `0/10`; resource/experiment closure `0/10`.

MODEL_READINESS: 24%

Readiness change: 0 percentage points. This iteration proves exact support necessity and prevents invalid reuse, but closes no additional model-level rubric sector.

## Exact next gate
Raw-consume repaired quarter-rank1 run `34094463024` after completion. Only raw-valid PASS may advance to quarter source rank2, preserving the same parent dynamics, mass-step `1.25e-6`, MP80/120 convention, z/phi/radial support, thresholds, and deterministic source order. ANSATZ-003 remains forbidden; Fisher/resources remain forbidden until a nonzero algebraic residual exists.
