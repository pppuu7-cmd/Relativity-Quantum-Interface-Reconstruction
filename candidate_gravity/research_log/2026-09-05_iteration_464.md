# 2026-09-05 — Iteration 464

## Exact leading truncation structure of the frozen central4×central4 mixed derivative

Repository front at entry: Iteration 463 is latest completed numerical/research authority; physical/operator authority remains 411 and blocker authority remains 421 with unresolved set `[2]`. Canonical rank-5 run `33962417750` remains in progress and was not duplicated.

Using the frozen central4 nodes `(-2,-1,+1,+2)` and coefficients `(1/12,-2/3,+2/3,-1/12)`, exact rational arithmetic extends the Iteration-462 moment audit to

`m0=0, m1=1, m2=m3=m4=0, m5=-4, m6=0, m7=-20, m8=0, m9=-84`.

Therefore the one-dimensional operator obeys

`D_h f = f' - (h^4/30) f^(5) - (h^6/252) f^(7) + O(h^8)`.

For the tensor-product mixed derivative with the same level step in both auxiliary-mass directions,

`D_uv,h F = F_uv - (h^4/30)(F_{5,1}+F_{1,5}) - (h^6/252)(F_{7,1}+F_{1,7}) + O(h^8)`;

the product of the two leading one-dimensional truncation errors first enters at order `h^8` with coefficient `+1/900` on `F_{5,5}`.

Because `h_HALF=h_BASE/2`, a genuinely leading `h^4` truncation contribution scales BASE/HALF by exactly `16`, while a leading `h^6` contribution scales by `64`. The algebraic combination `(16 D_half-D_base)/15` cancels a pure `h^4` term, but is frozen here as a **diagnostic-only** quantity: it MUST NOT replace the retained physical estimator `ds=-d_base`, MUST NOT weaken the frozen BASE↔HALF scaled discrepancy threshold `<=2e-5`, and MUST NOT replace the Iteration-458 assembled MP80↔MP120 threshold `<=2e-6`.

Scientific classification: `PASS_CENTRAL4_LEADING_TRUNCATION_STRUCTURE__DIAGNOSTIC_ONLY_NON_PROMOTING`.

This is not a Candidate-Gravity consistency FAIL, not comparator identity, not regime-specific non-identifiability, not near-degeneracy, and not a novelty certificate. Its use is diagnostic/provenance only: after full support closure, an observed BASE/HALF difference can be compared against the exact 16/64 asymptotic scaling signatures without changing any frozen gate. Failure to match either signature is not itself physics FAIL because multiple derivative orders and non-asymptotic terms may coexist.

`MODEL_READINESS: 24%`

Readiness change: 0 percentage points. The iteration sharpens the numerical diagnostic contract but closes no new component of the stable readiness rubric.

Next gate remains fail-closed raw consumption of canonical run `33962417750` at Iteration-455 distinct rank 5, `u=-5e-6, v=-5e-6`. No later coordinate is authorized before that raw consumption.
