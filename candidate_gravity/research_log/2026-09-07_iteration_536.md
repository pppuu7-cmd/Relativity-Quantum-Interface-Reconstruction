# Candidate Gravity Research Log — Iteration 536

Date: 2026-09-07

## Repository/source-of-truth check
At the start of this iteration, `CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_535.md`, the Iteration-535 research log, recent commits, and live Actions were checked. Canonical QUARTER rank2 run `34102627559` remained `in_progress`; no duplicate heavy Action was launched.

## Exact three-level truncation-error component inversion
Use the already frozen central4 three-level smooth expansion with BASE=`h`, HALF=`h/2`, QUARTER=`h/4`:

`X_h = D + A + B + C + O(h^10)`, with `A=a h^4`, `B=b h^6`, `C=c h^8`.

Define
- `d1 = BASE - HALF`,
- `d2 = HALF - QUARTER`.

Exact inversion of the h4/h6 two-component system gives

`Ahat = (-16 d1 + 1024 d2)/45`,

`Bhat = (256 d1 - 4096 d2)/189`.

These are exact coefficient-recovery diagnostics through h6. Including the first neglected h8 component gives the exact contaminations

`Ahat = A - (17/64) C + O(h^10)`,

`Bhat = B + (425/336) C + O(h^10)`.

The prospectively frozen Iteration-533 three-level Richardson diagnostic

`R3=(BASE-80 HALF+1024 QUARTER)/945`

therefore satisfies

`R3 = D + C/1344 + O(h^10)`.

This quantifies, before QUARTER assembly, exactly how the first neglected h8 term enters all three diagnostics. It also gives a direct post-assembly consistency check: if the inferred `Ahat`, `Bhat`, and the observed difference ratio are mutually incompatible beyond the expected h8 contamination structure, the common smooth three-level truncation model is not empirically dominant in that regime. Such a mismatch would be a scoped numerical/truncation diagnostic result, not by itself a Candidate-Gravity consistency FAIL.

Classification:
`PASS_ITER424_THREE_LEVEL_H4_H6_ERROR_COMPONENT_INVERSION_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`.

Reproducible audit: `candidate_gravity/code/iteration536_iter424_three_level_error_component_exact_audit.py`.
Machine-readable result: `candidate_gravity/results/iteration536_iter424_three_level_error_component_exact_audit.json`.

## Guardrails
No threshold, mass node, parent dynamics, parameter convention, precision setting, or successor order was changed. These formulas are diagnostic only. They cannot replace, weaken, extrapolate around, or rescue the frozen Iteration-424 five-clause physical acceptance gate. They do not promote physical index 2 and do not authorize ANSATZ-003, Fisher, or resources.

The fixed C3/C4/C5/nonlocal/asymptotic-safety comparator quotient remains operationally BLOCKED because the concrete upstream `Source/Ward/contact+K2` target and robust comparator-subtracted residual are still absent. No exact comparator identity, regime-specific non-identifiability, near-degeneracy, or novelty certificate is claimed.

## Model readiness
Comparator foundation `24/25`; robust unique residual `0/20`; frozen parent dynamics/ANSATZ `0/20`; consistency/positivity/Ward/causality `0/15`; identifiability/Fisher `0/10`; resource/experiment closure `0/10`.

MODEL_READINESS: 24%
Readiness change: 0 percentage points. A genuine diagnostic subgate was closed, but no additional stable model-readiness rubric sector was completed.

## Exact next gate
Do not duplicate rank2. After canonical run `34102627559` reaches terminal state, raw-consume its artifact fail-closed. Raw-valid PASS authorizes only frozen rank3 `(-1.25e-6,-2.5e-6)`; scientific FAIL/BLOCKED stops advancement; operational failure permits only minimal technical repair of rank2.
