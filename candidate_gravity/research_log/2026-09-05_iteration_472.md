# Candidate Gravity Research Log — Iteration 472

Date: 2026-09-05

## Starting audit
Read `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `ITERATION471_RECOVERY_DELTA.md`, `2026-09-05_iteration_471.md`, recent commits, iteration registry, and current Actions. Repository source of truth is at Iteration 471; rank-8 run `33980045356` remains `in_progress`, so no duplicate numerical run is allowed.

## Scientific step
Iterations 458 and 460 already forbid promoting local MP sample PASS directly to assembled derivative precision. This iteration closes the exact local-to-assembled implication and gives a constructive counterexample under the actual central4 sign structure.

For any linear assembly `D=sum_i w_i F_i`, define

`delta_i = F80_i-F120_i`,

`s_i = max(1,|F80_i|,|F120_i|)`,

`e_i = |delta_i|/s_i`,

`E = sum_i |w_i| s_i`.

If every local sample satisfies `e_i <= eps_local`, then exact triangle inequality gives

`|DeltaD| <= B_sample_delta <= eps_local * E`,

where `DeltaD=D80-D120` and `B_sample_delta=sum_i |w_i||delta_i|` is the Iteration-460 sample-level weighted discrepancy bound.

Therefore a sufficient condition for the assembled scaled MP threshold `tau` is

`eps_local * E / max(1,|D80|,|D120|) <= tau`.

Local scaled precision alone is not sufficient because the amplification envelope `E/max(1,|D80|,|D120|)` is not bounded by the local certificate.

An exact constructive counterexample uses two equal-and-opposite central4 sample weights `+4/9,-4/9`: set `F120=(10^30,10^30)` and `F80=(10^30+1,10^30-1)`. Both local scaled discrepancies are `<=1e-30`, yet `D120=0`, `D80=8/9`, so the assembled scaled discrepancy is exactly `8/9`, vastly above the frozen `2e-6` threshold. This is purely a linear-conditioning/provenance statement, not physics.

Classification: `PASS_LOCAL_TO_ASSEMBLED_MP_SUFFICIENCY_CONTRACT__NON_PROMOTING`.

Operational rule: after support completion, assembled BASE and HALF MP80/120 evaluation must report either the exact `B_sample_delta` or equivalently enough information to verify the stronger inequality above. A local MP PASS, irrespective of how small its per-sample scaled discrepancies appear, never substitutes for the frozen assembled gate. Failure to prove the assembled bound is `BLOCKED`/unproven precision, not Candidate-Gravity consistency FAIL.

No estimator, threshold, dynamics, support order, or promotion rule changed. `ds=-d_base`, assembled MP threshold `<=2e-6`, BASE↔HALF threshold `<=2e-5`, and all existing contracts remain binding.

MODEL_READINESS: 24%

Readiness change: 0 percentage points; this closes a precision-provenance implication but no stable readiness-rubric component.
