# RQIR Candidate Gravity — Research Log Iteration 577

Date: 2026-09-08  
MODEL_READINESS: 24%  
Readiness change: 0 pp

## Source-of-truth start
Authoritative repository front was Iteration576. Iter424 remains four frozen clauses PASS plus the unchanged original Iter421 tensor11 clause BLOCKED. Canonical 36-node matrix run `34180521559` remained `in_progress` and was not duplicated. Physical index2 remains unpromoted; exact15 remains unauthorized.

## Exact tensor11 leverage / single-orbit robustness audit
The frozen degree-(1,1) tensor11 design uses the 4x4 grid generated by the one-axis squared multiplier coordinates

`x in {1, 9/16, 1/4, 1/16}`

with basis `[1,x,y,xy]`. Hence the 16x4 design factorizes as the tensor product of the one-axis 4x2 design `A=[1,x]` with itself. Therefore the 2D hat matrix factorizes exactly as `H = H1 tensor H1`.

For the one-axis design,

`(A^T A)^(-1) = [[59/86,-40/43],[-40/43,256/129]]`,

and the exact one-axis leverages are

`[209/258, 23/86, 89/258, 149/258]`.

Thus each 2D symmetric-cross orbit leverage is the product of the corresponding one-axis leverages. Exact checks give:
- sum of all 16 leverages = 4, equal to the number of fitted parameters;
- minimum 2D leverage = `529/7396 ~= 0.07152514872904273`;
- maximum 2D leverage = `43681/66564 ~= 0.6562255874046031`;
- every leverage is strictly below 1;
- minimum diagonal element of the residual projector `I-H` is therefore `22883/66564 ~= 0.3437744125953969 > 0`.

Consequences:
1. Every one of the 16 `C(r,s)` observations has strictly nonzero residual sensitivity; there is no observation that is structurally interpolated with zero residual weight.
2. Because every leverage is `<1`, deleting any single observation preserves the full four-parameter design rank. No single sign-orbit is algebraically indispensable to rank identifiability.
3. This does **not** authorize dropping any frozen orbit or support node. All 64 signed F nodes remain mandatory because Iter421 froze the complete statistic before results.
4. This is a design-robustness statement only. It neither evaluates the tensor11 numerical residual nor changes its threshold, precision, radius grid, observable, or provenance.

Classification:
`PASS_ITER421_TENSOR11_LEVERAGE_SINGLE_ORBIT_ROBUSTNESS_AUDIT_EXACT__NON_PROMOTING`

Reproducible artifacts:
- `candidate_gravity/code/iteration577_tensor11_leverage_robustness_audit.py`
- `candidate_gravity/results/iteration577_tensor11_leverage_robustness_audit.json`

## Scientific state retained
- Iter424 mass-step: PASS.
- Iter424 direct-original: PASS.
- Iter424 tensor11: BLOCKED pending raw-valid completion of all 36 missing nodes and unchanged original fit.
- Iter424 Ds MP80/MP120: PASS.
- Iter424 finiteness: PASS.
- physical index2: not promoted.
- exact15: not authorized.
- comparator quotient: operational BLOCKED upstream of concrete Source/Ward/contact+K2 residual.
- `ANSATZ-003`: not created.
- Fisher/resources: forbidden.

## Exact next gate
When run `34180521559` is terminal, fail-closed consume all 36 rank artifacts and authority audits. If every rank is raw-valid PASS, combine those 36 nodes with the already validated 28 nodes and compute the unchanged original Iter421 tensor11 residual independently at MP80 and MP120. Only residual `<=2e-5` at the required precision, together with the other four frozen PASS clauses, may close Iter424 5/5 and promote physical index2.

MODEL_READINESS: 24%

Readiness remains unchanged because this iteration closes a genuine design-robustness subgate but completes no additional model-level readiness rubric point.
