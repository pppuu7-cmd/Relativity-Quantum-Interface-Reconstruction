# RQIR Candidate Gravity — Iteration 570

**Date:** 2026-09-08  
**Classification:** `PASS_ITER424_QUARTER_SEPARABLE_CORRELATED_COVARIANCE_CONTRACT_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`

## Objective
Freeze the exact propagation of correlated coordinate-level numerical errors through the already frozen Iteration-424 QUARTER 4x4 central4 mixed stencil, before the final rank12 support value is known. This prevents post-hoc use of an iid error model after assembly.

## Exact result
Let `w=(1,-8,8,-1)`, `C=w⊗w`, and

`D_uv=<C,F>/(144 h^2)`.

For a zero-mean numerical error grid `E` with separable covariance

`Cov(vec E)=Sigma_v ⊗ Sigma_u`, 

Kronecker contraction gives exactly

`Var(D_uv) = (w^T Sigma_u w)(w^T Sigma_v w)/(144^2 h^4)`.

For a symmetric factor `Sigma=(S_ij)`, the one-dimensional quadratic form is

`w^T Sigma w = S00 -16 S01 +16 S02 -2 S03 +64 S11 -128 S12 +16 S13 +64 S22 -16 S23 + S33`.

Because `sum(w)=0`, for the all-ones matrix `J` we have

`w^T J w=(sum w)^2=0`.

Therefore a perfectly common correlated error mode in either separable tensor factor is annihilated exactly by the mixed stencil. More generally only covariance projected onto the derivative-weight direction contributes to the assembled variance.

## Interpretation
This generalizes the prior iid/heteroskedastic error-conditioning results to a correlated separable covariance model. It is an exact assembly-error statement only. It is not a Candidate-Gravity consistency PASS/FAIL, not an exact comparator identity, not model-level non-identifiability, not near-degeneracy, and not a novelty certificate.

The result does not authorize support substitution, rank skipping, precision changes, threshold changes, smaller `h`, or any modification of frozen Iteration-424 acceptance. Rank12 remains mandatory.

## Heavy state during this iteration
Canonical rank12 `(+2.5e-6,+1.25e-6)` remains the only authorized successor. Run `34175330006`, job `101903475600` was still in progress during the iteration, with the scientific MP stage active and audit/upload pending. No duplicate run was launched.

## Model readiness
Stable rubric remains:
- comparator foundation `24/25`;
- robust unique residual `0/20`;
- frozen parent dynamics/ANSATZ `0/20`;
- consistency/positivity/Ward/causality `0/15`;
- identifiability/Fisher `0/10`;
- resource/experiment closure `0/10`.

`MODEL_READINESS: 24%`

Change from previous estimate: **0 percentage points**. A real covariance/assembly subgate is closed, but no additional model-level rubric sector is completed.
