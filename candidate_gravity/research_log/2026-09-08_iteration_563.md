# RQIR Candidate Gravity — Research Log, Iteration 563

## Goal
Freeze an exact, non-promoting assembly/error contract for the final two QUARTER support coordinates while canonical rank10 is already running. This is intentionally prospective: no rank11/rank12 raw values are used.

## Source-of-truth state
At iteration start, repository authority had advanced beyond Iteration561. Iteration562 raw-consumed QUARTER rank9 `(+1.25e-6,+1.25e-6)` as PASS, closing `9/12` new QUARTER coordinates and `13/16` total grid coordinates including the four exact HALF-overlap corners. Frozen rank10 `(+1.25e-6,+2.5e-6)` is the sole active heavy successor in run `34165534613`; no duplicate run is authorized.

## Exact post-rank10 tail reduction
The frozen central4 first-derivative weights on scaled nodes `[-2,-1,+1,+2]` are

`w=[1,-8,+8,-1]`,

so the mixed tensor numerator coefficient is `w_i w_j` and the normalized mixed derivative uses `1/(144 h^2)`.

After a future raw-valid PASS of rank10, every QUARTER grid value will be authoritative except the frozen final pair:

- rank11 `(+2h,-h)` with integer coefficient `+8`;
- rank12 `(+2h,+h)` with integer coefficient `-8`.

Therefore the entire remaining unknown contribution to the normalized QUARTER mixed stencil is exactly

`T_tail = [F(+2h,-h)-F(+2h,+h)]/(18 h^2)`.

Equivalently, with

`m=[F11+F12]/2`, `d=[F11-F12]/2`,

one has

`T_tail = d/(9 h^2)`

and the common mode `m` has exactly zero coefficient.

This reduces the remaining unknown assembly space from two support coordinates to one assembly-relevant differential mode. It does NOT reduce the required support set: both rank11 and rank12 remain mandatory under the frozen Iteration532 order.

## Fail-closed error propagation
For bounded coordinate errors

`|e11|<=eps11`, `|e12|<=eps12`,

exact linear propagation gives

`|delta T_tail| <= (eps11+eps12)/(18 h^2)`.

For independent equal-variance errors `Var(e11)=Var(e12)=sigma^2`,

`Var(T_tail)=sigma^2/(162 h^4)`,

so

`Std(T_tail)=sigma/(9 sqrt(2) h^2)`.

These are numerical/assembly conditioning statements only.

## Classification
`PASS_ITER424_POST_RANK10_TWO_POINT_TAIL_CONTRACT_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`

Reproducible audit:
`candidate_gravity/code/iteration563_iter424_post_rank10_two_point_tail_contract_exact.py`

Machine-readable authority:
`candidate_gravity/results/iteration563_iter424_post_rank10_two_point_tail_contract_exact.json`

## Scientific interpretation
This result is not a Candidate-Gravity consistency PASS/FAIL, not exact comparator identity, not regime-specific model non-identifiability, not model-level near-degeneracy, and not a novelty certificate. It is a prospective exact assembly-integrity/error-propagation contract.

No u<->v substitution, no rank skipping, no zero fill, no threshold/node/precision changes, no ANSATZ-003, and no Fisher/resources are authorized.

## Heavy state
Canonical rank10 run `34165534613` is active. Workflow status alone is not scientific authority; only fail-closed raw consumption may close rank10 and authorize rank11.

## MODEL_READINESS
`MODEL_READINESS: 24%`

Readiness change: `0 percentage points`. A genuine assembly/error subgate is closed, but no additional stable model-level rubric sector is complete.
