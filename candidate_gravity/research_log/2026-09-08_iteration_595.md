# RQIR Candidate Gravity — Research Log — Iteration 595

Date: 2026-09-08

## Starting authority
Repository source of truth at start was Iteration 594. `CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_594.md`, the latest research log, Iter594 routed source code, Iter587 Ward log and recent commits were read before work. No newer race-created front existed and no duplicate Action was running or started.

## Scientific question
Is the existing authority already sufficient to define the **full cubic nonlinear diffeomorphism Ward target** for the exact Iter594 13-term source response without introducing any new convention?

The available anchor is the authoritative Iter587 one-graviton identity

`q_mu V^{mu nu}=(p'^2-m^2)p^nu-(p^2-m^2)p'^nu`,

which on the frozen symmetric off-shell routing becomes

`q_mu V^{mu nu}=(m^2-s/4)q^nu`.

Iter594 in turn gives the complete same-action cubic inverse-kernel response

`d_abc G = -G Kabc G + sum_6(G Ki G Kjk G) - sum_6(G Ki G Kj G Kk G)`.

## Authority audit
A tempting shortcut would be to replace one source polarization by

`h_gauge(mu,nu)=q_mu xi_nu+q_nu xi_mu`

while leaving the other two source perturbations fixed, then call the resulting contraction the nonlinear Ward test.

That shortcut is **not authorized** as the full nonlinear diffeomorphism identity.

For a metric perturbation about flat space, the diffeomorphism variation already at the next order contains

`delta_xi h_{mu nu}=partial_mu xi_nu+partial_nu xi_mu + xi^rho partial_rho h_{mu nu}+h_{rho nu} partial_mu xi^rho+h_{mu rho} partial_nu xi^rho + O(h^2)`.

Therefore a cubic Ward contraction is not determined solely by the linear pure-gauge replacement of one graviton leg. It also needs the nonlinear Lie-derivative action on the spectator graviton legs plus the corresponding transformation of the scalar/source endpoints. These pieces carry momentum-routing and sign information in Fourier space.

The current authoritative chain freezes:
- the Iter587 one-leg inverse-propagator RHS;
- Iter588 three-mode momenta and singleton/pair routing;
- Iter589 same-action K1/K2 normalization;
- Iter590/594 complete cubic inverse-kernel family structure.

It does **not** yet freeze:
1. a Fourier-space gauge-parameter convention `xi_i` on the exact Iter588 fixture;
2. the nonlinear spectator-leg Lie-derivative routing/sign rule;
3. the associated scalar/source endpoint transformation terms;
4. the explicit cubic recursive Ward RHS tying K3, all six K1/K2 placements and all six K1^3 chains together.

Hence the full nonlinear Ward target is presently underdetermined by frozen authority. Inventing one now and immediately evaluating it would violate the no-post-hoc-convention rule.

## Classification
`PASS_NONLINEAR_WARD_TARGET_AUTHORITY_AUDIT__FULL_TARGET_NOT_YET_FROZEN__OPERATIONAL_BLOCKED_NON_RESIDUAL`

This is a protocol/authority result. It is **not**:
- Candidate-Gravity consistency FAIL;
- Candidate-Gravity consistency PASS;
- exact comparator identity;
- regime-specific non-identifiability;
- near-degeneracy;
- novelty certificate;
- comparator-subtracted residual.

Iter594 remains valid exactly as scoped: complete routed cubic source assembly and permutation-symmetry PASS, **NON_WARD**.

## Consequence
The next legal step is not numerical evaluation. First prospectively freeze the full Fourier-space nonlinear diffeomorphism action on the exact Iter588 fixture, including spectator-leg and scalar-endpoint terms, and prove its one-leg limit reproduces Iter587. Only then can the full 13-term Ward identity be evaluated.

Source/Born subtraction remains `NOT_PERFORMED`. The fixed C3/C4/C5/nonlocal/asymptotic-safety comparator quotient remains `OPERATIONAL_BLOCKED`. `ANSATZ-003` remains forbidden. Fisher/resources remain forbidden.

## Readiness rubric
- comparator foundation: `24/25`;
- unique residual discovery: `0/20`;
- frozen parent dynamics/ANSATZ: `0/20`;
- consistency/positivity/Ward/causality: `0/15`;
- identifiability/Fisher: `0/10`;
- resource/experiment closure: `0/10`.

MODEL_READINESS: 24%

Change from Iteration 594: **0 percentage points**. The iteration closes a real authority ambiguity and prevents a false nonlinear Ward PASS/FAIL, but it does not close a readiness sector or create a comparator-subtracted residual.

## Exact next gate
Prospectively define and freeze the momentum-space nonlinear diffeomorphism recursion for the exact Iter588 three-mode source fixture:
- choose the gauge leg and `xi` convention;
- include the Lie-derivative action on both spectator graviton legs;
- include scalar/source endpoint terms;
- require exact reduction to the Iter587 one-graviton inverse-propagator identity when spectator sources are removed.

After that contract is frozen in a separate pre-result artifact, evaluate the full Iter594 K3 + six K1/K2 + six K1^3 Ward identity. Only a closed full-source Ward gate permits source-to-Iter582 mapping and then the fixed comparator quotient.
