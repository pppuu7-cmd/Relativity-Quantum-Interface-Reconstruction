# Candidate Gravity Research Log — Iteration 521

**Date:** 2026-09-07  
**MODEL_READINESS: 24%**

## Start-state checks
Read repository-authoritative `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, latest `RECOVERY_DELTA_ITERATION_520.md`, research log Iteration 520, recent commits, and live GitHub Actions. Repository state superseded chat state: rank26 was already raw-consumed in Iteration 520 and certified support was `31/32 = 96.875%`. Canonical rank27 run `34078407905`, job `101608912613`, remained `in_progress`; no heavy duplicate was launched.

## Exact sharp minimax interval certificate
Iteration 519 established the sufficient local-scaled→assembled MP envelope. Iteration 521 sharpens that result under the same explicitly scoped coordinate-wise interval model.

For each sampled support coordinate let local MP comparison imply an interval error bound

`|e_i| <= eps*S_i`, with `S_i=max(1,|x80_i|,|x120_i|)`.

For any frozen assembly row `w_i` in units of `1/h^2`, the assembly perturbation is

`delta A = h^-2 sum_i w_i e_i`.

Because the admissible error set is a Cartesian product of intervals, linear optimization over this box gives the exact extremal radius

`sup_box |delta A| = eps*h^-2 sum_i |w_i| S_i`.

Equality is attained by choosing `e_i = eps*S_i*sign(w_i)` (or the global negative). Therefore the Iteration-519 triangle-inequality envelope is not merely sufficient or conservative within this interval model: it is the exact sharp minimax bound.

The frozen exact row norms remain

- `||W_BASE||_1 = 9/4`,
- `||W_HALF||_1 = 9`,
- `||W_BASE-W_HALF||_1 = 397/36`,
- support union = 28 distinct coordinates.

Classification:

`PASS_LOCAL_MP_INTERVAL_TO_ASSEMBLED_SHARP_MINIMAX_CERTIFICATE_EXACT__NON_PROMOTING`.

Reproducible audit: `candidate_gravity/code/iteration521_local_mp_sharp_minimax_certificate.py`.
Machine-readable result: `candidate_gravity/results/iteration521_local_mp_sharp_minimax_certificate.json`.

## Scientific meaning and guardrails
This is numerical/provenance authority only. It does not imply assembled MP PASS before the complete support amplitudes `S_i` are actually evaluated; it does not promote physical index 2; it is not Candidate-Gravity consistency PASS/FAIL; it is not comparator identity, regime-specific non-identifiability, near-degeneracy, or a novelty certificate. `ANSATZ-003` remains uncreated. Fisher/resources remain forbidden.

The result strengthens the downstream assembly logic: once rank27 is raw-consumed, the actual per-coordinate `S_i` values can be inserted into the sharp minimax expression before/alongside direct independent BASE/HALF MP80/120 assembly. This cannot replace the frozen assembled gate, because the latter evaluates the actual assembled quantities and the retained BASE↔HALF mass-step discrepancy.

## Retained blockers
Physical/operator authority remains Iteration 411. Physical blocker remains Iteration 421 `BLOCKED_CONVERGENCE`, unresolved set `[2]`. Fixed comparator quotient remains operationally BLOCKED because the concrete `Source/Ward/contact+K2` target does not yet exist. No robust comparator-subtracted residual exists.

**MODEL_READINESS: 24%**

Readiness change: **0 percentage points**. The exact numerical/assembly contract was strengthened, but no additional rubric sector closed.

## Exact next gate
Fail-closed raw-consume canonical rank27 run `34078407905` after completion. Only raw-valid PASS permits complete local support closure `32/32` and transition to frozen independent BASE/HALF MP80/120 central4 assembly. If rank27 is BLOCKED, stop support progression and localize the first failing `z/phi/radial` sample without changing frozen dynamics, support order, MP levels, radial hs, NPHI, z-grid, or thresholds.
