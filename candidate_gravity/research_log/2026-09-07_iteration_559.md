# RQIR Candidate Gravity — Iteration 559

Date: 2026-09-07

## Purpose
Pre-register an exact exchange-orbit closure/weight ledger for the frozen 4x4 QUARTER central4 grid while rank9 runs independently.

## Exact orbit census
Under `u<->v`, the 16 grid coordinates form exactly 10 orbits: 4 diagonal singletons and 6 off-diagonal pairs.

Using scaled nodes `{-2,-1,+1,+2}` and central4 integer weights `{1,-8,+8,-1}`, the exchange-orbit integer coefficient sums are:

- `(-2,-2)`: `+1` — exact HALF overlap;
- `(-2,-1)<->(-1,-2)`: `-16` — ranks1/3;
- `(-2,+1)<->(+1,-2)`: `+16` — ranks2/7;
- `(-2,+2)<->(+2,-2)`: `-2` — exact HALF-overlap pair;
- `(-1,-1)`: `+64` — rank4;
- `(-1,+1)<->(+1,-1)`: `-128` — ranks5/8;
- `(-1,+2)<->(+2,-1)`: `+16` — ranks6/11;
- `(+1,+1)`: `+64` — rank9;
- `(+1,+2)<->(+2,+1)`: `-16` — ranks10/12;
- `(+2,+2)`: `+1` — exact HALF overlap.

Total integer coefficient sum is exactly zero.

## Current closure
Before rank9 completion, 7/10 exchange orbits are fully raw-supported. Their integer coefficient sum is `-64`.

If rank9 raw-valid PASSes, it closes the `+64` diagonal orbit, giving 8/10 complete exchange orbits with completed-orbit coefficient sum exactly zero.

Then only two exchange orbits remain incomplete:
- ranks6/11 with coefficient sum `+16`;
- ranks10/12 with coefficient sum `-16`.

The remaining sector therefore also has exact total coefficient sum zero.

## Interpretation
This is a fail-closed assembly/provenance ledger only. It does not authorize support substitution, rank skipping or queue reordering. Every coordinate remains required in the prospectively frozen Iteration532 order.

After full support closure, the assembler should verify exact orbit membership and coefficient sums before evaluating the tensor stencil.

Machine-readable authority: `candidate_gravity/results/iteration559_iter424_quarter_exchange_orbit_closure_map.json`.

MODEL_READINESS: 24%

Readiness change: 0 percentage points.
