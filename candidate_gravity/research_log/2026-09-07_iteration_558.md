# RQIR Candidate Gravity — Iteration 558

Date: 2026-09-07

## Purpose
Strengthen the future QUARTER assembly with exact algebraic moment, null-space and operator-norm controls for the frozen central4 x central4 mixed auxiliary-mass stencil, independently of the running rank9 result.

## Exact single-axis central4 facts
For nodes `[-2h,-h,+h,+2h]` and integer weights `[1,-8,8,-1]`, normalized by `1/(12h)`, the integer moments for powers `k=0..6` are

`[0,12,0,0,0,-48,0]`.

Thus the first derivative is exact through polynomial degree 4 and has leading smooth truncation term `-(h^4/30) f^(5)(0)`.

Exact operator norms:
- L1 `=3/(2h)`;
- L2 `=sqrt(130)/(12h)`;
- Linf coefficient `=2/(3h)`.

## Tensor-product mixed stencil
The integer outer matrix is

`[[1,-8,8,-1],[-8,64,-64,8],[8,-64,64,-8],[-1,8,-8,1]]`

with normalization `1/(144 h^2)`.

Every row sum and every column sum is exactly zero. The matrix is exchange-symmetric. Therefore the mixed stencil exactly annihilates constants, any contribution depending on only one auxiliary-mass axis, and the exchange-odd component of a complete matched grid.

Exact mixed operator norms:
- L1 `=9/(4h^2)`;
- L2/Frobenius `=65/(72h^2)`;
- largest coefficient magnitude `=4/(9h^2)`.

Halving `h` multiplies these mixed norms by 4; QUARTER versus BASE multiplies them by 16. This is a deterministic conditioning fact, not a stochastic error model.

## Consequence
Future QUARTER assembly must fail closed if the frozen integer matrix, zero row/column sums, or exchange-even/full-grid identity is violated. No missing-node inference and no threshold change are authorized.

Machine-readable authority: `candidate_gravity/results/iteration558_iter424_central4_tensor_moment_norm_contract.json`.

MODEL_READINESS: 24%

Readiness change: 0 percentage points.
