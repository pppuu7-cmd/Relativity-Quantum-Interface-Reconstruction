# RQIR Candidate Gravity — Iteration 544

Date: 2026-09-07

Authoritative start was re-read from `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_543.md`, research log Iteration 543 and recent commits. Canonical QUARTER rank4 run `34126397439`, job `101755969291`, remained `in_progress`; no duplicate heavy Action was launched.

This iteration closes an independent exact ratio-free fail-closed diagnostic for the frozen Iteration-424 three-level truncation map. Define

`d1 = BASE-HALF`, `d2 = HALF-QUARTER`, `A=a h^4`, `B=b h^6`,

with the already frozen smooth two-component relations

`d1 = (15/16)A + (63/64)B`,

`d2 = (15/256)A + (63/4096)B`.

Two integer combinations isolate the components exactly:

`Q := d1-16 d2 = (189/256) B`,

`P := 64 d2-d1 = (45/16) A`.

Therefore

`A=(16/45)P`, `B=(256/189)Q`.

This removes the division by `d2` used by the step ratio and the division by `P` used by `B/A`. In exact arithmetic, the signs of `P,Q` are exactly the signs of the reconstructed `A,B`. Thus same-sign smooth h4+h6 truncation is equivalent (away from zero components) to `P*Q>0`; opposite-sign cancellation is equivalent to `P*Q<0`. The exact pure-component surfaces are `Q=0 <=> B=0` and `P=0 <=> A=0`.

For bounded observed-difference errors `|e1|<=eps1`, `|e2|<=eps2`, the induced radii are

`rho_P = eps1+64 eps2`,

`rho_Q = eps1+16 eps2`.

A fail-closed same-sign certificate is permitted only when both intervals `[P-rho_P,P+rho_P]` and `[Q-rho_Q,Q+rho_Q]` exclude zero and have the same sign. If both exclude zero with opposite signs, opposite-sign cancellation is certified for this truncation diagnostic. If either interval contains zero, classification is `BLOCKED/AMBIGUOUS_DIAGNOSTIC`; no sign/cancellation inference is permitted.

Classification: `PASS_ITER424_RATIO_FREE_H4_H6_CONE_FAIL_CLOSED_CONTRACT_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`.

This is numerical/truncation diagnostic authority only. It does not promote physical index 2 and is not Candidate-Gravity consistency PASS/FAIL, exact comparator identity, regime-specific non-identifiability, model-level near-degeneracy, or novelty certificate. No frozen threshold, dynamics, parameter convention, mass node, precision setting, or Iteration-532 successor order changed. `ANSATZ-003` remains uncreated; Fisher/resources remain forbidden until a nonzero comparator-surviving algebraic residual exists.

Machine authority: `candidate_gravity/results/iteration544_iter424_ratio_free_h4_h6_cone_contract.json`.
Reproducible audit: `candidate_gravity/code/iteration544_iter424_ratio_free_h4_h6_cone_contract.py`.

MODEL_READINESS: 24%

Readiness change: 0 percentage points. A robust diagnostic subgate closed, but no additional stable readiness-rubric sector closed.
