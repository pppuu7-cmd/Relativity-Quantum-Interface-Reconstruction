# RQIR Candidate Gravity — Iteration 568

**Date:** 2026-09-08

## Scope
Prospective exact conditioning audit for the frozen Iteration-424 QUARTER 4x4 central4 mixed-derivative stencil while rank11 is still in progress.

## Result
Classification:

`PASS_ITER424_QUARTER_COORDINATE_VARIANCE_LEVERAGE_MAP_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`

For `w=[1,-8,+8,-1]`, the tensor coefficient matrix is `C=w⊗w`, with `||C||_F^2=16900`. Under independent equal-variance coordinate errors, the exact fraction of assembled variance attributable to coordinate `(i,j)` is `C_ij^2/16900`.

The 16 coordinates split into three exact leverage classes:

- four central coordinates `(±h,±h)`: each `1024/4225 ≈ 24.236686%`; combined `4096/4225 ≈ 96.946746%`;
- eight edge coordinates with exactly one coordinate at `±2h`: each `16/4225 ≈ 0.378698%`; combined `128/4225 ≈ 3.029586%`;
- four corners `(±2h,±2h)`: each `1/16900 ≈ 0.005917%`; combined `1/4225 ≈ 0.023669%`.

The frozen final pair ranks11/12 are edge-class coordinates, so under this iid variance model their combined variance leverage is exactly `32/4225 ≈ 0.757396%`.

## Interpretation
This localizes numerical conditioning: if coordinate-level numerical errors are comparable and independent, the four central samples overwhelmingly control the mixed-stencil variance. This does **not** imply the edge/corner physical values are dispensable, nor does it authorize altered precision, skipped support, or substitution.

For heteroskedastic independent errors the exact variance is instead

`Var(D_uv)=sum_ij C_ij^2 sigma_ij^2/(144^2 h^4)`,

so equal-variance leverage percentages cannot be transferred blindly.

## Guardrails
- diagnostic-only, non-promoting;
- no rank skipping/reordering;
- no u↔v support substitution;
- no zero-fill;
- no precision/h/mass-node/threshold changes;
- no `ANSATZ-003`;
- no Fisher/resources.

## State
`MODEL_READINESS=24%` unchanged. At freeze time rank11 run `34168897005`, job `101885271903`, coordinate `(+2.5e-6,-1.25e-6)` remained in progress.
