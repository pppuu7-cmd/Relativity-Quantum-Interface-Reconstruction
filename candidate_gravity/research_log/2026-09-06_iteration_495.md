# RQIR Candidate Gravity — Research Log Iteration 495

Date: 2026-09-06
MODEL_READINESS: 24%

## Source-of-truth start
Read `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_494.md`, the Iteration-494 research log, recent commits, and live Actions state before work. Canonical rank17 run `34023698421`, job `101460814699`, remained `in_progress`; no duplicate heavy run was launched.

## Exact tensor-product central4 reproduction/nullspace authority
For frozen central4 nodes `[-2,-1,+1,+2]` and coefficients `c=[1/12,-2/3,2/3,-1/12]`, exact rational moments through degree four are

`M_k=sum_i c_i n_i^k = [0,1,0,0,0]` for `k=0..4`.

Because the mixed estimator is the tensor product, its monomial moments factor exactly: `M_ab=M_a M_b`. Therefore for all `0<=a,b<=4`, the only nonzero tensor moment is `M_11=1`. Hence, for every bivariate polynomial of degree at most four in each variable,

`D_h p(0,0) = partial_x partial_y p(0,0)`

exactly for every nonzero `h`. In particular `D_h(x*y)=1` exactly.

A stronger sample-space null identity also follows without any polynomial assumption. Since every row sum and every column sum of `w_ij=c_i c_j` vanishes exactly, every sampled additive field

`F_ij = f_i + g_j + const`

is annihilated exactly by the mixed assembly: `D_h=0`.

Classification: `PASS_CENTRAL4_TENSOR_NULLSPACE_POLYNOMIAL_REPRODUCTION_EXACT__NON_PROMOTING`.

This gives a fail-closed unit contract for future BASE and HALF assembly: any failure of bilinear normalization, additive nullspace, or degree-4 tensor reproduction is implementation/provenance `BLOCKED`, not Candidate-Gravity consistency FAIL. It does not change frozen dynamics, support order, thresholds, BASE/HALF conventions, or `ds=-d_base`; it does not promote physical index 2, comparator identity, non-identifiability, near-degeneracy, novelty, ansatz authority, Fisher authority, or resource closure.

Reproducible audit: `candidate_gravity/code/iteration495_central4_tensor_reproduction_audit.py`.
Machine-readable result: `candidate_gravity/results/iteration495_central4_tensor_reproduction_audit.json`.

## Active numerical gate
Canonical frozen rank17 `(u,v)=(-5e-6,+2.5e-6)`, multiplicity 1, run `34023698421`, job `101460814699`, remains the sole authorized heavy support gate and was still `in_progress` on the final live check. Scientific PASS requires raw artifact consumption after completion. Certified occurrence coverage remains `21/32 = 65.625%` pending raw consumption.

## Retained blockers
Physical/operator authority remains Iteration 411. Iteration 421 remains raw-valid `BLOCKED_CONVERGENCE` for unresolved set `[2]`. Robust comparator-subtracted residual is absent. `ANSATZ-003` remains uncreated; Fisher/resources remain BLOCKED.

## Readiness
MODEL_READINESS: 24%
Readiness change: 0 percentage points. Iteration 495 closes an exact assembly/provenance unit-contract subgate but no additional stable-rubric component.

## Exact next gate
Fail-closed raw-consume rank17 run `34023698421`. Only on raw scientific PASS may the next explicit `UNTESTED` Iteration-455 coordinate be authorized; on BLOCKED, localize the first failing `z/phi/radial` sample with frozen conventions unchanged.
