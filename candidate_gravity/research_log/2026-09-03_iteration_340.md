# RQIR Candidate Gravity Research Log — Iteration 340

Date: 2026-09-03

Repository front 339 and active Actions were checked first. Determinant Iteration 335 remained active and was not duplicated. The independent `e=2,c<=1` U2 branch was advanced.

Primary literature authority was re-read at arXiv:2006.04217v4, Eqs. (14)-(17). Eq. (17) fixes `A_{i gamma}=(D_i R^k_gamma) epsilon_k` and `U2=N A^T Hinv_VD A N Y` when `A` is stored field x ghost. Thus `V1_L=A.T` and `V1_R=A`; no ad-hoc transpose convention is allowed.

A second convention fact is now frozen: the paper defines `H * Hinv_VD=-1`. With the Iteration-319 differential operator called `K`, the actual U2 Green object is therefore `Hinv_VD=-K^{-1}`. Iteration 339's shifted routing for ordinary `K^{-1}` remains valid, but U2 assembly requires the global minus. This is a scoped convention correction, not a Candidate Gravity consistency FAIL.

An exact-rational executable check verifies the indexed Eq. (17) contraction against the typed matrix product and the global sign.

Authority: `PASS_U2_PRIMARY_AUTHORITY_V1_LEFT_RIGHT_ORIENTATION_AND_VD_GREEN_MINUS_SIGN__PHYSICAL_A1_A2_COMPONENTS_REMAIN_BLOCKED`.

MODEL_READINESS: 24%

Change from Iteration 339: `0 pp`; exact U2 orientation/sign authority closed, but no complete readiness bucket or robust comparator-subtracted residual closed.

Next gate: derive/validate physical same-parent `A1/A2` background coefficients from `(D R)*epsilon`, then close required N/Y inverse routing before physical `Tr U2` assembly.
