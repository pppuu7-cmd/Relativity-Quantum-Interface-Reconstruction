# Candidate Gravity Research Log — Iteration 477

Date: 2026-09-05

## Starting authority audit
Re-synchronized against repository source of truth after Iteration 476. `candidate_gravity/recovery/CURRENT_QG_FRONT.md` remains authoritative at MODEL_READINESS 24%. Physical/operator authority remains Iteration 411; physical blocker authority remains Iteration 421 with exact unresolved set `[2]`. Iteration 475 remains the latest completed numerical mass-support authority. The frozen Iteration-455 rank-11 coordinate `u=+5e-6, v=+1e-5` remains the sole permitted heavy gate in canonical run `33989317870`, job `101368577097`; it was not duplicated.

## Objective
Compress the Iteration-476 rank-1 precision-sensitive functional into the exact four-quartet representation already implied by the Iteration-467 odd-odd projector, so that future BASE/HALF assembled MP audits can evaluate the only derivative-sensitive discrepancy mode without materializing the full 4x4 discrepancy matrix.

No `u<->v` identity is assumed. In particular, the off-diagonal quartets `DeltaQ12` and `DeltaQ21` remain distinct.

## Exact quartet representation
For positive magnitudes `a,b in {1,2}`, define the signed discrepancy quartet

`DeltaQ_ab = DeltaF(+a,+b) - DeltaF(-a,+b) - DeltaF(+a,-b) + DeltaF(-a,-b)`.

The positive-node central4 derivative weights are

`g=(c_{+1},c_{+2})=(2/3,-1/12)=(1/12)(8,-1)`.

Therefore the frozen mixed-derivative MP discrepancy is exactly

`DeltaD = sum_{a,b in {1,2}} g_a g_b DeltaQ_ab`

and hence

`DeltaD = (64 DeltaQ11 - 8 DeltaQ12 - 8 DeltaQ21 + DeltaQ22)/144`.

Equivalently, with

`Wq = g g^T = (1/144) [[64,-8],[-8,1]]`,

`DeltaD=<Wq,DeltaQ>_F`.

The quartet coefficient matrix is rank 1. Exact Frobenius values are

`||Wq||_F = 65/144`,

`||Wq||_F^2 = 4225/20736`.

## Exact quartet nullspace and sensitive projection
The four-dimensional quartet discrepancy space has only one derivative-sensitive direction and a three-dimensional exact nullspace.

One explicit integer nullspace basis in vector order `(Q11,Q12,Q21,Q22)` is

- `(1,0,0,-64)`;
- `(0,1,0,8)`;
- `(0,0,1,8)`.

Each vector is exactly orthogonal to the integer sensitive vector `(64,-8,-8,1)`.

The unique quartet-space Frobenius projection carrying the entire assembled discrepancy is

`DeltaQ_sens = (DeltaD/||Wq||_F^2) Wq`

`= (144/4225) DeltaD [[64,-8],[-8,1]]`,

with

`||DeltaQ_sens||_F = (144/65)|DeltaD|`.

Every Frobenius-orthogonal quartet remainder is exactly invisible to the frozen mixed derivative.

## Exact bridge to the full odd-odd matrix diagnostic
Let `X=Qo DeltaF Qo`. Each positive-node entry of the odd-odd projected matrix equals `DeltaQ_ab/4`, and each magnitude pair is represented by four sign-related entries of equal absolute value. Therefore

`||X||_F = (1/2)||DeltaQ||_F`.

This gives the exact equivalent bound

`|DeltaD| <= (65/144)||DeltaQ||_F = (65/72)||Qo DeltaF Qo||_F`.

The quartet sensitive-projection norm is exactly twice the full 4x4 odd-odd sensitive-projection norm, consistent with Iteration 476:

`(1/2)(144/65)|DeltaD| = (72/65)|DeltaD|`.

## Interpretation and classification
Classification:

`PASS_QUARTET_RANK1_COMPRESSED_PRECISION_CERTIFICATE__DIAGNOSTIC_ONLY_NON_PROMOTING`.

This is an exact implementation/provenance compression. It changes no estimator, threshold, dynamics, support ordering, source occurrence accounting, coordinate state, or physical promotion rule. It does not authorize `u<->v` deduplication and does not identify `DeltaQ12` with `DeltaQ21`.

For future final BASE/HALF assembly, the derivative-sensitive precision discrepancy can be computed directly from the four signed quartets using the integer combination `64,-8,-8,+1`, while the full quartet norm remains available as a broader precision diagnostic. A mismatch between quartet and canonical-16 assemblies is implementation/provenance BLOCKED, not physics FAIL.

Reproducible audit: `candidate_gravity/code/iteration477_quartet_rank1_compressed_certificate.py`.
Machine-readable result: `candidate_gravity/results/iteration477_quartet_rank1_compressed_certificate.json`.

Physical/operator authority remains Iteration 411. Physical blocker authority remains Iteration 421. Exact unresolved physical set remains `[2]`. `ANSATZ-003` remains uncreated; comparator-subtracted residual and Fisher/resources remain BLOCKED.

MODEL_READINESS: 24%

Readiness change: **0 percentage points**. The implementation/provenance path to final assembled MP closure is simpler and more strongly cross-checkable, but no additional stable readiness-rubric component is fully closed.

## Exact next gate
Raw-consume canonical rank-11 run `33989317870` fail-closed after completion. PASS may advance only to the next UNTESTED coordinate in the frozen Iteration-455 manifest; BLOCKED requires localization of the first failing `z/phi/radial` sample at rank 11 without changing thresholds, dynamics, routing, or precision conventions.
