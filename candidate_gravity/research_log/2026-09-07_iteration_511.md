# Candidate Gravity Research Log — Iteration 511

**Date:** 2026-09-07  
**Scope:** exact BASE/HALF shared-node covariance/error-propagation audit while canonical rank23 remains in progress; no dynamics, thresholds, support order, or precision conventions changed.

## Start state
Repository source of truth was reread before work: `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_510.md`, research log Iteration 510, recent commits, and live Actions. Iteration 509 remains the latest completed numerical support authority with `27/32 = 84.375% = 2160/2560` certified occurrence-weighted support. Canonical rank23 `(u,v)=(+2.5e-6,-2.5e-6)`, HALF local index 9, run `34058694183`, job `101555199703`, remained `in_progress`; no duplicate heavy run was launched.

## Exact covariance result
Iteration 510 fixed the exact two-row Gram matrix for the frozen 28-coordinate BASE/HALF assembly:

`G = [[4225/5184, 4/81], [4/81, 4225/324]]`.

Under the explicitly scoped diagnostic model of iid equal-variance coordinate-level sampling errors with variance `sigma^2`, the assembled numerical-error covariance is exactly `sigma^2 G / h^4`. Therefore:

- `Var(BASE) = (4225/5184) sigma^2/h^4`;
- `Var(HALF) = (4225/324) sigma^2/h^4`;
- `Cov(BASE,HALF) = (4/81) sigma^2/h^4`;
- exact correlation `rho = 64/4225 ~= 0.0151479`;
- for retained discrepancy `DELTA=BASE-HALF`, `Var(DELTA) = (23771/1728) sigma^2/h^4`.

Classification: `PASS_BASE_HALF_SHARED_NODE_COVARIANCE_EXACT__NON_PROMOTING`.

## Scientific/operational implication
Because BASE and HALF share four exact sampled coordinates, their numerical sampling errors are not strictly independent even under iid coordinate noise. The covariance is small but exactly nonzero. Future uncertainty or diagnostic bookkeeping must therefore propagate the shared-node covariance rather than summing BASE and HALF variances as if the assemblies used disjoint samples. This strengthens the Iteration-510 guardrail that `BASE-HALF` is derived from the same two rows and cannot manufacture independent information.

The iid equal-variance assumption is only the scope of this diagnostic error-propagation certificate; it is not a claim about the physical covariance of Candidate-Gravity observables.

## Nonclaims
This is assembly numerical-error/provenance authority only: not Candidate-Gravity consistency PASS or FAIL; not physical promotion of unresolved index 2; not exact comparator identity; not regime-specific non-identifiability; not near-degeneracy; not novelty certificate; not Fisher/identifiability authority. `ANSATZ-003` remains uncreated and Fisher/resources remain forbidden until a nonzero algebraic residual exists.

Reproducible audit: `candidate_gravity/code/iteration511_base_half_shared_node_covariance_exact_audit.py`. Machine-readable result: `candidate_gravity/results/iteration511_base_half_shared_node_covariance_exact_audit.json`.

## Readiness
Stable rubric remains: comparator foundation `24/25`, unique residual `0/20`, frozen parent dynamics/ANSATZ `0/20`, consistency/positivity/Ward/causality `0/15`, identifiability/Fisher `0/10`, resource/experiment closure `0/10`.

MODEL_READINESS: 24%

Readiness change: **0 percentage points**. A real exact assembly error-propagation guardrail was closed, but no additional model-readiness rubric sector is complete.

## Exact next gate
Do not duplicate canonical rank23 run `34058694183`. After completion, raw-consume its artifact fail-closed. Only raw-valid PASS permits rank24 `(+2.5e-6,+2.5e-6)`, HALF local index 10. If rank23 is BLOCKED, freeze suffix progression and localize the first failing `z/phi/radial` sample under all unchanged frozen conventions.
