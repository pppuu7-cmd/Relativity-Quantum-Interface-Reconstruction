# Candidate Gravity Research Log — Iteration 522

**Date:** 2026-09-07  
**MODEL_READINESS: 24%**

## Start-state checks
Read repository-authoritative `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, latest recovery delta Iteration 521, latest research log, recent commits, and live GitHub Actions. Repository state remained authoritative at Iteration 521. Latest completed numerical mass-support authority is Iteration 520 with certified occurrence-weighted precision support `31/32 = 96.875% = 2480/2560`. Canonical final support rank27 `(u,v)=(+5e-6,+2.5e-6)`, run `34078407905`, job `101608912613`, remained `in_progress` at the start/live check, so no duplicate heavy Action was launched.

## Exact assembled scaled-metric identity
For either future independently assembled BASE or HALF MP pair, write

`M=(A80+A120)/2`, `d=|A80-A120|`.

For real numbers the exact identity

`max(|A80|,|A120|)=|M|+d/2`

holds. Therefore the repository's frozen assembled scaled MP discrepancy is exactly

`scaled = d / max(1, |M| + d/2)`.

This is not an approximation and requires no sign assumption on the MP80/MP120 assembled values.

With frozen assembled threshold `tau=2e-6=1/500000`, the exact PASS boundary splits cleanly:

- floor branch `|M|+d/2 <= 1`: PASS iff `d <= tau`;
- amplitude branch `|M|+d/2 > 1`: PASS iff

  `d <= [tau/(1-tau/2)] |M| = (2/999999)|M|`.

The amplitude-branch factor is thus exactly `2/999999`, not merely approximately `2e-6`.

Further, if an independent certificate provides only `0 <= d <= R` while the midpoint magnitude `m=|M|` is fixed, the function

`f(d)=d/max(1,m+d/2)`

is monotone nondecreasing. Hence the exact worst-case scaled transfer is

`sup_{0<=d<=R} f(d) = R/max(1,m+R/2)`.

This complements Iteration 521's sharp numerator-radius theorem by giving the exact downstream map into the actual frozen assembled scaled metric.

Classification:

`PASS_ASSEMBLED_SCALED_MIDPOINT_DENOMINATOR_IDENTITY_EXACT__NON_PROMOTING`.

Reproducible audit: `candidate_gravity/code/iteration522_assembled_scaled_midpoint_identity.py`.
Machine-readable result: `candidate_gravity/results/iteration522_assembled_scaled_midpoint_identity.json`.

## Scientific meaning and guardrails
This is numerical/assembly metric authority only. It does not establish actual BASE or HALF assembled MP PASS before rank27 is raw-consumed and complete support is assembled. It does not replace direct independent BASE/HALF MP80/MP120 evaluation, the retained BASE↔HALF mass-step discrepancy, or frozen conditioning/provenance checks. It does not promote physical index 2 and is not Candidate-Gravity consistency PASS/FAIL, exact comparator identity, regime-specific non-identifiability, near-degeneracy, or novelty certificate. `ANSATZ-003` remains uncreated; Fisher/resources remain forbidden.

Physical/operator authority remains Iteration 411. Physical blocker remains Iteration 421 `BLOCKED_CONVERGENCE`, unresolved set `[2]`. Fixed C3/C4/C5/nonlocal/asymptotic-safety comparator quotient remains operationally BLOCKED because the concrete upstream `Source/Ward/contact+K2` target is not assembled. No robust comparator-subtracted residual exists.

**MODEL_READINESS: 24%**

Readiness change: **0 percentage points**. A genuine exact assembly-metric subgate was closed, but no additional stable-rubric sector closed.

## Exact next gate
Fail-closed raw-consume canonical rank27 run `34078407905` after completion. Only raw-valid PASS permits local support closure `32/32` and transition to frozen independent BASE/HALF MP80/MP120 central4 assembly. The Iteration-522 identity must be used only as an exact audit/re-expression of that frozen scaled metric, not as a substitute for direct assembly. If rank27 is BLOCKED, stop support progression and localize the first failing `z/phi/radial` sample without changing frozen parent dynamics, support order, MP levels, radial hs, NPHI, z-grid, or thresholds.
