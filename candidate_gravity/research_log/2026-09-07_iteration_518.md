# Candidate Gravity Research Log — Iteration 518

**Date:** 2026-09-07  
**MODEL_READINESS: 24%**

## Start-state checks
Read repository-authoritative `CURRENT_QG_FRONT.md`, latest recovery delta (Iteration 517), latest research log, recent commits, and live GitHub Actions. Canonical rank26 run `34071968236`, job `101590847259`, remained `in_progress`; no duplicate heavy run was launched.

## Independent exact gate
Generalized the frozen BASE/HALF numerical-error propagation authority from Iteration 516 (independent diagonal coordinate errors) to an arbitrary positive-semidefinite covariance matrix `Sigma` on the 28-coordinate support union.

Let `A=[w_BASE^T; w_HALF^T]`. Then exactly

`C_A = h^-4 A Sigma A^T`,

so `Sigma >= 0` implies assembled covariance `C_A >= 0`. For retained `DELTA=BASE-HALF`,

`Var(DELTA)=h^-4 (w_BASE-w_HALF)^T Sigma (w_BASE-w_HALF)`.

Using the frozen exact Gram data,

- `||w_BASE||_2^2 = 4225/5184`,
- `||w_HALF||_2^2 = 4225/324`,
- `<w_BASE,w_HALF> = 4/81`,
- `||w_BASE-w_HALF||_2^2 = 23771/1728`.

Therefore with `lambda=lambda_max(Sigma)`:

- `Var(BASE) <= (4225/5184) lambda/h^4`,
- `Var(HALF) <= (4225/324) lambda/h^4`,
- `Var(BASE-HALF) <= (23771/1728) lambda/h^4`,
- `|Cov(BASE,HALF)| <= (4225/1296) lambda/h^4`.

The cross bound is exact at the norm level because `sqrt((4225/5184)(4225/324))=4225/1296`.

## Scientific interpretation
Classification: `PASS_BASE_HALF_FULL_PSD_COVARIANCE_SPECTRAL_CONTRACT_EXACT__NON_PROMOTING`.

Important correction/guardrail: under arbitrary correlated PSD coordinate noise, `Cov(BASE,HALF)` need not be nonnegative. The nonnegative sign established in Iteration 516 is valid only for diagonal/independent coordinate errors. This is a numerical assembly/error-provenance result only: it is not Candidate-Gravity physical covariance, consistency PASS/FAIL, comparator identity, regime-specific non-identifiability, near-degeneracy, novelty certificate, or Fisher authorization.

Reproducible audit: `candidate_gravity/code/iteration518_full_psd_covariance_contract.py`. Machine-readable result: `candidate_gravity/results/iteration518_full_psd_covariance_contract.json`.

## Retained blockers/nonclaims
Physical/operator authority remains Iteration 411. Physical blocker remains Iteration 421 `BLOCKED_CONVERGENCE`, unresolved set `[2]`. Certified local precision coverage remains `30/32 = 93.750%`; rank26 has no scientific PASS until raw artifact consumption. Robust comparator-subtracted residual is absent. `ANSATZ-003` remains uncreated. Fisher/resources remain forbidden.

Readiness change: **0 percentage points**. This closes a genuine error-propagation/provenance subgate but no additional stable readiness-rubric sector.

## Exact next gate
Fail-closed raw-consume canonical rank26 run `34071968236` after completion. Only raw-valid PASS permits frozen rank27 `(+5e-6,+2.5e-6)`, HALF local index 14, multiplicity 1. BLOCKED freezes suffix progression and requires first-failing `z/phi/radial` localization under unchanged frozen conventions.
