# RQIR Candidate Gravity — Iteration 572

**Date:** 2026-09-08  
**Classification:** `PASS_COMPLETE_QUARTER_RAW_SAMPLE_LAYER_ASSEMBLY__NON_PROMOTING`

All 12 new QUARTER support coordinates are raw-valid PASS through Iteration 571; together with the four exact HALF-overlap corners this gives `16/16 = 100%` authoritative QUARTER coordinate coverage.

Iteration 572 assembles the complete 4x4 central4 mixed-derivative **sample layer** from the raw MP80/MP120 artifacts at the 80 frozen `(z,phi)` samples. It is deliberately pre-spectral: these rows are stripped-numerator angular/radial samples, not the final degree-4/affine-log spectrum-integrated `F(u,v)` and not physical scalar `D_s`.

Exact sample-layer checks:
- all 80 QUARTER assembled outputs finite;
- QUARTER MP80↔MP120 scaled max `1.2787277797888786e-68`;
- HALF↔QUARTER sample-layer scaled max `2.8064444557403413e-6`;
- direct tensor vs u→v, v→u and exchange-orbit assembly agree at about `1e-114`;
- diagnostic raw-grid bilinear fit max `4.588236089297399e-15`.

These values validate the raw sample-layer assembly and numerical precision only. They do **not** close the frozen Iteration-424 physical mass-step or tensor-(1,1) clauses. The prior compact result was corrected in-place to make this scope explicit rather than overclaim physical PASS.

Next gate: reconstruct full spectrum-integrated `F(u,v)` at MP80/MP120 using the frozen Iteration-407 phi-average + degree-4 interpolation + affine-log recurrence, then assemble BASE/HALF/QUARTER physical `D_s`. In parallel, recover the exact authority mapping of the frozen tensor-degree-(1,1) clause before any physical promotion.

**MODEL_READINESS: 24%**; readiness change `+0 pp`.
