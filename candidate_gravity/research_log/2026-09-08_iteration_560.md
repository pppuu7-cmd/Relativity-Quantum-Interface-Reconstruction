# RQIR Candidate Gravity — Iteration 560

Date: 2026-09-08

## Purpose
Derive a fail-closed exact contribution/error contract for the only two exchange orbits that would remain incomplete after a raw-valid rank9 PASS, without changing the frozen rank order or launching duplicate heavy work.

## Exact remaining-orbit tail
For central4 scaled nodes `{-2,-1,+1,+2}` with integer first-derivative weights `{1,-8,+8,-1}`, the two post-rank9 incomplete exchange orbits are:

- ranks6/11: `(-1,+2)<->(+2,-1)`, coefficients `+8,+8`, orbit sum `+16`;
- ranks10/12: `(+1,+2)<->(+2,+1)`, coefficients `-8,-8`, orbit sum `-16`.

Their combined integer coefficient sum is exactly zero.

Define exchange-orbit averages

`E_A=[F(-1,+2)+F(+2,-1)]/2`,

`E_B=[F(+1,+2)+F(+2,+1)]/2`.

Then the entire remaining contribution to the frozen mixed central4 stencil is exactly

`T_rem = (E_A-E_B)/(9 h^2)`.

Thus a common constant offset across these four coordinates cancels exactly. The remaining tail is controlled by the difference of the two orbit averages, not by either orbit independently.

## Fail-closed bounded-error contract
If the four raw coordinate values have independent worst-case absolute bounds `eps1..eps4` (no stochastic independence assumption is required), then

`|delta T_rem| <= (eps1+eps2+eps3+eps4)/(18 h^2)`.

Equivalently, if the two orbit averages have absolute uncertainty bounds `eps_A, eps_B`, then

`|delta T_rem| <= (eps_A+eps_B)/(9 h^2)`.

These are sharp L1 worst-case propagation bounds under the stated bounded-error model.

## Classification
`PASS_ITER424_REMAINING_ORBIT_TAIL_FAIL_CLOSED_CONTRACT_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`

This is an exact numerical/assembly-control result. It is not a Candidate-Gravity consistency PASS/FAIL, not comparator identity, not regime-specific model non-identifiability, not model-level near-degeneracy, and not a novelty certificate.

No rank skipping, u<->v substitution, zero fill, threshold weakening or early physical promotion is authorized.

Machine-readable authority: `candidate_gravity/results/iteration560_iter424_remaining_orbit_tail_contract_exact.json`.
Reproducible audit: `candidate_gravity/code/iteration560_iter424_remaining_orbit_tail_contract_exact.py`.

## Heavy-run status at iteration start
Rank9 run `34160111095` remained `in_progress`; no duplicate was launched. Only a fail-closed raw-valid PASS may authorize rank10 `(+1.25e-6,+2.5e-6)`.

MODEL_READINESS: 24%

Readiness change: 0 percentage points. The iteration closes a real assembly/error-propagation subgate but completes no additional model-level rubric sector.
