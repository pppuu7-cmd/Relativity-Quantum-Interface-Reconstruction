# Candidate Gravity Research Log — Iteration 647

Date: 2026-09-09

Authoritative predecessor: Iter646 one-global-factor closed-CTP normalization BLOCKED.

## Result

A fail-closed full-snapshot history audit was pinned to commit `a86b41cfc4e90d65073faa872b8cfa1b85f29193`, the authoritative Iter644 front strictly before Iter645 reduced cut values existed. The audit scanned 1566 tracked `candidate_gravity/` files and searched for an explicit same-parent chain binding `W=-i ln Z` -> CTP Legendre transform -> real-scalar Gaussian global prefactor -> Fourier/loop measure -> native Gamma3 normalization.

No complete bridge file exists at the cutoff. In particular, there are zero `W=-i ln Z` hits in the pinned snapshot, while loop-measure formulas and i/2-like determinant formulas do exist elsewhere. Therefore the missing authority is not absence of standard formulas in general; it is absence of a pre-result same-parent normalization bridge into the exact native Gamma3 convention.

Exactly one common nonzero global scalar factor remains unbound. No root-, family-, q2-, or s-dependent normalization is licensed. Because Iter645 values already exist, choosing a conventional `i/2`, `+/-i`, or fitted constant now is permanently forbidden for this observable branch.

Canonical Action `34309801822` completed `success`; job `102333928118`; artifact `10087937807`; digest `sha256:b47ad24b181b07c9f7d9a22bea238b9d7693dae60a04cc4e4ad3563908856e76`. Raw artifact was consumed and agrees with the committed summary.

## Scientific classification

`BLOCKED_ITER647_PERMANENT_PRE645_NORMALIZATION_PROVENANCE_GAP__ONE_GLOBAL_FACTOR_UNBOUND__NON_RESIDUAL`

This is operational/provenance BLOCKED, not Candidate-Gravity consistency FAIL/PASS, exact comparator identity, regime-specific non-identifiability, near-degeneracy, novelty certificate, or Candidate residual.

Guardrails unchanged: Candidate values unused; `zero_fill=false`; Source/Born subtraction `NOT_PERFORMED`; native Y/T_cut projection `NOT_PERFORMED`; fixed C3/C4/C5/nonlocal/asymptotic-safety comparator quotient `NOT_PERFORMED`; no ANSATZ-003; no Fisher/resources.

MODEL_READINESS: 24%
Readiness change: 0 percentage points. The normalization branch is now permanently classified, but robust unique residual remains `0/20`; no full rubric sector closes.

Exact next gate — Iteration648: before reading any Iter645 reduced values, prospectively define a projective/ratio observable whose algebra algebraically cancels the one common global factor. The gate must prove invariance under `A(s) -> lambda A(s)` for one nonzero common complex `lambda`, state denominator/nonzero-domain conditions, preserve all frozen family/state/tensor conventions, and only then permit evaluation on Iter645 data. If no scientifically meaningful ratio can be frozen without using those values, retain BLOCKED.
