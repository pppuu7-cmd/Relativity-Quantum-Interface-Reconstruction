# RQIR Candidate Gravity — Iteration 566

**Date:** 2026-09-08  
**Classification:** `PASS_ITER424_QUARTER_FULL_STENCIL_GEOMETRY_ERROR_NORM_CONTRACT_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`

## Objective
Pre-register the exact geometry and error amplification of the frozen 4x4 QUARTER central4 mixed-derivative assembly while rank11 is still running, so no post-hoc normalization/error interpretation can be selected after the final support values are known.

## Exact coefficient geometry
The one-dimensional central4 integer weights are

`w = [1,-8,+8,-1]`.

The mixed stencil integer coefficient matrix is the outer product `C = w ⊗ w`:

`[[1,-8,8,-1],[-8,64,-64,8],[8,-64,64,-8],[-1,8,-8,1]]`,

with frozen normalization `1/(144 h^2)`.

Because `C` is an outer product, `rank(C)=1` exactly. Also `sum(w)=0`, hence every row sum and every column sum of `C` is zero. Therefore any additive grid nuisance of the form

`F_ij = a_i + b_j + c`

is annihilated exactly by the mixed stencil. The additive row/column nuisance subspace has dimension `4+4-1 = 7` inside the 16-coordinate grid.

This is stencil/assembly geometry only. It is NOT Candidate-Gravity model-level non-identifiability, not a physical near-degeneracy, not comparator identity, and not novelty evidence.

## Exact fail-closed error gains
For coordinate-wise bounded absolute errors `|e_ij| <= eps` with a common bound, the normalized worst-case L1 gain is

`sum_ij |C_ij| / (144 h^2) = 324/(144 h^2) = 9/(4 h^2)`.

Thus

`|delta D_uv| <= 9 eps/(4 h^2)`.

For independent equal-variance coordinate errors `Var(e_ij)=sigma^2`,

`sum_ij C_ij^2 = (1^2+8^2+8^2+1^2)^2 = 130^2 = 16900`,

so

`Var(delta D_uv) = 4225 sigma^2/(5184 h^4)`

and

`Std(delta D_uv) = 65 sigma/(72 h^2)`.

These constants are now frozen before rank11/rank12 values are known.

## Reproducibility
Audit: `candidate_gravity/code/iteration566_iter424_quarter_full_stencil_geometry_audit.py`.
Machine-readable result: `candidate_gravity/results/iteration566_iter424_quarter_full_stencil_geometry.json`.

## Heavy-compute state
Canonical rank11 `(+2.5e-6,-1.25e-6)` run `34168897005`, job `101885271903`, remained `in_progress` when this iteration was frozen. No duplicate/reordered heavy run was launched.

## Guardrails
Physical/operator authority remains Iteration411. Physical blocker remains Iteration421 `BLOCKED_CONVERGENCE`, unresolved physical set `[2]`. Concrete upstream `Source/Ward/contact+K2` target and robust comparator-subtracted residual remain absent. `ANSATZ-003` remains uncreated; Fisher/resources remain forbidden.

MODEL_READINESS: 24%

Readiness change: `0 pp`. This exact assembly/error subgate prevents a future interpretation error but closes no additional model-level readiness rubric sector.
