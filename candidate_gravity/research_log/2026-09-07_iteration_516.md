# Candidate Gravity Research Log — Iteration 516

**Date:** 2026-09-07  
**Scope:** exact stochastic assembly-error propagation for frozen BASE/HALF central4 rows under independent mean-zero coordinate errors with arbitrary per-coordinate variances; no heavy-run duplication and no frozen-threshold changes.

## Start state
Repository-authoritative Iteration 515 remains the latest completed numerical mass-support authority: rank24 raw-valid PASS, certified occurrence-weighted support `29/32 = 90.625% = 2320/2560`. Canonical rank25 `(u,v)=(+2.5e-6,+5e-6)` run `34066294587`, job `101575523337`, remains `in_progress`; it was not duplicated.

Physical/operator authority remains Iteration 411. Physical blocker authority remains Iteration 421 `BLOCKED_CONVERGENCE`, exact unresolved set `[2]`. Fixed comparator quotient remains operationally BLOCKED because the concrete upstream `Source/Ward/contact+K2` target is not assembled.

## Exact heteroscedastic covariance result
Use the frozen 28-coordinate BASE/HALF union and exact mixed-central4 row weights `w_B(p)` and `w_H(p)` in units of `1/h^2`. Let coordinate errors `e_p` be independent and mean zero with arbitrary variances `Var(e_p)=v_p >= 0`. Then diagonal covariance propagation gives

`Var(BASE) = h^-4 sum_p w_B(p)^2 v_p`,

`Var(HALF) = h^-4 sum_p w_H(p)^2 v_p`,

`Var(BASE-HALF) = h^-4 sum_p (w_B(p)-w_H(p))^2 v_p`.

Only the four exact shared nodes `(-1,-1), (-1,+1), (+1,-1), (+1,+1)` contribute to the cross-covariance. At each shared node,

`w_B(p) w_H(p) = 1/81`,

therefore

`Cov(BASE,HALF) = h^-4 (1/81) sum_{p in shared} v_p >= 0`.

Equality holds iff all four shared-node variances vanish. At each shared node the discrepancy variance coefficient is exactly

`(w_B(p)-w_H(p))^2 = 25/144`.

The iid equal-variance authority of Iteration 511 is recovered as the special case `v_p=sigma^2`, giving `Cov(BASE,HALF)=(4/81) sigma^2/h^4`.

Classification: `PASS_BASE_HALF_HETEROSCEDASTIC_DIAGONAL_COVARIANCE_EXACT__NON_PROMOTING`.

Reproducible audit: `candidate_gravity/code/iteration516_base_half_heteroscedastic_covariance_audit.py`. Machine-readable result: `candidate_gravity/results/iteration516_base_half_heteroscedastic_covariance_exact.json`.

## Scope and nonclaims
This is a numerical assembly/provenance PASS only. It assumes independent coordinate errors but does **not** assume identical variances. It is not a Candidate-Gravity consistency PASS/FAIL, not a physical covariance model, not an exact comparator identity, not regime-specific non-identifiability, not near-degeneracy, and not a novelty certificate. `BASE-HALF` remains derived rather than an independent Fisher constraint. The result does not replace frozen assembled scaled-discrepancy gates and creates no new threshold.

`ANSATZ-003` remains uncreated. Fisher/resources remain forbidden until a nonzero algebraic residual exists.

MODEL_READINESS: 24%

Readiness change: **0 percentage points**. Iteration 516 strengthens exact assembly-error provenance but completes no additional sector of the stable readiness rubric.

## Exact next gate
Fail-closed raw-consume canonical rank25 run `34066294587` after completion. Only raw-valid PASS permits frozen rank26 `(+5e-6,-2.5e-6)`, HALF local index 13, multiplicity 1. A raw scientific BLOCKED result freezes suffix progression and requires localization of the first failing `z/phi/radial` sample under unchanged frozen dynamics, support order, MP levels, radial hs, angular grid, and thresholds.
