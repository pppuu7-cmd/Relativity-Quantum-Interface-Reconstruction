# RQIR Candidate Gravity — Iteration 549

Date: 2026-09-07

Repository source of truth was re-read from `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_548.md`, `research_log/2026-09-07_iteration_548.md`, recent commits, and live Actions state before scientific work. Canonical QUARTER rank6 run `34146191135` remained `in_progress`; no duplicate heavy run was launched.

## Exact three-level saturation / no-goodness-of-fit result
For the frozen three-level smooth truncation diagnostic

`X_s = D + A s^4 + B s^6`, with `s={1,1/2,1/4}` for BASE/HALF/QUARTER,

the exact design matrix is

`M=[[1,1,1],[1,1/16,1/64],[1,1/256,1/4096]]`

with

`det(M)=-2835/65536 != 0`.

Thus `(D,A,B)` are algebraically identifiable **inside the assumed truncated model**, but there are exactly three observations for three fit parameters, hence residual degrees of freedom are zero. Consequently any three assembled BASE/HALF/QUARTER values are interpolated exactly by some `(D,A,B)`: a zero residual is automatic and cannot be used as a goodness-of-fit PASS for the `h^4+h^6` truncation hypothesis.

The exact inverse is

`M^{-1}=[[1/945,-16/189,1024/945],[-16/45,208/9,-1024/45],[256/189,-4352/189,4096/189]]`.

A pure omitted `h^8` contribution is exactly aliased into the fitted parameters as

`Delta(D,A,B)=C*(1/1344,-17/64,425/336)`.

Therefore BASE/HALF/QUARTER alone cannot identify whether an observed fitted `(D,A,B)` contains `h^8`/higher contamination. This is a **regime-specific non-identifiability of truncation-model adequacy**, not Candidate-Gravity model-level non-identifiability and not a consistency FAIL.

A fourth level would create one residual degree of freedom. For the illustrative scale `s=1/8`, the integer left-null combination

`[-1,81,-1104,1024]`

annihilates orders `h^0,h^4,h^6` exactly and responds to `h^8` with `-11475/16384`. This is prospective diagnostic design only: it does not authorize an EIGHTH heavy run, alter the frozen Iteration-424 gate, or reorder QUARTER successors.

Classification: `REGIME_SPECIFIC_NON_IDENTIFIABILITY_ITER424_THREE_LEVEL_H4_H6_GOF__DIAGNOSTIC_ONLY_NON_PROMOTING`.

Machine result: `candidate_gravity/results/iteration549_iter424_three_level_saturation_no_gof.json`. Reproducible audit: `candidate_gravity/code/iteration549_iter424_three_level_saturation_no_gof.py`.

Physical/operator authority remains Iteration 411. Iteration 421 remains `BLOCKED_CONVERGENCE` with unresolved physical set `[2]`. The concrete `Source/Ward/contact+K2` target and robust comparator-subtracted residual remain absent; `ANSATZ-003`, Fisher and resources remain blocked.

MODEL_READINESS: 24%

Readiness change: 0 percentage points. Iteration 549 closes a negative/diagnostic identifiability statement and prevents a false future goodness-of-fit claim, but no stable readiness-rubric sector closes.
