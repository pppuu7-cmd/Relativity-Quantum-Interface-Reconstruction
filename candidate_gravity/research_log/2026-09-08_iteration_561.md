# Candidate Gravity — Iteration 561

## Goal

While the sole authorized heavy rank9 computation remains in progress, freeze an exact, non-biasing assembly-integrity contract for the eventual Iteration-424 QUARTER mixed central4 derivative. The purpose is to make indexing/orientation corruption detectable independently of the physical value returned by the remaining support runs.

## Frozen stencil

Scaled nodes:

`[-2,-1,+1,+2]`

Central4 first-derivative integer weights:

`w=[1,-8,+8,-1]`

with normalization `1/(12 h)`. Therefore the mixed derivative uses the tensor coefficient matrix

`C_ij = w_i w_j/(144 h^2)`.

The integer numerator matrix is

```text
[[ 1, -8,   8, -1],
 [-8, 64, -64,  8],
 [ 8,-64,  64, -8],
 [-1,  8,  -8,  1]]
```

## Exact three-way/four-route identity

For any complete matched 4x4 grid `F_ij`, define the direct tensor assembly

`D_direct = sum_ij w_i w_j F_ij/(144 h^2)`.

Because the coefficient tensor factorizes as an outer product, applying the one-dimensional central4 operator first in `u` and then in `v` gives coefficient `(w_i/12h)(w_j/12h)`, exactly the same coefficient as `D_direct`. Reversing the order gives the same result because scalar multiplication commutes.

Hence exactly:

`D_direct = D_(u->v) = D_(v->u)`.

The tensor coefficient matrix is symmetric under transpose. Therefore, for each off-diagonal exchange pair `(i,j),(j,i)`,

`C_ij F_ij + C_ji F_ji = (C_ij+C_ji) E_ij`,

with exchange average `E_ij=(F_ij+F_ji)/2` and `C_ij=C_ji`.

Thus the 16-coordinate direct sum is also exactly equal to the 10-orbit compressed assembly retained from Iteration559:

```text
(-2,-2):                    +1
(-2,-1)<->(-1,-2):         -16
(-2,+1)<->(+1,-2):         +16
(-2,+2)<->(+2,-2):          -2
(-1,-1):                   +64
(-1,+1)<->(+1,-1):        -128
(-1,+2)<->(+2,-1):         +16
(+1,+1):                   +64
(+1,+2)<->(+2,+1):         -16
(+2,+2):                    +1
```

All integer orbit coefficients sum exactly to zero.

Therefore the eventual complete QUARTER assembly has four algebraically identical routes:

1. direct 16-point tensor sum;
2. sequential `u` then `v`;
3. sequential `v` then `u`;
4. exchange-orbit compressed 10-orbit sum.

Any discrepancy between these routes on the same raw-valid grid is an implementation/index/orientation error, not Candidate-Gravity physics.

## Synthetic polynomial audit

The one-dimensional central4 first derivative is exact through polynomial degree 4. Therefore its tensor product exactly reproduces the mixed derivative at the origin for every monomial

`u^a v^b`, `0<=a,b<=4`.

An exhaustive exact-rational machine audit over all 25 basis monomials passes. In that basis the only nonzero mixed derivative at the origin is `u v`, which returns exactly `1`.

This gives a future assembler unit-test that is independent of any physical raw values.

## Additive separable null sector

Every row and every column of the integer tensor matrix sums exactly to zero. Hence any additive contamination of the form

`A(u)+B(v)+constant`

is exactly annihilated by the mixed stencil.

This is an algebraic stencil property only. It is not permission to remove, substitute, symmetrize, or zero-fill physical support values.

## Result

`PASS_ITER424_QUARTER_THREE_WAY_ASSEMBLY_IDENTITY_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`

Machine-readable authority:

`candidate_gravity/results/iteration561_iter424_quarter_three_way_assembly_identity_exact.json`

Reproducible audit:

`candidate_gravity/code/iteration561_iter424_quarter_three_way_assembly_identity_exact.py`

## Scientific consequences

- Future QUARTER assembly can be cross-checked by independent implementations before any Iteration-424 physical promotion decision.
- The check is prospectively frozen before ranks9-12 complete, so it cannot be tuned to the eventual physical result.
- A failed equality among assembly routes is an implementation integrity failure and must stop promotion until localized.
- No physical threshold, mass node, precision, `h`, routing, numerator, normalization, or support order changes.
- No `u<->v` support substitution.
- No `ANSATZ-003`.
- No Fisher/resources.

## Current heavy state at pre-freeze check

Canonical rank9 remained the sole active heavy job:

- run `34160111095`;
- job `101859912600`;
- coordinate `(+1.25e-6,+1.25e-6)`;
- full-z MP80/MP120 stage in progress;
- raw audit/upload pending.

No duplicate was launched.

## MODEL_READINESS

`MODEL_READINESS: 24%`

Readiness change: `0 percentage points`. This closes an assembly-integrity diagnostic contract, not a stable model-level rubric sector.
