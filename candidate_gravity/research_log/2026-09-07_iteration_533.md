# Candidate Gravity Research Log — Iteration 533

Date: 2026-09-07

## Purpose
While the unique frozen Iteration-424 quarter-rank1 MP run remains active, close an independent exact diagnostic gate for the three already-frozen mass steps `h`, `h/2`, `h/4`. This gate must not inspect or tune against partial quarter numerical output and must not alter any Iteration-424 acceptance threshold.

## Exact central4 truncation geometry
For the frozen 1D central4 first-derivative stencil
`[f(-2h)-8 f(-h)+8 f(h)-f(2h)]/(12h)`, exact Taylor moments give

`D_h f = f' - h^4 f^(5)/30 - h^6 f^(7)/252 - h^8 f^(9)/4320 + O(h^10)`.

Applying the same stencil as a tensor product in the two auxiliary-mass directions yields, under sufficient smoothness,

`D_u(h)D_v(h) f = f_uv + A h^4 + B h^6 + O(h^8)`, 
where the common leading coefficients are fixed derivative combinations of the same parent function. Therefore the frozen levels BASE=`h`, HALF=`h/2`, QUARTER=`h/4` scale the common leading `h^4` error as `1 : 1/16 : 1/256` and the next `h^6` error as `1 : 1/64 : 1/4096`.

This gives exact prospective diagnostics, fixed before QUARTER assembly:
- if the common `h^4` term dominates and the denominator is nonzero, `(BASE-HALF)/(HALF-QUARTER) -> 16`;
- `R_BH=(16 HALF-BASE)/15` cancels the formal common `h^4` term;
- `R_HQ=(16 QUARTER-HALF)/15` cancels the same formal `h^4` term;
- `R_3=(BASE-80 HALF+1024 QUARTER)/945` cancels both formal common `h^4` and `h^6` terms.

Classification:
`PASS_ITER424_CENTRAL4_THREE_LEVEL_TRUNCATION_GEOMETRY_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`.

Machine-readable audit: `candidate_gravity/results/iteration533_iter424_three_level_truncation_geometry_exact_audit.json`.

## Critical guardrail
These are interpretation diagnostics only. No Richardson/extrapolated value may replace the frozen physical `D_s`; the ratio 16 is not a new acceptance threshold; a failing frozen physical mass-step clause cannot be rescued by this diagnostic; no smaller h, threshold weakening, ansatz adaptation, Fisher or resource work is authorized. The unchanged Iteration-424 five-clause gate remains the only physical fallback acceptance authority.

## Model readiness
Comparator foundation `24/25`; robust unique residual `0/20`; frozen parent dynamics/ANSATZ `0/20`; consistency/positivity/Ward/causality `0/15`; identifiability/Fisher `0/10`; resource/experiment closure `0/10`.

MODEL_READINESS: 24%
Readiness change: 0 percentage points.

## Iteration completion
ITERATION_TASK_COMPLETION: 100%

## Active heavy task
At the last live inspection, repaired quarter-rank1 run `34094463024`, job `101654832475`, remained in progress with principal stages 1–4 complete and stage 5 full-z MP active. Stage-level completion: approximately `57.1%`; no trustworthy within-stage percentage is exposed.

## Exact next gate
Consume the raw rank1 artifact only after the workflow becomes terminal. A raw-valid PASS permits only the already-preregistered rank2 `(-2.5e-6,+1.25e-6)`; any failure is handled under the frozen Iteration-532 transition rule.
