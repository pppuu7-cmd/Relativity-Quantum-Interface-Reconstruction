# Candidate Gravity Research Log — Iteration 646

Date: 2026-09-09

Authoritative predecessor: Iter645 reduced numerator-weighted ordinary s-channel cut densities computed, absolute promotion BLOCKED on one global closed-CTP normalization factor.

## Question

Does pre-Iter645 same-parent authority already fix the absolute closed-CTP-to-native-Gamma3 phase/prefactor needed to promote the reduced densities, without using Candidate/comparator values or the Iter645 outputs to choose it?

## Authority audit

The fail-closed audit reads the committed Iter627 combined SK contract, Iter630 closed retarded loop Keldysh structure, Iter632 Minkowski-vacuum G_K normalization, Iter311 determinant/logdet topology contract, and Iter645 reduced-density evaluator.

What is explicitly fixed:
- Iter627: `Z=Integral exp{iS_+-iS_-+...}` and the r/a rotation/measurement map;
- Iter630: exactly one G_K occurs in each closed retarded scalar-loop family;
- Iter632: `G_K=2*pi*i*delta(D)` in the frozen zero-temperature Minkowski vacuum convention;
- Iter311: the relative cubic Tr-log topology `Tr(GH3)-Tr(GH1GH2)+(1/3)Tr((GH1)^3)`;
- Iter645: the reduced ordinary-cut phase-space factor `beta/(16*pi^2)` and numerator contractions.

What is not explicitly bound by those same-parent files is the complete `W=-i ln Z` / Legendre-to-native-Gamma3 normalization statement carrying the real-scalar Gaussian one-loop global phase/prefactor into the exact native Gamma3 convention. Iter311 intentionally froze only relative logdet topology and contains no explicit real-scalar `i/2` prefactor.

Therefore precisely one common nonzero global scalar factor remains. No root-, family-, or q2-dependent freedom is licensed. Standard literature conventions motivate the familiar real-scalar Gaussian determinant factor, but literature cannot retroactively replace missing repository authority after Iter645 values have been evaluated; doing so here would violate the no-post-hoc-normalization guardrail.

Canonical Iter646 Action `34306240992` completed `success` after correcting one literal audit-string typo in the validator. Artifact `10086735213`, digest `sha256:c4348360d606f4707dff5d8f792aadbef74b62d8d5c08ae116b4a47b05392973`.

Scientific classification:
`BLOCKED_ITER646_ABSOLUTE_CLOSED_CTP_TO_NATIVE_GAMMA3_NORMALIZATION_NOT_EXPLICITLY_BOUND__ONE_GLOBAL_FACTOR_REMAINS__NON_RESIDUAL`.

This is operational/provenance BLOCKED, not Candidate-Gravity consistency FAIL/PASS, exact comparator identity, regime-specific non-identifiability, near-degeneracy, or novelty certificate.

Candidate values unused; `zero_fill=false`; Source/Born subtraction `NOT_PERFORMED`; native Y/T_cut projection `NOT_PERFORMED`; comparator quotient `NOT_PERFORMED`; ANSATZ-003/Fisher/resources forbidden.

MODEL_READINESS: 24%
Readiness change: 0 percentage points. The normalization uncertainty is now narrowed and raw-validated to one global factor, but robust unique residual remains `0/20` and no complete readiness sector closes.

Exact next gate — Iteration647: search only pre-Iter645 repository history/authority for an explicit same-parent `W=-i ln Z`, Legendre transform, real-scalar Gaussian determinant prefactor and Fourier/loop-measure convention that uniquely binds the remaining factor. If no such earlier authority exists, record a permanent normalization provenance BLOCKED for this observable branch rather than choosing `i/2`, `+/-i`, or a fitted constant post hoc; then evaluate whether projective/ratio observables can be defined prospectively in a new version without using Iter645 values.
