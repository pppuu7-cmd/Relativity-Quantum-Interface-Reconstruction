# RQIR Candidate Gravity — Iteration 481

Date: 2026-09-06

## Scope
Repository-first continuation from authoritative Iteration 480. This iteration resolves the previously explicit operational uncertainty about the next frozen Iteration-455 support coordinate and launches exactly one non-duplicating heavy gate. No ANSATZ-003, no Fisher/resources, no threshold, estimator, dynamics, source-order, or physical-authority change.

## Frozen manifest recovery
The Iteration-455 source-order generator is authoritative: BASE central4 nodes are `[-2h,-h,+h,+2h]`, BASE 4x4 is enumerated u-major/v-major before HALF 4x4, with `BASE_H=5e-6` and no unsupported u<->v deduplication. Therefore after raw-certified distinct rank 11 `(u,v)=(+5e-6,+1e-5)`, the exact next UNTESTED coordinate is distinct rank 12 `(u,v)=(+1e-5,-1e-5)`, source-occurrence multiplicity 1. This is derived from the frozen manifest order itself, not from symmetry.

## Race / anti-idle audit
Immediately before launch GitHub Actions reported `0` in_progress and `0` queued RQIR runs. No rank12 commit/workflow existed. A collision-safe one-coordinate stage and workflow were added without changing frozen numerical conventions.

Stage commit: `8e79541391934f9d391bec811f5e51eae732633d`.
Workflow commit: `93bd9f1c61d6499e439e15909146ee39d2d76171`.
Trigger/head commit: `54f9fee0fea91256b5c0d1a2297aae80b1dad6e3`.

Canonical run: `33997856739`.
Canonical job: `101391409387`.
Initial observed state after trigger: queued.

Frozen scientific settings retained: five training-z `[-0.86,-0.43,0,+0.43,+0.86]`, NPHI16, radial h `{0.002,0.001,0.0005}`, direct MP80/MP120 comparison, MP discrepancy threshold `1e-30`, radial Richardson scaled threshold `5e-4`, one mass coordinate only. No Richardson promotion and no blind grid sweep.

## Authority
Latest completed numerical mass-support authority remains Iteration 480 with rank11 raw PASS and occurrence-weighted certified coverage `16/32 = 50.000%` (`1280/2560`). Physical/operator authority remains Iteration 411. Physical blocker authority remains Iteration 421 with unresolved set `[2]`.

Rank12 is not scientific PASS until the uploaded raw result and authority audit are downloaded and checked fail-closed.

## Readiness
Stable rubric unchanged. MODEL_READINESS: 24%.

## Exact next gate
Raw-consume run `33997856739` / job `101391409387` fail-closed. If rank12 raw PASS is confirmed, advance only to frozen distinct rank 13 `(u,v)=(+1e-5,-5e-6)`, multiplicity 1. If BLOCKED, localize the first failing z/phi/radial sample at rank12 without changing thresholds.
