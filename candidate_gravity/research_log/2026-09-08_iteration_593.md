# RQIR Candidate Gravity — Iteration 593

Date: 2026-09-08

## Authority consumed
The run began by reading repository authority and then race-checking recent commits. `CURRENT_QG_FRONT.md` was stale at Iter590 while newer authoritative commits had already produced Iter591 and Iter592. Iter592 canonical raw authority is run `34214083326`, artifact `10051001153`, result SHA256 `f2765720fddaf578df48bfaf40820c66412282f81968d4ad77a04a940aac94a6`, failures `[]`.

Binding older protocol authority was also re-read:
- Iter183 split-invariant joint observable `Y=(K2,S_soft2_full)`;
- Iter205 linked discontinuity protocol `T_cut = D_s Gamma3_ret,soft - W[D_s K2]`;
- Iter238–240 observable-identity/executability guardrails;
- Iter590 full same-action cubic source-response completeness.

## Scientific question
Iter592 correctly established that the same-action K1^3 tree family is meromorphic and has no ordinary finite branch cut away from scalar poles, but left

`K1cubed_under_frozen_linked_T_cut = BLOCKED__NEEDS_EXPLICIT_SOURCE_TO_T_CUT_DISTRIBUTIONAL_MAP`.

The present gate asks whether the frozen RQIR protocol even authorizes an **individual** K1^3-to-`T_cut` projection.

## Exact split-invariance audit
Iteration 183 freezes the physical cubic coordinate as the **full source-completed** response `S`. It explicitly forbids promoting an internal decomposition as an observable. Write any bookkeeping decomposition

`S = S_A + S_B`.

The frozen protocol permits an internal repartition

`S_A -> S_A + C`,
`S_B -> S_B - C`,

with the full `S` unchanged. Applying the linear hard-channel discontinuity gives

`D_s S -> D_s S`,
`D_s S_A -> D_s S_A + D_s C`,
`D_s S_B -> D_s S_B - D_s C`.

Therefore the full discontinuity is split-invariant, but the discontinuity assigned to an individual internal family is **not** split-invariant whenever the admissible repartition carries nonanalytic/pole support.

This directly applies to the Iter592 question. K1^3 is an origin family in the exact derivative expansion of one same-action scalar inverse kernel, but Iter183 does not make that origin family a separately observable `T_cut` component. Without a separately frozen same-parent distributional map, assigning scalar-pole discontinuities to or away from `T_cut` family-by-family would introduce a new post-hoc decomposition convention.

## Consequence for K3 vs K1^3
The Iter591 K3 result remains valid: a strictly local analytic polynomial contact has `D_s K3=0` within the frozen analytic domain. That statement is origin-class invariant because the discontinuity annihilates the local analytic contribution itself.

By contrast, Iter592 K1^3 contains meromorphic scalar poles. Its **ordinary finite branch-cut** contribution is zero away from poles, but its pole-distribution assignment cannot be promoted or subtracted separately under the frozen split-invariant observable without an independent map.

Therefore freeze:

`K1cubed_individual_T_cut_projection = NOT_A_FROZEN_SPLIT_INVARIANT_OBSERVABLE`.

and retain:

`K1cubed = RETAIN_IN_FULL_SAME_ACTION_SOURCE_RESPONSE`.

The six K1/K2 placements are still necessary, but they are **not authorized as the complete discontinuity-bearing source block merely by separately discarding K1^3**.

## Classification
`PASS_SPLIT_INVARIANCE_AUDIT__INDIVIDUAL_K1CUBED_T_CUT_PROJECTION_NOT_A_FROZEN_OBSERVABLE__NON_RESIDUAL`.

This is a protocol/observable-definition negative result. It is not a Candidate-Gravity consistency FAIL, not exact comparator identity, not regime-specific non-identifiability, not near-degeneracy and not a novelty certificate.

No Source/Born subtraction is performed. `ANSATZ-003` remains forbidden. Fisher/resources remain forbidden.

## Exact next gate
Assemble and Ward-test the **complete same-action MSSC cubic source response** as one object:

`K3 + six(K1-G-K2 / K2-G-K1 placements) + six ordered K1-G-K1-G-K1 chains`,

with Iter588 routing and Iter589 normalization. The Ward test must preserve the Iter587 inverse-propagator RHS. Only after the full source-completed object is controlled may any source-to-Iter582/`T_cut` map be applied, and that map must act on the full object rather than defining an individual K1^3 Born subtraction post hoc.

MODEL_READINESS: 24%

Readiness change from Iter592: **0 percentage points**. A real protocol ambiguity is closed, but no full readiness-rubric sector closes and no robust comparator-subtracted residual exists.
