# RQIR Candidate Gravity — Iteration 543

Date: 2026-09-07

Authoritative start was re-read from `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_542.md`, the latest research log and recent commits. Canonical QUARTER rank4 run `34126397439`, job `101755969291`, remained `in_progress`, so no duplicate heavy Action was launched.

This iteration closes an independent exact conditioning/fail-closed contract for the already frozen Iteration-424 three-level h4/h6 truncation diagnostic. With `d1=BASE-HALF`, `d2=HALF-QUARTER`, `A=a h^4`, `B=b h^6`, the exact Iteration-536 inversion can be rewritten as

`A = 16(64 d2-d1)/45`,

`B = 256(d1-16 d2)/189`.

Therefore the reconstructed component ratio obeys the exact direct identity

`q := B/A = (80/21)(d1-16 d2)/(64 d2-d1)`.

For `r=d1/d2`, this becomes `q=(80/21)(r-16)/(64-r)`, with exact derivative

`dq/dr = 1280/[7(64-r)^2]`

and exact relative condition number

`kappa_rel(r->q)=|48 r/[(r-16)(64-r)]|`.

Thus the diagnostic ratio is relatively ill-conditioned near `r=16` because reconstructed `B` approaches zero, and singular near `r=64` because reconstructed `A` approaches zero. These are truncation-component coordinate effects, not model-level near-degeneracy.

The direct `(d1,d2)` Jacobian is

`dq/dd1=(1280/7)d2/(64d2-d1)^2`,

`dq/dd2=-(1280/7)d1/(64d2-d1)^2`.

A finite-error fail-closed pole guard was also derived. If `|e1|<=eps1`, `|e2|<=eps2`, define `D=64d2-d1`, `N=d1-16d2`, and `m=|D|-(eps1+64eps2)`. If `m>0`, the perturbed denominator cannot cross the reconstructed `A=0` pole and

`|q'-q| <= (80/21) [ (eps1+16eps2)|D| + |N|(eps1+64eps2) ] / (|D| m)`.

Exact zero surfaces are `d1=64d2 <=> A=0` and `d1=16d2 <=> B=0`.

Classification: `PASS_ITER424_H4_H6_RATIO_CONDITIONING_FAIL_CLOSED_CONTRACT_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`.

This is numerical/truncation authority only. It is not a Candidate-Gravity consistency PASS/FAIL, not exact comparator identity, not regime-specific non-identifiability, not model-level near-degeneracy and not a novelty certificate. No frozen threshold, parent dynamics, parameter convention, mass node, precision setting, or successor order was changed. `ANSATZ-003` remains uncreated; Fisher/resources remain forbidden until a nonzero comparator-surviving algebraic residual exists.

Machine authority: `candidate_gravity/results/iteration543_iter424_h4_h6_ratio_conditioning_contract.json`; reproducible audit: `candidate_gravity/code/iteration543_iter424_h4_h6_ratio_conditioning_contract.py`.

MODEL_READINESS: 24%

Readiness change: 0 percentage points. A diagnostic conditioning subgate closed, but no additional stable readiness-rubric sector closed.
