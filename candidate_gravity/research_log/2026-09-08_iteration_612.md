# Candidate Gravity Research Log — Iteration 612

Date: 2026-09-08

## Purpose
Continue from authoritative Iter611 by auditing whether the two missing auxiliary invariants held fixed under the frozen Iter205 native hard-channel discontinuity already exist in same-parent authority.

## Authority audit
Iter205 defines the positive-timelike hard-channel coordinate only structurally: `D_s F=Disc_s F/(2*pi*i)`, `D_s log_R(-s)=1`, and `T_cut=D_s Gamma3_ret,soft-W[D_s K2]`. It does not define routed scalar momentum `p0`, `u=p0^2`, `p0.q_i`, or a trajectory through those variables.

Iter217 is decisive against an easy shortcut: exact on-shell cut information does not uniquely fix the off-shell source-completed `T_cut`. Therefore on-shell constraints cannot be imported merely because they come from the same broad theory.

Iter218 fixes MSSC-001 from one covariant scalar action and forbids independent nonlinear contact tuning, but it does not choose a hard-channel path in off-shell source kinematics.

Iter221/222 do define a gauge-safe on-shell connected scalar-Compton cut and Born-fixed collinear factorization within MSSC-001. However that is a different observable identity from the full off-shell three-mode Iter594/605 source object. Importing its external on-shell constraints as the two missing Iter611 invariants would silently change the observable and contradict Iter217 unless an identity-preserving map is derived first.

Iter594/605 retain the exact three-mode source object with generic routed `p0` and all 13 source families. The existing fixture contains probes, not a native-`s` one-parameter family.

## Result
No existing authority supplies the two auxiliary constraints required by Iter611. This is now stronger than a search gap: the obvious candidate authorities either do not contain source kinematics (Iter205), explicitly establish on-shell/off-shell non-identifiability (Iter217), define only the parent action (Iter218), or belong to a different connected on-shell observable (Iter221/222).

Classification:
`BLOCKED_ITER612_TWO_AUXILIARY_HARD_CHANNEL_CONSTRAINTS_ABSENT_FROM_EXISTING_SAME_PARENT_AUTHORITY__KINEMATIC_COMPLETION_CONTRACT_REQUIRED__NON_RESIDUAL`

This is not a Candidate-Gravity PASS/FAIL and is not a residual. No zero-fill, Source/Born subtraction, comparator quotient, ANSATZ-003, Fisher/resources, or blind full-C5 run is permitted.

## Anti-idle conclusion
At the time of the audit GitHub Actions had 0 useful `queued` and 0 useful `in_progress` runs. A numerical run cannot determine a convention/observable trajectory missing from authority. The scientifically admissible next step is therefore not heavy computation but a prospectively versioned kinematic-completion contract derived from MSSC-001 plus the declared native linked observable, fixed before any pole projection result is inspected.

MODEL_READINESS: 24%

## Exact next gate
Construct a prospective identity-preserving hard-channel kinematic completion specifying either `(u(s),a_s(s),a_a(s))` directly or two equivalent auxiliary invariants held fixed under `D_s`. The contract must be derived from the same MSSC-001/native-linked observable definition, not selected from desired residues. Only after the contract is frozen may every retained `D_A(s)`, simple root, Jacobian and native `Y/T_cut` sign/normalization be evaluated.
