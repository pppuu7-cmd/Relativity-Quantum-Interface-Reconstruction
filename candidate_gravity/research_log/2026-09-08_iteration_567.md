# RQIR Candidate Gravity — Iteration 567

**Date:** 2026-09-08  
**Classification:** `PASS_ITER424_QUARTER_STENCIL_NULLSPACE_PROJECTOR_CONTRACT_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`

## Objective
While frozen QUARTER rank11 is still running, characterize the complete linear null-space of the already frozen 4x4 central4 mixed-derivative functional. This prevents over-interpreting a single assembled mixed derivative as identifying the full 16-coordinate grid structure.

## Exact result
With `w=[1,-8,8,-1]`, `C=w⊗w` and normalization `1/(144 h^2)`, the assembled derivative is

`D_uv=<C,F>/(144 h^2)`.

Because `C` is a nonzero outer product, `rank(C)=1`. Therefore the kernel of this linear functional in the 16-dimensional grid-data space has dimension

`16-1 = 15`.

The additive nuisance family `F_ij=a_i+b_j+c` has dimension `4+4-1=7` and lies inside this kernel because every row and column sum of `C` vanishes. Hence the quotient of invisible modes by the additive row/column nuisance subspace still has exact dimension

`15-7 = 8`.

Thus the mixed stencil is blind not only to the 7-dimensional additive nuisance family, but also to 8 additional non-additive grid directions. This is a property of the single stencil functional only.

The Frobenius norm is exact:

`||C||_F^2 = 16900`, `||C||_F=130`.

The orthogonal projector onto the sole stencil-visible grid mode is

`P_C(F)=<C,F>/16900 * C`.

Using `<C,F>=144 h^2 D_uv`, the norm of the visible component is

`||P_C(F)||_F = (72/65) h^2 |D_uv|`.

This gives an exact representer decomposition of the full grid into one stencil-visible mode plus a 15-dimensional invisible kernel.

## Interpretation discipline
Classification is **stencil-functional regime-specific non-identifiability**, diagnostic-only and non-promoting. It is NOT Candidate-Gravity model-level non-identifiability, not a physical near-degeneracy, not comparator identity, not a consistency PASS/FAIL, and not a novelty certificate. It does not alter any Iteration424/532 frozen support requirement or acceptance threshold.

## Reproducibility
Audit: `candidate_gravity/code/iteration567_iter424_quarter_stencil_nullspace_projector_audit.py`.
Machine-readable result: `candidate_gravity/results/iteration567_iter424_quarter_stencil_nullspace_projector.json`.

## Heavy-compute state
Canonical rank11 `(+2.5e-6,-1.25e-6)` run `34168897005`, job `101885271903`, remained `in_progress`; no duplicate/reordered heavy run was launched.

## Guardrails
Physical/operator authority remains Iteration411. Physical blocker remains Iteration421 `BLOCKED_CONVERGENCE`, unresolved physical set `[2]`. Concrete upstream `Source/Ward/contact+K2` target and robust comparator-subtracted residual remain absent. `ANSATZ-003` remains uncreated. Fisher/resources remain forbidden.

MODEL_READINESS: 24%

Readiness change: `0 pp`. This closes an exact identifiability/interpretation subgate for the numerical stencil, but no additional model-level readiness rubric sector.
