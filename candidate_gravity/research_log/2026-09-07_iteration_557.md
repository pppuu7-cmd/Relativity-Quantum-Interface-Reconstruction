# RQIR Candidate Gravity — Iteration 557

Date: 2026-09-07

## Purpose
Derive an exact non-promoting assembly invariant for the frozen central4 x central4 mixed auxiliary-mass derivative while rank9 heavy computation runs independently.

## Exact result
For central4 first-derivative weights `w=[1,-8,8,-1]` on nodes `[-2h,-h,+h,+2h]`, the mixed derivative uses coefficients

`C_ij = w_i w_j/(144 h^2)`.

Because `C_ij=C_ji`, decompose a complete fixed-mass grid as

`E_ij=(F_ij+F_ji)/2`, `A_ij=(F_ij-F_ji)/2`.

Then `A_ji=-A_ij`, so pairwise

`sum_ij C_ij A_ij = 0`

exactly. Therefore

`D_uv^central4[F] = D_uv^central4[E]`

on the complete matched grid.

The continuous counterpart in midpoint coordinates `m=(u+v)/2`, `d=(v-u)/2` is

`partial_u partial_v = (partial_m^2-partial_d^2)/4`.

## Interpretation
This does **not** authorize `u<->v` support substitution. Iterations 552–555 established that swapped routed points are physically distinct. The exchange-even average requires both values unless an independent equality theorem exists.

The result instead supplies a future QUARTER assembly control: after all nodes are raw-closed, assemble the mixed derivative independently from the full grid and from exchange-even pair averages. Any disagreement diagnoses an indexing/weight/orientation implementation error.

No compute-count reduction, no physical index2 promotion, no threshold change.

Machine-readable result: `candidate_gravity/results/iteration557_iter424_central4_exchange_even_projection_exact.json`.

MODEL_READINESS: 24%

Readiness change: 0 percentage points.
