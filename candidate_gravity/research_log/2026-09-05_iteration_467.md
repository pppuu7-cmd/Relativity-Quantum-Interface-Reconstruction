# 2026-09-05 — Iteration 467

## Exact central4×central4 parity-projector audit

Entry authority was Iteration 466 research/numerical front; physical/operator authority remains 411 and blocker authority remains 421 with unresolved set `[2]`. Canonical rank-6 run `33968129883` was rechecked and remains `in_progress`, so no duplicate heavy calculation was launched.

For the frozen one-dimensional central4 nodes `(-2,-1,+1,+2)` with coefficients `(1/12,-2/3,+2/3,-1/12)`, exact rational arithmetic gives the antisymmetry `c(-x)=-c(x)`. Therefore tensor weights `w(u,v)=c(u)c(v)` satisfy exactly

- `w(-u,v)=-w(u,v)`,
- `w(u,-v)=-w(u,v)`,
- `w(-u,-v)=w(u,v)`.

Consequently, for arbitrary sampled values on the symmetric stencil, not merely low-degree polynomials, the mixed operator annihilates the even-even, even-odd, and odd-even parity sectors and depends only on the odd-odd sector. For each positive-node pair `(a,b)` the canonical four signed source values combine as

`F(+a,+b)-F(-a,+b)-F(+a,-b)+F(-a,-b)`

with exact coefficient `c(a)c(b)/h^2`. The four dimensionless positive-node quartet coefficients are `4/9`, `-1/18`, `-1/18`, `1/144` for `(1,1),(1,2),(2,1),(2,2)` respectively. Re-expanding these four quartets reproduces all sixteen canonical tensor weights exactly.

Classification: `PASS_CENTRAL4_EXACT_ODD_ODD_PARITY_PROJECTOR__NON_PROMOTING`.

Scientific scope: exact algebra / implementation-provenance only. This is not a Candidate-Gravity consistency PASS/FAIL, comparator identity, regime-specific model non-identifiability, near-degeneracy, or novelty certificate. It does not authorize support deduplication and does not assume `F(u,v)=F(v,u)`. In particular, Iteration 454's rejection of `u<->v` deduplication remains untouched.

Post-support consequence: in addition to the Iteration-458/460/462 assembly checks, the canonical 16-term BASE and HALF assemblies should be cross-checked against the algebraically identical four-quartet representation at MP80 and MP120. Any discrepancy beyond explicitly bounded arithmetic roundoff is implementation/provenance `BLOCKED`, never physics FAIL. The canonical assembly remains authoritative; quartet grouping is an independent diagnostic representation and does not modify `ds=-d_base`, mass-step thresholds, or precision thresholds.

Reproducibility: `candidate_gravity/code/iteration467_central4_parity_projector_audit.py` and `candidate_gravity/results/iteration467_central4_parity_projector_audit.json`.

`MODEL_READINESS: 24%`

Readiness change: 0 percentage points. A stronger exact assembly invariant was closed, but no additional stable model-readiness rubric component was completed.

Exact next gate remains raw-consumption of run `33968129883` fail-closed at Iteration-455 distinct rank 6, `u=-5e-6, v=+5e-6`. PASS permits only rank 7; BLOCKED requires localization at rank 6. No later coordinate may be launched beforehand.
