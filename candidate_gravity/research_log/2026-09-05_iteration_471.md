# Candidate Gravity Research Log — Iteration 471

Date: 2026-09-05

## Starting audit
Read `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `ITERATION470_RECOVERY_DELTA.md`, `2026-09-05_iteration_470.md`, recent commits, iteration registry, and current Actions. Repository source of truth is at Iteration 470; rank-8 run `33980045356` is still `in_progress`, so no duplicate numerical run is allowed.

## Scientific step
Iteration 460 froze the sample-level weighted MP80↔MP120 difference bound and Iteration 467/469 froze exact four-quartet parity assembly and value-cancellation diagnostics. This iteration applies the same exact decomposition to the *precision discrepancy itself*.

For `DeltaF = F80-F120`, define each signed parity quartet

`DeltaQ_ab = DeltaF(+a,+b)-DeltaF(-a,+b)-DeltaF(+a,-b)+DeltaF(-a,-b)`

and frozen coefficients `alpha_11=4/9`, `alpha_12=alpha_21=-1/18`, `alpha_22=1/144`. Then

`DeltaD = sum_ab alpha_ab DeltaQ_ab`,

`B_quartet_delta = sum_ab |alpha_ab| |DeltaQ_ab|`,

`B_sample_delta = sum_ab |alpha_ab| sum_signs |DeltaF|`.

By two applications of the triangle inequality,

`|DeltaD| <= B_quartet_delta <= B_sample_delta`.

Thus the Iteration-460 bound is refined without weakening it. Define diagnostic ratios

`rho_precision_parity = B_quartet_delta/B_sample_delta`,

`rho_precision_shell = |DeltaD|/B_quartet_delta`,

with values in `[0,1]` whenever denominators are nonzero. The first localizes cancellation of MP disagreement inside parity quartets; the second localizes cancellation among quartet-scale MP disagreements. These are provenance diagnostics only.

Classification: `PASS_MP_DISCREPANCY_QUARTET_BOUND__DIAGNOSTIC_ONLY_NON_PROMOTING`.

A violation of `|DeltaD| <= B_quartet_delta <= B_sample_delta` in future assembled BASE/HALF MP80/120 evaluation is implementation/provenance `BLOCKED`, not Candidate-Gravity consistency FAIL. Large or small diagnostic ratios alone are not physics evidence, comparator identity, novelty, or model non-identifiability.

No estimator, threshold, dynamics, support order, or promotion rule changed. `ds=-d_base`, assembled MP threshold `<=2e-6`, BASE↔HALF threshold `<=2e-5`, and all frozen gates remain binding.

MODEL_READINESS: 24%

Readiness change: 0 percentage points; no stable rubric component closed.
