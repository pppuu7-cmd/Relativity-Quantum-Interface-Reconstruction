# RQIR Candidate Gravity — Iteration 489

Date: 2026-09-06  
MODEL_READINESS: 24%

Repository source of truth was re-read before work. Rank14 run `34008118612` had changed from `in_progress` to `completed/success`, artifact `9982729453`, artifact digest `sha256:125d9302521f6d8f54705d99f8310560454950fc92c0ad861574f1fe6bef6915`. Workflow success is not scientific PASS; raw artifact consumption remains a separate fail-closed gate, so support coverage is not advanced in this iteration.

This iteration closes an independent exact estimator/provenance subgate for the frozen central4 BASE↔HALF spectral transfer. From Iteration488

`A(theta)=i sin(theta)(4-cos(theta))/3`,

with BASE mixed symbol `D_h=A(theta_x)A(theta_y)/h^2` and HALF symbol `D_half=4 A(theta_x/2)A(theta_y/2)/h^2` at the same physical wave numbers. Away from BASE zeros, define

`rho(theta)=2 A(theta/2)/A(theta)`.

Using `sin(theta)=2 sin(theta/2) cos(theta/2)`, this simplifies exactly to

`rho(theta)=(4-cos(theta/2))/(cos(theta/2)*(4-cos(theta)))`,

so

`D_half/D_base = rho(theta_x) rho(theta_y)`.

For `0<|theta|<pi`, `cos(theta/2)>0` and `4-cos(theta)>0`, hence `rho(theta)>0`. It is not constant: its small-theta series is

`rho(theta)=1+theta^4/32-theta^6/256+469 theta^8/368640+O(theta^10)`.

Therefore no single mode-independent exact scalar maps BASE to HALF over the resolved spectrum. For generic multimode data the two-level BASE↔HALF discrepancy cannot be interpreted as one universal Richardson factor. This is an exact spectral strengthening of the Iteration487 no-Richardson result and does not change the frozen rule `ds=-d_base`.

Classification: `PASS_BASE_HALF_SPECTRAL_TRANSFER_RATIO_EXACT__NON_PROMOTING`.

Scientific scope is narrow: exact estimator/provenance authority only. This is not Candidate-Gravity consistency PASS/FAIL, not comparator identity, not regime-specific non-identifiability of the gravity model, not near-degeneracy, and not a novelty certificate. An implementation violating the exact transfer identity is `BLOCKED`, not physics FAIL.

No comparator/novelty claim changed, so no literature update was materially required. `ANSATZ-003` remains absent; Fisher/resources remain forbidden.

Readiness change: 0 percentage points. The spectral transfer subgate closes no additional component of the stable model-readiness rubric.

MODEL_READINESS: 24%
