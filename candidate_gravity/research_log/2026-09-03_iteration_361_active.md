# Candidate Gravity Research Log — Iteration 361 (active)

Date: 2026-09-03

Iteration 361 is the exact ordinary-simple continuation authorized by raw Iteration-360 authority. It evaluates only the 36 channels certified `REGULAR` by Iteration 360 and leaves all repeated-pole families untouched under the independent Iteration-359 derivative/distributional contract.

Normalization is the frozen Iteration-337 repository bridge `D_s I[F] = -sphere_mean(F)`. The stripped Iteration-355 physical denominator convention is retained explicitly: ghost denominator `-p^2`, graviton denominator `+p^2`. The separate connection effective-action weight from Iteration 308, `+(i/2) Tr U2`, is not folded into the stored `D_s Tr U2` coordinate.

Numerical gate: channel-resolved Gauss-Legendre-in-`z` times periodic-azimuth sphere means, low `(6,12)` versus high `(8,16)` plus a half-step azimuth phase cross-check. The convergence threshold is `2e-5`; the gate is fail-closed and does not weaken a threshold if a channel remains unresolved. Different external timelike `q^2` discontinuity variables are kept in separate buckets and are never summed into one coordinate.

Code commit: `9a906dffd04b9cf3afca305f13e64d52bc3a79ed`. Workflow commit: `02ae17bfe6f2a89d918e1c904373bd71dd06e12c`.

Active Actions run: `33795340192`, workflow `rqir-iteration361-u2-simple-normalized-channel-cut`; run was queued immediately after workflow registration.

Scientific authority remains Iteration 360 until the Iteration-361 raw artifact and sentinel/schema audit are validated. A green workflow alone is not authority.

MODEL_READINESS: 24%
