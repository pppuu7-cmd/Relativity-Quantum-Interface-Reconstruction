# Candidate Gravity Research Log — Iteration 555

Date: 2026-09-07

## Exact midpoint/antisymmetric decomposition of the u↔v routing pair
Continue from authoritative Iteration 554 without altering any frozen physical gate. Define exchange coordinates

`m=(u+v)/2`, `d=(v-u)/2`, so `u=m-d`, `v=m+d`.

For the frozen parent kinematics

`p=-a+alpha*q+rho*n`,
`alpha=-(s+u-v)/(2s)`,
`lambda=s^2+u^2+v^2-2su-2sv-2uv`,
`rho=sqrt(lambda)/(2 sqrt(s))`,

one obtains exactly

`alpha(u,v)=-1/2+d/s`,
`alpha(v,u)=-1/2-d/s`,

and

`lambda=s^2-4 s m+4 d^2`.

Therefore rho is even in the exchange coordinate `d`, while alpha has a fixed midpoint `-1/2` and a pure odd displacement. Writing

`p_mid=-a-q/2+rho(m,d^2)*n`,

we have the exact pair decomposition

`p(u,v)=p_mid+(d/s) q`,
`p(v,u)=p_mid-(d/s) q`.

Equivalently, the swap midpoint is exact and the full swap difference is purely q-directed:

`[p(u,v)+p(v,u)]/2=p_mid`,
`p(u,v)-p(v,u)=(2d/s)q=((v-u)/s)q`.

## Scientific meaning
This strengthens the Iterations 552–554 provenance result. The u↔v pair is not merely two unrelated numerical points: under the declared parent routing it is an exact centered pair around a common midpoint with alpha=-1/2, with rho unchanged because rho is even in d. This gives a clean exchange-even/exchange-odd coordinate decomposition.

The result does **not** imply that an arbitrary assembled observable is linear in d, odd/even in d, or a function of p alone; explicit u/v dependence elsewhere in the declared dynamics must still be retained. It is therefore a routing/provenance diagnostic only, not a substitute for any uncomputed QUARTER point.

Classification:
`PASS_ITER424_UV_SWAP_MIDPOINT_ANTISYMMETRIC_DECOMPOSITION_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`.

Reproducible code: `candidate_gravity/code/iteration555_uv_swap_midpoint_decomposition_audit.py`.
Machine-readable result: `candidate_gravity/results/iteration555_iter424_uv_swap_midpoint_antisymmetric_decomposition_exact.json`.

## Heavy route
Canonical QUARTER rank8 run `34153849866`, coordinate `(+1.25e-6,-1.25e-6)`, was still in progress at the beginning of this iteration. No duplicate, reorder, or substitute run was launched.

## Model readiness
Comparator foundation `24/25`; robust unique residual `0/20`; frozen parent dynamics/ANSATZ `0/20`; consistency/positivity/Ward/causality `0/15`; identifiability/Fisher `0/10`; resource/experiment closure `0/10`.

MODEL_READINESS: 24%
Readiness change: 0 percentage points. This closes an exact routing/provenance subgate but does not complete an additional stable model-level rubric sector.
ITERATION_TASK_COMPLETION: 100%

## Exact next gate
Raw-consume canonical rank8 run `34153849866` only after terminal completion. Only raw-valid PASS authorizes frozen rank9 `(+1.25e-6,+1.25e-6)`.
