# Candidate Gravity Research Log — Iteration 530

Date: 2026-09-07

## Source-of-truth audit
Read `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `candidate_gravity/recovery/RECOVERY_DELTA_ITERATION_529.md`, research log Iteration 529, recent commits, and live Actions. Repaired Iteration-424 quarter-rank1 run `34094463024` remains `in_progress`, so no duplicate heavy run was launched.

## Exact three-level central4 operator-geometry gate
To avoid duplicating the active quarter computation, this iteration closes an independent structural prerequisite for the eventual frozen Iteration-424 quarter assembly.

Using the same frozen tensor-product central4 first-derivative coefficients `(1,-8,+8,-1)/12` on each mass axis and the same base-step normalization `h=5e-6`, the relative derivative scalings are BASE `1`, HALF `4`, QUARTER `16` because the mixed derivative scales as `1/s^2`.

Exact support intersections are:
- BASE ∩ HALF: four coordinates `(±5e-6,±5e-6)`;
- HALF ∩ QUARTER: four coordinates `(±2.5e-6,±2.5e-6)`;
- BASE ∩ QUARTER: empty.

The exact row norm squares are
`||B||^2=4225/5184`, `||H||^2=4225/324`, `||Q||^2=16900/81`.
The exact inner products are
`<B,H>=4/81`, `<B,Q>=0`, `<H,Q>=64/81`.
Therefore the exact three-level Gram matrix is

`[[4225/5184,4/81,0],[4/81,4225/324,64/81],[0,64/81,16900/81]]`.

Its leading principal minors are
`4225/5184`, `5948843/559872`, and `75384279425/34012224`, all strictly positive. Hence the Gram matrix is positive definite and the three frozen assembly rows have exact row rank 3. In particular the QUARTER row is not a linear combination of BASE and HALF, while BASE and QUARTER are exactly support-orthogonal.

Classification:
`PASS_ITER424_BASE_HALF_QUARTER_CENTRAL4_GRAM_FULL_RANK_EXACT__NON_PROMOTING`.

This is a structural/operator-geometry PASS only. It does not imply any of the five frozen Iteration-424 physical clauses, physical promotion of index 2, Candidate-Gravity consistency PASS/FAIL, exact comparator identity, regime-specific non-identifiability, near-degeneracy, or novelty. Fisher/resources remain forbidden and ANSATZ-003 remains uncreated.

Machine-readable audit: `candidate_gravity/results/iteration530_iter424_three_level_central4_gram_exact_audit.json`.

## Model readiness
Comparator foundation `24/25`; robust unique residual `0/20`; frozen parent dynamics/ANSATZ `0/20`; consistency/positivity/Ward/causality `0/15`; identifiability/Fisher `0/10`; resource/experiment closure `0/10`.

MODEL_READINESS: 24%

Readiness change: 0 percentage points. A genuine operator-geometry/conditioning subgate is closed, but no additional stable model-readiness rubric sector is complete.

## Exact next gate
Raw-consume repaired quarter-rank1 run `34094463024` after completion. Only raw-valid PASS permits the frozen quarter source rank2. After all 12 new quarter coordinates close, assemble the QUARTER central4 row using the same declared dynamics and parameter convention, then execute the unchanged Iteration-424 five-clause physical reevaluation.
