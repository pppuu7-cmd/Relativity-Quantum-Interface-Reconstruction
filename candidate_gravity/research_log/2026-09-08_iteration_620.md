# RQIR Candidate Gravity — Iteration 620

Date: 2026-09-08

## Purpose

Perform the normalization-invariant projective-conditioning diagnostic explicitly allowed by the Iter619 current front, without inventing a value-dependent tolerance and without assigning `N_native`.

## Inputs

Authoritative Iter615 six-root source coefficients and Iter618/619 projective source-shape authority.

For each retained root, define the componentwise cancellation margin

`M = |A_aggregate| / (|A_pair| + sum |A_K1cubed|)`

and its reciprocal componentwise relative condition number `kappa = 1/M`.

These quantities are invariant under multiplication of all source coefficients by any common nonzero real or complex `N_native`.

## Result

No threshold is introduced. The six `kappa` values are approximately:

`[1.22705, 1.09379, 1.0, 1.27276, 2.45679, 1.27957]`.

The largest is the retained `D_b^+` root, `kappa=2.45679285736057`; its cancellation margin is `0.40703472293319004`.

The absolute projective coefficient dynamic range is `10.998916463857647`.

This is diagnostic only. It does not authorize dropping, rescaling, regrouping or summing roots and does not close the native normalization blocker.

## Guardrails

`N_native` remains BLOCKED. Source/Born subtraction remains `NOT_PERFORMED`. Comparator quotient, ANSATZ-003, Fisher/resources remain forbidden. No readiness point is awarded.

MODEL_READINESS: 24%
