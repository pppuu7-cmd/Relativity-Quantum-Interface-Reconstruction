# RQIR Candidate Gravity — Iteration 487

Date: 2026-09-06  
MODEL_READINESS: 24%

Rank14 canonical run `34008118612` remains `in_progress`; no duplicate heavy run was launched.

This iteration closes an exact estimator/truncation provenance gate for the frozen central4×central4 BASE/HALF mixed derivative. For one-dimensional central4 coefficients `c=[1/12,-2/3,2/3,-1/12]` on nodes `[-2,-1,+1,+2]`, exact moments through k=11 are `[0,1,0,0,0,-4,0,-20,0,-84,0,-340]`, giving one-dimensional expansion coefficients `a5=-1/30`, `a7=-1/252`, `a9=-1/4320`.

For `D_h=L_h^x L_h^y`, BASE at step `h` and HALF at `h/2`, the exact two-level mismatch through `h^8` is

`D_h-D_{h/2} = -(h^4/32)(f_51+f_15) -(h^6/256)(f_71+f_17) + h^8[-17/73728(f_91+f_19)+17/15360 f_55] + O(h^10)`.

Classification: `PASS_CENTRAL4_TWO_LEVEL_TRUNCATION_MISMATCH_EXACT__NON_PROMOTING`.

Scientific meaning is deliberately narrow. The BASE↔HALF discrepancy is a structured mixture of independent higher-derivative tensors, not a single identifiable `h^p` mode. Therefore two levels alone do not identify truncation order and do not authorize Richardson promotion. The frozen rule `ds=-d_base`, assembled MP threshold `2e-6`, and BASE↔HALF threshold `2e-5` are unchanged. Violating this estimator identity is implementation/provenance `BLOCKED`, not Candidate-Gravity consistency FAIL.

No comparator/novelty claim changed, so no literature update was materially required. `ANSATZ-003` remains absent; Fisher/resources remain forbidden.

Readiness change: 0 percentage points. This closes a provenance subgate only, not a stable readiness-rubric component.

MODEL_READINESS: 24%
