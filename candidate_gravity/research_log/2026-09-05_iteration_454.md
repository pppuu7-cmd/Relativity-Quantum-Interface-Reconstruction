# 2026-09-05 — Iteration 454

## Objective
While the authorized MP run at `u=-1e-5, v=-5e-6` remains in progress, test whether the remaining Iteration-407 mass support can be reduced by an exact `u<->v` symmetry without touching the active run.

## Source-of-truth audit
`CURRENT_QG_FRONT.md`, Iteration 453 recovery/research records, recent commits, and run `33940931120` were checked first. The run remains `in_progress`; it was not duplicated.

## Algebra/source result
Frozen Iteration-407 kinematics use
`lambda=s^2+u^2+v^2-2su-2sv-2uv`, `alpha=-(s+u-v)/(2s)`, `rho=sqrt(lambda)/(2sqrt(s))`, `beta=sqrt(lambda)/s`.

`lambda`, hence `rho` and `beta`, are symmetric under `u<->v`, but
`alpha(v,u)-alpha(u,v)=(u-v)/s`.
Therefore at fixed angular unit the routed momentum changes by
`p(v,u)-p(u,v)=((u-v)/s) q` for every off-diagonal mass coordinate.

Iteration-407 `numerator_at` sends this momentum into the frozen stripped numerator, and `direct_uncut` sends the same shifted momentum into the uncut denominator. No frozen identity certifies invariance of either under this q-directed shift. Hence `F(u,v)=F(v,u)` is not an exact comparator identity available for support deduplication.

## Classification
`PASS_MASS_SWAP_SHORTCUT_REJECTED__NON_PROMOTING`.

This is a methodological shortcut no-go, not a Candidate-Gravity consistency FAIL. The 32 source-occurrence denominator and 28 distinct-coordinate support from Iteration 452 remain authoritative; only exact BASE/HALF coordinate overlaps may share a precision certificate.

MODEL_READINESS: 24%

Readiness change: 0 percentage points; this prevents an invalid numerical shortcut but closes no stable model-readiness rubric point.

## Exact next gate
Raw-consume run `33940931120` fail-closed. No later coordinate may be launched before it completes and is validated.
