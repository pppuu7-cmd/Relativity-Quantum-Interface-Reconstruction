# RQIR Candidate Gravity — Iteration 492

Date: 2026-09-06

## Source-of-truth intake
Read `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_491.md`, research log Iteration 491, recent commits, and live Actions state before doing new work. Repository authority remained Iteration 491 at intake. Canonical rank16 run `34018646528` remained `in_progress`, so no duplicate heavy run was launched.

## Exact estimator/provenance result
For frozen central4 coefficients

`c = [1/12, -2/3, 2/3, -1/12]`

and mixed tensor weights `w_ij=c_i c_j`, exact arithmetic gives

- `sum_i |c_i| = 3/2`,
- `sum_ij |w_ij| = 9/4`,
- `sum_i c_i^2 = 65/72`,
- `sum_ij w_ij^2 = 4225/5184`,
- `max_ij |w_ij| = 4/9`.

Therefore, if every sampled value has a uniform absolute perturbation bound `|delta F_ij| <= eps`, then for

`D_h = (1/h^2) sum_ij c_i c_j F_ij`

we have the exact worst-case bound

`|delta D_BASE| <= (9/4) eps / h^2`.

For HALF with step `h/2`, under the same per-sample absolute-error cap,

`|delta D_HALF| <= 9 eps / h^2`,

so the purely stencil-induced worst-case absolute amplification bound is exactly four times larger for HALF than BASE.

Classification: `PASS_CENTRAL4_SAMPLE_ERROR_AMPLIFICATION_BOUND_EXACT__NON_PROMOTING`.

This is estimator/provenance authority only. It does not say the actual errors saturate the bound, does not alter any frozen MP80↔MP120 or BASE↔HALF threshold, and is not a Candidate-Gravity consistency PASS/FAIL, comparator identity, regime-specific non-identifiability, near-degeneracy, novelty certificate, or physical promotion.

Reproducible files:
- `candidate_gravity/code/iteration492_central4_sample_error_amplification.py`
- `candidate_gravity/results/iteration492_central4_sample_error_amplification.json`

## Active gate retained
Canonical frozen Iteration-455 rank16 `(u,v)=(-5e-6,-2.5e-6)`, multiplicity 1, run `34018646528`, remains `in_progress`. Certified occurrence-weighted support remains `20/32 = 62.5% = 1600/2560` until a raw artifact is consumed and passes the frozen audit.

Physical/operator authority remains Iteration 411; blocker authority remains Iteration 421 with unresolved set `[2]`. Robust comparator-subtracted residual remains absent; ANSATZ-003 and Fisher/resources remain BLOCKED.

MODEL_READINESS: 24%

Readiness change: 0 percentage points. A useful estimator/provenance subgate closed, but no additional stable-rubric component closed.
