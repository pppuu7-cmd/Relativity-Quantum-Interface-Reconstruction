# 2026-09-05 — Iteration 458

## Objective
While the sole authorized next-mass-node MP gate remains active, close a non-duplicative numerical-analysis ambiguity: whether local fixed-mass `F(u,v)` MP80/120 certificates are by themselves sufficient to certify the assembled frozen central4×central4 mixed auxiliary-mass derivative.

## Source-of-truth audit
Read `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_457.md`, the latest research log, recent commits, Iteration-455 support manifest, and live Actions metadata. Run `33946347229` remained `in_progress` and was not duplicated. The authoritative Iteration-407 implementation was also read directly.

## Exact algebra
Iteration 407 forms BASE and HALF derivatives separately and uses

`central4 coefficients = [1/12,-2/3,+2/3,-1/12]/h`

on each auxiliary-mass axis. It compares BASE and HALF through the frozen mass-step error but physically retains `ds=-d_base`; it does not Richardson-extrapolate BASE/HALF.

The first-derivative coefficient L1 sum is exactly `3/2`; therefore the tensor-product mixed derivative has pre-scaling L1 sum `9/4`. Hence

- BASE `h=5e-6`: `||D_uv||_1 = (9/4)/h^2 = 9.0e10`;
- HALF `h=2.5e-6`: `||D_uv||_1 = 3.6e11`.

If every fixed-mass sample had a uniform absolute MP80↔120 discrepancy bounded by `delta`, sufficient bounds for an assembled absolute discrepancy `<=2e-6` would be

- BASE: `delta <= 1/45000000000000000 = 2.2222222222222222e-17`;
- HALF: `delta <= 1/180000000000000000 = 5.5555555555555555e-18`.

The existing local gate is scaled (`<=1e-30`) rather than a global absolute bound, so local PASS cannot be promoted logically to derivative-level MP closure without also tracking sample scales / actual weighted differences. This is a precision-provenance distinction, not evidence of numerical failure.

## Frozen prospective post-support contract
After all 28 distinct `F(u,v)` coordinates have passed their local fixed-mass MP80/120 gates, but before Iteration-424 physical promotion:

1. assemble BASE mixed derivative independently from MP80 and MP120 samples using all 16 BASE source occurrences and the frozen central4 weights;
2. assemble HALF independently at MP80 and MP120 using all 16 HALF occurrences and its own weights;
3. require all four assembled values finite;
4. require scaled `|d_base80-d_base120| <= 2e-6` and scaled `|d_half80-d_half120| <= 2e-6`, each scaled by `max(1,|d80|,|d120|)`;
5. separately retain the frozen BASE-vs-HALF physical mass-step criterion `<=2e-5`; cross-precision and mass-step convergence are distinct gates;
6. report max absolute local MP difference, max local sample scale, and `sum |w_ij| |F80-F120|` for both levels.

Classification: `PASS_MIXED_DERIVATIVE_PRECISION_BUDGET_FROZEN__NON_PROMOTING`.

This is not a Candidate-Gravity consistency FAIL, comparator identity, regime-specific non-identifiability, near-degeneracy, or novelty certificate.

MODEL_READINESS: 24%

Readiness change: **0 percentage points**. A real precision-promotion ambiguity is closed, but no stable readiness-rubric component is completed.

## Exact next gate
Raw-consume run `33946347229` fail-closed at `u=-1e-5, v=+5e-6`. PASS permits only the next Iteration-455 manifest coordinate under unchanged five-z/NPHI16/radial/direct-MP80/120 conventions. The Iteration-458 assembly gate is deferred until full distinct-coordinate support is certified; it does not authorize skipping any local coordinate.
