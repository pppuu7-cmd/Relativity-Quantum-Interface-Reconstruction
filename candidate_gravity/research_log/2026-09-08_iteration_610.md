# RQIR Candidate Gravity — Research Log Iteration 610

Date: 2026-09-08

MODEL_READINESS: 24%

## Starting authority
Read `CURRENT_QG_FRONT.md`, Iter609 recovery/log, fresh commits and Actions state. No useful RQIR workflow was queued or in progress. Iter609 remains raw-valid repo authority and requires recovery/derivation of the frozen native cut-variable bridge before projection.

## Provenance audit
The original frozen linked-cut protocol was located at Iter205. Its certificate and validator explicitly define a positive timelike hard-channel variable `s` and

`D_s F = Disc_s F/(2*pi*i)`, with `D_s log_R(-s)=1`,

and the structural coordinate

`T_cut = D_s Gamma3_ret,soft - W[D_s K2]`.

Crucially, Iter205 labels itself structural and contains no physical MSSC source-tree momentum routing or map from a routed scalar momentum `p0` to `s`.

Iterations 238-240 preserve an identity guardrail: mixed matter radiation, relational/dressed bulk observables, and asymptotic/on-shell observables are not interchangeable with the frozen same-parent bulk `h^3` linked `h^2` coordinate without an explicit identity-preserving map. Iter592 separately preserved scalar-pole distributional terms as BLOCKED under linked `T_cut`. Iter608 supplies only the universal pole discontinuity law. Iter609 proves that the retained full source object has internal lines `r=p0+Q_A`, so pole support depends on `p0^2` and `p0.q_i`.

## Result
The native cut variable itself is therefore no longer unknown: it is `s`. The missing object is sharper and more specific: a same-parent kinematic relation `p0 -> s` (or direct `m^2-r_j^2(s,q_i,...)`) for every retained Iter594/605 scalar-propagator family, together with the induced simple-root Jacobian and native normalization/sign binding.

It is not authorized to set `s=p0^2`, `s=q_i^2`, or `s` equal to an Iter582 q2 bucket. It is also not authorized to reuse the isolated Iter588 symmetric routing for the already frozen full source object. The fact that Iter582 q2 buckets are spacelike does not prove scalar-pole distributional terms vanish under the separate hard-channel `D_s`.

Classification:

`BLOCKED_ITER610_NATIVE_T_CUT_S_EXISTS_BUT_NO_SAME_PARENT_MSSC_P0_TO_S_BRIDGE__NON_RESIDUAL`.

No Source/Born subtraction was performed; no zero-fill, ANSATZ-003, Fisher/resources or blind full-C5 is allowed.

## Anti-idle disposition
There is no independent scientifically admissible heavy numerical gate while the same-parent `p0 -> s` bridge is absent. The next action is an authority-improvement derivation, not a numerical projection. Starting a projection now would require inventing exactly the kinematic estimator and Jacobian prohibited by Iter606/609.

## Exact next gate
Prospectively derive from one declared same-parent source-completed dynamics the hard-channel invariant `s(p0,q_i,...)` for the full routed Iter594/605 source object, then derive each retained pole denominator as a function of `s` and fixed external invariants, its simple-root support/Jacobian, and the sign/normalization binding to the frozen Iter205 `D_s` convention. Only after that can the matched source-to-Iter582/native-linked projection run.
