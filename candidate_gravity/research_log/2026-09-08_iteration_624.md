# Candidate Gravity Research Log — Iteration 624

Date: 2026-09-08

Iteration 624 audits the functional type of the two objects separated by the remaining `N_native` bridge.

Classification:

`PASS_ITER624_SOURCE_VS_NATIVE_FUNCTIONAL_OBJECT_TYPE_AUDIT__LEGENDRE_SIGN_ALONE_CANNOT_FIX_N_NATIVE__MEASUREMENT_LEVEL_BRIDGE_REQUIRED_NON_RESIDUAL`.

The native object frozen in Iter147/149 is a gravitational retarded/1PI cubic response, schematically

`chi2R = - G_R Gamma3 G_R G_R`.

The MSSC source object frozen by Iter590/594/616 is the third background-metric derivative of the scalar propagator `G_phi=K_phi[g]^-1`, followed by amputation of the two common scalar endpoints.

These are not the same functional derivative. Therefore the remaining normalization cannot be repaired by importing only a generic Legendre-transform sign or the Iter338 effective-action `+i` factor. A real bridge must specify the combined matter+gravity measurement functional: which connected scalar-probe observable is measured, how gravitational propagators/vertices and source contacts enter, and how the result is projected/amputated onto the native Iter582 coordinate.

This explains why Iter151 correctly refused to import an incompatible amputated-vertex normalization.

The next gate is therefore constructive rather than numerical: freeze the combined CTP/in-in matter+gravity measurement-level bridge before consulting Candidate/comparator values. It must derive all remaining sign/i/factorial factors and preserve the Iter618/619 root-by-root projective ratios.

MODEL_READINESS: 24%, unchanged.
