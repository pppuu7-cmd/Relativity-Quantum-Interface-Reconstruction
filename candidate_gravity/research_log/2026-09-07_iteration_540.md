# RQIR Candidate Gravity — Iteration 540

## Scope
Repository authority was re-read before work: CURRENT_QG_FRONT, Recovery Delta 539, research log 539, recent commits, and live Action `34115105768`. Rank3 remained `in_progress`, so no duplicate heavy computation was launched.

This iteration closes an independent exact diagnostic for the already-frozen BASE/HALF/QUARTER central4 hierarchy. It does not alter the Iteration-424 physical acceptance contract.

## Exact three-level step-ratio structure
Use the common smooth expansion

`X_h = D + A + B + C + O(h^10)`,

with `A=a h^4`, `B=b h^6`, `C=c h^8`, and define

- `d1 = BASE - HALF`,
- `d2 = HALF - QUARTER`.

Then exactly through h^8,

`d1 = (15/16)A + (63/64)B + (255/256)C`,

`d2 = (15/256)A + (63/4096)B + (255/65536)C`.

Therefore the pure-order ratios are exact:

- pure h^4: `d1/d2 = 16`,
- pure h^6: `d1/d2 = 64`,
- pure h^8: `d1/d2 = 256`.

For the h^4+h^6 truncation model, set `t=B/A` and `q=(21/80)t`. For nonzero `d2`,

`r = d1/d2 = (16 + 64 q)/(1 + q)`.

The inverse map is

`B/A = (80/21) (r-16)/(64-r)`.

Exact cancellation locations are:

- `d1=0` at `B/A=-20/21`,
- `d2=0` at `B/A=-80/21`, where `r` is undefined.

If A and B have the same sign, `r` is a same-sign weighted average of 16 and 64, hence

`16 <= r <= 64`.

If A, B and C all have the same sign, the corresponding exact weighted-average statement gives

`16 <= r <= 256`.

Thus a future measured ratio outside these intervals is evidence for opposite-sign cancellation and/or omitted higher-order contamination under the stated expansion. It is **not**, by itself, a Candidate-Gravity consistency FAIL.

## Classification
`PASS_ITER424_THREE_LEVEL_STEP_RATIO_SIGN_STRUCTURE_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`

Reproducible authority:
- `candidate_gravity/code/iteration540_iter424_three_level_step_ratio_sign_contract.py`
- `candidate_gravity/results/iteration540_iter424_three_level_step_ratio_sign_contract.json`

## Guardrails
No dynamics, parameter convention, mass node, precision, threshold, support order or Richardson policy changed. This diagnostic does not replace any of the five frozen Iteration-424 physical clauses, does not promote index 2, does not establish comparator identity/non-identifiability/near-degeneracy/novelty, does not create `ANSATZ-003`, and does not authorize Fisher/resources.

Physical unresolved set remains `[2]`. The upstream fixed comparator quotient remains operationally BLOCKED pending a concrete `Source/Ward/contact+K2` target and robust comparator-subtracted residual.

## Readiness
`MODEL_READINESS: 24%`

Readiness change: **0 percentage points**. The exact diagnostic reduces ambiguity in interpreting future three-level truncation data, but closes none of the six model-readiness sectors.

## Exact next gate
Canonical rank3 run `34115105768` remains the sole heavy successor. After terminal completion, raw-consume its artifact fail-closed. Only raw-valid PASS authorizes frozen rank4 `(-1.25e-6,-1.25e-6)`.
