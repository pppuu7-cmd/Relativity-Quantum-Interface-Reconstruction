# Candidate Gravity Research Log — Iteration 514

**Date:** 2026-09-07  
**Scope:** exact distribution-free assembly-error propagation for frozen BASE/HALF central4 weights while canonical rank24 remains in progress.

## Start state
Repository source of truth was reread from `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `candidate_gravity/recovery/RECOVERY_DELTA_ITERATION_513.md`, the Iteration-513 research log, recent commits, and live Actions. Latest completed numerical authority is Iteration 513 with raw-valid rank23 and certified occurrence-weighted support `28/32 = 87.5%`. Canonical rank24 run `34063351852`, job `101567716622`, remains `in_progress`; no duplicate heavy workflow was launched.

## Exact deterministic error operator bound
Iteration 508 established exact frozen central4 row L1 norms, in units of `1/h^2`: `||W_BASE||_1 = 9/4`, `||W_HALF||_1 = 9`, and union-aggregated `||W_BASE-W_HALF||_1 = 397/36` on the 28-coordinate support union.

For arbitrary coordinate-level absolute perturbations `e_i` satisfying only `|e_i| <= epsilon`, triangle inequality therefore gives the exact induced `l_infinity -> absolute` bounds

- `|delta BASE| <= (9/4) epsilon / h^2`,
- `|delta HALF| <= 9 epsilon / h^2`,
- `|delta(BASE-HALF)| <= (397/36) epsilon / h^2`.

If BASE and HALF are bounded separately and their shared-node structure is ignored, the looser triangle coefficient would be `45/4`. Exact aggregation on the four shared coordinates reduces the coefficient by `2/9`, so the retained fraction is `397/405` and the exact relative worst-case-bound reduction is `8/405`.

Classification: `PASS_BASE_HALF_DETERMINISTIC_LINF_ERROR_OPERATOR_BOUND_EXACT__NON_PROMOTING`.

Reproducible audit: `candidate_gravity/code/iteration514_base_half_deterministic_error_operator_bound.py`. Machine-readable result: `candidate_gravity/results/iteration514_base_half_deterministic_error_operator_bound.json`.

## Scope and guardrails
This certificate is deterministic and distribution-free; unlike Iteration 511 it makes no iid, Gaussian, equal-variance, or covariance assumption. It is an absolute assembly-error operator norm only. It does **not** convert local scaled MP80↔MP120 discrepancies into assembled scaled discrepancies without an explicit common absolute normalization. It defines no new threshold, does not relax any frozen threshold, does not promote physical blocker index 2, and does not make BASE-HALF an independent observable or Fisher row.

No literature refresh was required because no external comparator, novelty, or consistency claim was changed.

## Nonclaims/readiness
Physical/operator authority remains Iteration 411. Physical blocker authority remains Iteration 421 `BLOCKED_CONVERGENCE`, unresolved set `[2]`. Fixed comparator quotient remains operationally BLOCKED until the concrete upstream `Source/Ward/contact+K2` target exists. Robust comparator-subtracted residual remains absent; `ANSATZ-003` remains uncreated; Fisher/resources remain forbidden.

MODEL_READINESS: 24%

Readiness change: **0 percentage points**. A real assembly/error-propagation subgate closed, but no additional stable readiness-rubric sector was completed.

## Exact next gate
Fail-closed raw-consume canonical rank24 run `34063351852` after completion. Only raw-valid PASS permits frozen rank25 `(+2.5e-6,+5e-6)`, HALF local index 11, multiplicity 1. BLOCKED freezes suffix progression and requires first-failing `z/phi/radial` localization under unchanged frozen conventions.
