# RQIR Candidate Gravity — Iteration 501

Date: 2026-09-06
MODEL_READINESS: 24%

## Source-of-truth recheck
Read `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_500.md`, Iteration-500 research log, recent commits, and live Actions state. Canonical rank20 run `34041245929` was `in_progress`, so no duplicate heavy run was launched.

## Exact BASE↔HALF discrepancy expansion
Using the frozen mixed-central4 smooth-field expansion retained from Iteration 499,

`D_h = dxy - h^4/30 S4 - h^6/252 S6 + h^8[-Spure8/4320 + Scross8/900] + O(h^10)`,

with `S4 = d_x^5 d_y + d_x d_y^5`, `S6 = d_x^7 d_y + d_x d_y^7`, `Spure8 = d_x^9 d_y + d_x d_y^9`, and `Scross8 = d_x^5 d_y^5`, the HALF estimator uses step `h/2`. Therefore BASE-minus-HALF is exactly, within that asymptotic expansion,

`D_BASE - D_HALF = -h^4/32 S4 - h^6/256 S6 + h^8[-17/73728 Spure8 + 17/15360 Scross8] + O(h^10)`.

The coefficients follow from exact rational factors `(1-2^-4)`, `(1-2^-6)`, `(1-2^-8)` and are reproduced by `candidate_gravity/code/iteration501_base_half_discrepancy_series.py` with machine-readable result `candidate_gravity/results/iteration501_base_half_discrepancy_series.json`.

Classification: `PASS_BASE_HALF_DISCREPANCY_ASYMPTOTIC_SERIES_EXACT__NON_PROMOTING`.

## Scientific scope
This is estimator/provenance authority only. It is a smooth-field asymptotic statement and does not create an exact mode-independent Richardson identity, does not alter frozen `ds=-d_base`, does not weaken the retained BASE↔HALF threshold, and does not promote physical index 2. Iterations 489–490 remain authority that exact spectral transfer is mode-dependent away from the low-frequency asymptotic regime.

## Guardrails/readiness
Physical/operator authority remains Iteration 411; blocker authority remains Iteration 421 `BLOCKED_CONVERGENCE`, unresolved set `[2]`. Robust comparator-subtracted residual is absent. `ANSATZ-003` remains uncreated; Fisher/resources remain forbidden.

MODEL_READINESS: 24%
Readiness change: 0 percentage points. The exact discrepancy-series subgate improves future assembly diagnostics but closes no additional stable-rubric model component.

## Exact next gate
Fail-closed raw-consume canonical rank20 run `34041245929` after completion. Only a raw scientific PASS authorizes the next explicit UNTESTED frozen Iteration-455 manifest coordinate; BLOCKED requires localization of the first failing `z/phi/radial` sample with frozen conventions unchanged.
