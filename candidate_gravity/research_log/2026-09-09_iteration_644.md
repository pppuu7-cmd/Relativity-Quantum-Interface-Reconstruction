# Candidate Gravity Research Log — Iteration 644

Date: 2026-09-09

Authoritative predecessor: Iter643 operational/provenance BLOCKED because the frozen Iter636 closed-Gamma3 invariant family had no pre-result external metric-probe continuation away from the exact Iter368/588 anchor `s0=1`.

## Question

Does same-parent Lorentz covariance plus the exact Iter368/588 anchor uniquely determine `h_s(s), h_a(s), h_b(s)` at fixed `t0=0.14,u0=0.34`; and, if not, can a minimal observable definition be frozen prospectively without inspecting numerator-weighted cut values?

## Exact authority audit

Iter368 explicitly generates three external tensors as arbitrary symmetric metric probes,

`rng=np.random.default_rng(319); h=0.12*(x+x.T)/2`,

not as TT/on-shell graviton polarizations. Therefore adding transversality, tracelessness, gauge fixing, or unit normalization now would change the observable and is forbidden.

The fixed-(t0,u0) closed-Gamma3 family admits the canonical representative

`q_s=(sqrt(s),0,0,0)`,

`q_a=E_a e0+k e1`, with `E_a=(u0-s-t0)/(2 sqrt(s))`, `k=sqrt(E_a^2-t0)`, `e1=(0,1/sqrt(2),1/sqrt(2),0)`,

and `q_b=-q_s-q_a`. It reduces to the exact Iter368 anchor at `s=1` and preserves `q_s^2=s`, `q_a^2=t0`, `q_b^2=u0` and exact momentum closure. The ordinary-cut region `s>=1.96` lies on the same real branch.

## Non-uniqueness certificate

Covariance plus anchor is not unique. A common spatial O(2) rotation around the `e1` axis leaves `q_s,q_a,q_b` unchanged for every s. Any smooth angle `theta(s)` with `theta(1)=0` therefore gives another anchor-matched covariant tensor continuation. At the prospective test point `s=2`, choosing `theta=1 rad` preserves the Lorentz metric to `1.11e-16` and all three momenta to `5.55e-17`, while changing the three generic seed-319 tensor probes by maximum absolute amounts

`[0.17057670785382034, 0.16351183910172382, 0.18117665064705094]`.

Hence the residual continuation freedom is function-valued; there is at least one arbitrary smooth function `theta(s)` with `theta(1)=0`. This is a genuine non-uniqueness of the observable continuation, not Candidate-Gravity regime-specific non-identifiability and not a model consistency FAIL.

## Prospective observable contract

Before evaluating any numerator-weighted cut value, freeze:

`MSSC001-CLOSED-GAMMA3-TENSOR-TRANSPORT-V1`.

Rule: use the canonical q_s-rest-frame fixed scattering-plane representative above; choose zero transverse twist; hold all tetrad components of each exact Iter368 metric probe fixed along the trajectory. In the exact Iter368 coordinate frame the tetrad is constant, so the numerical consequence is

`h_s(s)=h_s(1), h_a(s)=h_a(1), h_b(s)=h_b(1)`.

This equality is now a prospectively versioned observable definition and must not be confused with the previously forbidden post-hoc reuse of anchor tensors. No gauge/transversality/tracelessness condition is added. Symmetry `h_{mu nu}=h_{nu mu}` and the anchor tensor components are the normalization/shape convention.

Scientific classification:
`PASS_ITER644_PROSPECTIVE_EXTERNAL_TENSOR_TRANSPORT_V1__COVARIANCE_ALONE_NONUNIQUE__ZERO_TWIST_TETRAD_COMPONENTS_FROZEN__NON_RESIDUAL`.

This is a protocol/observable-definition PASS plus a negative non-uniqueness result. It is not Candidate-Gravity consistency PASS/FAIL, exact comparator identity, regime-specific non-identifiability, near-degeneracy, novelty certificate, or a Candidate residual.

Reproducible implementation: `candidate_gravity/code/iteration644_external_tensor_transport_contract.py`; committed machine-readable result: `results/iteration644_external_tensor_transport_contract/result.json`. Candidate values were not used and no numerator-weighted cut was evaluated.

Guardrails unchanged: all 13 families retained; K3 analytic/contact; K1/K2 and K1^3 ordinary support `s>=1.96`; `zero_fill=false`; Source/Born subtraction `NOT_PERFORMED`; native Y/T_cut projection `NOT_PERFORMED`; fixed C3/C4/C5/nonlocal/asymptotic-safety comparator quotient `NOT_PERFORMED`; no `ANSATZ-003`; no Fisher/resources.

MODEL_READINESS: 24%
Readiness change: 0 percentage points. A necessary observable-continuation prerequisite is now prospectively frozen, but robust unique residual remains `0/20` and no complete readiness-rubric sector closes.

Exact next gate — Iteration645: with only Iter594 same-parent K1/K2/K3 machinery, Iter627/632 retarded-state authority, Iter640/641 support/projector authority, and the now-frozen Tensor-Transport-V1, evaluate numerator-weighted ordinary s-channel cut densities family-by-family for K1/K2 and K1^3 on `s>=1.96`. Keep K3 analytic/contact. Do not perform native projection, Source/Born subtraction, comparator quotient, ANSATZ-003, Fisher or resources in that iteration.
