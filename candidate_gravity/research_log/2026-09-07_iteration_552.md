# Candidate Gravity Research Log — Iteration 552

Date: 2026-09-07

## Independent u↔v substitution audit
While the canonical rank8 successor was already queued/running independently, Iteration 552 tested a frozen guardrail rather than changing the heavy route: may a computed QUARTER coordinate be reused for its u↔v-swapped partner?

The test uses only already raw-consumed rank1 `(-2.5e-6,-1.25e-6)` and rank3 `(-1.25e-6,-2.5e-6)` artifacts. Both artifact/result/audit hashes were independently reverified before comparison. The 80 rows align exactly by z and phi index.

## Raw counterexample
Across the aligned 80 rows:
- MP120 imaginary parts are exactly equal in 80/80 rows (zero in this sample set);
- MP120 real parts are exactly equal in 0/80 rows;
- minimum absolute real difference is about `8.07090e-13`;
- maximum absolute real difference is about `2.67304e-10`;
- maximum relative difference with respect to the local value is about `1.12203e-4`;
- even the smallest raw difference exceeds the larger corresponding pairwise radial-error estimate by about `4.61e3`;
- the largest difference/error separation reaches about `3.26e6`.

Hence the discrepancy is not explained by the certified radial numerical error.

## Exact source-level reason
The inherited fixed-mass kinematics use
`lambda=s^2+u^2+v^2-2su-2sv-2uv`,
`rho=sqrt(lambda)/(2 sqrt(s))`,
and `alpha=-(s+u-v)/(2s)`.

`lambda` and `rho` are symmetric under u↔v, but `alpha` is not. Exactly,
`alpha(u,v)-alpha(v,u)=(v-u)/s`.
The physical routed point is `p=-a+alpha*q+rho*unit(z,phi)`, so for `u!=v` a swap shifts the actual cut momentum along q. The raw rank1/rank3 counterexample confirms that downstream evaluation does not restore universal equality.

Classification:
`PASS_ITER424_UV_SWAP_NON_EQUIVALENCE_RANK1_RANK3__DIAGNOSTIC_ONLY_NON_PROMOTING`.

Machine-readable authority: `candidate_gravity/results/iteration552_iter424_uv_swap_non_equivalence_audit.json`.

## Scientific implication
The Iteration-532 prohibition on u↔v support substitution is now positively justified rather than merely conservative. The current 12 new QUARTER coordinates must continue to be computed independently in frozen order. This is not a Candidate-Gravity consistency FAIL and does not promote physical index2.

## Model readiness
MODEL_READINESS: 24%
Readiness change: 0 percentage points.
ITERATION_TASK_COMPLETION: 100%

## Exact next gate
Do not alter the heavy route. Raw-consume canonical rank8 run `34153849866` after terminal completion; only raw-valid PASS permits frozen rank9 `(+1.25e-6,+1.25e-6)`.
