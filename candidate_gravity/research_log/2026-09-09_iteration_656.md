# RQIR Candidate Gravity — Iteration 656

Date: 2026-09-09

## Scope

Fail-closed compatibility audit between the prospectively frozen Iter655 soft-T_cut kinematics and the explicit same-parent Iter653 closed-SK Gamma2/Gamma3 Ward reconstruction. No Candidate cut values, Source/Born subtraction, comparator quotient, profiling, Fisher, or resource calculation is performed here.

## Authority consumed before this iteration

- Historical Iter175 source/result: null soft direction `k=(1,0,0,1)`, plus TT polarization `eps_xx=+1`, `eps_yy=-1`, and abstract soft split `Gamma3_soft = W[K2] + Rlin:B3`.
- Historical Iter205 source/result: `D_s(A)=Disc_s A/(2*pi*i)` and linked-cut-before-soft-limit protocol.
- Iter653 `MSSC001-GAMMA2-GAMMA3-WARD-CONTRACT-V1`: same-parent closed-SK Gamma2/Gamma3 Ward identity and explicit longitudinal/transverse reconstruction valid under the stated assumption `q^2 != 0`, with `xi_mu` containing `q^-2` and `q^-4`.
- Iter655 prospective kinematic contract: `n=(1,0,0,1)`, `q3(eps)=-eps*n`, plus-TT soft tensor. Canonical Iter655 Action `34327833414` completed `success`; artifact `10093741467`, digest `sha256:8e6cf7a3aab804baa964762b7fafd3d07310e16deeb9363120adb5342501065e`.

## Algebraic result

For every nonzero epsilon,

`q3(eps)^2 = eps^2 n^2 = 0`, because `n^2=0`.

The explicit Iter653 longitudinal reconstruction uses inverse powers `q^-2` and `q^-4` and was frozen only for `q^2 != 0`. Therefore that projector is not defined on the exactly null Iter655 soft leg. This is a domain mismatch, not evidence that the Ward image vanishes.

Accordingly, `W[D_s K2]` must NOT be zero-filled and must NOT be inferred by evaluating the Iter653 non-null projector at `q^2=0`.

## Historical-provenance result

Iter175/205 do fix the historical null soft direction/polarization structure and the linked-cut/soft-limit ordering, but they do not uniquely specify a full closed-SK hard momentum/tensor continuation together with an explicit null-safe tensor formula for the Ward image. Thus Iter655 is a prospective, versioned completion compatible with the historical soft structures; it is not a unique machine recovery of the entire historical observable.

This distinction is retained as a negative/provenance result and prevents retroactive relabelling.

## Scientific classification

`BLOCKED_ITER656_NULL_SOFT_WARD_IMAGE_NOT_DEFINED_BY_ITER653_Q2_NONZERO_PROJECTOR__ITER655_PROSPECTIVE_TRAJECTORY_NOT_UNIQUE_HISTORICAL_RECOVERY__NON_RESIDUAL`

This is an operational/mathematical/provenance BLOCKED result. It is NOT a Candidate-Gravity consistency FAIL/PASS, NOT an exact comparator identity, NOT regime-specific non-identifiability, NOT near-degeneracy, and NOT a novelty certificate.

## Guardrails

- `zero_fill=false`.
- Do not set the Ward image to zero from TT-ness alone.
- Do not introduce an off-null regulator after inspecting Candidate cut values.
- Native `T_cut`, Source/Born subtraction, and the fixed C3/C4/C5/nonlocal/asymptotic-safety comparator quotient remain `NOT_PERFORMED`.
- `ANSATZ-003` remains forbidden.
- Fisher/resources remain forbidden until a nonzero algebraic comparator-subtracted residual exists.

## Readiness

Comparator foundation 24/25; robust unique residual 0/20; frozen parent dynamics/ANSATZ 0/20; consistency/positivity/Ward/causality 0/15; identifiability/Fisher 0/10; resource/experiment closure 0/10.

MODEL_READINESS: 24%

Change from Iter655/Iter654 baseline: 0 percentage points. The gate is more sharply localized, but no readiness-rubric sector has closed and no robust unique residual exists.

## Exact next gate

Iteration657 must derive a null-compatible Ward reconstruction directly from the Iter653 action Ward identity without using the `q^-2/q^-4` longitudinal projector. Any auxiliary null vector or off-null regulator must be prospectively fixed before numerical cut inspection, and the derived soft Ward image must be proven independent of that auxiliary choice/path in the null limit. If such independence cannot be established, the residual gauge/path-completion freedom is to be preserved as BLOCKED rather than fitted or zero-filled.
