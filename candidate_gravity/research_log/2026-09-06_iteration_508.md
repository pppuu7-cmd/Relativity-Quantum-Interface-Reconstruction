# Candidate Gravity Research Log — Iteration 508

**Date:** 2026-09-06  
**Scope:** close an exact BASE/HALF assembly-coordinate provenance gate while the sole authorized rank22 heavy run remains in progress; do not duplicate the run and do not advance physical/model claims.

## Repository-authoritative start state
Latest authoritative research iteration at start: 507. Latest completed numerical mass-support authority: Iteration 505 rank21 raw PASS, with `26/32 = 81.25% = 2080/2560` occurrence-weighted rows certified. Physical unresolved set remains `[2]` under Iteration 421 `BLOCKED_CONVERGENCE`. Canonical rank22 run `34053331578` remains in progress and is not duplicated.

## Exact BASE/HALF coordinate-union audit
Frozen central4 uses 1D offsets `{-2,-1,+1,+2}`.

- BASE coordinates are those offsets in units of `BASE_H=h`, so the mixed tensor stencil has `16` source occurrences.
- HALF uses step `h/2`, so its coordinates in BASE-h units are `{-1,-1/2,+1/2,+1}`, again `16` source occurrences.
- Their exact intersection is only the four Cartesian points `(±1,±1)`.
- Therefore the union contains exactly `16+16-4=28` distinct mass coordinates while retaining `32` source occurrences.

This proves that the frozen bookkeeping `32 source occurrences / 28 distinct coordinates` is forced by the exact central4 BASE/HALF geometry, not by an arbitrary deduplication convention.

## Exact overlap derivative weights
With the frozen 1D central4 coefficients

`c[-2]=1/12, c[-1]=-2/3, c[+1]=+2/3, c[+2]=-1/12`,

the BASE mixed weights at the four shared `(±h,±h)` points, in units of `1/h^2`, are `±4/9`. HALF uses step `h/2`, so its physical 1D coefficients carry an extra factor `2`; the shared coordinates correspond to HALF offsets `±2`, producing mixed weights `±1/36`.

Thus the exact common-node contribution to `D_BASE-D_HALF` is `±5/12 * 1/h^2`, with the checkerboard signs inherited from the mixed derivative.

The L1 weight norms in units of `1/h^2` are:
- BASE: `9/4`;
- HALF: `9`;
- treating the two stencils separately: `45/4`;
- after exact union aggregation of `D_BASE-D_HALF`: `397/36`;
- exact overlap cancellation reduction: `2/9`.

This is an assembly/noise-propagation provenance fact only. It does not alter any frozen threshold and does not authorize Richardson promotion.

## Classification
`PASS_BASE_HALF_32_OCCURRENCES_28_DISTINCT_OVERLAP_WEIGHTS_EXACT__NON_PROMOTING`

Strict semantics:
- exact coordinate-union geometry: PASS;
- exact overlap weights: PASS;
- local precision certificate sharing at identical coordinates: permitted only as already frozen;
- BASE and HALF derivative weights remain distinct and must both be applied in assembly;
- physical index-2 promotion: no;
- assembled BASE/HALF numerical closure: not yet evaluated;
- Candidate-Gravity consistency PASS/FAIL: no;
- comparator identity / non-identifiability / near-degeneracy / novelty certificate: no;
- `ANSATZ-003`: still forbidden;
- Fisher/resources: still forbidden.

## Reproducibility
Added:
- `candidate_gravity/code/iteration508_base_half_union_overlap_exact_audit.py`
- `candidate_gravity/results/iteration508_base_half_union_overlap_exact_audit.json`

No literature refresh was required because no external comparator, novelty, consistency, or phenomenology claim was advanced.

## Readiness
Stable rubric unchanged:
- comparator foundation: `24/25`
- robust unique residual: `0/20`
- frozen parent dynamics/ANSATZ: `0/20`
- consistency/positivity/Ward/causality: `0/15`
- identifiability/Fisher: `0/10`
- resource/experiment closure: `0/10`

**MODEL_READINESS: 24%**

Readiness change: **0 percentage points**. Iteration 508 closes a real exact assembly/provenance subgate but completes no additional stable-rubric Candidate-Gravity component.

## Exact next gate
Continue the single canonical rank22 run `34053331578` without duplication. After completion, raw-audit and raw-consume its artifact. Only raw-valid PASS authorizes rank23 `(u,v)=(+2.5e-6,-2.5e-6)`. A raw BLOCKED result freezes support progression and requires localization of the first failing `z/phi/radial` sample under unchanged frozen dynamics, support order, precision convention, and thresholds.
