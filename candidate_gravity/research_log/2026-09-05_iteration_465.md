# 2026-09-05 — Iteration 465

## Two-level truncation-order non-identifiability

Entry authority: Iteration 464 research front; physical/operator authority 411; blocker authority 421 with unresolved set `[2]`. Canonical rank-5 run `33962417750` remains the sole authorized numerical gate and was not duplicated.

Iteration 464 derived exact central4 asymptotic single-term scalings 16 for an isolated `h^4` truncation term and 64 for an isolated `h^6` term. This iteration closes the interpretation gap: with only two derivative levels, BASE `B=D(h)` and HALF `H=D(h/2)`, the leading truncation exponent is not empirically identifiable.

Under the one-power family `D(h)=D0+a h^p`, for every assumed `p != 0` the same pair `(B,H)` is fit exactly by

`D0(p) = (2^p H - B)/(2^p-1)`.

Hence `p=4` gives `(16H-B)/15`, while `p=6` gives `(64H-B)/63`; both fit the same two numbers exactly but infer different continua. Their exact difference is

`D0(4)-D0(6)=16(H-B)/315`.

Classification: `REGIME_SPECIFIC_NON_IDENTIFIABILITY__TWO_LEVEL_TRUNCATION_ORDER__NON_PROMOTING`.

Scope is numerical truncation-order identification only. This is not Candidate-Gravity model non-identifiability, not consistency FAIL, not comparator identity, not near-degeneracy, and not novelty certification. It strengthens the guardrail on Iteration 464: the theoretical 16/64 scalings cannot be claimed as empirically selected from BASE/HALF alone. A third independent step level or an external continuum/truncation prior would be required to identify an order. No such new level is authorized because it would change the frozen numerical design.

Retained physics remains `ds=-d_base`; BASE↔HALF threshold remains `<=2e-5`; assembled MP80↔MP120 threshold remains `<=2e-6`. The diagnostic Richardson combinations remain non-promoting.

`MODEL_READINESS: 24%`

Readiness change: 0 percentage points; this closes a numerical interpretation ambiguity but no stable model-readiness component.

Exact next gate: raw-consume canonical run `33962417750` fail-closed at distinct rank 5, `u=-5e-6, v=-5e-6`. No later coordinate is authorized before raw consumption.
