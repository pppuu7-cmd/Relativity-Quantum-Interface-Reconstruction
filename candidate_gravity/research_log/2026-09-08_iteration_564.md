# RQIR Candidate Gravity — Iteration 564

**Date:** 2026-09-08  
**Classification:** `PASS_ITER424_FINAL_PAIR_COMMON_DIFFERENTIAL_MODE_DECOMPOSITION_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`

## Objective
Freeze, before the final QUARTER support values are known, the exact linear-algebra structure of the frozen rank11/rank12 pair in the central4 mixed derivative. This prevents post-hoc selection of a convenient combination after the heavy computations finish.

## Frozen pair
After a raw-valid rank10 PASS the final two required new QUARTER coordinates remain, in prospectively frozen order:
- rank11: `(+2h,-h)`;
- rank12: `(+2h,+h)`.

With central4 first-derivative integer weights `w(-2)=1, w(-1)=-8, w(+1)=+8, w(+2)=-1`, their tensor coefficients are respectively `+8` and `-8`.

## Exact mode decomposition
Let `F11` and `F12` be the values at the two final coordinates. Define orthonormal modes

`q_common=(F11+F12)/sqrt(2)`,

`q_diff=(F11-F12)/sqrt(2)`.

The coordinate-basis coefficient vector `[8,-8]` maps exactly to

`[0, 8*sqrt(2)]`.

Therefore the common mode is an exact null direction of the final-pair mixed-stencil contribution, while the differential mode is the only assembly-relevant direction.

Equivalently, with `c=(F11+F12)/2` and `d=(F11-F12)/2`,

`T_tail=d/(9 h^2)=(F11-F12)/(18 h^2)`.

The orthogonal transform has exact 2-norm condition number 1, so the mode split itself introduces no conditioning penalty.

## Error/covariance consequence
For coordinate error bounds `eps11, eps12`,

`|delta T_tail| <= (eps11+eps12)/(18 h^2)`.

For equal marginal variance `sigma^2` and correlation `rho`, the common/differential covariance becomes exactly diagonal:

`diag(sigma^2(1+rho), sigma^2(1-rho))`.

Thus

`Var(T_tail)=sigma^2(1-rho)/(162 h^4)`,

reducing to `sigma^2/(162 h^4)` for independent coordinate errors.

## Interpretation and guardrails
This is an assembly-geometry result, not evidence that Candidate Gravity is physically degenerate. Iterations 552–555 remain authoritative that `u<->v` is not a support symmetry of the full routed integrand. Hence both rank11 and rank12 remain mandatory heavy support points even though their common mode cancels in the final pair contribution.

No threshold, mass node, precision, rank order, or support rule changes. No `ANSATZ-003`. No Fisher/resources.

## Heavy authority state at freeze
Rank10 run `34165534613`, job `101875710349`, coordinate `(+1.25e-6,+2.5e-6)` remained `in_progress`; no duplicate run was launched.

## Readiness
**MODEL_READINESS remains 24%.** This exact diagnostic closes no additional stable model-level rubric sector.
