# Candidate Gravity Research Log — Iteration 616

Date: 2026-09-08

Iteration 616 continues from canonical Iter615 and narrows the remaining source-to-native binding without inspecting or fitting any projected Candidate value.

## Result

Classification:

`BLOCKED_ITER616_ENDPOINT_AMPUTATION_AND_Q2_BINDING_CLOSED__ONE_COMMON_NATIVE_NORMALIZATION_SCALAR_UNRESOLVED__NON_RESIDUAL`

This is a scientific/convention BLOCKED result, not a model FAIL.

### Closed in this iteration

1. **q2 bucket correspondence is not an open degree of freedom.** Iter588 already froze the same Iter368 mode vectors under `ETA_C=(-+++)` and `ETA_S=-ETA_C`, hence the exact mode-identity map is
   - source `s: +1.0` <-> Iter582 `-1.0`;
   - source `b: +0.34` <-> Iter582 `-0.34`;
   - source `a: +0.14` <-> Iter582 `-0.14`.
   The buckets remain distinct and are never summed.

2. **External scalar endpoint amputation is exact.** For the full Iter594 response

   `d_abc G = -G Kabc G + sum_6(G Ki G Kjk G) - sum_6(G Ki G Kj G Kk G)`,

   define

   `S_amp^(3) = K0_out (d_abc G) K0_in`.

   This gives

   `S_amp^(3) = -Kabc + sum_6(Ki G Kjk) - sum_6(Ki G Kj G Kk)`.

   It removes only the two common external source propagators. Internal scalar propagators, their poles, K3, and all K1^3 chains are preserved. `zero_fill=false`.

3. The two frozen Iter594 probes reproduce the termwise-amputated identity with maximum absolute discrepancy `8.673617379884035e-19`, entirely within stored floating-point rounding and far below the diagnostic `5e-15` bookkeeping envelope.

4. Relative normalization/signs inside the source sector remain fixed by Iter589 same-action K1/K2 normalization plus the complete Iter594 13-family identity. Scalar-pole distribution and native-s Jacobian support remain fixed by Iter608/613/614/615.

### What is still missing

The remaining cross-sector ambiguity has dimension one: one common nonzero factor

`N_native`

that binds the amputated MSSC source-response convention to the Iter582 connection effective-action/retarded native convention, including the common phase/sign normalization. Historical authority does not yet provide one explicit equation that fixes this scalar across the two sectors. Iter218 explicitly stripped the common gravitational coupling from the displayed one-graviton source vertex, whereas Iter582 applies its own frozen `+(i/2)` and `-(i/4)` effective-action weights. This does not authorize fitting `N_native` from the six Iter615 source coefficients.

Forbidden: separate normalization per root, separate normalization per q2 bucket, q2 regrouping, K1^3/K3 deletion, Source/Born subtraction, comparator quotient, ANSATZ-003, Fisher/resources.

## Exact next gate

Prospectively fix `N_native` from an independent lower-order/common-field convention identity that uses the same physical metric perturbation and retarded/effective-action normalization, without consulting any projected Iter615 Candidate value. Only then rerun the full native `Y/T_cut` binding with all six scalar roots and all 13 source families retained.

MODEL_READINESS: 24%

Readiness change from Iter615: `0 pp`. The mapping ambiguity is materially reduced to one common scalar, but no robust comparator-subtracted residual or readiness-rubric sector is closed.
