# RQIR Candidate Gravity — Iteration 490

Date: 2026-09-06  
MODEL_READINESS: 24%

Repository source of truth was re-read before work: `CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_489.md`, research log Iteration 489, recent commits, and the live rank15 Actions state. Canonical rank15 run `34013432799`, job `101433057311`, remained `in_progress`, so no duplicate heavy run was launched.

## Exact BASE↔HALF spectral-transfer monotonicity
Retain Iteration489

`rho(theta)=(4-cos(theta/2))/(cos(theta/2)*(4-cos(theta)))`

for the frozen central4 BASE↔HALF one-direction transfer, away from BASE zeros. Set

`t=cos(theta/2)`, so for `0<theta<pi`, `0<t<1`, and

`rho(t)=(4-t)/(t*(5-2t^2))`.

Exact differentiation gives

`d rho/dt = -4 (t-1)(t^2-5t-5) / [t^2 (2t^2-5)^2]`.

On `0<t<1`, both `(t-1)` and `(t^2-5t-5)` are negative while the denominator is positive, hence `d rho/dt<0`. Since `dt/dtheta=-(1/2)sin(theta/2)<0`, it follows exactly that

`d rho/dtheta > 0` for `0<theta<pi`.

The endpoint limits are

`lim_{theta->0+} rho(theta)=1`,

`lim_{theta->pi-} rho(theta)=+infinity`.

Therefore

`1 < rho(theta) < infinity` for every resolved nonzero one-dimensional mode `0<|theta|<pi`, and for a single mixed Fourier mode with both BASE factors nonzero,

`D_half/D_base = rho(theta_x) rho(theta_y) > 1`.

The divergence near `|theta|=pi` is an estimator effect tied to the BASE Nyquist zero identified in Iteration488; it is not by itself a gravity consistency failure. Conversely, a generic multimode assembled observable can contain cancellations, so this single-mode inequality must not be promoted into a universal sign/magnitude rule for the full nonlinear assembled signal.

Classification: `PASS_BASE_HALF_TRANSFER_STRICT_MONOTONICITY_EXACT__NON_PROMOTING`.

This is estimator/provenance authority only. It is not Candidate-Gravity consistency PASS/FAIL, exact comparator identity, regime-specific non-identifiability, near-degeneracy, or novelty certificate. Frozen `ds=-d_base`, thresholds, dynamics, source order, precision conventions, and all physical gates remain unchanged.

Reproducible code: `candidate_gravity/code/iteration490_base_half_transfer_monotonicity.py`. Machine-readable result: `candidate_gravity/results/iteration490_base_half_transfer_monotonicity.json`.

## Active heavy gate
Rank15 `(u,v)=(+1e-5,+1e-5)`, multiplicity 1, run `34013432799`, job `101433057311`, remains the sole authorized heavy support gate and is still `in_progress`. Certified occurrence-weighted coverage remains `19/32 = 59.375% = 1520/2560` until raw scientific consumption succeeds.

Physical/operator authority remains Iteration 411; blocker authority remains Iteration 421 with unresolved set `[2]`. Robust comparator-subtracted residual remains absent; `ANSATZ-003` and Fisher/resources remain BLOCKED.

Readiness change: 0 percentage points. The new exact estimator/provenance subgate does not close an additional component of the stable model-readiness rubric.

MODEL_READINESS: 24%
