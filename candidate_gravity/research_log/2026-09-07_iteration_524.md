# Candidate Gravity Research Log — Iteration 524

**Date:** 2026-09-07  
**MODEL_READINESS: 24%**

## Start-state / source-of-truth check
Repository authority was reread from `CURRENT_QG_FRONT.md`, recovery delta 523, research log 523, recent commits and live Actions. Iteration 523 remains the latest scientific/numerical support authority: all 28 distinct frozen coordinates / 32 source occurrences are locally certified. The next permitted scientific gate remains independent frozen BASE/HALF MP80/MP120 central4 assembly.

## Operational finding
The post-support assembly workflow failed before numerical assembly. Run `34082758627`, job `101621055550`, resolved only `14/28` authority coordinates. This is operational BLOCKED, not scientific FAIL: no BASE/HALF values were produced.

Root cause was partly identified as historical provenance-schema drift. `rqir_raw_consumption_v1` records such as rank13 store validated `artifact_id` and `scientific_json_sha256` under `canonical_provenance`, whereas the first runtime normalizer only handled `provenance`.

A fail-closed repair added `canonical_provenance` fallback without changing dynamics, parameter conventions, mass support, central4 weights, thresholds, `ds=-d_base`, precision, or Richardson policy. Replacement run `34086331638`, job `101630974650`, increased exact authority resolution from `14/28` to `19/28` but still stopped before assembly.

Nine coordinates remain unresolved by the current schema resolver:
`(-1e-5,-5e-6)`, `(-1e-5,+5e-6)`, `(-2.5e-6,+2.5e-6)`, `(-2.5e-6,+5e-6)`, `(+2.5e-6,-5e-6)`, `(+5e-6,-1e-5)`, `(+5e-6,-5e-6)`, `(+5e-6,+5e-6)`, `(+5e-6,+1e-5)`.

Attempt-3 raw artifact ID is `10005319300`. Because manifest resolution failed, download/assembly/audit steps did not execute.

Classification:
`BLOCKED_ASSEMBLY_LEGACY_PROVENANCE_RESOLUTION_19_OF_28__NON_PROMOTING`.

This is an operational provenance blocker only. It is not Candidate-Gravity consistency FAIL, exact comparator identity, regime-specific non-identifiability, near-degeneracy, or absence/presence of a novelty certificate. No article-level physical negative claim follows from it.

## Guardrails
No fourth blind retry is authorized. The nine coordinates must be recovered from committed historical raw-valid authority and validated Actions provenance, with exact artifact IDs and scientific result SHA-256 values. No symmetry substitution, later-coverage inference, zero fill, threshold weakening, or dynamics change is allowed.

Only a fail-closed `28/28` provenance preflight permits one new assembly run. The frozen scientific thresholds remain BASE MP `<=2e-6`, HALF MP `<=2e-6`, BASE↔HALF `<=2e-5`, all finite, same 80 common samples, `ds=-d_base`, no Richardson promotion.

Comparator quotient remains operational BLOCKED pending the concrete `Source/Ward/contact+K2` target. `ANSATZ-003` remains uncreated. Fisher/resources remain forbidden.

**MODEL_READINESS: 24%**

Readiness change: **0 percentage points**. This iteration improved blocker localization and provenance coverage but closed no additional sector of the stable model-readiness rubric.

## Exact next gate
Recover exact historical authority/provenance for the nine listed coordinates and produce a machine-readable `28/28` preflight manifest. Only then rerun independent BASE/HALF assembly.
