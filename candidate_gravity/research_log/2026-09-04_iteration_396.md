# Candidate Gravity Research Log — Iteration 396

Date: 2026-09-04

Iteration 395 raw Actions output was downloaded and independently checked rather than inferred from green workflow status. Its execution is valid, but the sole unresolved physical `Tr U1^2` double-double channel remains `BLOCKED_CONVERGENCE`.

The decisive scaled discrepancy is

`|10? no|`: preserved Iteration-395 8x16 base vs old 6x12 high = `2.792425135668121e-5`, above the unchanged `2e-5` threshold.

Internal 8x16 checks are already below threshold: base vs phi-shift `8.612371283035585e-6`; base vs h/2 `6.807110085796765e-7`. Shell, radial Richardson, uncut-denominator and serial/parallel oracle checks pass strongly. Therefore this is a scoped angular-convergence blocker, not a consistency FAIL, not a zero, not near-degeneracy and not a Candidate residual.

Iteration 396 materializes the exact raw JSON in the repository without recomputation and records its source run/artifact/digest/SHA provenance.

A new prospectively frozen Iteration 397 has been added and launched. It keeps the physical arithmetic and all thresholds unchanged, increases only angular quadrature to 10x20, and tests 10x20 against the preserved 8x16 base plus same-grid phi-shift and h/2 checks. If it still blocks, the authorized next method is analytic/spectral angular reduction rather than another blind grid escalation.

MODEL_READINESS: 24%

Change: 0 pp. The double-double blocker is now precisely localized and reproducible, but no full new readiness bucket closes.
