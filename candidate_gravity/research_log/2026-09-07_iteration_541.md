# RQIR Candidate Gravity — Iteration 541

## Scope
Repository authority was re-read before work: `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, the latest recovery delta and research log (Iteration 540), recent commits, and live Action run `34115105768`. Rank3 remained `in_progress`, so no duplicate heavy computation was launched.

This iteration closes an independent exact numerical/truncation diagnostic for the already-frozen BASE/HALF/QUARTER hierarchy. No comparator, dynamics, support, threshold, precision, parameter convention, or acceptance clause is changed.

## Frozen three-level extrapolator
Retain the Iteration-536 diagnostic estimator

`R3 = (BASE - 80 HALF + 1024 QUARTER)/945`.

Its exact weights are

`w = (1/945, -16/189, 1024/945)`.

For input perturbations `(e_B,e_H,e_Q)`, the extrapolator perturbation is

`e_R3 = (e_B - 80 e_H + 1024 e_Q)/945`.

Therefore the exact operator norms are

- `||w||_1 = 221/189 ~= 1.1693121693121693`,
- `||w||_2^2 = 50237/42525`, hence `||w||_2 ~= 1.0869002464792206`,
- `||w||_inf = 1024/945`.

Thus, if all three assembled levels obey the same absolute error envelope `|e_i|<=eps`, then

`|e_R3| <= (221/189) eps`.

The bound is sharp because the three input errors can align with the signs of the weights. Under independent equal-variance zero-mean noise with standard deviation `sigma`, the output standard deviation is exactly

`sigma_R3 = sqrt(50237/42525) sigma ~= 1.0869002464792206 sigma`.

Hence the frozen extrapolator cancels the leading truncation orders without large numerical-noise amplification: the worst-case uniform absolute-error amplification is about 1.1693 and the equal-variance RMS amplification is about 1.0869. This is materially milder than the separate h4/h6 component inversion conditioning (`kappa_2 ~= 42.78`) from Iteration 537; the two diagnostics must not be conflated.

## Exact truncation leakage
For a common smooth expansion

`X_h = D + a h^4 + b h^6 + c h^8 + e h^10 + ...`,

with HALF at `h/2` and QUARTER at `h/4`, the same frozen weights give exact coefficients

- h4 leakage: `0`,
- h6 leakage: `0`,
- h8 leakage: `1/1344`,
- h10 leakage: `1/1024`.

So

`R3 = D + (c h^8)/1344 + (e h^10)/1024 + ...`.

This extends the Iteration-536 h8 statement by quantifying both numerical-noise amplification and the next h10 leakage term.

## Classification
`PASS_ITER424_THREE_LEVEL_EXTRAPOLATOR_NOISE_CONTRACT_EXACT__DIAGNOSTIC_ONLY_NON_PROMOTING`

This is a numerical/truncation diagnostic PASS only. It is not Candidate-Gravity consistency PASS/FAIL, exact comparator identity, regime-specific non-identifiability, model-level near-degeneracy, or novelty certification. It cannot replace any of the five frozen Iteration-424 physical acceptance clauses and cannot promote physical index 2.

Reproducible authority:
- `candidate_gravity/code/iteration541_iter424_three_level_extrapolator_noise_contract.py`
- `candidate_gravity/results/iteration541_iter424_three_level_extrapolator_noise_contract.json`

## Guardrails
Physical unresolved set remains `[2]`. The concrete `Source/Ward/contact+K2` target and robust comparator-subtracted residual remain absent; fixed C3/C4/C5/nonlocal/asymptotic-safety quotient remains operationally BLOCKED. `ANSATZ-003` remains uncreated. Fisher/resources remain forbidden.

## Readiness
`MODEL_READINESS: 24%`

Readiness change: **0 percentage points**. A genuine diagnostic subgate closed, but no additional stable readiness-rubric sector closed.

## Exact next gate
Canonical QUARTER rank3 run `34115105768` remains the sole heavy successor. After terminal completion, fail-closed raw-consume its artifact. Only raw-valid PASS authorizes frozen rank4 `(-1.25e-6,-1.25e-6)`.
