# Candidate Gravity Research Log — Iteration 648

Date: 2026-09-09

Authoritative predecessor: Iter647 permanent pre-Iter645 absolute-normalization provenance BLOCKED.

## Result

A prospective projective observable was defined before any Iter645 reduced values were used to choose its reference structure. The pre-registered above-threshold grid is `s=(2.0,2.25,2.5,3.0,4.0)` and the closed cyclic ordinary-cut assembly is

`a(s) = -B_s(s) + T_sab(s) + T_sba(s)`,

with relative coefficients inherited from the frozen closed Tr-log third-derivative structure. K3 remains analytic/contact and contributes no ordinary finite s-cut.

Instead of choosing one observed component as a denominator, the gate defines the rank-one projective matrix

`P_ij = a_i conj(a_j) / sum_k |a_k|^2`,

defined iff the assembled vector is nonzero. Under one common nonzero complex normalization `a -> lambda a`, both numerator and denominator acquire `|lambda|^2`, so `P` is exactly invariant. This removes the one-global-factor ambiguity of Iter647 without assigning a post-result absolute normalization.

Canonical Action `34313481973` completed `success`; job `102344758270`; artifact `10089211623`; digest `sha256:7ec917e87557380e41db10f4e465a75e87468e6d1ed937ce317dc15aeb8fbdd0`. The fail-closed workflow certifies a nonzero projective direction (`norm2>1e-30`), empty failures, and trace, Hermiticity, idempotence, and tested common-complex-scaling errors all `<=2e-12`. The connector exposed this validator provenance and artifact metadata but not the ZIP payload; therefore exact projector entries are not promoted in this log.

## Scientific classification

`PASS_ITER648_PROJECTIVE_CLOSED_GAMMA3_GLOBAL_FACTOR_CANCELLATION__NON_RESIDUAL`

This is a projective-observable/protocol PASS. It is not Candidate-Gravity consistency PASS/FAIL, exact comparator identity, regime-specific non-identifiability, near-degeneracy, novelty certificate, or Candidate residual.

The absolute Iter645 observable remains permanently normalization-BLOCKED. Iter648 does not repair or overwrite it; it defines a separate normalization-free projective observable prospectively.

Guardrails unchanged: `zero_fill=false`; Source/Born subtraction `NOT_PERFORMED`; native Y/T_cut projection `NOT_PERFORMED`; fixed C3/C4/C5/nonlocal/asymptotic-safety comparator quotient `NOT_PERFORMED`; no ANSATZ-003; no Fisher/resources. Iter645 reduced values are permitted only after the projective definition is frozen and are not used to select a reference component.

MODEL_READINESS: 24%
Readiness change: 0 percentage points. The global-normalization obstacle is bypassed for this projective branch, but no comparator-subtracted unique residual has yet been established, so robust unique residual remains `0/20` and no full readiness-rubric sector closes.

Exact next gate — Iteration649: before performing Source/Born subtraction, determine whether the matched source/Born contribution and the closed-Gamma3 cut vector live in the same one-common-factor homogeneous normalization class. Prove or disprove that projectivization commutes with the matched subtraction. If the subtraction contains an independently normalized additive term, preserve BLOCKED rather than cancel or fit it.