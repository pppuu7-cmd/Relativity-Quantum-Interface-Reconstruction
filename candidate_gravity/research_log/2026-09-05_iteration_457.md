# 2026-09-05 — Iteration 457

## Objective
While the sole authorized next-mass-node MP gate remains active, close a non-duplicative algebraic/provenance ambiguity in the four exact BASE/HALF mass-coordinate overlaps of the frozen Iteration-407 mixed auxiliary-mass derivative.

## Source-of-truth audit
Read `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, Iteration-456 recovery delta and research log, recent commits, Iteration-455 manifest, Iteration-379 central4 mixed-derivative implementation, and the active run metadata. Run `33946347229` remained in progress and was not duplicated.

## Exact result
The frozen first-derivative central4 coefficients are

`[1/12, -2/3, +2/3, -1/12] / h`.

At the four coordinates shared exactly by the BASE grid (`h=5e-6`, interior nodes `±h`) and HALF grid (`h=2.5e-6`, endpoint nodes `±2h`), the tensor-product mixed-derivative coefficients have the same sign but differ in magnitude by an exact factor

`w_BASE / w_HALF = 16`.

For equal-sign overlaps the normalized coefficients are `+4/9 / h_BASE^2` versus `+1/36 / h_BASE^2`; for opposite-sign overlaps they are `-4/9 / h_BASE^2` versus `-1/36 / h_BASE^2`.

Therefore a single direct MP precision certificate for an identical `F(u,v)` coordinate may validly serve both source occurrences, but the BASE and HALF derivative occurrences themselves must remain distinct in downstream derivative assembly. Collapsing the 32-occurrence derivative bookkeeping to 28 weighted occurrences would be algebraically wrong even though only 28 distinct `F(u,v)` evaluations are needed for precision certification.

Classification: `PASS_OVERLAP_PRECISION_SHARE_BUT_STENCIL_WEIGHT_COLLAPSE_FORBIDDEN__NON_PROMOTING`.

This is a provenance/algebra closure, not a Candidate-Gravity consistency FAIL, comparator identity, non-identifiability result, near-degeneracy, or novelty certificate.

MODEL_READINESS: 24%

Readiness change: **0 percentage points**. A genuine bookkeeping/algebra ambiguity is closed, but no stable model-readiness rubric component is completed.

## Exact next gate
Raw-consume run `33946347229` fail-closed at `u=-1e-5, v=+5e-6`. PASS permits only the next Iteration-455 distinct coordinate with unchanged five-z/NPHI16/radial/direct-MP80/120 conventions. BLOCKED requires localization at the active coordinate. The overlap result of this iteration does not authorize any additional physical or support shortcut.
