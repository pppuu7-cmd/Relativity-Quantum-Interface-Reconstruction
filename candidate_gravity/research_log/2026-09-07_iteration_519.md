# Candidate Gravity Research Log — Iteration 519

**Date:** 2026-09-07  
**MODEL_READINESS: 24%**

## Start-state checks
Read repository-authoritative `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_518.md`, research log Iteration 518, recent commits, and live GitHub Actions. The repository remained at authoritative Iteration 518. Canonical rank26 `(u,v)=(+5e-6,-2.5e-6)` run `34071968236`, job `101590847259`, remained `in_progress`; no duplicate heavy run was launched.

## Independent exact gate — local scaled precision versus assembled precision
The frozen local MP gate uses

`scaled(x80,x120)=|x80-x120|/max(1,|x80|,|x120|) <= eps`, with `eps=1e-30`.

For each coordinate define `S_i=max(1,|x80_i|,|x120_i|)`. Then local PASS implies exactly

`|x80_i-x120_i| <= eps S_i`.

For any frozen central4 assembly row with dimensionless weights `w_i` in units `1/h^2`, triangle inequality gives

`|A80-A120| <= eps/h^2 * sum_i |w_i| S_i`.

Since the assembled scaled denominator `max(1,|A80|,|A120|)` is at least one, the same RHS is an upper bound on the assembled scaled MP80↔MP120 discrepancy. Thus a sufficient condition for the frozen assembled tolerance `tau=2e-6` is

`sum_i |w_i| S_i <= tau h^2 / eps`.

With frozen `h=5e-6`, this exact weighted-envelope budget is

`tau h^2/eps = 5e13`.

Using exact Iteration-508/514 row L1 norms, a still stronger uniform envelope sufficient condition `S_i<=Smax` is:

- BASE: `Smax <= 200000000000000/9 ≈ 2.22222222222222e13`;
- HALF: `Smax <= 50000000000000/9 ≈ 5.55555555555556e12`;
- for a cross-precision BASE−HALF diagnostic, if evaluated: `Smax <= 1800000000000000/397 ≈ 4.53400503778338e12`.

## Scientific interpretation
Classification: `PASS_LOCAL_SCALED_TO_ASSEMBLED_MP_SUFFICIENT_ENVELOPE_CONTRACT_EXACT__NON_PROMOTING`.

The scientifically important result is a guardrail: **local scaled MP PASS alone does not logically imply assembled scaled MP PASS**. The local normalization carries amplitude information through `S_i`, while cancellations in the assembly can leave the assembled normalization denominator at its floor value one. Therefore the already-frozen independent BASE/HALF assembled MP gate remains mandatory even after all 28 coordinates obtain local precision certificates.

This result adds no threshold, does not weaken any gate, and is not Candidate-Gravity consistency PASS/FAIL, comparator identity, regime-specific non-identifiability, near-degeneracy, novelty certificate, or Fisher authorization.

Reproducible audit: `candidate_gravity/code/iteration519_local_scaled_to_assembled_mp_sufficient_contract.py`. Machine-readable result: `candidate_gravity/results/iteration519_local_scaled_to_assembled_mp_sufficient_contract.json`.

## Retained blockers/nonclaims
Physical/operator authority remains Iteration 411. Physical blocker remains Iteration 421 `BLOCKED_CONVERGENCE`, unresolved set `[2]`. Certified local precision coverage remains `30/32 = 93.750%` until rank26 raw consumption. Fixed comparator quotient remains operationally BLOCKED because the concrete `Source/Ward/contact+K2` target is not assembled. Robust comparator-subtracted residual is absent; `ANSATZ-003` remains uncreated; Fisher/resources remain forbidden.

**MODEL_READINESS: 24%**

Readiness change: **0 percentage points**. The local→assembled logical contract is now exact, but no additional stable readiness-rubric sector closed.

## Exact next gate
Fail-closed raw-consume canonical rank26 run `34071968236` after completion. Only raw-valid PASS permits frozen rank27 `(+5e-6,+2.5e-6)`, HALF local index 14, multiplicity 1. If rank26 is BLOCKED, freeze suffix progression and localize the first failing `z/phi/radial` sample without changing frozen dynamics, support order, thresholds, MP levels, radial hs, or angular grid.
