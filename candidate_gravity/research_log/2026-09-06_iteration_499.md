# RQIR Candidate Gravity — Iteration 499

Date: 2026-09-06
MODEL_READINESS: 24%

## Scope
Repository source of truth was re-read before work: `CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_498.md`, Iteration-498 research log, recent commits, and live rank19 Actions state. Canonical rank19 run `34035315540` remained `in_progress`, so no duplicate heavy run was launched.

## Exact frozen-central4 truncation audit
For nodes `[-2,-1,+1,+2]` and coefficients `c=(1/12,-2/3,2/3,-1/12)`, the exact one-dimensional moments are

`M0=0, M1=1, M2=M3=M4=0, M5=-4, M7=-20, M9=-84`, with even moments through 9 vanishing.

Therefore the frozen first-derivative stencil has the smooth-field asymptotic expansion

`L_h = d - h^4/30 d^5 - h^6/252 d^7 - h^8/4320 d^9 + O(h^10)`.

For the tensor-product mixed estimator `D_h=L_h^x L_h^y`, through order `h^8`,

`D_h = d_x d_y - h^4/30 (d_x^5 d_y + d_x d_y^5) - h^6/252 (d_x^7 d_y + d_x d_y^7) + h^8[ -(d_x^9 d_y + d_x d_y^9)/4320 + d_x^5 d_y^5/900 ] + O(h^10)`.

At HALF step `h/2`, order-`h^4`, `h^6`, and `h^8` truncation sectors scale by exactly `1/16`, `1/64`, and `1/256`, respectively. Hence the corresponding BASE-minus-HALF factors are `15/16`, `63/64`, and `255/256`.

Classification: `PASS_CENTRAL4_MIXED_TRUNCATION_SERIES_EXACT__NON_PROMOTING`.

Scientific interpretation is deliberately scoped: the familiar 16x leading-error reduction is an asymptotic smooth/low-frequency statement only. It is not an exact mode-independent Richardson law and does not supersede the exact spectral-transfer authority of Iterations 489-490. No Richardson promotion is licensed; retained `ds=-d_base` and all frozen thresholds/conventions remain unchanged.

This is estimator/provenance authority only. A mismatch of future assembled BASE/HALF data with these low-frequency asymptotics can motivate conditioning/provenance diagnosis, but is not by itself a Candidate-Gravity consistency FAIL.

## Active heavy gate
Frozen rank19 `(u,v)=(-2.5e-6,-2.5e-6)`, multiplicity 1, canonical run `34035315540`, remains the sole authorized heavy support gate while in progress. Certified support remains `23/32 = 71.875%` until raw scientific consumption.

## Readiness
MODEL_READINESS: 24%
Readiness change: 0 percentage points. This iteration closes an exact estimator/provenance subgate but no additional stable-rubric model component.

## Exact next gate
Fail-closed raw-consume rank19 run `34035315540` after completion. Only raw scientific PASS authorizes the next explicit `UNTESTED` coordinate in the frozen Iteration-455 manifest. `ANSATZ-003` remains uncreated; Fisher/resources remain forbidden.
