# Candidate Gravity Research Log — Iteration 626

Date: 2026-09-09

Iteration 626 performs the historical authority audit required by Iter625 before any new CTP branch assignment is introduced.

Classification:

`PASS_ITER626_HISTORICAL_CTP_MEASUREMENT_AUTHORITY_AUDIT__GRAVITY_RETARDED_AND_LINKED_CTP_PROTOCOL_EXIST__MSSC_INTERNAL_SCALAR_COMPONENT_ASSIGNMENT_ABSENT__OPERATIONAL_BLOCKED_NON_RESIDUAL`.

The audit distinguishes three layers that had previously been easy to conflate:

1. Iter149 freezes a source-completed gravitational probe convention in the Minkowski interacting vacuum and explicitly inherits the retarded/in-in tree sector from Iter147.
2. Iter171 freezes the linked/amputated CTP cubic protocol and the r/a normalization `h_+=r+a/2`, `h_-=r-a/2`; Iter172 then uses the resulting gravitational vertex coordinates `Gamma_arr`, `Gamma_aar`, `Gamma_aaa`, `WardLock`.
3. None of these authorities specifies the `+/-` contour assignment, r/a component, or doubled source functional for the internal matter-scalar propagators that appear in the MSSC Iter594/615 source response.

Therefore the fact that the native gravitational observable is retarded does **not** determine that every retained MSSC internal scalar propagator should be replaced by a scalar retarded propagator. Iter625 matters precisely because the six supported roots contain five positive-energy and one negative-energy internal scalar root. A naive all-retarded replacement would introduce a root-dependent prescription sign, while the historical source calculation used one common algebraic/Feynman-like `K+i0` rule.

The correct classification is operational `BLOCKED`, not consistency FAIL and not zero residual. The negative-energy `D_s^-(s=0.09)` root may not be flipped, removed, or reinterpreted by hand. Iter608/615 remain authoritative in their declared pole convention.

A genuinely new prospective contract is now required: a combined matter+gravity Schwinger-Keldysh generating functional with doubled scalar-probe and metric sources. Its source insertion branches, r/a rotation, endpoint amputation and internal scalar component algebra must be frozen before any projected Candidate/native values are inspected, and it must reduce to the existing Iter149/171 gravitational CTP conventions in their overlap.

Source/Born subtraction remains `NOT_PERFORMED`; comparator quotient, `ANSATZ-003`, Fisher and resources remain forbidden.

MODEL_READINESS: 24%.

Readiness change: 0 percentage points. The CTP blocker is now authority-certified, but robust unique residual remains `0/20` and no additional readiness-rubric sector is closed.
