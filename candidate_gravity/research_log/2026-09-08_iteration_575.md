# RQIR Candidate Gravity — Research Log Iteration 575

Date: 2026-09-08  
MODEL_READINESS: 24%  
Readiness change: 0 pp

## Source-of-truth start
Iteration574 independently raw-consumed the full Iter407 spectrum reconstruction for unresolved index2/class3/q^2=-1. Iter424 now has four frozen clauses PASS and only the original Iter421 tensor degree-(1,1) residual unresolved. The independent post574 binding decision confirmed that the exact pre-result mapping exists but its legacy evaluator is NumPy float/complex, while Iter424 requires MP80/MP120.

## Exact blocker localization
The original Iter421 tensor11 design is retained exactly:

`C(r,s)=[F(r,s)-F(r,-s)-F(-r,s)+F(-r,-s)]/(4rs)`

with `R=1e-5`, multipliers `[1,0.75,0.5,0.25]`, fit coordinates `x=(r/R)^2`, `y=(s/R)^2`, degree-(1,1) basis `[1,x,y,xy]`, and residual threshold `2e-5`.

This requires 64 signed F(u,v) nodes over magnitudes `{1e-5,7.5e-6,5e-6,2.5e-6}`. Existing validated MP80/MP120 BASE/HALF support covers 28/64 = 43.75%. The exact deficit is 36/64 = 56.25%: 28 nodes involving `7.5e-6` and 8 cross-scale `1e-5 <-> 2.5e-6` nodes not present in either central4 grid.

The complete 36-node deficit was frozen before any new numerical result in `candidate_gravity/contracts/iteration575_iter421_tensor11_exact_mp_support_manifest.json`. No raw-grid bilinear diagnostic, central4 polynomial identity, u-v exchange assumption or altered node set is allowed to substitute for the original statistic.

## Prospective execution
Because every missing node is mandatory and the complete set/order is determined from the original Iter421 F-cache first-occurrence order before results, all 36 can be executed in parallel without result-dependent selection bias.

Reproducible stage:
`candidate_gravity/code/iteration575_iter421_tensor11_missing_support_full_z_mp_stage.py`

Workflow:
`.github/workflows/rqir-iteration575-tensor11-missing-support-matrix.yml`

Canonical matrix run:
- run `34180521559`
- head `519ceb6dc8efc3e5a9aebe86f5e6523152a4b284`
- 36 matrix jobs
- `max-parallel: 18`
- each rank evaluates five frozen z nodes x 16 phi nodes at MP80 and MP120 with inherited radial Richardson support
- per-rank numerical limits: MP80-vs-MP120 `<=1e-30`, radial Richardson `<=5e-4`, 80 rows, all finite.

A per-rank PASS is support-only and cannot promote physical index2. A valid BLOCKED result is a scientific/representation blocker for the exact original tensor11 support, not an infrastructure failure. Malformed provenance/audit is infrastructure failure and cannot be converted to scientific PASS/FAIL.

## Current scientific state
- Iter424 mass-step: PASS.
- Iter424 direct-original: PASS via Iter573 mapping to raw-valid Iter421.
- Iter424 tensor11: BLOCKED pending exact MP support and unchanged original fit.
- Iter424 Ds MP80/MP120: PASS.
- Iter424 finiteness: PASS.
- Five-clause state: 4 PASS + 1 BLOCKED.
- physical index2: not promoted.
- exact15: not authorized.
- `ANSATZ-003`: not created.
- Fisher/resources: forbidden.

## Exact next gate
Fail-closed consume all 36 Iter575 matrix artifacts. Only if all 36 support nodes are raw-valid may they be combined with the already validated 28 existing MP nodes and the unchanged original Iter421 degree-(1,1) residual be evaluated independently at MP80/MP120. Only residual `<=2e-5` together with the already closed four clauses can authorize physical index2 promotion and exact15.

MODEL_READINESS: 24%
