# RQIR Candidate Gravity — Iteration 657

Date: 2026-09-09

## Scope
Exact, fail-closed linear-algebra test of whether the Iter653 action-level Ward identity supplies a canonical null-compatible longitudinal/transverse completion on the prospective Iter655 soft trajectory. No Candidate cut values, subtraction, comparator quotient, profiling, Fisher or resources were used.

## Authority
Iter653 same-parent closed-SK Gamma2/Gamma3 Ward identity; Iter655 `q3(eps)=-eps*(1,0,0,1)` plus-TT prospective soft contract; Iter656 prohibition on applying the non-null `q^-2/q^-4` projector to the exact null leg.

## Exact algebra
Let `L_q(xi)_{mu nu}=q_mu xi_nu+q_nu xi_mu` and `D_q h=q^mu h_{mu nu}`. For null `q`,

`D_q L_q(xi) = q_lower (q^upper xi)`.

For nonzero null q the exact ranks are `rank(L_q)=4`, `rank(D_q)=4`, `rank(D_q L_q)=1`. Hence

`dim(Im L_q ∩ Ker D_q)=4-1=3`.

Thus a three-dimensional family of nonzero pure-gauge tensors is simultaneously transverse. The decomposition into a Ward/pure-gauge part plus a transverse remainder is not canonical from q and the metric alone.

The frozen plus-TT tensor is transverse and is not itself in `Im L_q`. Nevertheless its decomposition is nonunique: with `eta_x=(0,1,0,0)`, both

`h_plus=L_q(0)+h_plus`

and

`h_plus=L_q(eta_x)+(h_plus-L_q(eta_x))`

have transverse remainders because `q.eta_x=0`.

This is an exact rank statement, not a numerical near-degeneracy. No auxiliary null vector, off-null regulator or complement was selected after seeing cut values.

## Classification
`BLOCKED_ITER657_NULL_WARD_COMPLETION_NONUNIQUE__IMAGE_LQ_INTERSECTS_TRANSVERSE_SUBSPACE_DIM3__AUXILIARY_COMPLEMENT_REQUIRED__NON_RESIDUAL`

Operational/mathematical BLOCKED only. Not Candidate consistency FAIL/PASS, not exact comparator identity, not regime-specific non-identifiability, not near-degeneracy, and not a novelty certificate.

## Guardrails
`zero_fill=false`. Do not infer `W[D_s K2]=0` from TT-ness. Do not choose an auxiliary complement/regulator post hoc. Source/Born subtraction, native soft `T_cut`, and fixed C3/C4/C5/nonlocal/asymptotic-safety quotient remain `NOT_PERFORMED`. No ANSATZ-003; no Fisher/resources.

## Readiness
Comparator foundation 24/25; robust unique residual 0/20; frozen parent dynamics/ANSATZ 0/20; consistency/positivity/Ward/causality 0/15; identifiability/Fisher 0/10; resource/experiment closure 0/10.

MODEL_READINESS: 24%

Delta: 0 percentage points. The null Ward obstruction is now an exact three-dimensional gauge-overlap theorem, but no robust residual or complete rubric sector closes.

## Exact next gate
Iteration658: test whether the already frozen plus-TT soft `T_cut` contraction descends directly to the quotient `Ker(D_q)/(Im(L_q)∩Ker(D_q))`, i.e. is invariant under all three null-gauge-overlap directions. If yes, derive the quotient observable without choosing a complement. If no, preserve the soft Ward completion as BLOCKED or prospectively version a complement before any new cut inspection.
