# Candidate Gravity Research Log — Iteration 510

**Date:** 2026-09-07  
**Scope:** exact frozen BASE/HALF assembly linear-independence audit while canonical rank23 heavy support run remains in progress; no dynamics, thresholds, support order, or precision conventions changed.

## Start state
Repository source of truth was reread before work: `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, `RECOVERY_DELTA_ITERATION_509.md`, research log Iteration 509, recent commits, and live Actions. Iteration 509 is the latest completed numerical authority. Rank22 is raw-consumed PASS and certified occurrence-weighted support is `27/32 = 84.375% = 2160/2560`. Canonical rank23 `(u,v)=(+2.5e-6,-2.5e-6)`, HALF local index 9, run `34058694183`, job `101555199703`, remained `in_progress`; no duplicate heavy run was launched.

## Exact assembly linear-algebra result
Frozen central4 BASE contributes 16 source occurrences on offsets `{-2,-1,+1,+2}^2 h`; HALF contributes 16 occurrences on `{-1,-1/2,+1/2,+1}^2 h`. Their union is the already-authoritative 28 distinct coordinates with four exact overlaps.

Using exact rational weights in units of `1/h^2`, form two 28-component rows `W_BASE` and `W_HALF`. Exact rational row reduction gives

- `rank([W_BASE,W_HALF]) = 2`;
- `rank([W_BASE,W_HALF,W_DELTA]) = 2` for `W_DELTA = W_BASE-W_HALF`;
- therefore the nullity of the two-row assembly map on the 28-coordinate sample space is `28-2 = 26`.

An exact independence witness exists because there are 12 BASE-only nonzero coordinates and 12 HALF-only nonzero coordinates. The exact two-row Gram matrix is

`[[4225/5184, 4/81], [4/81, 4225/324]]`

with positive determinant `5948843/559872`.

Classification: `PASS_BASE_HALF_ASSEMBLY_LINEAR_INDEPENDENCE_EXACT__NON_PROMOTING`.

## Scientific/operational implication
BASE and HALF are genuinely two independent assembled linear functionals, so both must be evaluated independently after support closure. However, the retained BASE-minus-HALF discrepancy is exactly a derived linear combination of those same two rows. It must not later be counted as a third statistically independent observable/constraint, nor may a future Fisher/resource stage infer extra rank merely by appending the discrepancy row.

This does not weaken the frozen BASE↔HALF discrepancy threshold; the discrepancy remains a required consistency diagnostic. It only fixes its linear-algebra status.

## Nonclaims
This is assembly/provenance authority only: not Candidate-Gravity consistency PASS or FAIL; not physical promotion of unresolved index 2; not exact comparator identity; not regime-specific non-identifiability; not near-degeneracy; not novelty certificate; not Fisher/identifiability authority. `ANSATZ-003` remains uncreated and Fisher/resources remain forbidden until a nonzero algebraic residual exists.

Reproducible audit: `candidate_gravity/code/iteration510_base_half_assembly_linear_independence_exact_audit.py`. Machine-readable result: `candidate_gravity/results/iteration510_base_half_assembly_linear_independence_exact_audit.json`.

## Readiness
Stable rubric remains: comparator foundation `24/25`, unique residual `0/20`, frozen parent dynamics/ANSATZ `0/20`, consistency/positivity/Ward/causality `0/15`, identifiability/Fisher `0/10`, resource/experiment closure `0/10`.

MODEL_READINESS: 24%

Readiness change: **0 percentage points**. A real exact assembly guardrail was closed, but no additional model-readiness rubric sector is complete.

## Exact next gate
Do not duplicate canonical rank23 run `34058694183`. After completion, raw-consume its artifact fail-closed. Only raw-valid PASS permits rank24 `(+2.5e-6,+2.5e-6)`, HALF local index 10. If rank23 is BLOCKED, freeze suffix progression and localize the first failing `z/phi/radial` sample with all frozen conventions unchanged.
