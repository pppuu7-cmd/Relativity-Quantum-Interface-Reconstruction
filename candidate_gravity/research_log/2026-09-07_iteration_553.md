# Candidate Gravity Research Log — Iteration 553

Date: 2026-09-07

## Independent replication of u↔v non-equivalence
Iteration 552 rejected universal u↔v support equivalence using the raw-closed rank1/rank3 swapped pair and the exact parent kinematics. Iteration 553 independently repeats the raw-data test on the second already closed swapped pair: rank2 `(-2.5e-6,+1.25e-6)` versus rank7 `(+1.25e-6,-2.5e-6)`.

Both raw artifacts and their authority-audit/result hashes were independently reverified before comparison. All 80 rows align in z/phi.

Observed:
- MP120 imaginary parts exactly equal in 80/80 rows;
- MP120 real parts exactly equal in 0/80 rows;
- absolute real differences range from approximately `2.42140e-12` to `8.01905e-10`;
- maximum local relative difference approximately `3.36547e-4`;
- every discrepancy exceeds the corresponding larger radial-error estimate by at least approximately `1.388e4`; maximum separation approximately `9.437e6`.

## Cross-pair mechanism check
From the exact parent kinematics, the u↔v change in `alpha` is `(v-u)/s`. The rank2/rank7 pair therefore has exactly three times the alpha-swap gap of the rank1/rank3 pair used in Iteration 552.

The raw output differences show the same local scaling: for corresponding z/phi rows, the signed rank2-rank7 swap difference divided by the rank1-rank3 swap difference has the same sign in 80/80 rows and ranges from `2.999763206...` to `3.000157660...`, with mean `2.999976034...`. This supports the alpha-shift explanation over these small offsets, but is recorded only as a local implementation-response diagnostic, not a general linearity theorem.

Classification: `PASS_ITER424_UV_SWAP_NON_EQUIVALENCE_REPLICATION_RANK2_RANK7__DIAGNOSTIC_ONLY_NON_PROMOTING`.

Machine-readable authority: `candidate_gravity/results/iteration553_iter424_uv_swap_replication_rank2_rank7.json`.

## Consequence
The Iteration-552 result is independently replicated. No remaining QUARTER node may be filled from its swapped partner; the frozen 12-rank route remains necessary.

MODEL_READINESS: 24%
Readiness change: 0 percentage points.
ITERATION_TASK_COMPLETION: 100%

## Exact next gate
Do not modify the active heavy route. Raw-consume canonical rank8 run `34153849866` only after terminal completion; only raw-valid PASS permits rank9.
