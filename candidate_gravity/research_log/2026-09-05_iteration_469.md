# Candidate Gravity Research Log — Iteration 469

Date: 2026-09-05

## Starting authority
Read `candidate_gravity/recovery/CURRENT_QG_FRONT.md`, Iteration 468 recovery delta, recent research history and commits before doing new work. Source of truth remains the repository. Physical/operator authority remains Iteration 411; blocker authority remains Iteration 421; exact unresolved physical set remains `[2]`. Canonical rank-7 run `33973849536` was still `in_progress`, so no duplicate heavy run was launched.

## Question
Iteration 460 froze a single sample-level cancellation condition number, while Iteration 467 exposed the exact four-quartet odd-odd representation of the same central4×central4 operator. Can cancellation be decomposed exactly into (i) cancellation internal to parity quartets and (ii) cancellation between the four weighted quartet scales, without changing any physical estimator or threshold?

## Exact result
Let

`Q_ab = F(+a,+b)-F(-a,+b)-F(+a,-b)+F(-a,-b)`

with frozen quartet coefficients

`alpha_11=4/9`, `alpha_12=-1/18`, `alpha_21=-1/18`, `alpha_22=1/144`.

Define

`D = sum_ab alpha_ab Q_ab`,

`S_quartet = sum_ab |alpha_ab Q_ab|`,

`S_sample = sum_ab |alpha_ab| * sum_signs |F(sign*a,sign*b)|`.

By two applications of the triangle inequality,

`|D| <= S_quartet <= S_sample`.

For `D != 0`, therefore

`kappa_sample = S_sample/|D| >= kappa_quartet = S_quartet/|D| >= 1`.

Introduce two bounded cancellation fractions:

`rho_parity = S_quartet/S_sample in [0,1]`,

`rho_shell = |D|/S_quartet in [0,1]`.

Then exactly

`kappa_sample = 1/(rho_parity*rho_shell)`

and

`kappa_quartet = 1/rho_shell`.

Hence `rho_parity` isolates cancellation caused by the exact odd-odd parity projection inside each quartet, whereas `rho_shell` isolates cancellation among the four already-projected weighted quartet scales. This decomposition cannot be inferred from the old single `kappa_cancel` alone.

The coefficient audit also gives `sum |alpha_ab| = 9/16`; multiplying by four samples per quartet recovers the canonical dimensionless sample L1 norm `9/4`, independently matching Iteration 458.

## Reproducibility
Added `candidate_gravity/code/iteration469_quartet_cancellation_decomposition_audit.py` and machine-readable result `candidate_gravity/results/iteration469_quartet_cancellation_decomposition.json`. The audit uses exact rational arithmetic and stress-tests 5000 reproducible rational sample sets while asserting all exact inequalities/identities.

## Classification
`PASS_QUARTET_CANCELLATION_DECOMPOSITION__DIAGNOSTIC_ONLY_NON_PROMOTING`.

This is an implementation/provenance diagnostic. A small `rho_parity`, small `rho_shell`, or large condition number is not a Candidate-Gravity consistency FAIL, not comparator identity, not regime-specific model non-identifiability, not near-degeneracy, and not a novelty certificate. It does not change `ds=-d_base`, any MP threshold, BASE↔HALF threshold, support ordering, parent dynamics, or physical gate.

## Forward contract
After all 28 distinct mass-support coordinates are certified, the Iteration-458/460/462/467 assembly closure should additionally report `S_quartet`, `kappa_quartet`, `rho_parity`, and `rho_shell` independently for BASE/HALF and MP80/MP120. The canonical 16-term and four-quartet assemblies must still agree within bounded arithmetic roundoff.

Canonical rank-7 run `33973849536` remains the sole active numerical gate; no later coordinate is authorized before raw consumption.

MODEL_READINESS: 24%

Readiness change: 0 percentage points. This iteration sharpens numerical provenance and cancellation attribution but closes no additional component of the stable model-readiness rubric.
