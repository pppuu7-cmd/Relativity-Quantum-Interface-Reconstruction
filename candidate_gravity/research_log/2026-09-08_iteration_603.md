# RQIR Candidate Gravity — Research Log, Iteration 603

Date: 2026-09-08

The completed Iter603 Actions artifact was consumed independently rather than promoted from workflow colour. Canonical run `34240339709`, job `102108562629`, artifact `10061784026`, digest `sha256:fb1755978a482279325a711e2995e58c3040a64721deb529bf59a16baf3326e6`; raw result SHA-256 `44d1266cffaf4d4579f10a3eb0e72efe0dcd90a5f4a429eeabcc7f00ebd67d3d`; raw audit reports `failures=[]`.

Iter603 is diagnostic-only and preserves the Iter602 frozen FAIL. Changing only `LAT_R` from 3 to 4 to 5 drives the three problematic `s/xi0..2` last residual magnitudes from roughly `1e-2` through `1e-3` to `1e-11–1e-12`, while `s/xi3` and all `a/b` rows remain small at `R=5` (maximum other row `5.64e-12`). This is strong finite-lattice-radius sensitivity, not a model PASS.

Because the `s`-sector kernel condition estimates remain of order `1e18`, no single enlarged radius may be selected post-hoc as authority. The next scientifically permitted step is therefore a prospectively frozen boundary/convergence contract with nested radii and fail-closed `BLOCKED_CONVERGENCE`, before any production Ward rerun.

Guardrails remain unchanged: no Source/Born subtraction; no source-to-Iter582 mapping; no comparator quotient; no `ANSATZ-003`; no Fisher/resources.

MODEL_READINESS: 24%
