# Candidate Gravity C5 — Iteration 267: condensed-index/Fourier momentum routing

**Date:** 2026-09-02  
**Authoritative scientific continuation:** Iteration 266 -> 267  
**MODEL_READINESS: 24%**

## Purpose

Iteration 266 reduced the null-soft cubic `B3[s,a,b]` from 15 surviving operator terms to 8 independent transpose classes. Before instantiating those classes as physical kernels, the condensed-index/Fourier momentum support must be made explicit. A local pointwise matrix product is not yet a valid condensed-index operator product.

For a background Fourier insertion carrying momentum `k_x`, a polarized operator coefficient `O1[x]` has support

`<p_out|O1[x]|p_in> proportional delta(p_out-p_in-k_x)`.

Likewise `O2[x,y]` carries `k_x+k_y` and `O3[x,y,z]` carries the sum of all three external momenta. `Q0` carries zero background momentum but remains a nontrivial function of the orbit-space momentum at the point where it is inserted.

## Exact routing result

For every one of the 8 independent Iteration-266 representatives, right-to-left operator composition gives the same total support

`K = k_s + k_a + k_b`,

so each forward kernel has the form

`<p+K|X|p>`.

Examples:

- `Q0 A3[s,a,b] Q0`: the right `Q0` is evaluated at `p`, `A3` shifts to `p+K`, and the left `Q0` is evaluated at `p+K`.
- `Q1[s] A2[a,b] Q0`: `Q0(p)` first, then `A2[a,b]` shifts to `p+k_a+k_b`, and `Q1[s]` shifts to `p+K`.
- `Q1[s] A1[a] Q1[b]`: the right `Q1[b]` shifts `p -> p+k_b`, `A1[a]` shifts to `p+k_b+k_a`, and `Q1[s]` shifts to `p+K`.

This routing is exact and follows only from translation covariance of the frozen Fourier-expanded same-parent operator.

Freeze:

`PASS_EXACT_B3_CONDENSED_INDEX_MOMENTUM_SUPPORT`

## Kernel transpose is not a fixed-routing matrix transpose

The abstract Iteration-266 statement that transpose partners need not be recomputed remains correct in full condensed-index operator space. But its numerical implementation must exchange the kernel endpoints:

`<p+K|X|p>^T = <p|X^T|p+K>`.

If one rewrites the transpose partner again in canonical forward orientation, its total background shift is `-K`, not `+K`. For a real metric perturbation, `h(-k)=h(k)^*`, this is implemented by endpoint reversal together with `k_s,k_a,k_b -> -k_s,-k_a,-k_b` and complex conjugation where appropriate.

Therefore a raw finite-dimensional `4x4` matrix transpose at unchanged loop/orbit momentum `p` and unchanged `+k` background legs is not the condensed-index kernel transpose.

Freeze guardrail:

`NO_FIXED_PLUS_K_MATRIX_TRANSPOSE_AS_KERNEL_TRANSPOSE`.

This does **not** invalidate the exact 15-to-8 transpose-class reduction. It sharpens how the seven partner kernels must be reconstructed.

## Scientific classification

This is a scoped exact operator-support result. It is not:

- a consistency FAIL;
- an exact Candidate-vs-GR comparator identity;
- regime-specific non-identifiability;
- near-degeneracy;
- a nonzero physical `B3` numerator;
- a novelty certificate.

The C5 state remains operationally/derivationally

`BLOCKED_4D_EINSTEIN_VD_RESOLVENT_VERTEX_LIBRARY_TENSOR_REDUCTION_AND_SOURCE_PROJECTION`

with `BLOCKED_NOT_ZERO`.

`ANSATZ-003` remains NOT CREATED. Fisher/resources remain FORBIDDEN. Heavy full-C5 integration remains NOT AUTHORIZED.

## Consequence for the next implementation

The next physical kernel code must carry an explicit pair `(p_out,p_in)` (or an equivalent routed base momentum plus accumulated external shift) for every `Q/A/N/K` factor. The 8 independent representatives should be evaluated only after that routing layer exists. Seven transpose partners are then generated by endpoint-reversed kernel transpose, not by a raw same-routing matrix transpose.

## Readiness

MODEL_READINESS: 24%

Change from Iteration 266: **0 percentage points**. A potentially invalid numerical implementation route has been excluded and the exact momentum-support convention is now frozen, but no explicit contracted physical `A/N/Q/B3` or final C5 coordinate has yet been obtained.
