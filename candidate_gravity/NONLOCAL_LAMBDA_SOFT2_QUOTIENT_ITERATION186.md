# QG-NL-EXP-001 conditioned lambda soft2 quotient — Iteration 186

**Date:** 2026-08-31  
**MODEL_READINESS: 24%**

## Question

Does the fixed nonlocal comparator `QG-NL-EXP-001` leave an independent cubic soft2 direction after exact `K2` calibration and quotient by the frozen zero-`K2` local-C5 curvature-cubic rank-4 span?

## Frozen parent dynamics

Use exactly

`S ~ int sqrt(-g) [R + G_mn F(Box) R^mn]`,

with

`F(Box)=(exp(-lambda Box)-1)/Box`, `lambda=1`.

Therefore

`d_lambda F|_1 = -exp(-Box)`.

No form factor, row, local coefficient, or projection convention is refit in this iteration.

## Why the lambda tangent is computable from the Iteration-185 calculus

On every frozen external TT leg, the linear scalar curvature is exactly zero. At cubic order every term from

`-(1/2) R [d_lambda F] R`

contains at least one linear scalar-curvature factor, including the pieces generated by variation of `Box` inside the form factor. Hence this scalar sector is an exact protocol zero for the present cubic soft2 observable.

The scoped full lambda tangent is therefore

`R_mn [-exp(-Box)] R^mn = sum_n (-1)^(n+1)/n! R_mn Box^n R^mn`.

The same exact multilinear plane-wave covariant `Box` recursion as Iteration 185 is used. Connection, inverse-metric, and operator/Frechet variations are therefore included action-level rather than inserted as a scalar momentum ansatz.

## Numerical convergence

The exponential was evaluated through `n=14`. Maximum row-wise changes of the summed tangent are

- `N14-N13 = 1.51e-14`;
- `N13-N12 = 3.76e-13`;
- `N12-N11 = 8.82e-12`;
- `N11-N10 = 2.04e-10`;
- `N10-N9 = 4.39e-9`.

The code reproduces the authoritative Iteration-185 `n=2,3,4` local columns exactly to stored precision.

The raw nonlocal lambda soft2 tangent is

`[4.0151619898, 0.1885142177, 6.8958645469, -2.1606040754, -0.6546536227, -0.7636291997]`.

Adding the frozen Iteration-185 local `K2`-compensation soft2 piece gives the exact hard-conditioned relation direction

`S_cond = [4.6900726517, 0.2789326350, 8.5017458637, -3.0062751678, -0.7364444751, -0.7232162209]`.

## Quotient by frozen zero-K2 local C5

The Iteration-178 exact local span is

`V4 = Riemann3_soft2 * {1, -q^2, q^4, -q^6}`

and has rank `4/6`. Its singular ratio is `s_min/s_max = 3.2575e-5`; therefore the fit coefficients themselves are ill-conditioned and are not interpreted physically. The orthogonal row-space residual is the invariant object.

Projection of `S_cond` onto `V4` leaves

`r = [0.0268839979, -0.1639003715, 0.5945393888, -1.9589856765, 1.8687871851, 1.0763391630]`,

with

`||r||_2 = 2.9781805828`,

`max |r_i| = 1.9589856765`,

and

`||r||_2 / ||S_cond||_2 = 0.2913946`.

Against the frozen Iteration-178 numerical envelope `5.2625580e-6`,

`||r||_2 / envelope = 5.659188e5`.

This is decisively resolved on the frozen rows and is not the near-degeneracy found for the scalar surrogate shapes in Iteration 181.

## Classification

Retain:

- `NL-NG-005 — FULL_QG_NL_EXP_001_LAMBDA_SOFT2_TANGENT_IS_RESOLVABLE_AFTER_EXACT_K2_COMPENSATION`;
- `REL-NG-004 — K2_PRESERVING_NONLOCAL_DIRECTION_SURVIVES_THE_FROZEN_ZERO_K2_LOCAL_C5_RANK4_QUOTIENT`;
- `NG-FUNNEL-040 — A_RESOLVED_NONLOCAL_COMPARATOR_DIRECTION_IS_NUISANCE_AUTHORITY_NOT_CANDIDATE_NOVELTY`.

This is **not** a Candidate Gravity residual and does not certify novelty. It strengthens the fixed comparator quotient by adding one resolved nonlocal nuisance direction. It is also not an exact identity, not a consistency FAIL, and not a near-degeneracy.

## Promotion state

`ANSATZ-003`: NOT CREATED.  
Fisher/resources: FORBIDDEN.  
Candidate novelty certificate: NONE.

## Next gate

Freeze the augmented conditioned comparator span `span(V4, S_cond)` and perform the asymptotic-safety relation audit in the same joint `(K2,S_soft2)` protocol. Published Euclidean/symmetric-point data may only be promoted if they determine the required source-completed Lorentzian relation without an invented continuation. If not, preserve `BLOCKED_AS_REALTIME_RELATION_COMPLETION` and proceed to the remaining C3 ordered-completion audit without zero-filling AS.
