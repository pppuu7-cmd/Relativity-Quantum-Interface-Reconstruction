# RQIR Candidate Gravity — Iteration 177

**Date:** 2026-08-31  
**MODEL_READINESS: 24%**

## Goal

Instantiate the first actual action-level local-C5 Ward-subtracted soft-transverse comparator columns `B_T`, using the same covariant parent operators as Iteration 150 but a new soft-deformed protocol as required by Iteration 176.

## Frozen protocol

Soft leg:

`k1(eps)=eps*(1,0,0,1)`

with the normalized physical plus TT polarization used in Iteration 175. For each of the six target-independent rows, reuse the six Iteration-150 hard `q_i` values and impose exact conservation

`k2=q_i`, `k3=-q_i-k1`.

The hard-leg tensor polarizations are regenerated by the same spin-2 projector convention from fixed deterministic seeds.

## Ward subtraction

For both curvature-cubic operators

- `Tr(Ricci^3)`;
- `Riemann_mn^rs Riemann_rs^ab Riemann_ab^mn`,

the expansion around flat space starts at cubic order. Therefore their quadratic inverse-kernel contribution is exactly absent:

`K2_operator=0`, hence `W[K2_operator]=0`.

Their cubic terms are products of linearized curvatures and are separately invariant under a pure-gauge replacement of any one linearized leg. The numerical soft-leg gauge regression gives maximum absolute residue `2.82e-22`.

## Analytic null-soft simplification

For the null TT soft graviton,

`R_mn^(1)[k_soft,e_TT]=0`

exactly, while

`||R_mnrs^(1)[k0,e_TT]||=2`.

Consequently the `Tr(Ricci^3)` soft-transverse column is an exact **protocol zero**. This does not mean the operator is absent: Iteration 150 already established a nonzero finite off-shell column for it. The zero is specific to the physical null-TT soft leg.

The cyclic `Riemann^3` operator remains nonzero. Extrapolating `Gamma3/eps^2` to `eps -> 0` on the six frozen rows gives

`[-1.6411697072, 0.0638588272, 0.8548821188, -0.1705521567, -0.3261917311, -0.1655609265]`.

Two independent smooth extrapolants disagree by at most `5.27e-6`.

## Rank/SVD certificate

The first two-column local C5 soft-transverse matrix is

`V_C5_B_T = [0, v_Riemann3]`,

so

`rank = 1/2`,

with singular values

`[1.8950564368, 0]`.

This is **regime-specific non-identifiability**, not a consistency failure and not an exact operator identity.

## Retained results

- `C5-NG-008 — NULL_TT_SOFT_PROTOCOL_ANNIHILATES_RICCI_CUBED_B_T_BUT_NOT_CYCLIC_RIEMANN_CUBED`.
- `SOFT-NG-004 — FIRST_ACTION_LEVEL_LOCAL_C5_B_T_BASIS_HAS_RANK_ONE_ON_SIX_NULL_SOFT_TT_ROWS`.
- `NG-FUNNEL-037 — PROTOCOL_ZERO_FROM_ONSHELL_SOFT_RICCI_IS_REGIME_SPECIFIC_NOT_OPERATOR_ABSENCE`.

## Scientific implication

The first genuine `B_T` comparator direction is now instantiated, but the full transverse quotient is still incomplete. A candidate residual cannot be promoted until further local C5 operators authorized by the frozen EFT truncation are projected, followed by fixed C4 and `QG-NL-EXP-001` columns. C3 ordered and asymptotic-safety real-time `B_T` entries remain BLOCKED unless explicitly derived.

`ANSATZ-003` remains uncreated. Fisher/resources remain forbidden.
