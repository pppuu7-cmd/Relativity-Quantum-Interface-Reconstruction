# RECOVERY DELTA — Iteration 625

Date: 2026-09-08

Iteration 625 adds a CTP branch-orientation prerequisite to the remaining native bridge.

Classification:
`PASS_ITER625_INTERNAL_SCALAR_ENERGY_SIGN_CENSUS__CTP_LINE_TYPE_ASSIGNMENT_REQUIRED_BEFORE_ONE_COMMON_N_NATIVE_RETARDED_BINDING__NON_RESIDUAL`.

Under the frozen Iter613 trajectory, five supported internal scalar roots have positive energy and one root, `D_s^-(s=0.09)`, has negative internal energy `r0=-0.7`.

Iter608/615 use one algebraic/Feynman-like `K+i0` pole rule and remain authoritative in that scope. A retarded scalar prescription is energy-oriented, so a naive conversion of every retained internal scalar line to a retarded propagator would generate a root-dependent branch sign. Therefore the actual combined CTP measurement map must specify the contour/r-a component of every retained scalar source line before a one-common-`N_native` retarded binding may be claimed.

Do not flip any root by hand, do not erase the negative-energy root, and do not declare Iter615 invalid. The correct next step is to derive the measurement-level CTP line assignment; depending on that result, the existing six coefficients either acquire one common bridge or the affected source component must be recomputed under its proper CTP prescription.

Full native projection, Source/Born subtraction, comparator quotient, `ANSATZ-003`, Fisher/resources remain forbidden.

MODEL_READINESS: 24%.
