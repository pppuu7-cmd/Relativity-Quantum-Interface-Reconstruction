# RECOVERY DELTA — Iteration 631

Date: 2026-09-09

Authoritative predecessor: Iter630. It established that every closed retarded scalar-loop family contains exactly one `G_K`; therefore the all-internal-`G_R` open scalar response cannot be promoted to retarded gravitational `Gamma3` by normalization.

Iter631 audits the missing statistical-state authority. The frozen Iter627 CTP contract specifies the causal r/a slots but not an initial scalar density matrix, occupation distribution, temperature, vacuum/Hadamard prescription, or absolute `G_K` state normalization. A fail-closed non-uniqueness witness holds `G_R/G_A` fixed while comparing stationary Gaussian bosonic occupations `n=0` and `n=1`, for which the statistical factors differ (`1` versus `3`). Thus `G_K` is not derivable from the Iter627 causal contract alone.

Classification:
`BLOCKED_ITER631_ITER627_CTP_CAUSAL_CONTRACT_DOES_NOT_FIX_SCALAR_KELDYSH_STATE__GK_REQUIRES_PROSPECTIVE_STATE_BOUNDARY_CONDITION__NON_RESIDUAL`.

Raw validation authority: Actions run `34289607051`, job `102272978390`, head `0cbb7daf76000f1b00ac0d109be52ef92b1efacd`, conclusion `success`; exact audit and artifact upload succeeded. Artifact `10080814393`, digest `sha256:6c8fb7fd6802bf4a996d3336c5e2dc76eb07cd0b83bd00b96eb368713145697c`. The committed gate has `failures=[]`, `candidate_values_used=false`, `zero_fill=false`, Source/Born subtraction `NOT_PERFORMED`, native projection `NOT_PERFORMED`.

Scope guardrail: this is operational BLOCKED, not a Candidate-Gravity consistency FAIL, exact comparator identity, regime-specific non-identifiability, near-degeneracy, or novelty certificate. Iter628 open-response data remain preserved in their own scope. No vacuum/thermal/statistical state may be inferred post hoc from Iter582/Candidate values.

MODEL_READINESS: 24%
Readiness change: 0 percentage points; the statistical-state prerequisite is isolated but the robust unique residual remains absent.

Exact next gate: prospectively freeze a same-parent scalar state/boundary-condition contract before any closed-loop coefficient is evaluated. Then derive `G_K` in the already-frozen Iter627 convention and evaluate the Iter630 one-`G_K` closed retarded scalar-loop support on the unchanged Iter613 trajectory.