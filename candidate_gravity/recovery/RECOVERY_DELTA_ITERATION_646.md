# RECOVERY DELTA — Iteration 646

Date: 2026-09-09

Authoritative predecessor: Iter645 reduced numerator-weighted ordinary cut densities computed; absolute closed-CTP promotion BLOCKED.

## New authority

A fail-closed repository audit compared the committed Iter627 SK generating-functional contract, Iter630 one-G_K closed-loop structure, Iter632 vacuum `G_K=2*pi*i*delta(D)` normalization, Iter311 relative cubic Tr-log topology, and Iter645 reduced phase-space/numerator evaluator.

The existing authority fixes causal slots, state/spectral normalization, relative determinant topology, and the reduced `beta/(16*pi^2)` kernel, but does not explicitly bind the complete `W=-i ln Z` / Legendre-to-native-Gamma3 normalization carrying the real-scalar Gaussian one-loop global phase/prefactor into the native Gamma3 convention.

Exactly one common nonzero global scalar factor therefore remains unbound; there is no authority for root-, family-, or q2-dependent factors. Because Iter645 reduced values were already evaluated, the remaining factor must not be selected post hoc from those outputs. Standard literature conventions do not substitute for missing same-parent pre-result authority.

Canonical Action `34306240992` succeeded; artifact `10086735213`; digest `sha256:c4348360d606f4707dff5d8f792aadbef74b62d8d5c08ae116b4a47b05392973`.

Classification:
`BLOCKED_ITER646_ABSOLUTE_CLOSED_CTP_TO_NATIVE_GAMMA3_NORMALIZATION_NOT_EXPLICITLY_BOUND__ONE_GLOBAL_FACTOR_REMAINS__NON_RESIDUAL`.

This is operational/provenance BLOCKED. It is not consistency FAIL/PASS, exact comparator identity, regime-specific non-identifiability, near-degeneracy, novelty certificate, or Candidate residual.

Guardrails unchanged: Candidate values unused; `zero_fill=false`; Source/Born subtraction `NOT_PERFORMED`; native Y/T_cut projection `NOT_PERFORMED`; comparator quotient `NOT_PERFORMED`; no ANSATZ-003/Fisher/resources.

MODEL_READINESS: 24%
Readiness change: 0 percentage points. The blocker is now reduced to one global normalization factor, but robust unique residual remains `0/20`.

Exact next gate — Iter647: provenance-search only pre-Iter645 repository history for an explicit CTP W/Gamma plus real-scalar determinant and Fourier/loop-measure normalization. If absent, preserve a permanent normalization BLOCKED for this observable branch and only then consider a prospectively versioned projective/ratio observable that cancels the global factor, without using Iter645 values to choose its definition.
