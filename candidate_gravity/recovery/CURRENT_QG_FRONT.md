# Candidate Gravity Current Front

**Updated:** 2026-09-01  
**Infrastructure status:** READY — 100%  
**MODEL_READINESS:** **23%** under the frozen model-only rubric  
**Permanent C5 reference:** `ANSATZ-PQG-EFT-001` v0.1  
**Active promotable ansatz:** none — intentionally withheld  
**Authoritative Candidate Gravity front:** **Iteration 203**

## Scientific state in one sentence

The authoritative observable remains the joint source-completed `(K2,S_soft2_full)` relation with prospectively frozen polarization settings. The previous dimension-12 local C5 rank-4 soft2 complement is now classified as a **scoped truncation result**, because the declared local Riemann-chain `Box^n` continuation generates a row-scaled Vandermonde tower that can saturate any finite single-polarization row set when sufficiently high derivative order is admitted. Physical Wilson coefficients must nevertheless be shared across polarization settings: on the frozen v3-A+v3-B 24-row stack the single Riemann-chain tower has rank 12 and obeys twelve exact cross-polarization relations. The full all-orders C5 tensor remainder, AS real-time three-graviton soft2 relation, and C3 nonlinear ordered relation remain BLOCKED; no Candidate Gravity residual exists.

## Stable readiness rubric

Authority: `candidate_gravity/MODEL_READINESS_RUBRIC.md`.

- comparator foundation `23/25`;
- robust unique residual `0/20`;
- frozen parent dynamics/ANSATZ `0/20`;
- consistency/positivity/Ward/causality `0/15`;
- identifiability/Fisher `0/10`;
- resource/experiment closure `0/10`.

`MODEL_READINESS = 23%` from Iteration 202 onward. The score was reduced from 24% because the previously credited local-C5 comparator closure depended on a dimension-12 truncation whose omitted analytic remainder is not controlled over the current hard range.

## Frozen rules

- Repository/recovery is source of truth.
- Unsupported comparator coordinates are `BLOCKED`, never zero-filled.
- Exact hard constraints precede profiling/Fisher.
- Structural rank and finite-noise conditioning are separate gates.
- TT polarization is part of the observable definition, not a numerical seed convention.
- Physical comparator Wilson coefficients are shared across all polarization/kinematic settings generated by the same theory.
- Alternate protocols may not be selected, reseeded, reweighted or dropped after seeing a candidate residual.
- A finite local-EFT residual is not model-independent unless omitted higher-order local analytic directions are physically bounded by power counting/remainder control.
- Do not create `ANSATZ-003` before a concrete residual survives C3/C4/C5/nonlocal/AS subtraction, all declared truncation uncertainty, and prospectively frozen robustness tests.
- Fisher/resources remain forbidden until the robust residual gate passes.

## Existing comparator authority retained

### Hard K2

For the supported local quadratic C5 directions plus fixed nonlocal `QG-NL-EXP-001` lambda direction, the hard functions are

`[x,x^2,...,x^6,x^2 exp(x)]`.

After factoring positive x, `{1,x,...,x^5,x exp(x)}` is an extended complete Chebyshev system with final Wronskian

`34560 (x+6) exp(x) > 0` for `x>0`.

Thus any seven distinct positive hard nodes give exact rank 7. Exact hard calibration removes the supported local-quadratic/nonlocal parameter variations. Finite-noise conditioning remains poor and is a separate issue.

### C4

Compatible local unitary massless-spin-2 boundary remains inside C5 at the frozen soft scope. Fixed nonzero-mass dRGT is physically incompatible with the null-soft carrier and remains a scoped protocol mismatch rather than zero.

### Nonlocal

The fixed exponential nonlocal parent is resolved on the original discovery block. On prospective >=7-node hard blocks its lambda K2 tangent is exact-independent of the frozen local polynomial hard family and therefore cannot survive exact K2 calibration as a free nuisance direction.

### AS

`BLOCKED_AS_REALTIME_RELATION_COMPLETION`: modern authority does not yet supply the required same-parent Lorentzian/in-in source-completed off-shell three-graviton `O(k_soft^2)` relation with K2.

### C3 PQCG

`BLOCKED_C3_CTP_ORDERED_COMPLETION`: published nonlinear OM dynamics and linearized conserved-diffusion MSR/JD construction do not fix the nonlinear metric-dependent diffusion/ordered soft2 relation required by RQIR.

## v3 polarization authority through Iteration 201

Two independently prospectively frozen protocols share the same twelve hard nodes but use different valid TT polarization settings.

- v3-A local dimension-12 C5 rank `4/12`;
- v3-B local dimension-12 C5 rank `4/12`.

Their four-dimensional dimension-12 nuisance images are strongly rotated, with principal angles

`[1.307°,70.598°,76.740°,83.659°]`.

Therefore polarization is part of the physical row definition. A future candidate must be generated from one parent dynamics and one parameter convention across all frozen settings.

## Iteration 202 — local C5 derivative-tower truncation blocker

Iteration 178 established for `n=1,2,3`

`B_T[RiemannChain Box^n]=(2/3)(-q^2)^n B_T[Riemann^3]`.

The cubic flat-background argument extends to arbitrary `n>=1`: Box on the null soft leg vanishes; Box on either hard momentum eigenmode contributes `(-q^2)^n`; four of six permutations survive.

Hence on rows `i`

`v_0(i)=r_i`,

`v_n(i)=(2/3) r_i (-x_i)^n`, `n>=1`.

For N distinct x_i with nonzero r_i, the first N columns are `diag(r)` times a Vandermonde matrix up to nonzero column scalings and have exact rank N.

For both twelve-row v3-A and v3-B the high-precision 12-column determinants are nonzero:

- A: `3.62199999487769657e-56`;
- B: `-3.21015349445100242e-54`.

Therefore the dimension-12 rank-four complement is not a model-independent C5 residual space without a controlled EFT truncation/remainder bound.

Current hard range:

`x in [0.184448,0.994896]`.

At the largest row, `x^11≈0.94527`, so higher derivative powers are not suppressed by x alone across the full protocol.

Retain:

- `C5-NG-019`;
- `REL-NG-015`;
- `NG-FUNNEL-057`;
- `READINESS-CORR-001`.

Authority:

- `analysis/c5_local_derivative_tower_truncation_iteration202.py`;
- `results/c5_local_derivative_tower_truncation_iteration202.json`;
- `candidate_gravity/C5_LOCAL_DERIVATIVE_TOWER_TRUNCATION_ITERATION202.md`;
- `recovery/RECOVERY_DELTA_ITERATION_202.md`.

## Iteration 203 — shared-Wilson cross-polarization relation

Physical local-EFT coefficients must be common between v3-A and v3-B.

For the single Riemann-chain derivative family,

`S_A(i)=r_A(i) f(x_i)`,

`S_B(i)=r_B(i) f(x_i)`

with one shared analytic function f. Therefore every member of this family obeys twelve exact relations

`r_B(i) S_A(i)-r_A(i) S_B(i)=0`.

With Box powers `n=0,...,11`, the vertical A+B matrix has

- shape `24x12`;
- rank `12`;
- left-null dimension `12`;
- relation-matrix rank `12`;
- maximum relation residual `9.03e-16`.

This shows that separate finite interpolation does not imply physical shared-coefficient saturation across multiple polarization settings.

However these are family-specific relations, not yet full-C5 null relations. Independent higher-dimension tensor/derivative carriers in the all-orders gravitational EFT remain to be enumerated/bounded.

Retain:

- `REL-NG-016`;
- `C5-NG-020`;
- `NG-FUNNEL-058`.

Authority:

- `analysis/c5_shared_wilson_cross_polarization_iteration203.py`;
- `results/c5_shared_wilson_cross_polarization_iteration203.json`;
- `candidate_gravity/C5_SHARED_WILSON_CROSS_POLARIZATION_ITERATION203.md`;
- `recovery/RECOVERY_DELTA_ITERATION_203.md`.

## Candidate state

There is **no robust Candidate Gravity residual**.

`ANSATZ-003`: NOT CREATED.  
Fisher/resources: FORBIDDEN.

## Immediate next scientific priority — Iteration 204

Resolve or bound the newly exposed **all-orders C5 tensor-remainder problem** before spending heavy compute on a candidate.

Parallel tasks:

1. enumerate the independent parity-even higher-derivative cubic/tensor carriers that can survive the frozen null-soft two-hard-TT cross-polarization observable, using nonredundant gravitational EFT/amplitude-basis authority while preserving off-shell/source-completed caution;
2. design a prospectively frozen low-energy alternative with `x_max << 1` and an explicit truncation/remainder criterion; do not assume Wilson naturalness silently;
3. continue AS and C3 authority searches independently;
4. investigate linked nonanalytic multi-point/CTP relations as a possible route that cannot be defeated by arbitrary finite local analytic interpolation.

Do not increase readiness until a genuinely reopened comparator block is re-closed. Do not create `ANSATZ-003`, Fisher, or resource forecasts.
