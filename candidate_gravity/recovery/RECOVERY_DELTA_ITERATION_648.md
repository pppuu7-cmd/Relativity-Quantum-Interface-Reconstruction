# RECOVERY DELTA — Iteration 648

Date: 2026-09-09

Authoritative predecessor: Iter647 permanently classified the absolute closed-CTP -> native Gamma3 normalization branch as provenance-BLOCKED by exactly one common nonzero global complex factor.

## New authority

A separate projective closed-Gamma3 ordinary-cut observable is now prospectively defined on the pre-registered above-threshold grid `s=(2.0,2.25,2.5,3.0,4.0)`:

`a(s)=-bubble_K1s_K2ab+triangle_sab+triangle_sba`,

`P_ij=a_i*conj(a_j)/sum_k|a_k|^2`.

The denominator is the full vector norm and is not a component selected after inspecting values. The observable is defined iff the assembled vector is nonzero. Algebraically, for one common `lambda in C*`, `a -> lambda a` gives numerator and denominator the same `|lambda|^2`, hence `P` is unchanged.

Canonical Action `34313481973` succeeded; job `102344758270`; artifact `10089211623`; digest `sha256:7ec917e87557380e41db10f4e465a75e87468e6d1ed937ce317dc15aeb8fbdd0`. The fail-closed validator certifies `norm2>1e-30`, `failures=[]`, and projector trace/Hermiticity/idempotence/common-scaling errors all `<=2e-12`. Exact projector entries are not promoted here because the available GitHub connector exposed Action/artifact metadata and validation status but not the artifact ZIP payload.

Classification:
`PASS_ITER648_PROJECTIVE_CLOSED_GAMMA3_GLOBAL_FACTOR_CANCELLATION__NON_RESIDUAL`.

This is a normalization-free observable-definition PASS, not Candidate-Gravity consistency PASS/FAIL, exact comparator identity, regime-specific non-identifiability, near-degeneracy, novelty certificate, or Candidate residual.

The absolute Iter645 branch remains permanently BLOCKED and is not retroactively repaired. Source/Born subtraction remains `NOT_PERFORMED`; native Y/T_cut projection `NOT_PERFORMED`; comparator quotient `NOT_PERFORMED`; `ANSATZ-003` and Fisher/resources remain forbidden.

MODEL_READINESS: 24%
Readiness change: 0 percentage points. A lawful projective bypass exists, but robust unique residual remains `0/20` until matched subtraction and the fixed comparator quotient are completed.

Exact next gate — Iter649: audit the normalization class and algebraic role of the source/Born subtraction before performing it. Projectivization may proceed through subtraction only if the candidate and subtraction terms share the same single common multiplicative factor. If an independently normalized additive term is required, classify the projective matched observable as BLOCKED rather than fitting or cancelling it post hoc.