# RECOVERY DELTA — Iteration 627

Date: 2026-09-09

Iteration 627 closes the Iter626 measurement-map blocker prospectively by freezing `MSSC001-GRAVITY-SK-MEAS-V1` before any native projected Candidate/comparator value is used.

Classification:
`PASS_ITER627_PROSPECTIVE_COMBINED_MATTER_GRAVITY_SK_CONTRACT__RETARDED_SCALAR_PROBE__PHYSICAL_HR_INSERTIONS__INTERNAL_SCALAR_LINES_INDUCED_RETARDED__RAW_VALID_NON_RESIDUAL`.

Frozen combined SK structure:

- doubled matter+gravity functional `Z[J_phi+,J_phi-,J_g+,J_g-]` with `S_+ - S_-`;
- `X_+=X_r+X_a/2`, `X_-=X_r-X_a/2` for scalar and gravity;
- physical background `h_a=0`, metric insertions are `h_r` derivatives;
- measured scalar probe is `G_phi^{ra}=G_R`;
- remove only the two common external retarded scalar endpoints after the cubic derivative;
- retain K3 + six K1/K2 placements + six K1^3 ordered chains, total 13 families;
- derive internal scalar components by r/a matrix multiplication, never root-by-root.

Exact validator results are `rrr=0`, `arr=rar=rra=1`, `aaa=1/4`; the measured `ra` chains reduce to `R dK_R R`, `R dK_R R dK_R R`, and the cubic retarded chain. Thus all surviving internal scalar propagators in this observable are induced as retarded propagators.

Raw validation authority: Actions run `34284449139`, conclusion `success`, head `e701b1d0aa8a0222e7ec6cfbc6b5baf8263378c0`, artifact `10078890134`, digest `sha256:03a8167e23d946006024971ad02824070b700c94124465bb11982fed87dbe67e`; fail-closed exact comparison PASS.

Iter608/615 remain historical authority in their common algebraic/Feynman-like `K+i0` scope. They are not modified post hoc. Native binding requires a fresh same-dynamics retarded recomputation under the new prospective observable contract.

Source/Born subtraction remains `NOT_PERFORMED`; no comparator quotient; no `ANSATZ-003`; no Fisher/resources.

MODEL_READINESS: 24%.
Readiness change: 0 percentage points because the measurement-map prerequisite closed but robust unique residual remains 0/20 and no full readiness sector closed.

Exact next gate: Iter628 recomputes the six supported pole coefficients under the frozen retarded internal-line rule using Iter613/614 kinematics and all 13 source families, and derives the single common source-to-native phase/normalization from the same SK convention before native projection.