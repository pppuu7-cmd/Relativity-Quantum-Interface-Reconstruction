# Recovery Delta — Candidate Gravity Iteration 348

Date: 2026-09-03

## Scope

Matched-timelike re-specialization of the already frozen physical same-parent `A1/A2` provider for

`A_{i gamma}=(D_i R^k_gamma) epsilon_k`

on the exact Iteration-332 closed timelike triad and the same seed-319/common-background metric tensors required by the U2 route contract. This iteration changes neither parent dynamics nor the Iteration-340 `A.T/A` orientation or `Hinv_VD=-K^-1` sign binding.

## Raw Actions authority

- run: `33777026420`
- job: `100721180705`
- artifact: `9901993803` (`iteration348-result`)
- artifact digest: `sha256:37c60e89f919f5a61c3abb3d797dbe966097b9bab5c384d20361294eb915bb29`
- scientific JSON SHA-256: `89201feaea36e12e6342e0ede4bed6b4dedc423365beade497e62adba9b4a3e6`
- workflow head: `8e1b7237325b1a5b5c25f924fea88fa7a755f2f6`

The raw artifact and authority-audit JSON were downloaded and inspected independently of the workflow conclusion. The authority audit found exactly one Iteration-348 top-level object and `scientific_authority_pass=true`.

## Result

The matched common background retains the timelike invariants

`q_i^2 = (-1.0, -0.14, -0.34)`

with metric tensor seed `319`, tensor scale `0.12`, `D=4`, `Lambda=0`, `a=-1/2`, `gamma1=1`, `gamma2=0` and split `g=eta+h`.

The independent exact-geometry plus coordinate-finite-difference/amplitude-fit oracle gives:

- `A0_max_abs = 0.0` against frozen limit `1e-12`;
- `A1_oracle_max_abs_error = 7.69426189428657e-12` against frozen limit `1e-9`;
- `A2_oracle_max_abs_error = 5.675424640666504e-09` against frozen limit `2e-7`;
- three first-order modes and six second-order partitions were checked;
- maximum physical kernel magnitudes are nonzero: `|A1|_max=0.15244821081057558`, `|A2|_max=0.1154017158446708`.

No threshold was changed after the result. The physical `A1/A2` provider therefore survives re-specialization onto the exact matched timelike parent.

Authority:

`PASS_U2_A1_A2_MATCHED_TIMELIKE_COMMON_BACKGROUND_PROVIDER_EQ55_EXACT_GEOMETRY_ORACLE__NY_HINV_ROUTE_ASSEMBLY_NEXT`.

This is a component-provider PASS only. It is not a physical `Tr U2` cut, not a Candidate Gravity consistency result, not a comparator identity, not non-identifiability, not a near-degeneracy result and not a novelty certificate.

## Next active gate

Re-specialize the frozen Iteration-342 `N/Y` bridge and the Iteration-339 shifted graviton inverse with the Iteration-340 `Hinv_VD=-K^-1` sign on this exact same timelike/common metric background. Then substitute physical `A/N/Y/Hinv` route-by-route into all 12 Iteration-346 survivors, preserving exact incoming momentum and Iteration-345 functional transpose, before numerator/denominator-family canonicalization and any cut integration.

The determinant Iteration-335 timeout remains operational only; do not launch a third blind copy. Its unresolved channel requires analytic/symbolic angular reduction or a demonstrably different method.

Unsupported remains `BLOCKED`; no zero-fill, no Source/Born subtraction, no `ANSATZ-003`, no Fisher/resources and no blind full-C5.

MODEL_READINESS: 24%

Change from Iteration 347: `0 pp`. A real U2 physical-component rebase prerequisite closed, but no complete readiness-rubric bucket and no robust comparator-subtracted residual closed.
