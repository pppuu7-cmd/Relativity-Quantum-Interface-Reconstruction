# RQIR Candidate Gravity — Iteration 281

## Result

Recent commits extend the factual chain through Iteration 280. Iteration 280 established a rank-3 scalar cut-support basis on the controlled timelike slice,

`{1/s, 1/(s+0.2), log(s/(s+0.2))/(s-(s+0.2))}`,

while Iteration 279 stored the pre-integration non-scaleless family-resolved orbit trace over the same eight s-points.

A diagnostic least-squares test was performed against the hypothesis that the Iteration-279 non-scaleless trace could already be represented by three kinematics-independent coefficients multiplying the Iteration-280 scalar support functions. The best fit is

`c = (-0.18793735, -21.47800082, 7.01934896)`

but the relative L2 residual is

`0.08675993597017234`

with maximum absolute residual

`3.39573961`.

Freeze:

`FAIL_SCOPED_CONSTANT_MASTER_COEFFICIENT_SURROGATE_ON_TIMELIKE_SLICE`.

This is not a consistency FAIL. It says only that the already-computed pre-integration family trace must not be collapsed to three constant master coefficients. The true C5 coefficients are kinematic functions generated by tensor/IBP reduction of the p-dependent family numerators.

## Guardrail

Do not infer master coefficients by fitting the pre-integration family-summed trace directly to scalar discontinuity shapes. Reconstruct the family numerators with their loop-momentum and invariant dependence, then tensor/IBP reduce them.

## MODEL_READINESS

MODEL_READINESS: 24%

Change from the last recorded estimate: 0 percentage points. This iteration closes an implementation ambiguity and records a scoped negative result, but it does not yet produce the linked comparator coefficient vector or a comparator-subtracted residual.

## Exact next gate

For bubble-a, bubble-b and triangle families separately, reconstruct the combined p-dependent numerator in a finite Lorentz-covariant tensor/rational basis on the translation-closed timelike family. Verify on held-out p points. Then perform one-loop tensor/IBP reduction inside the already-frozen raised bubble/triangle topology and extract coefficient functions multiplying the Iteration-280 cut-support basis. Source/Ward/contact completion and comparator quotient remain downstream.
