name: rqir-iteration335-det-triangle-q2minus1-product-quadrature

on:
  workflow_dispatch:
  push:
    paths:
      - 'candidate_gravity/code/iteration335_det_triangle_q2minus1_product_quadrature.py'
      - '.github/workflows/rqir-iteration335-det-triangle-q2minus1-product-quadrature.yml'

jobs:
  iteration335:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - name: Install numerical dependency
        run: python -m pip install --disable-pip-version-check numpy
      - name: Run independent product-quadrature q2=-1 triangle resolution and preserve diagnostics
        run: |
          set +e
          python candidate_gravity/code/iteration335_det_triangle_q2minus1_product_quadrature.py > iteration335_result.json
          code=$?
          echo "$code" > iteration335_scientific_exit_code.txt
          exit 0
      - name: Sentinel/schema audit
        run: |
          python - <<'PY'
          import json,hashlib
          raw=open('iteration335_result.json','rb').read(); o=json.loads(raw)
          ok=isinstance(o,dict) and o.get('iteration')==335 and 'scientific_gate_pass' in o and 'runs' in o and 'status' in o
          a={'schema':'rqir_action_result_authority_audit_v1','expected_iteration':335,'found_iterations':[o.get('iteration')],
             'top_level_object_count':1,'valid_single_top_level':True,'valid_expected_iteration':o.get('iteration')==335,
             'scientific_authority_pass':bool(ok and o.get('scientific_gate_pass')),
             'sha256':hashlib.sha256(raw).hexdigest(),'bytes':len(raw),
             'scientific_exit_code':int(open('iteration335_scientific_exit_code.txt').read())}
          open('iteration335_authority_audit.json','w').write(json.dumps(a,indent=2,sort_keys=True)+'\n')
          assert ok
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: iteration335-result
          path: |
            iteration335_result.json
            iteration335_authority_audit.json
            iteration335_scientific_exit_code.txt
      - name: Enforce scientific gate
        run: python -c "import json; o=json.load(open('iteration335_result.json')); assert o['scientific_gate_pass'], o"
