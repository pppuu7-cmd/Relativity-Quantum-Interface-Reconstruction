name: rqir-iteration325-det-arbitrary-incoming-momentum-numerators

on:
  workflow_dispatch:
  push:
    paths:
      - 'candidate_gravity/code/iteration325_det_arbitrary_incoming_momentum_numerator_gate.py'
      - '.github/workflows/rqir-iteration325-det-arbitrary-incoming-momentum-numerators.yml'

jobs:
  iteration325:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - name: Install numerical dependency
        run: python -m pip install --disable-pip-version-check numpy
      - name: Run arbitrary-incoming-momentum H/N routed numerator gate and preserve diagnostics
        run: |
          set +e
          python candidate_gravity/code/iteration325_det_arbitrary_incoming_momentum_numerator_gate.py > iteration325_result.json
          code=$?
          echo "$code" > iteration325_scientific_exit_code.txt
          exit 0
      - name: Sentinel/schema audit
        run: |
          python - <<'PY'
          import json,hashlib
          raw=open('iteration325_result.json','rb').read(); o=json.loads(raw)
          ok=isinstance(o,dict) and o.get('iteration')==325 and 'scientific_gate_pass' in o
          a={'schema':'rqir_action_result_authority_audit_v1','expected_iteration':325,'found_iterations':[o.get('iteration')],'top_level_object_count':1,'valid_single_top_level':True,'valid_expected_iteration':o.get('iteration')==325,'scientific_authority_pass':bool(ok and o.get('scientific_gate_pass')),'sha256':hashlib.sha256(raw).hexdigest(),'bytes':len(raw),'scientific_exit_code':int(open('iteration325_scientific_exit_code.txt').read())}
          open('iteration325_authority_audit.json','w').write(json.dumps(a,indent=2,sort_keys=True)+'\n')
          assert ok
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: iteration325-result
          path: |
            iteration325_result.json
            iteration325_authority_audit.json
            iteration325_scientific_exit_code.txt
      - name: Enforce scientific gate
        run: python -c "import json; o=json.load(open('iteration325_result.json')); assert o['scientific_gate_pass'], o"
